const SV_VERSION = "2.4.3";
const calibration = {"schema_version":1,"schema":"switch-vision-interactive-calibration-v1","model":"cisco-3650-48p-4-uplink","generated_by":`Switch Vision v${SV_VERSION}`,"image":{"file":"faceplates/48rj45-4sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[176,245],"number":[176,181],"led_left":[154,221],"led_right":[199,221],"hitbox":[34,56]},"2":{"center":[177,338],"number":[176,409],"led_left":[153,365],"led_right":[200,365],"hitbox":[34,56]},"3":{"center":[236,245],"number":[237,181],"led_left":[213,221],"led_right":[259,221],"hitbox":[34,56]},"4":{"center":[237,337],"number":[237,409],"led_left":[213,365],"led_right":[259,365],"hitbox":[34,56]},"5":{"center":[294,245],"number":[296,181],"led_left":[273,221],"led_right":[319,221],"hitbox":[34,56]},"6":{"center":[297,337],"number":[297,409],"led_left":[273,365],"led_right":[319,365],"hitbox":[34,56]},"7":{"center":[355,245],"number":[356,181],"led_left":[333,221],"led_right":[379,221],"hitbox":[34,56]},"8":{"center":[357,337],"number":[357,409],"led_left":[333,365],"led_right":[379,365],"hitbox":[34,56]},"9":{"center":[414,245],"number":[415,181],"led_left":[393,221],"led_right":[440,221],"hitbox":[34,56]},"10":{"center":[416,337],"number":[415,409],"led_left":[393,365],"led_right":[439,365],"hitbox":[34,56]},"11":{"center":[474,245],"number":[475,181],"led_left":[453,221],"led_right":[499,221],"hitbox":[34,56]},"12":{"center":[476,336],"number":[476,409],"led_left":[453,365],"led_right":[499,365],"hitbox":[34,56]},"13":{"center":[550,245],"number":[551,181],"led_left":[524,221],"led_right":[570,221],"hitbox":[34,56]},"14":{"center":[549,337],"number":[551,409],"led_left":[524,365],"led_right":[571,365],"hitbox":[34,56]},"15":{"center":[610,245],"number":[611,181],"led_left":[584,221],"led_right":[631,221],"hitbox":[34,56]},"16":{"center":[608,336],"number":[612,409],"led_left":[584,365],"led_right":[631,365],"hitbox":[34,56]},"17":{"center":[669,245],"number":[670,181],"led_left":[644,221],"led_right":[690,221],"hitbox":[34,56]},"18":{"center":[668,337],"number":[670,409],"led_left":[644,365],"led_right":[690,365],"hitbox":[34,56]},"19":{"center":[729,245],"number":[729,181],"led_left":[704,221],"led_right":[750,221],"hitbox":[34,56]},"20":{"center":[728,337],"number":[729,409],"led_left":[704,365],"led_right":[750,365],"hitbox":[34,56]},"21":{"center":[788,245],"number":[790,181],"led_left":[764,221],"led_right":[810,221],"hitbox":[34,56]},"22":{"center":[788,337],"number":[790,409],"led_left":[764,365],"led_right":[810,365],"hitbox":[34,56]},"23":{"center":[848,245],"number":[849,181],"led_left":[824,221],"led_right":[870,221],"hitbox":[34,56]},"24":{"center":[850,337],"number":[849,409],"led_left":[824,365],"led_right":[870,365],"hitbox":[34,56]},"25":{"center":[923,245],"number":[925,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56]},"26":{"center":[925,336],"number":[925,409],"led_left":[903,365],"led_right":[950,365],"hitbox":[34,56]},"27":{"center":[983,245],"number":[985,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56]},"28":{"center":[984,336],"number":[985,409],"led_left":[963,365],"led_right":[1009,365],"hitbox":[34,56]},"29":{"center":[1043,245],"number":[1043,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56]},"30":{"center":[1045,337],"number":[1043,409],"led_left":[1023,365],"led_right":[1069,365],"hitbox":[34,56]},"31":{"center":[1105,245],"number":[1104,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56]},"32":{"center":[1104,337],"number":[1104,409],"led_left":[1083,365],"led_right":[1129,365],"hitbox":[34,56]},"33":{"center":[1162,245],"number":[1163,181],"led_left":[1143,221],"led_right":[1189,221],"hitbox":[34,56]},"34":{"center":[1164,337],"number":[1163,409],"led_left":[1143,365],"led_right":[1189,365],"hitbox":[34,56]},"35":{"center":[1225,245],"number":[1221,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56]},"36":{"center":[1223,337],"number":[1221,409],"led_left":[1203,365],"led_right":[1249,365],"hitbox":[34,56]},"37":{"center":[1297,245],"number":[1298,181],"led_left":[1273,221],"led_right":[1319,221],"hitbox":[34,56]},"38":{"center":[1298,337],"number":[1298,409],"led_left":[1273,365],"led_right":[1318,365],"hitbox":[34,56]},"39":{"center":[1355,245],"number":[1357,181],"led_left":[1333,221],"led_right":[1379,221],"hitbox":[34,56]},"40":{"center":[1358,337],"number":[1357,409],"led_left":[1332,365],"led_right":[1379,365],"hitbox":[34,56]},"41":{"center":[1416,245],"number":[1418,181],"led_left":[1393,221],"led_right":[1439,221],"hitbox":[34,56]},"42":{"center":[1417,337],"number":[1418,409],"led_left":[1393,365],"led_right":[1438,365],"hitbox":[34,56]},"43":{"center":[1475,245],"number":[1476,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56]},"44":{"center":[1477,336],"number":[1476,409],"led_left":[1453,365],"led_right":[1499,365],"hitbox":[34,56]},"45":{"center":[1534,245],"number":[1535,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56]},"46":{"center":[1537,337],"number":[1535,409],"led_left":[1513,365],"led_right":[1559,365],"hitbox":[34,56]},"47":{"center":[1594,245],"number":[1595,181],"led_left":[1573,221],"led_right":[1618,221],"hitbox":[34,56]},"48":{"center":[1596,337],"number":[1595,409],"led_left":[1572,365],"led_right":[1618,365],"hitbox":[34,56]}},"sfp":{"G1":{"center":[1710,246],"hitbox":[94,48]},"G2":{"center":[1835,246],"hitbox":[94,48]},"G3/TE3":{"center":[1711,341],"hitbox":[94,48]},"G4/TE4":{"center":[1836,341],"hitbox":[94,48]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"title":[152.5,18],"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"__default__","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"title":[152.5,23],"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"default_cisco_48_port"};
const calibration2960X24 = {"schema":"switch-vision-interactive-calibration-v1","model":"cisco-2960x-24p-4-uplink","generated_by":"Switch Vision v1.8.9","image":{"file":"faceplates/24rj45-4sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[926,245],"number":[926,181],"led_left":[907,221],"led_right":[942,221],"hitbox":[34,56]},"2":{"center":[927,338],"number":[926,409],"led_left":[906,365],"led_right":[943,365],"hitbox":[34,56]},"3":{"center":[986,245],"number":[987,181],"led_left":[966,221],"led_right":[1002,221],"hitbox":[34,56]},"4":{"center":[987,337],"number":[987,409],"led_left":[966,365],"led_right":[1002,365],"hitbox":[34,56]},"5":{"center":[1044,245],"number":[1046,181],"led_left":[1026,221],"led_right":[1062,221],"hitbox":[34,56]},"6":{"center":[1047,337],"number":[1047,409],"led_left":[1026,365],"led_right":[1062,365],"hitbox":[34,56]},"7":{"center":[1105,245],"number":[1106,181],"led_left":[1086,221],"led_right":[1122,221],"hitbox":[34,56]},"8":{"center":[1107,337],"number":[1107,409],"led_left":[1086,365],"led_right":[1122,365],"hitbox":[34,56]},"9":{"center":[1164,245],"number":[1165,181],"led_left":[1146,221],"led_right":[1183,221],"hitbox":[34,56]},"10":{"center":[1166,337],"number":[1165,409],"led_left":[1146,365],"led_right":[1182,365],"hitbox":[34,56]},"11":{"center":[1224,245],"number":[1225,181],"led_left":[1206,221],"led_right":[1242,221],"hitbox":[34,56]},"12":{"center":[1226,336],"number":[1226,409],"led_left":[1206,365],"led_right":[1242,365],"hitbox":[34,56]},"13":{"center":[1300,245],"number":[1301,181],"led_left":[1277,221],"led_right":[1313,221],"hitbox":[34,56]},"14":{"center":[1299,337],"number":[1301,409],"led_left":[1277,365],"led_right":[1314,365],"hitbox":[34,56]},"15":{"center":[1360,245],"number":[1361,181],"led_left":[1337,221],"led_right":[1374,221],"hitbox":[34,56]},"16":{"center":[1358,336],"number":[1362,409],"led_left":[1337,365],"led_right":[1374,365],"hitbox":[34,56]},"17":{"center":[1419,245],"number":[1420,181],"led_left":[1397,221],"led_right":[1433,221],"hitbox":[34,56]},"18":{"center":[1418,337],"number":[1420,409],"led_left":[1397,365],"led_right":[1433,365],"hitbox":[34,56]},"19":{"center":[1479,245],"number":[1479,181],"led_left":[1457,221],"led_right":[1493,221],"hitbox":[34,56]},"20":{"center":[1478,337],"number":[1479,409],"led_left":[1457,365],"led_right":[1493,365],"hitbox":[34,56]},"21":{"center":[1538,245],"number":[1540,181],"led_left":[1517,221],"led_right":[1553,221],"hitbox":[34,56]},"22":{"center":[1538,337],"number":[1540,409],"led_left":[1517,365],"led_right":[1553,365],"hitbox":[34,56]},"23":{"center":[1598,245],"number":[1599,181],"led_left":[1577,221],"led_right":[1613,221],"hitbox":[34,56]},"24":{"center":[1600,337],"number":[1599,409],"led_left":[1577,365],"led_right":[1613,365],"hitbox":[34,56]}},"sfp":{"G1":{"center":[1710,246],"hitbox":[94,48],"label":[1709,188]},"G2":{"center":[1835,246],"hitbox":[94,48],"label":[1838,188]},"G3":{"center":[1711,341],"hitbox":[94,48],"label":[1710,402]},"G4":{"center":[1836,341],"hitbox":[94,48],"label":[1839,402]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"24rj45-4sfp.png","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"cisco_2960x_24p"};
const calibration2960S48 = {"schema_version":1,"schema":"switch-vision-interactive-calibration-v1","model":"cisco-2960s-48p-2x10g","generated_by":"Switch Vision v1.8.9","image":{"file":"faceplates/48rj45-2sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[176,245],"number":[176,181],"led_left":[154,221],"led_right":[199,221],"hitbox":[34,56]},"2":{"center":[177,338],"number":[176,409],"led_left":[153,365],"led_right":[200,365],"hitbox":[34,56]},"3":{"center":[236,245],"number":[237,181],"led_left":[213,221],"led_right":[259,221],"hitbox":[34,56]},"4":{"center":[237,337],"number":[237,409],"led_left":[213,365],"led_right":[259,365],"hitbox":[34,56]},"5":{"center":[294,245],"number":[296,181],"led_left":[273,221],"led_right":[319,221],"hitbox":[34,56]},"6":{"center":[297,337],"number":[297,409],"led_left":[273,365],"led_right":[319,365],"hitbox":[34,56]},"7":{"center":[355,245],"number":[356,181],"led_left":[333,221],"led_right":[379,221],"hitbox":[34,56]},"8":{"center":[357,337],"number":[357,409],"led_left":[333,365],"led_right":[379,365],"hitbox":[34,56]},"9":{"center":[414,245],"number":[415,181],"led_left":[393,221],"led_right":[440,221],"hitbox":[34,56]},"10":{"center":[416,337],"number":[415,409],"led_left":[393,365],"led_right":[439,365],"hitbox":[34,56]},"11":{"center":[474,245],"number":[475,181],"led_left":[453,221],"led_right":[499,221],"hitbox":[34,56]},"12":{"center":[476,336],"number":[476,409],"led_left":[453,365],"led_right":[499,365],"hitbox":[34,56]},"13":{"center":[550,245],"number":[551,181],"led_left":[524,221],"led_right":[570,221],"hitbox":[34,56]},"14":{"center":[549,337],"number":[551,409],"led_left":[524,365],"led_right":[571,365],"hitbox":[34,56]},"15":{"center":[610,245],"number":[611,181],"led_left":[584,221],"led_right":[631,221],"hitbox":[34,56]},"16":{"center":[608,336],"number":[612,409],"led_left":[584,365],"led_right":[631,365],"hitbox":[34,56]},"17":{"center":[669,245],"number":[670,181],"led_left":[644,221],"led_right":[690,221],"hitbox":[34,56]},"18":{"center":[668,337],"number":[670,409],"led_left":[644,365],"led_right":[690,365],"hitbox":[34,56]},"19":{"center":[729,245],"number":[729,181],"led_left":[704,221],"led_right":[750,221],"hitbox":[34,56]},"20":{"center":[728,337],"number":[729,409],"led_left":[704,365],"led_right":[750,365],"hitbox":[34,56]},"21":{"center":[788,245],"number":[790,181],"led_left":[764,221],"led_right":[810,221],"hitbox":[34,56]},"22":{"center":[788,337],"number":[790,409],"led_left":[764,365],"led_right":[810,365],"hitbox":[34,56]},"23":{"center":[848,245],"number":[849,181],"led_left":[824,221],"led_right":[870,221],"hitbox":[34,56]},"24":{"center":[850,337],"number":[849,409],"led_left":[824,365],"led_right":[870,365],"hitbox":[34,56]},"25":{"center":[923,245],"number":[925,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56]},"26":{"center":[925,336],"number":[925,409],"led_left":[903,365],"led_right":[950,365],"hitbox":[34,56]},"27":{"center":[983,245],"number":[985,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56]},"28":{"center":[984,336],"number":[985,409],"led_left":[963,365],"led_right":[1009,365],"hitbox":[34,56]},"29":{"center":[1043,245],"number":[1043,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56]},"30":{"center":[1045,337],"number":[1043,409],"led_left":[1023,365],"led_right":[1069,365],"hitbox":[34,56]},"31":{"center":[1105,245],"number":[1104,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56]},"32":{"center":[1104,337],"number":[1104,409],"led_left":[1083,365],"led_right":[1129,365],"hitbox":[34,56]},"33":{"center":[1162,245],"number":[1163,181],"led_left":[1143,221],"led_right":[1189,221],"hitbox":[34,56]},"34":{"center":[1164,337],"number":[1163,409],"led_left":[1143,365],"led_right":[1189,365],"hitbox":[34,56]},"35":{"center":[1225,245],"number":[1221,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56]},"36":{"center":[1223,337],"number":[1221,409],"led_left":[1203,365],"led_right":[1249,365],"hitbox":[34,56]},"37":{"center":[1297,245],"number":[1298,181],"led_left":[1273,221],"led_right":[1319,221],"hitbox":[34,56]},"38":{"center":[1298,337],"number":[1298,409],"led_left":[1273,365],"led_right":[1318,365],"hitbox":[34,56]},"39":{"center":[1355,245],"number":[1357,181],"led_left":[1333,221],"led_right":[1379,221],"hitbox":[34,56]},"40":{"center":[1358,337],"number":[1357,409],"led_left":[1332,365],"led_right":[1379,365],"hitbox":[34,56]},"41":{"center":[1416,245],"number":[1418,181],"led_left":[1393,221],"led_right":[1439,221],"hitbox":[34,56]},"42":{"center":[1417,337],"number":[1418,409],"led_left":[1393,365],"led_right":[1438,365],"hitbox":[34,56]},"43":{"center":[1475,245],"number":[1476,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56]},"44":{"center":[1477,336],"number":[1476,409],"led_left":[1453,365],"led_right":[1499,365],"hitbox":[34,56]},"45":{"center":[1534,245],"number":[1535,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56]},"46":{"center":[1537,337],"number":[1535,409],"led_left":[1513,365],"led_right":[1559,365],"hitbox":[34,56]},"47":{"center":[1594,245],"number":[1595,181],"led_left":[1573,221],"led_right":[1618,221],"hitbox":[34,56]},"48":{"center":[1596,337],"number":[1595,409],"led_left":[1572,365],"led_right":[1618,365],"hitbox":[34,56]}},"sfp":{"TE1":{"center":[1712,285],"hitbox":[94,48],"label":[1709,228]},"TE2":{"center":[1837,285],"hitbox":[94,48],"label":[1838,228]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"48rj45-2sfp.png","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"cisco_2960s_48p"};
const calibration3560CG8PC = {"schema":"switch-vision-interactive-calibration-v1","model":"cisco-3560cg-8pc-8p-2dual","generated_by":"Switch Vision v1.8.24","image":{"file":"faceplates/24rj45-2sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[926,245],"number":[926,181],"led_left":[907,221],"led_right":[942,221],"hitbox":[34,56]},"2":{"center":[927,338],"number":[926,409],"led_left":[906,365],"led_right":[943,365],"hitbox":[34,56]},"3":{"center":[986,245],"number":[987,181],"led_left":[966,221],"led_right":[1002,221],"hitbox":[34,56]},"4":{"center":[987,337],"number":[987,409],"led_left":[966,365],"led_right":[1002,365],"hitbox":[34,56]},"5":{"center":[1044,245],"number":[1046,181],"led_left":[1026,221],"led_right":[1062,221],"hitbox":[34,56]},"6":{"center":[1047,337],"number":[1047,409],"led_left":[1026,365],"led_right":[1062,365],"hitbox":[34,56]},"7":{"center":[1105,245],"number":[1106,181],"led_left":[1086,221],"led_right":[1122,221],"hitbox":[34,56]},"8":{"center":[1107,337],"number":[1107,409],"led_left":[1086,365],"led_right":[1122,365],"hitbox":[34,56]}},"sfp":{"G1":{"center":[1712,285],"hitbox":[94,48],"label":[1712,228]},"G2":{"center":[1837,285],"hitbox":[94,48],"label":[1837,228]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"24rj45-2sfp.png","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"cisco_3560cg_8pc"};

function stockCalibrationClone(base, profile, model, faceplate, sfp = null) {
  const clone = JSON.parse(JSON.stringify(base));
  clone.profile = profile;
  clone.model = model;
  clone.generated_by = `Switch Vision v${SV_VERSION}`;
  clone.image.file = `faceplates/${faceplate}`;
  clone.ui.faceplate.file = faceplate;
  if (sfp) clone.sfp = JSON.parse(JSON.stringify(sfp));
  return clone;
}

const calibrationStock24RJ452SFP = stockCalibrationClone(
  calibration2960X24,
  "stock_24rj45_2sfp",
  "stock-24rj45-2sfp",
  "24rj45-2sfp.png",
  {
    G1: calibration2960S48.sfp.TE1,
    G2: calibration2960S48.sfp.TE2,
  },
);

const calibrationStock24RJ454SFP = stockCalibrationClone(
  calibration2960X24,
  "stock_24rj45_4sfp",
  "stock-24rj45-4sfp",
  "24rj45-4sfp.png",
);

const calibrationStock48RJ452SFP = stockCalibrationClone(
  calibration2960S48,
  "stock_48rj45_2sfp",
  "stock-48rj45-2sfp",
  "48rj45-2sfp.png",
  {
    G1: calibration2960S48.sfp.TE1,
    G2: calibration2960S48.sfp.TE2,
  },
);

const calibrationStock48RJ454SFP = stockCalibrationClone(
  calibration,
  "stock_48rj45_4sfp",
  "stock-48rj45-4sfp",
  "48rj45-4sfp.png",
  {
    G1: calibration.sfp.G1,
    G2: calibration.sfp.G2,
    G3: calibration.sfp["G3/TE3"],
    G4: calibration.sfp["G4/TE4"],
  },
);

const SV_FACTORY_CALIBRATIONS = {
  stock_24rj45_2sfp: calibrationStock24RJ452SFP,
  stock_24rj45_4sfp: calibrationStock24RJ454SFP,
  stock_48rj45_2sfp: calibrationStock48RJ452SFP,
  stock_48rj45_4sfp: calibrationStock48RJ454SFP,
  default_cisco_48_port: calibration,
  cisco_2960x_24p: calibration2960X24,
  cisco_2960s_48p: calibration2960S48,
  cisco_3560cg_8pc: calibration3560CG8PC,
};
const SV_FACEPLATE_PROFILE_FILES = {
  stock_24rj45_2sfp: "24rj45-2sfp.png",
  stock_24rj45_4sfp: "24rj45-4sfp.png",
  stock_48rj45_2sfp: "48rj45-2sfp.png",
  stock_48rj45_4sfp: "48rj45-4sfp.png",
  unifi_24p_rj45_2sfp: "unifi-24p-rj45-2sfp.png",
};

function factoryCalibrationForProfile(profile) {
  const key = String(profile || "").trim();
  const direct = SV_FACTORY_CALIBRATIONS[key] || null;
  if (direct) return direct;
  const filename = SV_FACEPLATE_PROFILE_FILES[key];
  return filename ? faceplateFactoryCalibrationForFile(filename) : null;
}

const SV_FACEPLATE_FACTORY_CALIBRATIONS = {
  "24rj45-2sfp.png": {"schema_version":2,"schema":"switch-vision-interactive-calibration-v1","model":"stock-24rj45-2sfp","profile":"stock_24rj45_2sfp","generated_by":"Switch Vision v2.4.0","image":{"file":"faceplates/24rj45-2sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[926,245],"number":[926,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"2":{"center":[927,338],"number":[926,409],"led_left":[903,364],"led_right":[950,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"3":{"center":[986,245],"number":[987,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"4":{"center":[987,337],"number":[987,409],"led_left":[963,364],"led_right":[1009,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"5":{"center":[1044,245],"number":[1046,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"6":{"center":[1047,337],"number":[1047,409],"led_left":[1023,364],"led_right":[1069,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"7":{"center":[1105,245],"number":[1106,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"8":{"center":[1107,337],"number":[1107,409],"led_left":[1083,364],"led_right":[1129,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"9":{"center":[1164,245],"number":[1165,181],"led_left":[1143,221],"led_right":[1190,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"10":{"center":[1166,337],"number":[1165,409],"led_left":[1143,364],"led_right":[1189,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"11":{"center":[1224,245],"number":[1225,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"12":{"center":[1226,336],"number":[1226,409],"led_left":[1203,364],"led_right":[1249,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"13":{"center":[1300,245],"number":[1301,181],"led_left":[1274,221],"led_right":[1319,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"14":{"center":[1299,337],"number":[1301,409],"led_left":[1273,364],"led_right":[1319,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"15":{"center":[1360,245],"number":[1361,181],"led_left":[1333,221],"led_right":[1379,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"16":{"center":[1358,336],"number":[1362,409],"led_left":[1333,364],"led_right":[1379,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"17":{"center":[1419,245],"number":[1420,181],"led_left":[1393,221],"led_right":[1438,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"18":{"center":[1418,337],"number":[1420,409],"led_left":[1393,364],"led_right":[1439,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"19":{"center":[1479,245],"number":[1479,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"20":{"center":[1478,337],"number":[1479,409],"led_left":[1453,364],"led_right":[1499,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"21":{"center":[1538,245],"number":[1540,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"22":{"center":[1538,337],"number":[1540,409],"led_left":[1513,364],"led_right":[1559,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"23":{"center":[1598,245],"number":[1599,181],"led_left":[1573,221],"led_right":[1619,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"24":{"center":[1600,337],"number":[1599,409],"led_left":[1573,364],"led_right":[1619,364],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true}},"sfp":{"G1":{"center":[1712,285],"hitbox":[94,48],"label":[1709,228],"label_show":true},"G2":{"center":[1837,285],"hitbox":[94,48],"label":[1838,228],"label_show":true}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"fit":"fill","opacity":1,"show":true,"file":"24rj45-2sfp.png"},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}},"port_led_shape":"circle","port_label_color":"#eef7ff","port_number_color":"#eef7ff","port_label_font_weight":"bold","port_number_font_size":13,"sfp_label_font_size":13.5,"status_leds":{"hidden":[],"text_color":"#eef7ff","font_family":"Arial, Helvetica, sans-serif","font_size":16.5,"font_weight":"900"}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""}},
  "24rj45-4sfp.png": {"schema_version":2,"schema":"switch-vision-interactive-calibration-v1","model":"stock-24rj45-4sfp","profile":"stock_24rj45_4sfp","generated_by":"Switch Vision v2.4.0","image":{"file":"faceplates/24rj45-4sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[926,245],"number":[926,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"2":{"center":[927,338],"number":[926,409],"led_left":[903,365],"led_right":[949,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"3":{"center":[986,245],"number":[987,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"4":{"center":[987,337],"number":[987,409],"led_left":[963,365],"led_right":[1009,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"5":{"center":[1044,245],"number":[1046,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"6":{"center":[1047,337],"number":[1047,409],"led_left":[1023,365],"led_right":[1069,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"7":{"center":[1105,245],"number":[1106,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"8":{"center":[1107,337],"number":[1107,409],"led_left":[1083,365],"led_right":[1129,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"9":{"center":[1164,245],"number":[1165,181],"led_left":[1143,221],"led_right":[1190,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"10":{"center":[1166,337],"number":[1165,409],"led_left":[1143,365],"led_right":[1189,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"11":{"center":[1224,245],"number":[1225,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"12":{"center":[1226,336],"number":[1226,409],"led_left":[1203,365],"led_right":[1249,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"13":{"center":[1300,245],"number":[1301,181],"led_left":[1274,221],"led_right":[1319,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"14":{"center":[1299,337],"number":[1301,409],"led_left":[1273,365],"led_right":[1319,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"15":{"center":[1360,245],"number":[1361,181],"led_left":[1332,221],"led_right":[1379,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"16":{"center":[1358,336],"number":[1362,409],"led_left":[1333,365],"led_right":[1379,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"17":{"center":[1419,245],"number":[1420,181],"led_left":[1393,221],"led_right":[1439,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"18":{"center":[1418,337],"number":[1420,409],"led_left":[1392,365],"led_right":[1439,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"19":{"center":[1479,245],"number":[1479,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"20":{"center":[1478,337],"number":[1479,409],"led_left":[1453,365],"led_right":[1499,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"21":{"center":[1538,245],"number":[1540,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"22":{"center":[1538,337],"number":[1540,409],"led_left":[1513,365],"led_right":[1559,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"23":{"center":[1598,245],"number":[1599,181],"led_left":[1573,221],"led_right":[1619,221],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"24":{"center":[1600,337],"number":[1599,409],"led_left":[1573,365],"led_right":[1619,365],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true}},"sfp":{"G1":{"center":[1710,246],"hitbox":[94,48],"label":[1709,188],"label_show":true},"G2":{"center":[1835,246],"hitbox":[94,48],"label":[1838,188],"label_show":true},"G3":{"center":[1711,341],"hitbox":[94,48],"label":[1710,402],"label_show":true},"G4":{"center":[1836,341],"hitbox":[94,48],"label":[1839,402],"label_show":true}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"fit":"fill","opacity":1,"show":true,"file":"24rj45-4sfp.png"},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}},"port_led_shape":"circle","port_label_color":"#eef7ff","port_number_color":"#eef7ff","port_label_font_weight":"bold","port_number_font_size":13,"sfp_label_font_size":13.5,"status_leds":{"hidden":[],"text_color":"#eef7ff","font_family":"Arial, Helvetica, sans-serif","font_size":16.5,"font_weight":"900"}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""}},
  "48rj45-2sfp.png": {"schema_version":1,"schema":"switch-vision-interactive-calibration-v1","model":"stock-48rj45-2sfp","generated_by":"Switch Vision v2.3.6","image":{"file":"faceplates/48rj45-2sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[176,245],"number":[176,181],"led_left":[154,221],"led_right":[199,221],"hitbox":[34,56]},"2":{"center":[177,338],"number":[176,409],"led_left":[153,365],"led_right":[200,365],"hitbox":[34,56]},"3":{"center":[236,245],"number":[237,181],"led_left":[213,221],"led_right":[259,221],"hitbox":[34,56]},"4":{"center":[237,337],"number":[237,409],"led_left":[213,365],"led_right":[259,365],"hitbox":[34,56]},"5":{"center":[294,245],"number":[296,181],"led_left":[273,221],"led_right":[319,221],"hitbox":[34,56]},"6":{"center":[297,337],"number":[297,409],"led_left":[273,365],"led_right":[319,365],"hitbox":[34,56]},"7":{"center":[355,245],"number":[356,181],"led_left":[333,221],"led_right":[379,221],"hitbox":[34,56]},"8":{"center":[357,337],"number":[357,409],"led_left":[333,365],"led_right":[379,365],"hitbox":[34,56]},"9":{"center":[414,245],"number":[415,181],"led_left":[393,221],"led_right":[440,221],"hitbox":[34,56]},"10":{"center":[416,337],"number":[415,409],"led_left":[393,365],"led_right":[439,365],"hitbox":[34,56]},"11":{"center":[474,245],"number":[475,181],"led_left":[453,221],"led_right":[499,221],"hitbox":[34,56]},"12":{"center":[476,336],"number":[476,409],"led_left":[453,365],"led_right":[499,365],"hitbox":[34,56]},"13":{"center":[550,245],"number":[551,181],"led_left":[524,221],"led_right":[570,221],"hitbox":[34,56]},"14":{"center":[549,337],"number":[551,409],"led_left":[524,365],"led_right":[571,365],"hitbox":[34,56]},"15":{"center":[610,245],"number":[611,181],"led_left":[584,221],"led_right":[631,221],"hitbox":[34,56]},"16":{"center":[608,336],"number":[612,409],"led_left":[584,365],"led_right":[631,365],"hitbox":[34,56]},"17":{"center":[669,245],"number":[670,181],"led_left":[644,221],"led_right":[690,221],"hitbox":[34,56]},"18":{"center":[668,337],"number":[670,409],"led_left":[644,365],"led_right":[690,365],"hitbox":[34,56]},"19":{"center":[729,245],"number":[729,181],"led_left":[704,221],"led_right":[750,221],"hitbox":[34,56]},"20":{"center":[728,337],"number":[729,409],"led_left":[704,365],"led_right":[750,365],"hitbox":[34,56]},"21":{"center":[788,245],"number":[790,181],"led_left":[764,221],"led_right":[810,221],"hitbox":[34,56]},"22":{"center":[788,337],"number":[790,409],"led_left":[764,365],"led_right":[810,365],"hitbox":[34,56]},"23":{"center":[848,245],"number":[849,181],"led_left":[824,221],"led_right":[870,221],"hitbox":[34,56]},"24":{"center":[850,337],"number":[849,409],"led_left":[824,365],"led_right":[870,365],"hitbox":[34,56]},"25":{"center":[923,245],"number":[925,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56]},"26":{"center":[925,336],"number":[925,409],"led_left":[903,365],"led_right":[950,365],"hitbox":[34,56]},"27":{"center":[983,245],"number":[985,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56]},"28":{"center":[984,336],"number":[985,409],"led_left":[963,365],"led_right":[1009,365],"hitbox":[34,56]},"29":{"center":[1043,245],"number":[1043,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56]},"30":{"center":[1045,337],"number":[1043,409],"led_left":[1023,365],"led_right":[1069,365],"hitbox":[34,56]},"31":{"center":[1105,245],"number":[1104,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56]},"32":{"center":[1104,337],"number":[1104,409],"led_left":[1083,365],"led_right":[1129,365],"hitbox":[34,56]},"33":{"center":[1162,245],"number":[1163,181],"led_left":[1143,221],"led_right":[1189,221],"hitbox":[34,56]},"34":{"center":[1164,337],"number":[1163,409],"led_left":[1143,365],"led_right":[1189,365],"hitbox":[34,56]},"35":{"center":[1225,245],"number":[1221,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56]},"36":{"center":[1223,337],"number":[1221,409],"led_left":[1203,365],"led_right":[1249,365],"hitbox":[34,56]},"37":{"center":[1297,245],"number":[1298,181],"led_left":[1273,221],"led_right":[1319,221],"hitbox":[34,56]},"38":{"center":[1298,337],"number":[1298,409],"led_left":[1273,365],"led_right":[1318,365],"hitbox":[34,56]},"39":{"center":[1355,245],"number":[1357,181],"led_left":[1333,221],"led_right":[1379,221],"hitbox":[34,56]},"40":{"center":[1358,337],"number":[1357,409],"led_left":[1332,365],"led_right":[1379,365],"hitbox":[34,56]},"41":{"center":[1416,245],"number":[1418,181],"led_left":[1393,221],"led_right":[1439,221],"hitbox":[34,56]},"42":{"center":[1417,337],"number":[1418,409],"led_left":[1393,365],"led_right":[1438,365],"hitbox":[34,56]},"43":{"center":[1475,245],"number":[1476,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56]},"44":{"center":[1477,336],"number":[1476,409],"led_left":[1453,365],"led_right":[1499,365],"hitbox":[34,56]},"45":{"center":[1534,245],"number":[1535,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56]},"46":{"center":[1537,337],"number":[1535,409],"led_left":[1513,365],"led_right":[1559,365],"hitbox":[34,56]},"47":{"center":[1594,245],"number":[1595,181],"led_left":[1573,221],"led_right":[1618,221],"hitbox":[34,56]},"48":{"center":[1596,337],"number":[1595,409],"led_left":[1572,365],"led_right":[1618,365],"hitbox":[34,56]}},"sfp":{"G1":{"center":[1712,285],"hitbox":[94,48],"label":[1709,228]},"G2":{"center":[1837,285],"hitbox":[94,48],"label":[1838,228]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"faceplates/48rj45-2sfp.png","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"stock_48rj45_2sfp"},
  "48rj45-4sfp.png": {"schema_version":1,"schema":"switch-vision-interactive-calibration-v1","model":"stock-48rj45-4sfp","generated_by":"Switch Vision v2.3.6","image":{"file":"faceplates/48rj45-4sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[176,245],"number":[176,181],"led_left":[154,221],"led_right":[199,221],"hitbox":[34,56]},"2":{"center":[177,338],"number":[176,409],"led_left":[153,365],"led_right":[200,365],"hitbox":[34,56]},"3":{"center":[236,245],"number":[237,181],"led_left":[213,221],"led_right":[259,221],"hitbox":[34,56]},"4":{"center":[237,337],"number":[237,409],"led_left":[213,365],"led_right":[259,365],"hitbox":[34,56]},"5":{"center":[294,245],"number":[296,181],"led_left":[273,221],"led_right":[319,221],"hitbox":[34,56]},"6":{"center":[297,337],"number":[297,409],"led_left":[273,365],"led_right":[319,365],"hitbox":[34,56]},"7":{"center":[355,245],"number":[356,181],"led_left":[333,221],"led_right":[379,221],"hitbox":[34,56]},"8":{"center":[357,337],"number":[357,409],"led_left":[333,365],"led_right":[379,365],"hitbox":[34,56]},"9":{"center":[414,245],"number":[415,181],"led_left":[393,221],"led_right":[440,221],"hitbox":[34,56]},"10":{"center":[416,337],"number":[415,409],"led_left":[393,365],"led_right":[439,365],"hitbox":[34,56]},"11":{"center":[474,245],"number":[475,181],"led_left":[453,221],"led_right":[499,221],"hitbox":[34,56]},"12":{"center":[476,336],"number":[476,409],"led_left":[453,365],"led_right":[499,365],"hitbox":[34,56]},"13":{"center":[550,245],"number":[551,181],"led_left":[524,221],"led_right":[570,221],"hitbox":[34,56]},"14":{"center":[549,337],"number":[551,409],"led_left":[524,365],"led_right":[571,365],"hitbox":[34,56]},"15":{"center":[610,245],"number":[611,181],"led_left":[584,221],"led_right":[631,221],"hitbox":[34,56]},"16":{"center":[608,336],"number":[612,409],"led_left":[584,365],"led_right":[631,365],"hitbox":[34,56]},"17":{"center":[669,245],"number":[670,181],"led_left":[644,221],"led_right":[690,221],"hitbox":[34,56]},"18":{"center":[668,337],"number":[670,409],"led_left":[644,365],"led_right":[690,365],"hitbox":[34,56]},"19":{"center":[729,245],"number":[729,181],"led_left":[704,221],"led_right":[750,221],"hitbox":[34,56]},"20":{"center":[728,337],"number":[729,409],"led_left":[704,365],"led_right":[750,365],"hitbox":[34,56]},"21":{"center":[788,245],"number":[790,181],"led_left":[764,221],"led_right":[810,221],"hitbox":[34,56]},"22":{"center":[788,337],"number":[790,409],"led_left":[764,365],"led_right":[810,365],"hitbox":[34,56]},"23":{"center":[848,245],"number":[849,181],"led_left":[824,221],"led_right":[870,221],"hitbox":[34,56]},"24":{"center":[850,337],"number":[849,409],"led_left":[824,365],"led_right":[870,365],"hitbox":[34,56]},"25":{"center":[923,245],"number":[925,181],"led_left":[904,221],"led_right":[949,221],"hitbox":[34,56]},"26":{"center":[925,336],"number":[925,409],"led_left":[903,365],"led_right":[950,365],"hitbox":[34,56]},"27":{"center":[983,245],"number":[985,181],"led_left":[963,221],"led_right":[1009,221],"hitbox":[34,56]},"28":{"center":[984,336],"number":[985,409],"led_left":[963,365],"led_right":[1009,365],"hitbox":[34,56]},"29":{"center":[1043,245],"number":[1043,181],"led_left":[1023,221],"led_right":[1069,221],"hitbox":[34,56]},"30":{"center":[1045,337],"number":[1043,409],"led_left":[1023,365],"led_right":[1069,365],"hitbox":[34,56]},"31":{"center":[1105,245],"number":[1104,181],"led_left":[1083,221],"led_right":[1129,221],"hitbox":[34,56]},"32":{"center":[1104,337],"number":[1104,409],"led_left":[1083,365],"led_right":[1129,365],"hitbox":[34,56]},"33":{"center":[1162,245],"number":[1163,181],"led_left":[1143,221],"led_right":[1189,221],"hitbox":[34,56]},"34":{"center":[1164,337],"number":[1163,409],"led_left":[1143,365],"led_right":[1189,365],"hitbox":[34,56]},"35":{"center":[1225,245],"number":[1221,181],"led_left":[1203,221],"led_right":[1249,221],"hitbox":[34,56]},"36":{"center":[1223,337],"number":[1221,409],"led_left":[1203,365],"led_right":[1249,365],"hitbox":[34,56]},"37":{"center":[1297,245],"number":[1298,181],"led_left":[1273,221],"led_right":[1319,221],"hitbox":[34,56]},"38":{"center":[1298,337],"number":[1298,409],"led_left":[1273,365],"led_right":[1318,365],"hitbox":[34,56]},"39":{"center":[1355,245],"number":[1357,181],"led_left":[1333,221],"led_right":[1379,221],"hitbox":[34,56]},"40":{"center":[1358,337],"number":[1357,409],"led_left":[1332,365],"led_right":[1379,365],"hitbox":[34,56]},"41":{"center":[1416,245],"number":[1418,181],"led_left":[1393,221],"led_right":[1439,221],"hitbox":[34,56]},"42":{"center":[1417,337],"number":[1418,409],"led_left":[1393,365],"led_right":[1438,365],"hitbox":[34,56]},"43":{"center":[1475,245],"number":[1476,181],"led_left":[1453,221],"led_right":[1499,221],"hitbox":[34,56]},"44":{"center":[1477,336],"number":[1476,409],"led_left":[1453,365],"led_right":[1499,365],"hitbox":[34,56]},"45":{"center":[1534,245],"number":[1535,181],"led_left":[1513,221],"led_right":[1559,221],"hitbox":[34,56]},"46":{"center":[1537,337],"number":[1535,409],"led_left":[1513,365],"led_right":[1559,365],"hitbox":[34,56]},"47":{"center":[1594,245],"number":[1595,181],"led_left":[1573,221],"led_right":[1618,221],"hitbox":[34,56]},"48":{"center":[1596,337],"number":[1595,409],"led_left":[1572,365],"led_right":[1618,365],"hitbox":[34,56]}},"sfp":{"G1":{"center":[1710,246],"hitbox":[94,48]},"G2":{"center":[1835,246],"hitbox":[94,48]},"G3":{"center":[1711,341],"hitbox":[94,48]},"G4":{"center":[1836,341],"hitbox":[94,48]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":338,"y":30,"width":313,"height":132,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"title":[152.5,18],"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"show":true,"file":"faceplates/48rj45-4sfp.png","fit":"fill","opacity":1},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx"],"sfp":["vlan","desc","link","rx","tx"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"title":[152.5,23],"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""},"profile":"stock_48rj45_4sfp"},
  "c3560cg-8pc-s.png": {"schema_version":2,"schema":"switch-vision-interactive-calibration-v1","model":"cisco-3560cg-8pc-8p-2dual","profile":"cisco_3560cg_8pc","generated_by":"Switch Vision v2.3.6","image":{"file":"faceplates/c3560cg-8pc-s.png","width":2048,"height":448,"master":"c3560cg-8pc-s-calibrated","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[516,325],"number":[176,181],"led_left":[480,303],"led_right":[549,303],"hitbox":[74,71],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"2":{"center":[651,329],"number":[176,409],"led_left":[617,303],"led_right":[684,303],"hitbox":[84,76],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"3":{"center":[786,303],"number":[237,181],"led_left":[752,303],"led_right":[820,303],"hitbox":[15.1,8.7],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"4":{"center":[917,327],"number":[237,409],"led_left":[885,303],"led_right":[953,303],"hitbox":[79,76],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"5":{"center":[1159,330],"number":[296,181],"led_left":[1125,303],"led_right":[1193,303],"hitbox":[79,66],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"6":{"center":[1297,332],"number":[297,409],"led_left":[1261,303],"led_right":[1329,303],"hitbox":[79,76],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"7":{"center":[1430,330],"number":[356,181],"led_left":[1397,303],"led_right":[1465,303],"hitbox":[79,76],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false},"8":{"center":[1566,332],"number":[357,409],"led_left":[1532,303],"led_right":[1601,303],"hitbox":[89,66],"led_left_size":[15.1,8.7],"led_right_size":[15.1,8.7],"number_show":false}},"sfp":{"G1":{"center":[1924,232],"hitbox":[94,48],"label":[1709,188],"label_show":false},"G2":{"center":[2046,232],"hitbox":[94,48],"label":[1838,188],"label_show":false}},"status_leds":{"STAT":[574,84],"SYST":[574,149],"DUPLX":[772,84],"ACTV":[772,149],"SPEED":[984,84],"STACK":[984,144],"PoE":[1189,84]},"ui":{"logo":{"show":true,"file":"cisco-dark.png","source":"custom","x":-213,"y":-1,"width":187,"height":188},"status_panel":{"show":true,"x":93,"y":50,"width":363,"height":152,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":20,"title_font_size":16,"font_weight":"normal","text_color":"#121212","label_color":"#121212","title_color":"#121212","background_color":"rgba(0,0,0,.06)","border_color":"#0a0c0f","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"fit":"fill","opacity":1,"show":true,"file":"c3560cg-8pc-s.png","source":"custom"},"calibration_button":{"show":true,"x":1808,"y":34,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1525,"y":45,"width":328,"height":85,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":20,"title_font_size":16,"font_weight":"normal","text_color":"#050505","label_color":"#050505","title_color":"#050505","background_color":"rgba(0,0,0,.06)","border_color":"#050505","border_show":true,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}},"port_led_shape":"rectangle","port_label_color":"#eef7ff","status_leds":{"hidden":[],"text_color":"#030303","font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":20,"font_weight":"normal"},"port_number_color":"#eef7ff"},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""}},
  "submarine-48rj45-4sfp.png": {"schema_version":2,"schema":"switch-vision-interactive-calibration-v1","model":"cisco-3650-48p-4-uplink","profile":"default_cisco_48_port","generated_by":"Switch Vision factory faceplate defaults","image":{"file":"faceplates/submarine-48rj45-4sfp.png","width":2048,"height":448,"master":"switch-master-v4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[176,217],"number":[174,162],"led_left":[157,196],"led_right":[191,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"2":{"center":[171,297],"number":[173,365],"led_left":[156,322],"led_right":[192,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"3":{"center":[236,217],"number":[235,162],"led_left":[214,196],"led_right":[251,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"4":{"center":[231,296],"number":[231,365],"led_left":[215,322],"led_right":[251,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"5":{"center":[294,217],"number":[292,162],"led_left":[274,196],"led_right":[309,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"6":{"center":[291,296],"number":[291,365],"led_left":[273,322],"led_right":[309,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"7":{"center":[355,217],"number":[352,162],"led_left":[334,196],"led_right":[368,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"8":{"center":[351,296],"number":[351,365],"led_left":[333,322],"led_right":[367,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"9":{"center":[414,217],"number":[411,162],"led_left":[393,196],"led_right":[427,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"10":{"center":[410,296],"number":[409,365],"led_left":[392,322],"led_right":[427,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"11":{"center":[474,217],"number":[469,162],"led_left":[452,196],"led_right":[486,196],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"12":{"center":[470,295],"number":[468,365],"led_left":[452,322],"led_right":[486,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"13":{"center":[535,217],"number":[538,161],"led_left":[522,198],"led_right":[556,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"14":{"center":[536,296],"number":[538,365],"led_left":[520,322],"led_right":[555,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"15":{"center":[595,217],"number":[597,161],"led_left":[580,198],"led_right":[614,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"16":{"center":[595,295],"number":[597,365],"led_left":[579,322],"led_right":[614,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"17":{"center":[654,217],"number":[656,161],"led_left":[639,198],"led_right":[674,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"18":{"center":[655,296],"number":[656,365],"led_left":[638,322],"led_right":[673,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"19":{"center":[714,217],"number":[715,161],"led_left":[696,198],"led_right":[733,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"20":{"center":[715,296],"number":[715,365],"led_left":[698,322],"led_right":[732,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"21":{"center":[773,217],"number":[774,161],"led_left":[757,198],"led_right":[792,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"22":{"center":[775,296],"number":[773,365],"led_left":[756,322],"led_right":[791,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"23":{"center":[833,217],"number":[833,161],"led_left":[816,198],"led_right":[851,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"24":{"center":[837,296],"number":[834,365],"led_left":[816,322],"led_right":[851,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"25":{"center":[902,217],"number":[904,162],"led_left":[887,198],"led_right":[923,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"26":{"center":[901,295],"number":[904,365],"led_left":[886,322],"led_right":[921,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"27":{"center":[962,217],"number":[963,162],"led_left":[946,198],"led_right":[980,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"28":{"center":[960,295],"number":[963,365],"led_left":[945,322],"led_right":[980,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"29":{"center":[1022,217],"number":[1021,162],"led_left":[1006,198],"led_right":[1040,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"30":{"center":[1021,296],"number":[1022,365],"led_left":[1005,322],"led_right":[1038,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"31":{"center":[1084,217],"number":[1081,162],"led_left":[1062,198],"led_right":[1097,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"32":{"center":[1080,296],"number":[1080,365],"led_left":[1063,322],"led_right":[1097,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"33":{"center":[1141,217],"number":[1139,162],"led_left":[1122,198],"led_right":[1157,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"34":{"center":[1140,296],"number":[1139,365],"led_left":[1122,322],"led_right":[1156,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"35":{"center":[1204,217],"number":[1199,162],"led_left":[1182,198],"led_right":[1217,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"36":{"center":[1199,296],"number":[1198,365],"led_left":[1182,322],"led_right":[1216,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"37":{"center":[1268,217],"number":[1268,162],"led_left":[1251,198],"led_right":[1285,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"38":{"center":[1266,296],"number":[1266,365],"led_left":[1250,322],"led_right":[1285,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"39":{"center":[1326,217],"number":[1327,162],"led_left":[1309,198],"led_right":[1343,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"40":{"center":[1326,296],"number":[1326,365],"led_left":[1309,322],"led_right":[1344,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"41":{"center":[1387,217],"number":[1385,162],"led_left":[1368,198],"led_right":[1403,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"42":{"center":[1385,296],"number":[1384,365],"led_left":[1368,322],"led_right":[1402,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"43":{"center":[1446,217],"number":[1443,162],"led_left":[1426,198],"led_right":[1462,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"44":{"center":[1445,295],"number":[1443,365],"led_left":[1426,322],"led_right":[1460,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"45":{"center":[1505,217],"number":[1502,162],"led_left":[1485,198],"led_right":[1521,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"46":{"center":[1505,296],"number":[1502,365],"led_left":[1486,322],"led_right":[1520,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"47":{"center":[1565,213],"number":[1562,162],"led_left":[1544,198],"led_right":[1580,198],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true},"48":{"center":[1564,296],"number":[1562,365],"led_left":[1545,322],"led_right":[1579,322],"hitbox":[34,56],"led_left_size":[8.1,4.7],"led_right_size":[8.1,4.7],"number_show":true}},"sfp":{"G1":{"center":[1699,233],"hitbox":[94,48],"label":[1700,170]},"G2":{"center":[1835,233],"hitbox":[94,48],"label":[1835,170]},"G3/TE3":{"center":[1696,320],"hitbox":[94,48],"label":[1695,380]},"G4/TE4":{"center":[1834,320],"hitbox":[94,48],"label":[1834,380]}},"status_leds":{"STAT":[854,54],"SYST":[854,109],"DUPLX":[962,54],"ACTV":[962,109],"SPEED":[1074,54],"STACK":[1074,114],"PoE":[1199,54]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":143,"y":0,"width":174,"height":150},"status_panel":{"show":true,"x":500,"y":20,"width":313,"height":102,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#42c5d7","label_color":"#42c5d7","title_color":"#42c5d7","background_color":"rgba(0,0,0,.35)","border_color":"#42c5d7","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu","temp","poe"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[107,39],"row2_key":[19,58],"row2_value":[107,58],"row3_key":[19,78],"row3_value":[107,78],"row4_key":[19,97],"row4_value":[107,97],"row5_key":[19,116],"row5_value":[107,116],"row6_key":[19,135],"row6_value":[107,135]}},"faceplate":{"fit":"fill","opacity":1,"show":true,"file":"submarine-48rj45-4sfp.png","source":"custom"},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":true,"x":1605,"y":25,"width":278,"height":55,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":16,"title_font_size":16,"font_weight":"bold","text_color":"#ffffff","label_color":"#eef2f4","title_color":"#f4f6f7","background_color":"rgba(0,0,0,.06)","border_color":"rgba(245,248,250,.75)","border_show":false,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}},"port_led_shape":"circle","port_label_color":"#eef7ff","status_leds":{"hidden":[]}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""}},
  "unifi-24p-rj45-2sfp.png": {"schema_version":2,"schema":"switch-vision-interactive-calibration-v1","model":"unifi-24p-rj45-2sfp","profile":"unifi_24p_rj45_2sfp","generated_by":"Switch Vision v2.4.0","image":{"file":"faceplates/unifi-24p-rj45-2sfp.png","width":2048,"height":448,"master":"unifi-24p-rj45-2sfp-v2.4.0","alpha_min":0,"alpha_max":255},"ports":{"1":{"center":[711,175],"number":[712,74],"led_left":[653,124],"led_right":[770,124],"hitbox":[104,71],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"2":{"center":[712,301],"number":[711,412],"led_left":[653,355],"led_right":[770,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"3":{"center":[868,175],"number":[865,74],"led_left":[806,124],"led_right":[926,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"4":{"center":[866,301],"number":[867,412],"led_left":[806,355],"led_right":[926,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"5":{"center":[1023,175],"number":[1021,74],"led_left":[1023,124],"led_right":[1082,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"6":{"center":[1024,301],"number":[1022,412],"led_left":[1023,355],"led_right":[1082,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"7":{"center":[1180,175],"number":[1178,74],"led_left":[1183,124],"led_right":[1238,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"8":{"center":[1179,301],"number":[1179,412],"led_left":[1118,355],"led_right":[1238,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"9":{"center":[1336,175],"number":[1330,74],"led_left":[1275,124],"led_right":[1396,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"10":{"center":[1336,301],"number":[1330,412],"led_left":[1275,355],"led_right":[1396,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"11":{"center":[1489,175],"number":[1490,74],"led_left":[1431,124],"led_right":[1551,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"12":{"center":[1491,301],"number":[1490,412],"led_left":[1431,355],"led_right":[1551,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"13":{"center":[1674,175],"number":[1670,74],"led_left":[1615,124],"led_right":[1735,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"14":{"center":[1674,301],"number":[1670,412],"led_left":[1615,355],"led_right":[1735,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"15":{"center":[1829,175],"number":[1823,74],"led_left":[1769,124],"led_right":[1889,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"16":{"center":[1830,301],"number":[1827,412],"led_left":[1769,355],"led_right":[1889,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"17":{"center":[1984,175],"number":[1982,74],"led_left":[1925,124],"led_right":[2045,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"18":{"center":[1986,301],"number":[1982,412],"led_left":[1925,355],"led_right":[2045,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"19":{"center":[2142,175],"number":[2134,74],"led_left":[2081,124],"led_right":[2202,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"20":{"center":[2141,301],"number":[2139,412],"led_left":[2081,355],"led_right":[2202,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"21":{"center":[2297,175],"number":[2295,74],"led_left":[2237,124],"led_right":[2358,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"22":{"center":[2295,301],"number":[2298,412],"led_left":[2237,355],"led_right":[2358,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"23":{"center":[2452,175],"number":[2449,74],"led_left":[2394,124],"led_right":[2514,124],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true},"24":{"center":[2450,301],"number":[2452,412],"led_left":[2394,355],"led_right":[2514,355],"hitbox":[104,72],"led_left_size":[12,9],"led_right_size":[12,9],"number_show":true}},"sfp":{"G1":{"center":[2770,166],"hitbox":[194,68],"label":[2769,85],"label_show":true,"led_left_size":[40,9],"led_left":[2731,224],"led_right":[2814,225],"led_right_size":[40,9]},"G2":{"center":[2775,306],"hitbox":[194,88],"label":[2769,396],"label_show":true,"led_right":[2814,358],"led_right_size":[40,9],"led_left":[2731,358],"led_left_size":[40,9]}},"status_leds":{"STAT":[674,84],"SYST":[674,149],"DUPLX":[852,84],"ACTV":[852,149],"SPEED":[1026,84],"STACK":[1204,84],"PoE":[1369,84]},"ui":{"logo":{"show":true,"file":"sv-logo.png","source":"default","x":-707,"y":49,"width":287,"height":338},"status_panel":{"show":true,"x":-182,"y":96,"width":643,"height":282,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":40,"title_font_size":16,"font_weight":"bold","text_color":"#000000","label_color":"#000000","title_color":"#000000","background_color":"rgba(0,0,0,.06)","border_color":"#000000","border_show":true,"field_order":{"switch":["model","ip","cpu","temp","poe","uptime","vendor","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["vendor","os","firmware","serial","stack","fans","psu"],"port":[],"sfp":[]},"fields":{"row1_key":[19,39],"row1_value":[237,39],"row2_key":[19,58],"row2_value":[237,58],"row3_key":[19,78],"row3_value":[237,78],"row4_key":[19,97],"row4_value":[237,97],"row5_key":[19,116],"row5_value":[237,116],"row6_key":[19,135],"row6_value":[237,135]}},"faceplate":{"fit":"fill","opacity":1,"show":true,"file":"unifi-24p-rj45-2sfp.png"},"calibration_button":{"show":true,"x":1888,"y":14,"width":138,"height":34,"anchor":"top_right"},"status_panel_2":{"show":false,"x":205,"y":75,"width":378,"height":305,"font_family":"SwitchVisionCustom, Arial, Helvetica, sans-serif","font_size":40,"title_font_size":16,"font_weight":"bold","text_color":"#000000","label_color":"#000000","title_color":"#000000","background_color":"transparent","border_color":"rgba(245,248,250,.75)","border_show":false,"title_mode":"custom","title_field":"model","custom_title":"","field_order":{"switch":["vendor","uptime","model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu"],"port":["vlan","desc","link","rx","tx","mode"],"sfp":["vlan","desc","link","rx","tx","mode"]},"hidden_fields":{"switch":["model","ip","cpu","temp","poe","os","firmware","serial","stack","fans","psu","vendor","uptime"],"port":[],"sfp":[]},"fields":{"row1_key":[24,55],"row1_value":[112,55],"row2_key":[24,72],"row2_value":[112,72],"row3_key":[24,89],"row3_value":[112,89],"row4_key":[24,106],"row4_value":[112,106],"row5_key":[24,123],"row5_value":[112,123],"row6_key":[24,140],"row6_value":[112,140]}},"port_led_shape":"rectangle","port_label_color":"#000000","port_number_color":"#000000","port_label_font_weight":"normal","port_number_font_size":40,"sfp_label_font_size":40,"status_leds":{"hidden":["STAT","SYST","DUPLX","ACTV","SPEED","STACK","PoE"],"text_color":"#000000","font_family":"Arial, Helvetica, sans-serif","font_size":16.5,"font_weight":"normal"}},"stack":{"enabled":false,"stack_id":"","uptime_source":"","members":{}},"management":{"switch_ip":""}}
};

function faceplateFactoryCalibrationForFile(value) {
  const filename = safeAssetFilename(String(value || "").split("/").pop());
  return SV_FACEPLATE_FACTORY_CALIBRATIONS[filename] || null;
}

function clonePlainData(value) {
  if (value === undefined) return undefined;
  return JSON.parse(JSON.stringify(value));
}

function applyCalibrationIdentity(factory, current) {
  const next = cloneCalibrationData(factory || {});
  const source = current && typeof current === "object" ? current : {};
  for (const key of ["profile", "profile_name", "profile_scope", "base_profile_name", "mirrored_from_profile"]) {
    if (source[key] !== undefined) next[key] = clonePlainData(source[key]);
  }
  next.stack = clonePlainData(source.stack || next.stack || { enabled: false, stack_id: "", uptime_source: "", members: {} });
  next.management = clonePlainData(source.management || next.management || { switch_ip: "" });
  return next;
}

function factoryProfileNameForCalibration(cal) {
  const explicit = String(cal?.profile || "").trim();
  if (factoryCalibrationForProfile(explicit)) return explicit;

  // Older/custom profiles may retain the internal model identifier even when
  // the optional factory-profile field is missing. Use that stable identifier
  // as a model-aware Reset All fallback instead of the generic 48/4 layout.
  const model = String(cal?.model || "").trim().toLowerCase();
  if (model.includes("2960x-24p")) return "cisco_2960x_24p";
  if (model.includes("2960s-48p") || model.includes("2960x-48p")) return "cisco_2960s_48p";
  if (model.includes("3560cg-8pc")) return "cisco_3560cg_8pc";
  if (model.includes("3650-48p")) return "default_cisco_48_port";
  return "";
}
const layout = {"schema":"switch-vision-layout","version":"source-0.1","logo":{"file":"sv-logo.png","x":143,"y":-1,"width":187,"height":188,"fallback_text":"SV","fallback_x":72,"fallback_y":99},"model_text":{"text":"Catalyst 3650 48 PoE+ 2X10G","x":1742,"y":48,"anchor":"middle"},"status":{"radius":6.3,"mode_radius":7.2,"labels":{"MODE":{"dx":-34,"dy":0,"anchor":"end"},"STAT":{"dx":24,"dy":0,"anchor":"start"},"SYST":{"dx":24,"dy":0,"anchor":"start"},"DUPLX":{"dx":24,"dy":0,"anchor":"start"},"ACTV":{"dx":24,"dy":0,"anchor":"start"},"SPEED":{"dx":24,"dy":0,"anchor":"start"},"STACK":{"dx":24,"dy":0,"anchor":"start"},"PoE":{"dx":24,"dy":0,"anchor":"start"}}},"ports":{"odd":7,"even":-7,"r":3.1},"sfp":{"r":3.3,"labels":{"G1":{"x":1709,"y":188,"text":"G1"},"G2":{"x":1838,"y":188,"text":"G2"},"G3/TE3":{"x":1710,"y":402,"text":"G3 / TE3"},"G4/TE4":{"x":1839,"y":402,"text":"G4 / TE4"}}}};
function e(name) {
  return document.createElementNS("http://www.w3.org/2000/svg", name);
}

function circle(svg, x, y, r, cls) {
  const el = e("circle");
  el.setAttribute("class", cls);
  el.setAttribute("cx", x);
  el.setAttribute("cy", y);
  el.setAttribute("r", r);
  svg.appendChild(el);
  return el;
}

function defaultPortLedRectangleSize(radius = layout.ports?.r) {
  const r = Number.isFinite(Number(radius)) ? Number(radius) : 3.1;
  return [
    Math.max(2, Math.round(r * 2.6 * 10) / 10),
    Math.max(2, Math.round(r * 1.5 * 10) / 10)
  ];
}

function normalisePortLedRectangleSize(value, radius = layout.ports?.r) {
  const fallback = defaultPortLedRectangleSize(radius);
  if (!Array.isArray(value) || value.length !== 2) return fallback;
  const width = Number(value[0]);
  const height = Number(value[1]);
  return [
    Number.isFinite(width) && width > 0 ? Math.max(2, Math.round(width * 10) / 10) : fallback[0],
    Number.isFinite(height) && height > 0 ? Math.max(2, Math.round(height * 10) / 10) : fallback[1]
  ];
}

function portLedSizeKey(part) {
  if (part === "led_left") return "led_left_size";
  if (part === "led_right") return "led_right_size";
  return null;
}

function portLedColourOverride(activeCalibration, part) {
  const ui = uiFromCalibration(activeCalibration);
  const field = part === "led_left" ? "link_led_color" : (part === "led_right" ? "activity_led_color" : "");
  const value = field ? String(ui?.[field] || "").trim() : "";
  return /^#[0-9a-f]{3}(?:[0-9a-f]{3})?$/i.test(value) ? normaliseHexColour(value) : "";
}

function applyPortLedColourOverride(element, activeCalibration, part, cls) {
  if (!element || String(cls || "") === "cv-led-off") return element;
  const colour = portLedColourOverride(activeCalibration, part);
  if (!colour) return element;
  element.style.fill = colour;
  element.style.filter = `drop-shadow(0 0 2.5px ${colour})`;
  return element;
}

function portLed(svg, x, y, r, cls, activeCalibration, rectangleSize = null, part = "") {
  const shape = String(uiFromCalibration(activeCalibration)?.port_led_shape || "circle").toLowerCase();
  let element;
  if (shape === "rectangle") {
    const [width, height] = normalisePortLedRectangleSize(rectangleSize, r);
    element = rect(svg, x - width / 2, y - height / 2, width, height, 0.8, cls);
  } else {
    element = circle(svg, x, y, r, cls);
  }
  return applyPortLedColourOverride(element, activeCalibration, part, cls);
}

function sfpLedFallbackPoint(sfp, part) {
  const center = Array.isArray(sfp?.center) ? sfp.center : [0, 0];
  const x = Number(center[0] || 0);
  const y = Number(center[1] || 0);
  return part === "led_right" ? [x + 24, y - 31] : [x - 24, y - 31];
}

function sfpLedPoint(sfp, part, create = false) {
  if (!sfp || !["led_left", "led_right"].includes(part)) return null;
  if (Array.isArray(sfp[part]) && sfp[part].length >= 2) return sfp[part];
  const fallback = sfpLedFallbackPoint(sfp, part);
  if (create) sfp[part] = [...fallback];
  return create ? sfp[part] : fallback;
}

function sfpLedRectangleSize(sfp, part, create = false) {
  const sizeKey = portLedSizeKey(part);
  if (!sfp || !sizeKey) return null;
  const existing = sfp[sizeKey];
  const size = normalisePortLedRectangleSize(existing, layout.sfp?.r);
  if (create) sfp[sizeKey] = [...size];
  return create ? sfp[sizeKey] : size;
}

function sfpLed(svg, sfp, part, cls, activeCalibration) {
  const point = sfpLedPoint(sfp, part, false);
  if (!point) return null;
  const hasExplicitGeometry = Array.isArray(sfp?.[part]) || Array.isArray(sfp?.[portLedSizeKey(part)]);
  if (hasExplicitGeometry) {
    return portLed(svg, point[0], point[1], layout.sfp.r, cls, activeCalibration, sfpLedRectangleSize(sfp, part, false), part);
  }
  return applyPortLedColourOverride(circle(svg, point[0], point[1], layout.sfp.r, cls), activeCalibration, part, cls);
}

function text(svg, x, y, value, cls, anchor = "middle") {
  const el = e("text");
  el.setAttribute("class", cls);
  el.setAttribute("x", x);
  el.setAttribute("y", y);
  el.setAttribute("text-anchor", anchor);
  el.textContent = value;
  svg.appendChild(el);
  return el;
}

function image(svg, x, y, w, h, href, cls = "cv-logo-img") {
  const el = e("image");
  el.setAttribute("class", cls);
  el.setAttribute("x", x);
  el.setAttribute("y", y);
  el.setAttribute("width", w);
  el.setAttribute("height", h);
  el.setAttribute("href", href);
  el.setAttribute("preserveAspectRatio", "xMidYMid meet");
  svg.appendChild(el);
  return el;
}


function rect(svg, x, y, w, h, rx, cls) {
  const el = e("rect");
  el.setAttribute("class", cls);
  el.setAttribute("x", x);
  el.setAttribute("y", y);
  el.setAttribute("width", w);
  el.setAttribute("height", h);
  el.setAttribute("rx", rx);
  el.setAttribute("ry", rx);
  svg.appendChild(el);
  return el;
}

function line(svg, x1, y1, x2, y2, cls) {
  const el = e("line");
  el.setAttribute("class", cls);
  el.setAttribute("x1", x1);
  el.setAttribute("y1", y1);
  el.setAttribute("x2", x2);
  el.setAttribute("y2", y2);
  svg.appendChild(el);
  return el;
}

function applySvgStyle(el, styles = {}) {
  if (!el) return el;
  for (const [key, value] of Object.entries(styles || {})) {
    if (value !== null && value !== undefined && value !== "") el.style[key] = value;
  }
  return el;
}

function entityState(hass, entityId) {
  const entity = hass && entityId ? hass.states[entityId] : undefined;
  return entity ? String(entity.state).toLowerCase().trim() : null;
}

function rawEntityState(hass, entityId) {
  const entity = hass && entityId ? hass.states[entityId] : undefined;
  return entity ? String(entity.state).trim() : null;
}

function portRange(port) {
  const n = Number(port);
  if (n >= 1 && n <= 24) return "1_24";
  if (n >= 25 && n <= 48) return "25_48";
  return "";
}

function templateEntity(template, port) {
  const portKey = String(port);
  return String(template)
    .replaceAll("{port}", portKey)
    .replaceAll("{range}", portRange(port))
    .replaceAll("{range_dash}", portRange(port).replace("_", "-"));
}

function mappedPortNumber(config, port) {
  const n = Number(port);
  const offset = Number(config?.port_entity_offset || 0);
  return Number.isFinite(n) && Number.isFinite(offset) ? n + offset : port;
}

function displayedPortNumber(config, port) {
  const n = Number(port);
  const offset = Number(config?.port_label_offset || 0);
  return Number.isFinite(n) && Number.isFinite(offset) ? n + offset : port;
}

function portLabelIsRemapped(config) {
  const offset = Number(config?.port_label_offset || 0);
  return Number.isFinite(offset) && offset !== 0;
}

function portEntity(config, port, type = "status") {
  const key = `${type}_entity`;
  const mappedPort = mappedPortNumber(config, port);
  const portKey = String(mappedPort);

  if (config.ports?.[portKey]?.[key]) {
    return config.ports[portKey][key];
  }

  const templateKey = `${type}_entity_template`;
  if (config[templateKey]) {
    return templateEntity(config[templateKey], mappedPort);
  }

  const prefix = config[`${type}_entity_prefix`];
  const suffix = config[`${type}_entity_suffix`];

  if (prefix !== undefined && suffix !== undefined) {
    return `${prefix}${portKey}${suffix}`;
  }

  if (type === "speed" && config.auto_speed_entity !== false) {
    const member = String(config.member || "sw1").toLowerCase();
    return `sensor.${member}_port_${portKey}_speed_mbps`;
  }

  return null;
}

function listFromConfig(value, fallback) {
  if (Array.isArray(value)) return value.map(v => String(v).toLowerCase().trim());
  if (typeof value === "string") return value.split(",").map(v => v.toLowerCase().trim()).filter(Boolean);
  return fallback;
}

function unifiRuntime(config) {
  const value = config?.__unifi_runtime;
  return value && typeof value === "object" && Array.isArray(value.ports) ? value : null;
}

function unifiPortByIndex(config, index) {
  const runtime = unifiRuntime(config);
  if (!runtime) return null;
  const wanted = Number(index);
  return runtime.ports.find((item) => Number(item?.idx) === wanted) || null;
}

function unifiAccessPort(config, port) {
  if (String(config?.data_source || "").toLowerCase() !== "unifi_api") return null;
  return unifiPortByIndex(config, mappedPortNumber(config, port));
}

function unifiSfpPort(config, port) {
  if (String(config?.data_source || "").toLowerCase() !== "unifi_api") return null;
  const offset = Math.max(0, Number(config?.unifi_sfp_port_offset || config?.unifi_rj45_ports || 0));
  return unifiPortByIndex(config, offset + Number(port));
}

function linkStateIsUp(value) {
  return ["up", "on", "1", "true", "connected", "active", "online", "linkup", "link_up"].includes(String(value || "").toLowerCase());
}

function portIsUp(hass, config, port) {
  if (config.demo) return true;

  const unifi = unifiAccessPort(config, port);
  if (unifi) return linkStateIsUp(unifi.state);

  const state = entityState(hass, portEntity(config, port, "status"));

  const upValues = listFromConfig(config.status_up_values, [
    "up", "on", "1", "true", "connected", "active", "online", "linkup", "link_up"
  ]);

  const downValues = listFromConfig(config.status_down_values, [
    "down", "off", "0", "2", "false", "disconnected", "inactive", "offline", "unavailable", "unknown", "linkdown", "link_down"
  ]);

  if (state === null) return false;
  if (downValues.includes(state)) return false;
  if (upValues.includes(state)) return true;

  return config.unknown_is_up === true;
}

function portSpeed(hass, config, port) {
  if (config.demo) {
    const n = Number(port);
    if ([7, 8, 31].includes(n)) return "10";
    if ([14, 27, 33].includes(n)) return "100";
    return "1000";
  }

  const unifi = unifiAccessPort(config, port);
  if (unifi) return linkStateIsUp(unifi.state) && unifi.speed_mbps != null ? String(unifi.speed_mbps) : "";

  const raw = rawEntityState(hass, portEntity(config, port, "speed"));

  if (raw && !["unknown", "unavailable", "none", "null"].includes(raw.toLowerCase())) {
    return raw;
  }

  // Older/limited IF-MIB agents may expose ifSpeed but omit ifHighSpeed.
  // Discovery names that fallback sensor "Speed Bps"; convert it here so all
  // existing utilization and presentation code continues to consume Mbps.
  const member = normalizeMember(config);
  const mappedPort = mappedPortNumber(config, port);
  const rawBps = rawEntityState(hass, `sensor.${member}_port_${mappedPort}_speed_bps`);
  const bps = Number(String(rawBps || "").replace(/,/g, ""));
  if (Number.isFinite(bps) && bps > 0) return String(bps / 1000000);

  return "";
}


function isJuniperEx3300(config) {
  // Discovery-generated cards expose the detected hardware as switch_model.
  // Keep the older fields as fallbacks for manually-authored cards.
  const model = String(
    config?.switch_model || config?.device_model || config?.model || config?.detected_switch_model || ""
  ).toLowerCase();
  const vendor = String(config?.device_manufacturer || config?.manufacturer || config?.vendor || "").toLowerCase();
  return model.includes("ex3300") || (vendor.includes("juniper") && model.includes("3300"));
}

function configuredPortCountAllows(config, field, port) {
  if (!config) return true;

  // setConfig supplies numeric defaults for compatibility, so looking only at
  // the merged config would make an omitted count look like an explicit zero.
  // Preserve legacy/manual cards when the field was omitted, while still
  // honouring an explicit `0` (for example a UniFi switch with no SFP ports).
  const rawConfig = config.__raw_config;
  const hasRawConfig = rawConfig && typeof rawConfig === "object";
  if (hasRawConfig && !Object.prototype.hasOwnProperty.call(rawConfig, field)) return true;
  if (!hasRawConfig && !Object.prototype.hasOwnProperty.call(config, field)) return true;

  const raw = hasRawConfig ? rawConfig[field] : config[field];
  if (raw === null || raw === undefined || raw === "") return true;
  const count = Number(raw);
  if (!Number.isFinite(count) || count < 0) return true;
  return count > 0 && Number(port) <= count;
}

function sfpVisibleLabel(config, sfpPort, fallback) {
  if (isJuniperEx3300(config) && sfpPort >= 1 && sfpPort <= 4) return `TE${sfpPort}`;
  return fallback;
}

function sfpEntity(config, member, port) {
  const m = String(member).toLowerCase();
  return `sensor.${m}_sfp_10g_${port}_status`;
}

function sfpStatusEntities(config, member, port) {
  const m = String(member).toLowerCase();
  const n = Number(port);

  // Prefer an explicit template emitted by Discovery, then clean
  // Switch Vision / SNMP2MQTT entity IDs. Keep older entity IDs as fallbacks.
  const configured = config?.sfp_status_entity_template
    ? templateEntity(config.sfp_status_entity_template, n)
    : null;
  if (n === 1 || n === 2) {
    return [
      configured,
      `sensor.${m}_sfp_10g_${n}_status`,
      `sensor.${m}_sfp_1g_${n}_status`,
      `sensor.${m}_uplink_${n}_status`,
      `sensor.cisco_3650_${m}_uplinks_${m}_uplink_${n}_status`,
      `sensor.cisco_3650_${m}_uplink_${n}_status`
    ];
  }

  return [
    configured,
    `sensor.${m}_sfp_10g_${n}_status`,
    `sensor.${m}_sfp_1g_${n}_status`,
    `sensor.cisco_3650_${m}_sfp_10g_${n}_status`,
    `sensor.cisco_3650_${m}_uplinks_${m}_sfp_10g_${n}_status`,
    `sensor.cisco_3650_${m}_sfp_10g_${n}_${m}_sfp_10g_${n}_status`
  ];
}

function sfpIsUp(hass, config, port) {
  if (config.demo) return true;

  const unifi = unifiSfpPort(config, port);
  if (unifi) return linkStateIsUp(unifi.state);

  for (const entityId of sfpStatusEntities(config, config.member || "sw1", port)) {
    if (!entityId) continue;
    const state = entityState(hass, entityId);
    if (state === "1" || state === "up" || state === "on" || state === "true") return true;
    if (state === "2" || state === "down" || state === "off" || state === "false") return false;
  }

  return false;
}


function normalizePanelMember(config) {
  return String(config.member || "sw1").toLowerCase();
}

function usableValue(value) {
  if (value === null || value === undefined) return null;
  const raw = String(value).trim();
  if (!raw) return null;
  const low = raw.toLowerCase();
  if (["unknown", "unavailable", "none", "null", "nan"].includes(low)) return null;
  return raw;
}


function explicitConfigValue(config, keys) {
  const raw = config?.__raw_config && typeof config.__raw_config === "object" ? config.__raw_config : config;
  for (const key of keys) {
    if (!Object.prototype.hasOwnProperty.call(raw || {}, key)) continue;
    const value = usableValue(raw[key]);
    if (value !== null) return value;
  }
  return null;
}

function managementIpFromCalibration(cal) {
  const management = cal?.management && typeof cal.management === "object" ? cal.management : {};
  return usableValue(
    management.switch_ip ??
    management.management_ip ??
    management.mgmt_ip ??
    management.host_ip ??
    cal?.switch_ip ??
    cal?.management_ip
  );
}

function firstEntityValue(hass, entityIds) {
  for (const entityId of entityIds.filter(Boolean)) {
    const value = usableValue(rawEntityState(hass, entityId));
    if (value !== null) return value;
  }
  return null;
}

function configuredEntity(config, port, type) {
  const key = `${type}_entity`;
  const mappedPort = mappedPortNumber(config, port);
  const portKey = String(mappedPort);

  if (config.ports?.[portKey]?.[key]) return config.ports[portKey][key];

  const templateKey = `${type}_entity_template`;
  if (config[templateKey]) return templateEntity(config[templateKey], mappedPort);

  return null;
}

function switchConfiguredEntity(config, type) {
  const key = `${type}_entity`;
  return config[key] || null;
}

function trimText(value, max = 24) {
  const v = usableValue(value) || "—";
  return v.length > max ? `${v.slice(0, max - 1)}…` : v;
}

function formatSpeedMbps(raw) {
  const value = usableValue(raw);
  if (!value) return null;

  const n = Number(String(value).replace(/[^0-9.]/g, ""));
  if (!Number.isFinite(n) || n <= 0) return null;

  if (n >= 1000) {
    const gbps = Number((n / 1000).toFixed(3));
    return `${gbps}G`;
  }

  return `${Math.round(n)}M`;
}

function speedLabel(raw, up = true) {
  const value = usableValue(raw);
  if (!up) return "Down";
  if (!value) return "—";

  const formatted = formatSpeedMbps(value);
  return formatted ? `${formatted} Full` : value;
}

function formatBps(bps) {
  if (bps === null || bps === undefined) return "—";
  const n = Number(bps);
  if (!Number.isFinite(n)) return "—";

  // Traffic counters arrive as byte counters, then updateRateState converts the
  // delta to bits/sec for LED scaling. For the status window, display user-facing
  // throughput as bytes/sec instead of bits/sec.
  const bytesPerSecond = n / 8;

  if (bytesPerSecond <= 0) return "0 B/s";
  if (bytesPerSecond >= 1000000000) return `${(bytesPerSecond / 1000000000).toFixed(bytesPerSecond >= 10000000000 ? 0 : 2)} GB/s`;
  if (bytesPerSecond >= 1000000) return `${(bytesPerSecond / 1000000).toFixed(bytesPerSecond >= 100000000 ? 0 : 1)} MB/s`;
  if (bytesPerSecond >= 1000) return `${(bytesPerSecond / 1000).toFixed(bytesPerSecond >= 100000 ? 0 : 1)} KB/s`;
  return `${Math.round(bytesPerSecond)} B/s`;
}


function cleanVlanValue(value) {
  const raw = usableValue(value);
  if (!raw) return null;
  const up = raw.toUpperCase();

  // Trunk ports can report TRUNK directly from the VLAN ID sensor.
  if (up.includes("TRUNK")) return "TRUNK";

  // Access ports can report either a plain number or a VLAN-labelled string.
  const vlanMatch = raw.match(/\bVLAN\s*([0-9]{1,4})\b/i) || raw.match(/\bVID\s*([0-9]{1,4})\b/i);
  if (vlanMatch) return vlanMatch[1];

  if (/^[0-9]{1,4}$/.test(raw)) return raw;

  return null;
}

function cleanVlanList(value) {
  const raw = usableValue(value);
  if (!raw) return [];
  const numbers = String(raw).match(/\b\d{1,4}\b/g) || [];
  return [...new Set(numbers.map((item) => String(Number(item))).filter((item) => Number(item) >= 1 && Number(item) <= 4094))]
    .sort((a, b) => Number(a) - Number(b));
}

function isJuniperPortPresentation(hass, config) {
  const member = normalizePanelMember(config).toLowerCase();
  const configured = [
    config?.switch_model,
    config?.detected_switch_model,
    config?.exact_model,
    config?.model,
    config?.vendor,
    config?.manufacturer,
    config?.os,
  ].filter(Boolean).join(" ");
  if (/Juniper|JUNOS|EX3300/i.test(configured)) return true;

  const discovered = firstEntityValue(hass, [
    `sensor.${member}_vendor`,
    `sensor.${member}_manufacturer`,
    `sensor.${member}_model`,
    `sensor.${member}_system_description`,
    `sensor.${member}_sysdescr`,
  ]);
  return /Juniper|JUNOS|EX3300/i.test(String(discovered || ""));
}

function cleanTrunkStatus(value) {
  const raw = usableValue(value);
  if (!raw) return null;
  const up = raw.toUpperCase().replace(/[\s_-]+/g, "");

  // CISCO-VTP-MIB::vlanTrunkPortDynamicStatus
  // 1 = trunking, 2 = notTrunking. This is now the authority for trunk detection.
  if (raw === "1" || up === "TRUNK" || up === "TRUNKING") return true;
  if (raw === "2" || up === "NOTTRUNK" || up === "NOTTRUNKING" || up === "ACCESS" || up === "STATIC") return false;

  return null;
}

function deriveVlan(vlanCandidate, trunkStatusCandidate = null) {
  const trunk = cleanTrunkStatus(trunkStatusCandidate);
  if (trunk === true) return "TRUNK";

  const vlan = cleanVlanValue(vlanCandidate);
  if (vlan) return vlan;

  return "—";
}

function normalizeDescription(value) {
  const raw = usableValue(value);
  if (!raw) return "—";
  return raw;
}

function selectedPortDetails(hass, config, port) {
  const internalPort = Number(port);
  const n = Number(mappedPortNumber(config, internalPort));
  const member = normalizePanelMember(config);
  const range = portRange(n);

  // Prefer the mapped entity port, while retaining the internal calibration
  // position as a compatibility fallback for older hand-written cards.
  const staticPort = config.ports?.[String(n)] || config.ports?.[String(internalPort)] || {};

  const modeCandidate = usableValue(staticPort.mode) || firstEntityValue(hass, [
    configuredEntity(config, internalPort, "mode"),
    `sensor.${member}_port_${n}_mode`
  ]);

  const vlanCandidate = usableValue(staticPort.native_vlan) || usableValue(staticPort.vlan) || firstEntityValue(hass, [
    // v0.4.51 real VLAN sensors. Prefer these before any alias/description guessing.
    configuredEntity(config, internalPort, "vlan_id"),
    configuredEntity(config, internalPort, "vlan"),
    `sensor.${member}_port_${n}_vlan_id`,
    `sensor.${member}_port_${n}_vlan`,

    // Legacy compatibility fallbacks.
    `sensor.cisco_3650_${member}_ports_${range}_${member}_port_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_ports_${range}_${member}_port_${n}_vlan`,
    `sensor.cisco_3650_${member}_port_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_port_${n}_vlan`
  ]);

  const allowedVlansCandidate = usableValue(staticPort.allowed_vlans) || usableValue(staticPort.vlans) || firstEntityValue(hass, [
    configuredEntity(config, internalPort, "allowed_vlans"),
    configuredEntity(config, internalPort, "vlans"),
    `sensor.${member}_port_${n}_allowed_vlans`,
    `sensor.${member}_port_${n}_vlans`
  ]);

  const trunkStatusCandidate = usableValue(staticPort.trunk_status) || modeCandidate || firstEntityValue(hass, [
    // CISCO-VTP-MIB::vlanTrunkPortDynamicStatus. 1 = trunking, 2 = notTrunking.
    configuredEntity(config, internalPort, "trunk_status"),
    `sensor.${member}_port_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_ports_${range}_${member}_port_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_port_${n}_trunk_status`
  ]);

  const descriptionRaw =
    usableValue(staticPort.alias) ||
    usableValue(staticPort.description) ||
    usableValue(staticPort.name) ||
    firstEntityValue(hass, [
      // Alias first.
      configuredEntity(config, internalPort, "alias"),
      `sensor.${member}_port_${n}_alias`,
      `sensor.cisco_3650_${member}_port_${n}_alias`,

      // Description second.
      configuredEntity(config, internalPort, "description"),
      `sensor.${member}_port_${n}_description`,
      `sensor.${member}_port_${n}_desc`,
      `sensor.cisco_3650_${member}_port_${n}_description`,
      `sensor.cisco_3650_${member}_port_${n}_desc`,

      // Name last.
      configuredEntity(config, internalPort, "name"),
      `sensor.${member}_port_${n}_name`,
      `sensor.cisco_3650_${member}_port_${n}_name`
    ]);

  const speed = portSpeed(hass, config, internalPort);
  const up = portIsUp(hass, config, internalPort);

  const trunk = cleanTrunkStatus(trunkStatusCandidate) === true;
  const nativeVlan = cleanVlanValue(vlanCandidate);
  const isJuniper = isJuniperPortPresentation(hass, config);
  const allowedVlans = cleanVlanList(allowedVlansCandidate);
  const juniperVlans = allowedVlans.length ? allowedVlans : (nativeVlan ? [nativeVlan] : []);

  return {
    isJuniper,
    mode: trunk ? "TRUNK" : "ACCESS",
    vlanLabel: isJuniper && juniperVlans.length > 1 ? "VLANS" : "VLAN",
    vlan: isJuniper
      ? trimText(juniperVlans.length ? juniperVlans.join(", ") : (trunk ? "TRUNK" : "—"), 20)
      : trimText(deriveVlan(vlanCandidate, trunkStatusCandidate), 12),
    description: trimText(normalizeDescription(descriptionRaw), 22),
    link: speedLabel(speed, up)
  };
}

function selectedSfpDetails(hass, config, port) {
  const n = Number(port);
  const member = normalizePanelMember(config);
  const up = sfpIsUp(hass, config, n);
  const staticSfp = config.sfp?.[String(n)] || {};

  const modeCandidate = usableValue(staticSfp.mode) || firstEntityValue(hass, [
    configuredEntity(config, n, "sfp_vlan_mode"),
    configuredEntity(config, n, "sfp_mode"),
    `sensor.${member}_sfp_10g_${n}_vlan_mode`,
    `sensor.${member}_uplink_${n}_vlan_mode`,
    `sensor.${member}_sfp_10g_${n}_mode`,
    `sensor.${member}_uplink_${n}_mode`
  ]);

  const vlanCandidate = usableValue(staticSfp.native_vlan) || usableValue(staticSfp.vlan) || firstEntityValue(hass, [
    configuredEntity(config, n, "sfp_native_vlan"),
    configuredEntity(config, n, "native_vlan"),
    `sensor.${member}_sfp_10g_${n}_native_vlan`,
    `sensor.${member}_uplink_${n}_native_vlan`,
    configuredEntity(config, n, "sfp_vlan_id"),
    configuredEntity(config, n, "sfp_vlan"),
    `sensor.${member}_sfp_10g_${n}_vlan_id`,
    `sensor.${member}_sfp_10g_${n}_vlan`,
    `sensor.${member}_uplink_${n}_vlan_id`,
    `sensor.${member}_uplink_${n}_vlan`,
    `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_vlan`,
    `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_vlan`,
    `sensor.cisco_3650_${member}_sfp_10g_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_sfp_10g_${n}_vlan`,
    `sensor.cisco_3650_${member}_uplink_${n}_vlan_id`,
    `sensor.cisco_3650_${member}_uplink_${n}_vlan`
  ]);

  const trunkStatusCandidate = usableValue(staticSfp.trunk_status) || modeCandidate || firstEntityValue(hass, [
    // CISCO-VTP-MIB::vlanTrunkPortDynamicStatus. 1 = trunking, 2 = notTrunking.
    configuredEntity(config, n, "sfp_trunk_status"),
    configuredEntity(config, n, "trunk_status"),
    `sensor.${member}_sfp_10g_${n}_trunk_status`,
    `sensor.${member}_uplink_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_sfp_10g_${n}_trunk_status`,
    `sensor.cisco_3650_${member}_uplink_${n}_trunk_status`
  ]);

  const descriptionRaw =
    usableValue(staticSfp.alias) ||
    usableValue(staticSfp.description) ||
    usableValue(staticSfp.name) ||
    firstEntityValue(hass, [
      // Alias first.
      configuredEntity(config, n, "sfp_alias"),
      `sensor.${member}_sfp_10g_${n}_alias`,
      `sensor.${member}_uplink_${n}_alias`,
      `sensor.cisco_3650_${member}_sfp_10g_${n}_alias`,
      `sensor.cisco_3650_${member}_uplink_${n}_alias`,
      `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_alias`,
      `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_alias`,

      // Description second.
      configuredEntity(config, n, "sfp_description"),
      `sensor.${member}_sfp_10g_${n}_description`,
      `sensor.${member}_uplink_${n}_description`,
      `sensor.cisco_3650_${member}_sfp_10g_${n}_description`,
      `sensor.cisco_3650_${member}_uplink_${n}_description`,
      `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_description`,
      `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_description`,

      // Name last.
      configuredEntity(config, n, "sfp_name"),
      `sensor.${member}_sfp_10g_${n}_name`,
      `sensor.${member}_uplink_${n}_name`,
      `sensor.cisco_3650_${member}_sfp_10g_${n}_name`,
      `sensor.cisco_3650_${member}_uplink_${n}_name`,
      `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_name`,
      `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_name`
    ]);

  const isJuniper = isJuniperPortPresentation(hass, config);
  // Juniper exposes a dedicated native VLAN alongside MODE, so keep the
  // numeric native/PVID value in the VLAN row. Cisco reports trunk state
  // separately; prefer that authority before the numeric PVID so active
  // trunks display TRUNK instead of their native VLAN.
  const sfpVlan = isJuniper
    ? (cleanVlanValue(vlanCandidate) || deriveVlan(vlanCandidate, trunkStatusCandidate))
    : deriveVlan(vlanCandidate, trunkStatusCandidate);
  const sfpMode = cleanTrunkStatus(modeCandidate) === true
    ? "TRUNK"
    : cleanTrunkStatus(modeCandidate) === false
      ? "ACCESS"
      : (usableValue(modeCandidate) || "—");

  return {
    isJuniper,
    vlan: trimText(sfpVlan, 10),
    mode: trimText(sfpMode, 10),
    description: trimText(normalizeDescription(descriptionRaw), 22),
    link: (() => {
      const unifi = unifiSfpPort(config, n);
      if (unifi) return speedLabel(unifi.speed_mbps, up);
      return up ? "10G Full" : "Down";
    })()
  };
}

function switchPoeActive(hass, config) {
  const runtime = unifiRuntime(config);
  if (runtime) {
    return runtime.ports.some((port) => String(port?.poe?.state || "").toUpperCase() === "UP");
  }
  const member = normalizePanelMember(config).toLowerCase();
  const poeUsed = firstEntityValue(hass, [
    `sensor.${member}_poe_used_mw`,
    `sensor.${member}_poe_used`,
    `sensor.cisco_3650_${member}_poe_used_mw`,
    `sensor.cisco_3650_${member}_poe_used`
  ]);

  if (!poeUsed) return false;

  const numeric = Number(String(poeUsed).replace(/[^0-9.]/g, ""));
  return Number.isFinite(numeric) && numeric > 0;
}

function formatPercent(value) {
  const raw = usableValue(value);
  if (!raw) return "—";
  const text = String(raw).trim();
  if (text.includes("%")) return text;
  const n = Number(text.replace(/[^0-9.-]/g, ""));
  if (!Number.isFinite(n)) return text;
  return `${Math.round(n)}%`;
}

function formatTemperature(value) {
  const raw = usableValue(value);
  if (!raw) return "—";
  const text = String(raw).trim();
  if (/°|c|f/i.test(text)) return text;
  const n = Number(text.replace(/[^0-9.-]/g, ""));
  if (!Number.isFinite(n)) return text;
  return `${Math.round(n)}°C`;
}

function formatUptime(value) {
  const raw = usableValue(value);
  if (!raw) return "—";
  const text = String(raw).trim();

  // Net-SNMP often returns Timeticks as: "(123456789) 14 days, 6:56:07.89".
  const daysMatch = text.match(/(\d+)\s+days?,\s*(\d+):(\d+):(\d+)/i);
  if (daysMatch) return `${daysMatch[1]}d ${daysMatch[2]}h ${daysMatch[3]}m`;

  const timeMatch = text.match(/(?:(\d+)\s+days?,\s*)?(\d+):(\d+):(\d+)/i);
  if (timeMatch) {
    const d = Number(timeMatch[1] || 0);
    const h = Number(timeMatch[2] || 0);
    const m = Number(timeMatch[3] || 0);
    return d > 0 ? `${d}d ${h}h ${m}m` : `${h}h ${m}m`;
  }

  // Raw SNMP Timeticks are hundredths of a second.
  const n = Number(text.replace(/[^0-9.]/g, ""));
  if (Number.isFinite(n) && n > 0) {
    let seconds = Math.floor(n / 100);
    const days = Math.floor(seconds / 86400);
    seconds %= 86400;
    const hours = Math.floor(seconds / 3600);
    seconds %= 3600;
    const minutes = Math.floor(seconds / 60);
    return days > 0 ? `${days}d ${hours}h ${minutes}m` : `${hours}h ${minutes}m`;
  }

  return text;
}

function formatUptimeSeconds(value) {
  const n = Number(value);
  if (!Number.isFinite(n) || n < 0) return "—";
  let seconds = Math.floor(n);
  const days = Math.floor(seconds / 86400);
  seconds %= 86400;
  const hours = Math.floor(seconds / 3600);
  seconds %= 3600;
  const minutes = Math.floor(seconds / 60);
  return days > 0 ? `${days}d ${hours}h ${minutes}m` : `${hours}h ${minutes}m`;
}

function defaultStackProfile() {
  return {
    enabled: false,
    stack_id: "",
    uptime_source: "",
    members: {}
  };
}

function normaliseStackUptimeMode(value) {
  const mode = String(value || "standalone").trim().toLowerCase().replace(/[\s-]+/g, "_");
  if (["inherit", "inherited", "inherit_stack", "inherited_from_stack", "stack_inherit", "shared"].includes(mode)) return "inherit_stack";
  if (["stack_source", "source", "primary", "shared_source"].includes(mode)) return "stack_source";
  return "standalone";
}

function ensureCalibrationStack(cal) {
  if (!cal) return cal;
  const defaults = defaultStackProfile();
  const stack = cal.stack && typeof cal.stack === "object" && !Array.isArray(cal.stack) ? cal.stack : {};
  stack.enabled = stack.enabled === true;
  stack.stack_id = String(stack.stack_id || stack.id || defaults.stack_id || "");
  stack.uptime_source = String(stack.uptime_source || defaults.uptime_source || "");
  stack.members = stack.members && typeof stack.members === "object" && !Array.isArray(stack.members) ? stack.members : {};

  for (const [key, value] of Object.entries(stack.members)) {
    const member = value && typeof value === "object" && !Array.isArray(value) ? value : {};
    member.member_number = Number(member.member_number ?? member.number ?? "") || Number(String(key).replace(/[^0-9]/g, "")) || "";
    member.uptime_mode = normaliseStackUptimeMode(member.uptime_mode || member.mode);
    member.uptime_source = String(member.uptime_source || "");
    stack.members[String(key).toUpperCase()] = member;
    if (String(key).toUpperCase() !== key) delete stack.members[key];
  }

  cal.stack = stack;
  return cal;
}

function stackFromCalibration(cal) {
  return ensureCalibrationStack(cal)?.stack || defaultStackProfile();
}

function configuredStackProfile(config, cal) {
  const calStack = stackFromCalibration(cal);
  const cfgStack = config?.stack && typeof config.stack === "object" && !Array.isArray(config.stack) ? config.stack : {};
  const stack = {
    ...calStack,
    ...cfgStack,
    members: { ...(calStack.members || {}), ...(cfgStack.members || {}), ...(config?.stack_members || {}) }
  };
  if (config?.stack_enabled !== undefined) stack.enabled = config.stack_enabled === true;
  if (config?.stack_id !== undefined) stack.stack_id = String(config.stack_id || "");
  if (config?.stack_uptime_source !== undefined) stack.uptime_source = String(config.stack_uptime_source || "");
  if (config?.stack_member_number !== undefined || config?.stack_uptime_mode !== undefined || config?.stack_member_uptime_source !== undefined) {
    const member = String(normalizePanelMember(config)).toUpperCase();
    stack.members[member] = {
      ...(stack.members[member] || {}),
      member_number: Number(config.stack_member_number || stack.members[member]?.member_number || "") || "",
      uptime_mode: normaliseStackUptimeMode(config.stack_uptime_mode || stack.members[member]?.uptime_mode),
      uptime_source: String(config.stack_member_uptime_source || stack.members[member]?.uptime_source || "")
    };
  }
  return stack;
}

function stackMemberSettings(config, cal, memberName = normalizePanelMember(config)) {
  const stack = configuredStackProfile(config, cal);
  const key = String(memberName || "").toUpperCase();
  const raw = stack.members?.[key] || {};
  const memberNumber = Number(raw.member_number ?? raw.number ?? "") || Number(key.replace(/[^0-9]/g, "")) || "";
  return {
    stack,
    member: {
      ...raw,
      member_number: memberNumber,
      uptime_mode: normaliseStackUptimeMode(raw.uptime_mode || raw.mode),
      uptime_source: String(raw.uptime_source || "")
    }
  };
}

function entityIdsFromValue(value) {
  if (value === null || value === undefined || value === false) return [];
  if (Array.isArray(value)) return value.flatMap(entityIdsFromValue);
  return String(value).split(/[\s,]+/).map((v) => v.trim()).filter(Boolean);
}

function switchUptimeCandidates(config, cal, memberName) {
  const member = String(memberName || normalizePanelMember(config)).toUpperCase();
  const m = member.toLowerCase();
  const { stack, member: stackMember } = stackMemberSettings(config, cal, member);
  const mode = normaliseStackUptimeMode(stackMember.uptime_mode);
  const inheritedSource = stackMember.uptime_source || stack.uptime_source;
  const explicit = [switchConfiguredEntity(config, "uptime")].filter(Boolean);

  if (mode === "inherit_stack" || mode === "stack_source") {
    explicit.push(...entityIdsFromValue(inheritedSource));
  }

  return [
    ...explicit,
    `sensor.${m}_uptime`,
    `sensor.${m}_system_uptime`
  ];
}

function switchStackLabel(config, cal, memberName) {
  const { stack, member } = stackMemberSettings(config, cal, memberName);
  if (!stack.enabled) return "Healthy";
  const rawNumber = Number(member.member_number || 0);
  const derivedNumber = Number(String(memberName || "").replace(/[^0-9]/g, "")) || 0;
  const n = rawNumber >= 1 && rawNumber <= 16 ? rawNumber : (derivedNumber >= 1 && derivedNumber <= 16 ? derivedNumber : "");
  const mode = normaliseStackUptimeMode(member.uptime_mode);
  if (mode === "inherit_stack") return n ? `Member ${n}` : "Stack member";
  if (mode === "stack_source") return n ? `Member ${n} source` : "Stack source";
  return n ? `Member ${n}` : "Stacked";
}

function firstEntityRecord(hass, candidates) {
  for (const entityId of candidates.filter(Boolean)) {
    const entity = hass?.states?.[entityId];
    if (entity && usableValue(entity.state) !== null) return { entityId, entity };
  }
  return null;
}

function poeWatts(hass, candidates) {
  const record = firstEntityRecord(hass, candidates);
  if (!record) return null;
  const text = String(record.entity.state ?? "").trim();
  const numeric = Number(text.replace(/[^0-9.-]/g, ""));
  if (!Number.isFinite(numeric)) return null;
  const unit = String(record.entity.attributes?.unit_of_measurement || "").trim().toLowerCase();
  const id = record.entityId.toLowerCase();
  if (unit === "mw" || id.endsWith("_mw")) return numeric / 1000;
  if (unit === "kw" || id.endsWith("_kw")) return numeric * 1000;
  if (unit === "w" || id.endsWith("_w")) return numeric;
  // Legacy generated entities sometimes had no unit attribute. Cisco 2960
  // aggregate PoE values are watts; 3650 aggregate values are milliwatts.
  return numeric >= 10000 ? numeric / 1000 : numeric;
}

function parseSystemDescription(value) {
  const raw = usableValue(value);
  if (!raw) return { vendor: null, os: null, firmware: null };
  const text = String(raw).trim();

  if (/Juniper Networks|\bJUNOS\b|\bEX3300\b/i.test(text)) {
    const version = text.match(/\bJUNOS\s+([^,\s]+)/i);
    return {
      vendor: "Juniper",
      os: "Junos OS",
      firmware: version ? version[1].trim() : null
    };
  }

  const vendor = /Cisco|Catalyst/i.test(text) ? "Cisco Systems" : null;
  let os = null;
  if (/IOS XE/i.test(text) || /\[Gibraltar\]|\[Amsterdam\]|CAT3K/i.test(text)) os = "Cisco IOS XE";
  else if (/Cisco IOS Software/i.test(text)) os = "Cisco IOS";
  const version = text.match(/\bVersion\s+([^,]+)/i);
  return { vendor, os, firmware: version ? version[1].trim() : null };
}

function formatJuniperOperatingState(value) {
  const raw = usableValue(value);
  if (!raw) return "—";
  const numeric = Number(String(raw).replace(/[^0-9.-]/g, ""));
  if (!Number.isFinite(numeric)) return raw;
  const labels = {
    1: "Unknown",
    2: "Healthy",
    3: "Ready",
    4: "Resetting",
    5: "Healthy",
    6: "Down",
    7: "Standby"
  };
  return labels[numeric] || raw;
}

function switchStatusValues(hass, config, cal = calibration) {
  const member = normalizePanelMember(config).toUpperCase();
  const m = member.toLowerCase();
  const runtime = unifiRuntime(config);
  if (runtime) {
    const system = runtime.system && typeof runtime.system === "object" ? runtime.system : {};
    return {
      member,
      model: usableValue(runtime.model) || usableValue(config.switch_model) || "—",
      vendor: "Ubiquiti",
      os: "UniFi Network",
      firmware: usableValue(runtime.firmware) || "—",
      serial: "—",
      stack: "Standalone",
      cpu: formatPercent(system.cpu_utilization_pct),
      memory: formatPercent(system.memory_utilization_pct),
      temp: "—",
      uptime: formatUptimeSeconds(system.uptime_sec),
      fans: "—",
      psu: "—",
      poe: "—",
      poeAvailable: false,
      ip: explicitConfigValue(config, ["switch_ip", "management_ip"]) || "—",
      uplink_rx: formatBps(Number(system.uplink_rx_rate_bps)),
      uplink_tx: formatBps(Number(system.uplink_tx_rate_bps)),
    };
  }

  const cpu = firstEntityValue(hass, [
    switchConfiguredEntity(config, "cpu"),
    switchConfiguredEntity(config, "cpu_5min"),
    `sensor.${m}_cpu_5min`,
    `sensor.${m}_cpu_1min`,
    `sensor.${m}_cpu_5sec`,
    `sensor.cisco_3650_${m}_cpu_5min`,
    `sensor.cisco_3650_${m}_cpu_1min`,
    `sensor.cisco_3650_${m}_cpu_5sec`
  ]);

  const uptime = firstEntityValue(hass, switchUptimeCandidates(config, cal, member));

  const temp = firstEntityValue(hass, [
    switchConfiguredEntity(config, "temperature"),
    switchConfiguredEntity(config, "temp"),
    `sensor.${m}_temperature`,
    `sensor.${m}_temp`,
    `sensor.${m}_switch_temperature`,
    `sensor.${m}_system_temperature`,
    `sensor.cisco_3650_${m}_temperature`,
    `sensor.cisco_3650_${m}_temp`,
    `sensor.cisco_3650_${m}_switch_temperature`,
    `sensor.cisco_3650_${m}_system_temperature`
  ]);

  const fans = firstEntityValue(hass, [
    switchConfiguredEntity(config, "fans"),
    `sensor.${m}_fans`,
    `sensor.${m}_fan_status`,
    `sensor.cisco_3650_${m}_fans`,
    `sensor.cisco_3650_${m}_fan_status`
  ]) || "OK";

  const psu = firstEntityValue(hass, [
    switchConfiguredEntity(config, "psu"),
    `sensor.${m}_psu`,
    `sensor.${m}_psu_status`,
    `sensor.cisco_3650_${m}_psu`,
    `sensor.cisco_3650_${m}_psu_status`
  ]) || "OK";

  const poeUsed = poeWatts(hass, [
    switchConfiguredEntity(config, "poe_used"),
    switchConfiguredEntity(config, "poe_used_mw"),
    `sensor.${m}_poe_used`,
    `sensor.${m}_poe_used_w`,
    `sensor.${m}_poe_used_mw`,
    `sensor.cisco_3650_${m}_poe_used`,
    `sensor.cisco_3650_${m}_poe_used_w`,
    `sensor.cisco_3650_${m}_poe_used_mw`
  ]);
  const poeBudget = poeWatts(hass, [
    switchConfiguredEntity(config, "poe_budget"),
    switchConfiguredEntity(config, "poe_budget_mw"),
    `sensor.${m}_poe_budget`,
    `sensor.${m}_poe_budget_w`,
    `sensor.${m}_poe_budget_mw`,
    `sensor.cisco_3650_${m}_poe_budget`,
    `sensor.cisco_3650_${m}_poe_budget_w`,
    `sensor.cisco_3650_${m}_poe_budget_mw`
  ]);

  const poeAvailable = poeUsed !== null && poeBudget !== null;
  const poe = poeAvailable
    ? `${Math.round(poeUsed)} / ${Math.round(poeBudget)} W`
    : "—";

  const ip = explicitConfigValue(config, ["switch_ip", "management_ip", "mgmt_ip", "host_ip", "snmp_host"]) ||
    managementIpFromCalibration(cal) ||
    firstEntityValue(hass, [
      switchConfiguredEntity(config, "ip"),
      switchConfiguredEntity(config, "switch_ip"),
      switchConfiguredEntity(config, "management_ip"),
      `sensor.${m}_ip`,
      `sensor.${m}_switch_ip`,
      `sensor.${m}_management_ip`,
      `sensor.${m}_mgmt_ip`,
      `sensor.cisco_3650_${m}_ip`,
      `sensor.cisco_3650_${m}_management_ip`
    ]) || "—";

  const identityValue = (key, fallbacks = []) => explicitConfigValue(config, [key, `switch_${key}`]) || firstEntityValue(hass, [
    switchConfiguredEntity(config, key),
    `sensor.${m}_${key}`,
    ...fallbacks
  ]) || "—";
  const model = identityValue("model", [`sensor.${m}_model_text`, `sensor.${m}_model_name`]);
  const systemDescription = firstEntityValue(hass, [
    switchConfiguredEntity(config, "system_description"),
    `sensor.${m}_system_description`,
    `sensor.${m}_sysdescr`
  ]);
  const parsedIdentity = parseSystemDescription(systemDescription);
  const vendor = explicitConfigValue(config, ["vendor", "switch_vendor"]) || firstEntityValue(hass, [
    switchConfiguredEntity(config, "vendor"),
    `sensor.${m}_vendor`,
    `sensor.${m}_manufacturer`
  ]) || parsedIdentity.vendor || "—";
  const osName = explicitConfigValue(config, ["os", "switch_os"]) || firstEntityValue(hass, [
    switchConfiguredEntity(config, "os"),
    `sensor.${m}_os`,
    `sensor.${m}_platform`,
    `sensor.${m}_operating_system`
  ]) || parsedIdentity.os || "—";
  const firmware = explicitConfigValue(config, ["firmware", "switch_firmware"]) || firstEntityValue(hass, [
    switchConfiguredEntity(config, "firmware"),
    `sensor.${m}_firmware`,
    `sensor.${m}_software_version`,
    `sensor.${m}_ios_version`
  ]) || parsedIdentity.firmware || "—";
  const serial = identityValue("serial", [`sensor.${m}_serial_number`]);

  const isJuniper = /Juniper/i.test(String(model || "")) || /Juniper/i.test(String(vendor || "")) || /JUNOS/i.test(String(systemDescription || ""));

  return {
    member,
    model, vendor, os: osName, firmware, serial,
    stack: isJuniper && !configuredStackProfile(config, cal).enabled ? "Standalone" : switchStackLabel(config, cal, member),
    cpu: formatPercent(cpu),
    memory: formatPercent(firstEntityValue(hass, [switchConfiguredEntity(config, "memory"), `sensor.${m}_memory`, `sensor.${m}_memory_utilization`])),
    temp: formatTemperature(temp),
    uptime: formatUptime(uptime),
    fans: isJuniper ? formatJuniperOperatingState(fans) : fans,
    psu: isJuniper ? formatJuniperOperatingState(psu) : psu,
    poe,
    poeAvailable,
    ip,
    uplink_rx: "—",
    uplink_tx: "—"
  };
}

function makeRows(values, fields, labels) {
  return fields.map((field) => [labels[field] || field.toUpperCase(), values[field] ?? "—"]);
}

const STATUS_PANEL_ROW_DEFS = {
  switch: {
    defaults: ["model", "ip", "cpu", "temp", "poe", "uptime", "vendor", "os", "firmware", "serial", "stack", "fans", "psu"],
    hidden: ["vendor", "os", "firmware", "serial", "stack", "fans", "psu"],
    labels: { model: "MODEL", vendor: "VENDOR", os: "OS", firmware: "FIRMWARE", serial: "SERIAL", stack: "STACK", cpu: "CPU", temp: "TEMP", uptime: "UPTIME", fans: "FANS", psu: "PSU", poe: "POE", ip: "IP" }
  },
  port: {
    defaults: ["vlan", "mode", "desc", "link", "rx", "tx"],
    hidden: ["mode"],
    labels: { vlan: "VLAN", mode: "MODE", desc: "DESC", link: "LINK", rx: "RX", tx: "TX" }
  },
  sfp: {
    defaults: ["vlan", "mode", "desc", "link", "rx", "tx"],
    hidden: ["mode"],
    labels: { vlan: "VLAN", mode: "MODE", desc: "DESC", link: "LINK", rx: "RX", tx: "TX" }
  }
};

function normalStatusPanelRowType(type) {
  const t = String(type || "switch").toLowerCase();
  return t === "port" || t === "sfp" ? t : "switch";
}

function fieldListFromValue(value) {
  if (value === null || value === undefined || value === false) return null;
  if (Array.isArray(value)) return value.map((v) => String(v).trim().toLowerCase()).filter(Boolean);
  if (typeof value === "string") return value.split(",").map((v) => v.trim().toLowerCase()).filter(Boolean);
  return null;
}

function normaliseStatusPanelFieldOrder(value, type) {
  type = normalStatusPanelRowType(type);
  const defs = STATUS_PANEL_ROW_DEFS[type]?.defaults || STATUS_PANEL_ROW_DEFS.switch.defaults;
  const seen = new Set();
  const out = [];
  for (const field of (fieldListFromValue(value) || defs)) {
    if (!defs.includes(field) || seen.has(field)) continue;
    seen.add(field);
    out.push(field);
  }
  for (const field of defs) {
    if (!seen.has(field)) out.push(field);
  }
  return out;
}

function ensureStatusPanelFieldState(panel) {
  if (!panel || typeof panel !== "object") return panel;
  panel.field_order = panel.field_order && typeof panel.field_order === "object" && !Array.isArray(panel.field_order) ? panel.field_order : {};
  panel.hidden_fields = panel.hidden_fields && typeof panel.hidden_fields === "object" && !Array.isArray(panel.hidden_fields) ? panel.hidden_fields : {};

  for (const type of Object.keys(STATUS_PANEL_ROW_DEFS)) {
    panel.field_order[type] = normaliseStatusPanelFieldOrder(panel.field_order[type], type);
    const defs = STATUS_PANEL_ROW_DEFS[type].defaults;
    const fallbackHidden = STATUS_PANEL_ROW_DEFS[type].hidden || [];
    const hidden = fieldListFromValue(panel.hidden_fields[type]);
    panel.hidden_fields[type] = (hidden || fallbackHidden).filter((field, idx, arr) => defs.includes(field) && arr.indexOf(field) === idx);
  }
  return panel;
}

function statusPanelRowTypeForConfig(config, panelNumber = 1) {
  // Status Box 2 is always a switch-summary box. Port and SFP selection
  // details belong exclusively to Status Box 1.
  const selected = panelNumber === 2 ? null : selectedInterface(config);
  if (selected?.type === "port") return "port";
  if (selected?.type === "sfp") return "sfp";
  return "switch";
}

function statusPanelFieldSelection(config, type, cal, panelNumber = 1) {
  type = normalStatusPanelRowType(type);
  const defaults = STATUS_PANEL_ROW_DEFS[type]?.defaults || STATUS_PANEL_ROW_DEFS.switch.defaults;
  const panel = ensureStatusPanelFieldState(uiFromCalibration(cal)[panelNumber === 2 ? "status_panel_2" : "status_panel"]);

  // Status Box 2 must remain fully independent from Status Box 1. Earlier builds
  // reused the unsuffixed card-level field keys for both boxes, which could hide
  // Box 2 rows even after they were unhidden and saved in its calibration profile.
  const prefix = panelNumber === 2 ? "status_panel_2" : "status_panel";
  const scopedKey = `${prefix}_${type}_fields`;
  const hiddenKey = `${prefix}_${type}_hidden_fields`;
  const genericFieldsKey = `${prefix}_fields`;
  const genericHiddenKey = `${prefix}_hidden_fields`;

  let selected = fieldListFromValue(config[scopedKey]);

  if (!selected && config[genericFieldsKey] && typeof config[genericFieldsKey] === "object" && !Array.isArray(config[genericFieldsKey])) {
    selected = fieldListFromValue(config[genericFieldsKey][type]);
  }

  if (!selected) selected = fieldListFromValue(config[genericFieldsKey]);

  // Preserve compatibility with old manual YAML only for Status Box 1. Box 2
  // must never inherit Box 1 visibility overrides.
  if (panelNumber === 1) {
    if (!selected && config.status_panel_fields && typeof config.status_panel_fields === "object" && !Array.isArray(config.status_panel_fields)) {
      selected = fieldListFromValue(config.status_panel_fields[type]);
    }
    if (!selected) selected = fieldListFromValue(config.status_panel_fields);
  }

  if (!selected) selected = panel?.field_order?.[type];
  selected = normaliseStatusPanelFieldOrder(selected, type);

  const hidden = new Set([
    ...(fieldListFromValue(panel?.hidden_fields?.[type]) || []),
    ...(fieldListFromValue(config[genericHiddenKey]) || []),
    ...(fieldListFromValue(config[hiddenKey]) || [])
  ]);

  if (panelNumber === 1) {
    for (const field of fieldListFromValue(config.status_panel_hidden_fields) || []) hidden.add(field);
    for (const field of fieldListFromValue(config[`status_panel_${type}_hidden_fields`]) || []) hidden.add(field);
  }

  return selected.filter((field) => defaults.includes(field) && !hidden.has(field));
}

function switchSummaryDetails(hass, config, cal, panelNumber = 1) {
  const values = switchStatusValues(hass, config, cal);
  let fields = statusPanelFieldSelection(config, "switch", cal, panelNumber);
  if (!values.poeAvailable) fields = fields.filter((field) => field !== "poe");
  const labels = STATUS_PANEL_ROW_DEFS.switch.labels;

  return {
    member: values.member,
    rows: makeRows(values, fields, labels)
  };
}

function speedClass(speed, up = true) {
  if (!up) return "cv-led-off";

  const s = String(speed || "").toLowerCase().replace(/\s+/g, "");
  const numeric = Number(s.replace(/[^0-9.]/g, ""));

  if (numeric >= 10000 || s.includes("10g") || s.includes("10gb")) return "cv-led-blue";
  if (numeric >= 1000 || s.includes("1g") || s.includes("gigabit")) return "cv-led-green";
  if (numeric >= 100 || s.includes("100m") || s.includes("fast")) return "cv-led-amber";
  if (numeric >= 10 || s.includes("10m")) return "cv-led-red";

  return "cv-led-green";
}

function activityClass(config, up = true) {
  if (!up) return "cv-led-off";
  return config.activity_led_class || "cv-led-amber";
}

function calibrationTestStatusLedClass(name) {
  if (name === "STACK") return "cv-led-blue";
  if (name === "PoE") return "cv-led-poe";
  return "cv-led-green";
}

function statusLedClass(name, demo = true, options = {}) {
  const poeActive = options.poeActive === true;
  const stackActive = options.stackActive === true;

  // STACK is intentionally vendor-neutral and configuration-driven. It is blue
  // only when this switch is explicitly confirmed as a stack member; otherwise
  // the LED remains off, including demo mode and standalone/unknown devices.
  if (name === "STACK") return stackActive ? "cv-led-blue" : "cv-led-off";

  if (demo) {
    if (name === "DUPLX") return "cv-led-off";
    if (name === "PoE") return poeActive ? "cv-led-poe" : "cv-led-off";
    return "cv-led-green";
  }

  if (["MODE", "STAT", "SYST", "ACTV"].includes(name)) return "cv-led-green";

  if (name === "PoE") return poeActive ? "cv-led-poe" : "cv-led-off";

  return "cv-led-off";
}

const portActivityState = new Map();
const sfpActivityState = new Map();

function normalizeMember(config) {
  return String(config.member || "sw1").toLowerCase();
}

function readCounterSample(hass, entityId) {
  const entity = hass && entityId ? hass.states[entityId] : undefined;
  if (!entity) return null;

  const n = Number(String(entity.state).replace(/,/g, ""));
  if (!Number.isFinite(n)) return null;

  const updated = Date.parse(entity.last_updated || entity.last_changed || "");
  return { value: n, updated: Number.isFinite(updated) ? updated : 0 };
}

function readCounter(hass, entityId) {
  return readCounterSample(hass, entityId)?.value ?? null;
}

function readFirstCounterSample(hass, entityIds) {
  for (const entityId of entityIds) {
    const sample = readCounterSample(hass, entityId);
    if (sample !== null) return sample;
  }
  return null;
}

function readFirstCounter(hass, entityIds) {
  return readFirstCounterSample(hass, entityIds)?.value ?? null;
}

function portByteEntity(config, port, direction) {
  if (String(config?.data_source || "").toLowerCase() === "unifi_api" && config?.unifi_per_port_traffic !== true) return null;
  const member = normalizeMember(config);
  const mappedPort = mappedPortNumber(config, port);

  // Clean Switch Vision / SNMP2MQTT entity IDs. Always use the mapped entity
  // number so zero-based models such as the Juniper EX3300 line bind their
  // traffic counters to the same port used by link status.
  return `sensor.${member}_port_${mappedPort}_${direction}_bytes`;
}

function sfpByteEntities(config, port, direction) {
  if (String(config?.data_source || "").toLowerCase() === "unifi_api" && config?.unifi_per_port_traffic !== true) return [];
  const member = normalizeMember(config);
  const n = Number(port);

  // Prefer clean Switch Vision / SNMP2MQTT entity IDs.
  // Keep older entity IDs as fallbacks for existing dashboards.
  if (n === 1 || n === 2) {
    return [
      `sensor.${member}_sfp_10g_${n}_${direction}_bytes`,
      `sensor.${member}_sfp_1g_${n}_${direction}_bytes`,
      `sensor.${member}_uplink_${n}_${direction}_bytes`,
      `sensor.cisco_3650_${member}_uplinks_${member}_uplink_${n}_${direction}_bytes`,
      `sensor.cisco_3650_${member}_uplink_${n}_${direction}_bytes`
    ];
  }

  return [
    `sensor.${member}_sfp_10g_${n}_${direction}_bytes`,
    `sensor.${member}_sfp_1g_${n}_${direction}_bytes`,
    `sensor.cisco_3650_${member}_sfp_10g_${n}_${direction}_bytes`,
    `sensor.cisco_3650_${member}_uplinks_${member}_sfp_10g_${n}_${direction}_bytes`,
    `sensor.cisco_3650_${member}_sfp_10g_${n}_${member}_sfp_10g_${n}_${direction}_bytes`
  ];
}

function sfpSpeedMbps(hass, config, port) {
  const unifi = unifiSfpPort(config, port);
  if (unifi && linkStateIsUp(unifi.state) && unifi.speed_mbps != null) {
    const value = Number(unifi.speed_mbps);
    if (Number.isFinite(value) && value > 0) return value;
  }

  const member = normalizeMember(config);
  const n = Number(port);
  const mbpsCandidates = [
    `sensor.${member}_sfp_10g_${n}_speed_mbps`,
    `sensor.${member}_sfp_1g_${n}_speed_mbps`,
    `sensor.${member}_uplink_${n}_speed_mbps`
  ];
  for (const entityId of mbpsCandidates) {
    const raw = rawEntityState(hass, entityId);
    const value = Number(String(raw || "").replace(/,/g, ""));
    if (Number.isFinite(value) && value > 0) return value;
  }
  const bpsCandidates = [
    `sensor.${member}_sfp_10g_${n}_speed_bps`,
    `sensor.${member}_sfp_1g_${n}_speed_bps`,
    `sensor.${member}_uplink_${n}_speed_bps`
  ];
  for (const entityId of bpsCandidates) {
    const raw = rawEntityState(hass, entityId);
    const value = Number(String(raw || "").replace(/,/g, ""));
    if (Number.isFinite(value) && value > 0) return value / 1000000;
  }
  return null;
}

const SV_ACTIVITY_LED_PRESETS = Object.freeze({
  low: Object.freeze({ mediumStartsPct: 1.0, fastStartsPct: 10.0 }),
  normal: Object.freeze({ mediumStartsPct: 0.10, fastStartsPct: 1.0 }),
  high: Object.freeze({ mediumStartsPct: 0.02, fastStartsPct: 0.25 }),
});

function activityThresholds(config = {}) {
  const presetName = String(config.activity_led_sensitivity_preset || "normal").trim().toLowerCase();
  const preset = presetName === "custom" ? null : (SV_ACTIVITY_LED_PRESETS[presetName] || SV_ACTIVITY_LED_PRESETS.normal);
  const mediumStartsPct = preset
    ? preset.mediumStartsPct
    : Math.max(0.001, Number(config.activity_slow_max_utilization_pct ?? 0.10));
  const fastStartsPct = preset
    ? preset.fastStartsPct
    : Math.max(mediumStartsPct, Number(config.activity_medium_max_utilization_pct ?? 1.0));
  return {
    mediumStarts: mediumStartsPct / 100,
    fastStarts: fastStartsPct / 100,
  };
}

function activityLevel(utilization, config = {}, previousLevel = 0) {
  if (!Number.isFinite(utilization) || utilization <= 0) return 0;

  // Activity LED 2.0 classifies real RX + TX throughput as a percentage of the
  // negotiated link rate. Presets are intentionally sensitive enough that a
  // normally busy network remains visually alive instead of spending most of
  // its time in the Slow band. Custom mode exposes both boundaries in Core
  // settings. Hysteresis prevents chatter when utilisation sits on a boundary.
  const { mediumStarts, fastStarts } = activityThresholds(config);
  const hysteresis = Math.max(0, Math.min(0.50, Number(config.activity_hysteresis_pct ?? 20) / 100));
  const mediumRise = mediumStarts;
  const mediumFall = mediumStarts * (1 - hysteresis);
  const fastRise = fastStarts;
  const fastFall = fastStarts * (1 - hysteresis);

  if (previousLevel >= 0.66) {
    if (utilization >= fastFall) return 1;
    if (utilization >= mediumFall) return 0.50;
    return 0.25;
  }

  if (previousLevel >= 0.33) {
    if (utilization >= fastRise) return 1;
    if (utilization >= mediumFall) return 0.50;
    return 0.25;
  }

  if (utilization >= fastRise) return 1;
  if (utilization >= mediumRise) return 0.50;
  return 0.25;
}

function periodMs(config, level) {
  const slow = Number(config.activity_slow_period_ms ?? 500);
  const medium = Number(config.activity_medium_period_ms ?? 250);
  const fast = Number(config.activity_fast_period_ms ?? 120);
  const clamped = Math.max(0, Math.min(1, level));

  // Keep the three visible blink bands predictable.
  if (clamped <= 0.33) return Math.round(slow);
  if (clamped <= 0.66) return Math.round(medium);
  return Math.round(fast);
}

function dutyCycle(level) {
  // Higher traffic stays lit for more of each blink period.
  return Math.max(0.18, Math.min(0.72, 0.18 + (level * 0.54)));
}

function configuredActivityHoldMs(config) {
  const seconds = Number(
    config.activity_hold_seconds ??
    config.activity_poll_seconds ??
    config.traffic_poll_seconds ??
    config.traffic_scan_interval ??
    0
  );

  if (Number.isFinite(seconds) && seconds > 0) {
    return Math.max(500, seconds * 1000);
  }

  return null;
}

function defaultActivityHoldMs(config) {
  const windowMs = Number(config.activity_window_ms ?? 6500);
  const bridgeMs = Number(config.activity_bridge_ms ?? 3500);
  const decayMs = Number(config.activity_decay_ms ?? 3000);
  return Math.max(500, windowMs, bridgeMs + decayMs);
}

function activityHoldMs(config) {
  return configuredActivityHoldMs(config) ?? defaultActivityHoldMs(config);
}

function smoothedActivityLevel(config, state, now) {
  if (!state || !state.lastDetectedAt || state.level <= 0) return 0;

  const configuredHoldMs = configuredActivityHoldMs(config);
  const bridgeMs = Math.max(0, Number(config.activity_bridge_ms ?? 3500));
  const decayMs = Math.max(0, Number(config.activity_decay_ms ?? 3000));
  const age = Math.max(0, now - state.lastDetectedAt);

  // When activity_hold_seconds is configured, keep the last detected activity
  // alive across the expected SNMP traffic poll gap. This prevents activity LEDs
  // from stopping before the next slower/chunked traffic poll arrives.
  if (configuredHoldMs !== null && age <= configuredHoldMs) return state.level;

  // Legacy/default smoothing for dashboards without an explicit poll-aware hold.
  if (age <= bridgeMs) return state.level;

  if (decayMs <= 0) return 0;

  const decayAge = age - bridgeMs;
  if (decayAge >= decayMs) return 0;

  const remaining = 1 - (decayAge / decayMs);
  return Math.max(0, state.level * remaining);
}

function parseSpeedMbps(rawSpeed) {
  const raw = String(rawSpeed ?? "").trim().toLowerCase();
  const numeric = Number(raw.replace(/[^0-9.]/g, ""));
  if (!Number.isFinite(numeric) || numeric <= 0) return null;

  if (raw.includes("tbps") || raw.includes("tb/s") || raw.includes("tbit") || /\d\s*t$/.test(raw)) {
    return numeric * 1000000;
  }
  if (raw.includes("gbps") || raw.includes("gb/s") || raw.includes("gbit") || /\d\s*g$/.test(raw)) {
    return numeric * 1000;
  }
  if (raw.includes("kbps") || raw.includes("kb/s") || raw.includes("kbit") || /\d\s*k$/.test(raw)) {
    return numeric / 1000;
  }

  // Bare values and Mbps-style values are treated as megabits per second.
  return numeric;
}

function updateActivityState(stateMap, key, rxSample, txSample, config, maxBitsPerSecond) {
  const now = Date.now();

  let state = stateMap.get(key);
  if (!state) {
    state = {
      rx: rxSample?.value ?? null,
      tx: txSample?.value ?? null,
      rxUpdated: rxSample?.updated || 0,
      txUpdated: txSample?.updated || 0,
      activeUntil: 0,
      level: 0,
      startedAt: 0,
      lastDetectedAt: 0
    };
    stateMap.set(key, state);
    return false;
  }

  const rxValue = rxSample?.value ?? null;
  const txValue = txSample?.value ?? null;
  const rxDelta = rxValue !== null && state.rx !== null ? Math.max(0, rxValue - state.rx) : 0;
  const txDelta = txValue !== null && state.tx !== null ? Math.max(0, txValue - state.tx) : 0;
  const delta = rxDelta + txDelta;

  const sampleUpdated = Math.max(rxSample?.updated || 0, txSample?.updated || 0);
  const previousUpdated = Math.max(state.rxUpdated || 0, state.txUpdated || 0);
  const sampleIsNew = sampleUpdated > 0
    ? sampleUpdated > previousUpdated
    : delta > 0;

  if (sampleIsNew && delta > 0) {
    const fallbackSeconds = Math.max(
      0.25,
      Number(config.activity_sample_seconds ?? config.traffic_poll_seconds ?? 10)
    );
    const elapsedSeconds = sampleUpdated > 0 && previousUpdated > 0
      ? Math.max(0.25, (sampleUpdated - previousUpdated) / 1000)
      : fallbackSeconds;
    const bitsPerSecond = (delta * 8) / elapsedSeconds;
    const ceiling = Number(maxBitsPerSecond);

    if (Number.isFinite(ceiling) && ceiling > 0 && bitsPerSecond <= ceiling * 1.05) {
      const utilization = Math.max(0, bitsPerSecond / ceiling);
      const holdMs = activityHoldMs(config);
      const wasInactive = state.activeUntil <= now || state.level <= 0;

      state.activeUntil = now + holdMs;
      state.level = activityLevel(utilization, config, state.level);
      state.lastDetectedAt = now;

      // Only restart the blink phase when a port newly becomes active.
      if (wasInactive || !state.startedAt) state.startedAt = now;
    }
  }

  if (rxValue !== null) state.rx = rxValue;
  if (txValue !== null) state.tx = txValue;
  if (rxSample?.updated) state.rxUpdated = rxSample.updated;
  if (txSample?.updated) state.txUpdated = txSample.updated;

  const displayLevel = smoothedActivityLevel(config, state, now);
  if (state.activeUntil <= now || displayLevel <= 0) return false;

  const period = Math.max(120, periodMs(config, displayLevel));
  const elapsed = Math.max(0, now - state.startedAt);
  const phase = elapsed % period;

  return phase < (period * dutyCycle(displayLevel));
}

function testPortActivity(hass, config, port) {
  if (config.demo) return true;

  const member = normalizeMember(config);
  const mappedPort = mappedPortNumber(config, port);
  const key = `${member}:port:${mappedPort}`;
  const rx = readCounterSample(hass, portByteEntity(config, port, "rx"));
  const tx = readCounterSample(hass, portByteEntity(config, port, "tx"));
  const speedMbps = parseSpeedMbps(portSpeed(hass, config, port));
  const maxBitsPerSecond = Number.isFinite(speedMbps) && speedMbps > 0
    ? speedMbps * 1000000
    : Number(config.activity_default_port_speed_mbps ?? 1000) * 1000000;

  return updateActivityState(portActivityState, key, rx, tx, config, maxBitsPerSecond);
}

function updateRateState(stateMap, key, rxSample, txSample, config, maxBitsPerSecond = null) {
  let state = stateMap.get(key);
  if (!state) {
    state = {
      rx: rxSample?.value ?? null,
      tx: txSample?.value ?? null,
      rxUpdated: rxSample?.updated || 0,
      txUpdated: txSample?.updated || 0,
      rxBps: null,
      txBps: null
    };
    stateMap.set(key, state);
    return { rxBps: null, txBps: null };
  }

  const plausibleRate = (rate) => {
    if (!Number.isFinite(rate) || rate < 0) return null;
    const ceiling = Number(maxBitsPerSecond);
    if (Number.isFinite(ceiling) && ceiling > 0 && rate > ceiling * 1.05) {
      return null;
    }
    return rate;
  };

  if (rxSample !== null) {
    const sampleUpdated = rxSample.updated || 0;
    const isNewSample = sampleUpdated > 0
      ? sampleUpdated > state.rxUpdated
      : (state.rx !== null && rxSample.value !== state.rx);

    if (isNewSample && state.rx !== null) {
      const elapsed = sampleUpdated > 0 && state.rxUpdated > 0
        ? Math.max(0.25, (sampleUpdated - state.rxUpdated) / 1000)
        : 0.25;
      const delta = rxSample.value - state.rx;
      if (delta < 0) {
        state.rxBps = 0;
      } else {
        const rate = plausibleRate((delta * 8) / elapsed);
        if (rate !== null) state.rxBps = rate;
      }
    }

    if (isNewSample || state.rx === null) {
      state.rx = rxSample.value;
      if (sampleUpdated > 0) state.rxUpdated = sampleUpdated;
    }
  }

  if (txSample !== null) {
    const sampleUpdated = txSample.updated || 0;
    const isNewSample = sampleUpdated > 0
      ? sampleUpdated > state.txUpdated
      : (state.tx !== null && txSample.value !== state.tx);

    if (isNewSample && state.tx !== null) {
      const elapsed = sampleUpdated > 0 && state.txUpdated > 0
        ? Math.max(0.25, (sampleUpdated - state.txUpdated) / 1000)
        : 0.25;
      const delta = txSample.value - state.tx;
      if (delta < 0) {
        state.txBps = 0;
      } else {
        const rate = plausibleRate((delta * 8) / elapsed);
        if (rate !== null) state.txBps = rate;
      }
    }

    if (isNewSample || state.tx === null) {
      state.tx = txSample.value;
      if (sampleUpdated > 0) state.txUpdated = sampleUpdated;
    }
  }

  return { rxBps: state.rxBps, txBps: state.txBps };
}

const portRateState = new Map();
const sfpRateState = new Map();

function portTrafficRates(hass, config, port) {
  if (config.demo) return { rxBps: 1420000000, txBps: 438000000 };

  const member = normalizeMember(config);
  const mappedPort = mappedPortNumber(config, port);
  const key = `${member}:port:${mappedPort}`;
  const rx = readCounterSample(hass, portByteEntity(config, port, "rx"));
  const tx = readCounterSample(hass, portByteEntity(config, port, "tx"));

  const rawSpeed = portSpeed(hass, config, port);
  const speedMbps = parseSpeedMbps(rawSpeed);
  const maxBitsPerSecond = Number.isFinite(speedMbps) && speedMbps > 0
    ? speedMbps * 1000000
    : null;

  return updateRateState(portRateState, key, rx, tx, config, maxBitsPerSecond);
}

function sfpTrafficRates(hass, config, port) {
  if (config.demo) return { rxBps: 6200000000, txBps: 2100000000 };

  const member = normalizeMember(config);
  const key = `${member}:sfp:${port}`;
  const rx = readFirstCounterSample(hass, sfpByteEntities(config, port, "rx"));
  const tx = readFirstCounterSample(hass, sfpByteEntities(config, port, "tx"));

  const speedMbps = sfpSpeedMbps(hass, config, port);
  const maxBitsPerSecond = Number.isFinite(speedMbps) && speedMbps > 0
    ? speedMbps * 1000000
    : 10000000000;
  return updateRateState(sfpRateState, key, rx, tx, config, maxBitsPerSecond);
}

function testSfpActivity(hass, config, port) {
  if (config.demo) return true;

  const member = normalizeMember(config);
  const key = `${member}:sfp:${port}`;
  const rx = readFirstCounterSample(hass, sfpByteEntities(config, port, "rx"));
  const tx = readFirstCounterSample(hass, sfpByteEntities(config, port, "tx"));

  const speedMbps = sfpSpeedMbps(hass, config, port);
  const maxBitsPerSecond = Number.isFinite(speedMbps) && speedMbps > 0
    ? speedMbps * 1000000
    : 10000000000;
  return updateActivityState(sfpActivityState, key, rx, tx, config, maxBitsPerSecond);
}



const STATUS_PANEL_FIELD_DEFS = [
  ["row1_key", "Row 1 label", [24, 55], "start"], ["row1_value", "Row 1 value", [112, 55], "start"],
  ["row2_key", "Row 2 label", [24, 72], "start"], ["row2_value", "Row 2 value", [112, 72], "start"],
  ["row3_key", "Row 3 label", [24, 89], "start"], ["row3_value", "Row 3 value", [112, 89], "start"],
  ["row4_key", "Row 4 label", [24, 106], "start"], ["row4_value", "Row 4 value", [112, 106], "start"],
  ["row5_key", "Row 5 label", [24, 123], "start"], ["row5_value", "Row 5 value", [112, 123], "start"],
  ["row6_key", "Row 6 label", [24, 140], "start"], ["row6_value", "Row 6 value", [112, 140], "start"]
];

const STATUS_PANEL_1_FIELD_DEFAULTS = {
  row1_key: [19, 39], row1_value: [107, 39],
  row2_key: [19, 58], row2_value: [107, 58],
  row3_key: [19, 78], row3_value: [107, 78],
  row4_key: [19, 97], row4_value: [107, 97],
  row5_key: [19, 116], row5_value: [107, 116],
  row6_key: [19, 135], row6_value: [107, 135]
};

function statusPanelFieldReference(key, panelNumber = 1, fallback = [24, 55]) {
  if (panelNumber === 1 && STATUS_PANEL_1_FIELD_DEFAULTS[key]) return STATUS_PANEL_1_FIELD_DEFAULTS[key];
  const found = STATUS_PANEL_FIELD_DEFS.find(([id]) => id === key);
  return found?.[2] || fallback;
}

function statusFieldLabel(key) {
  const found = STATUS_PANEL_FIELD_DEFS.find(([id]) => id === key);
  return found ? found[1] : key;
}

function normaliseStatusFieldGroupPart(value) {
  const part = String(value || "field").trim().toLowerCase();
  return ["field", "label", "value"].includes(part) ? part : "field";
}

function statusFieldKeyMatchesPart(key, part = "field") {
  const normalised = normaliseStatusFieldGroupPart(part);
  if (normalised === "label") return String(key || "").endsWith("_key");
  if (normalised === "value") return String(key || "").endsWith("_value");
  return true;
}

function statusFieldPointsForPart(panel, part = "field") {
  return Object.entries(panel?.fields || {})
    .filter(([key]) => statusFieldKeyMatchesPart(key, part))
    .map(([, point]) => point);
}

function statusFieldGroupDescription(part = "field") {
  const normalised = normaliseStatusFieldGroupPart(part);
  if (normalised === "label") return "LABELS";
  if (normalised === "value") return "VALUES";
  return "FIELDS";
}

function normaliseHexColour(value, fallback = "#ffffff") {
  const raw = String(value || "").trim();
  if (/^#[0-9a-f]{6}$/i.test(raw)) return raw.toLowerCase();
  if (/^#[0-9a-f]{3}$/i.test(raw)) return `#${raw.slice(1).split("").map((ch) => ch + ch).join("")}`.toLowerCase();
  const rgb = raw.match(/^rgba?\(\s*(\d{1,3})\s*,\s*(\d{1,3})\s*,\s*(\d{1,3})/i);
  if (rgb) {
    const toHex = (part) => Math.max(0, Math.min(255, Number(part))).toString(16).padStart(2, "0");
    return `#${toHex(rgb[1])}${toHex(rgb[2])}${toHex(rgb[3])}`;
  }
  return /^#[0-9a-f]{6}$/i.test(String(fallback || "")) ? String(fallback).toLowerCase() : "#ffffff";
}

function hexToHsvColour(value, fallback = "#ffffff") {
  const hex = normaliseHexColour(value, fallback).slice(1);
  const r = parseInt(hex.slice(0, 2), 16) / 255;
  const g = parseInt(hex.slice(2, 4), 16) / 255;
  const b = parseInt(hex.slice(4, 6), 16) / 255;
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const delta = max - min;
  let h = 0;
  if (delta > 0) {
    if (max === r) h = 60 * (((g - b) / delta) % 6);
    else if (max === g) h = 60 * (((b - r) / delta) + 2);
    else h = 60 * (((r - g) / delta) + 4);
  }
  if (h < 0) h += 360;
  const s = max === 0 ? 0 : (delta / max) * 100;
  return { h, s, v: max * 100 };
}

function hsvToHexColour(h, s, v) {
  const hue = ((Number(h) % 360) + 360) % 360;
  const sat = Math.max(0, Math.min(100, Number(s))) / 100;
  const val = Math.max(0, Math.min(100, Number(v))) / 100;
  const c = val * sat;
  const x = c * (1 - Math.abs(((hue / 60) % 2) - 1));
  const m = val - c;
  let rgb = [0, 0, 0];
  if (hue < 60) rgb = [c, x, 0];
  else if (hue < 120) rgb = [x, c, 0];
  else if (hue < 180) rgb = [0, c, x];
  else if (hue < 240) rgb = [0, x, c];
  else if (hue < 300) rgb = [x, 0, c];
  else rgb = [c, 0, x];
  return `#${rgb.map((part) => Math.round((part + m) * 255).toString(16).padStart(2, "0")).join("")}`;
}

function customColourControlsHtml(field, value, defaultValue, resetValue = defaultValue) {
  const colour = normaliseHexColour(value, defaultValue);
  const hsv = hexToHsvColour(colour, defaultValue);
  const fieldHtml = htmlEscape(field);
  return `<span class="cv-cal-colour-controls" data-cv-colour-control="${fieldHtml}">
    <button type="button" class="cv-cal-colour-swatch" data-cv-colour-open="${fieldHtml}" style="background:${colour}" title="Open colour picker" aria-expanded="false"><span class="cv-cal-sr-only">Open colour picker</span></button>
    <input class="cv-cal-input cv-cal-hex-input" data-cv-colour-hex="${fieldHtml}" value="${colour.toUpperCase()}" maxlength="7" spellcheck="false" aria-label="Custom colour hex value">
    <span class="cv-cal-colour-popover" data-cv-colour-popover="${fieldHtml}" hidden>
      <span class="cv-cal-colour-sv" data-cv-colour-sv="${fieldHtml}" data-cv-saturation="${hsv.s.toFixed(2)}" data-cv-value="${hsv.v.toFixed(2)}" style="--cv-picker-hue:${hsv.h.toFixed(2)}">
        <span class="cv-cal-colour-marker" data-cv-colour-marker style="left:${hsv.s.toFixed(2)}%;top:${(100 - hsv.v).toFixed(2)}%"></span>
      </span>
      <span class="cv-cal-colour-slider-label"><span>Hue</span>
        <input class="cv-cal-colour-hue" data-cv-colour-hue="${fieldHtml}" type="range" min="0" max="360" step="1" value="${Math.round(hsv.h)}">
      </span>
      <span class="cv-cal-colour-slider-label"><span>Brightness</span>
        <input class="cv-cal-colour-brightness" data-cv-colour-brightness="${fieldHtml}" type="range" min="0" max="100" step="1" value="${Math.round(hsv.v)}">
      </span>
      <span class="cv-cal-colour-footer">
        <span class="cv-cal-colour-preview" data-cv-colour-preview style="background:${colour}"></span>
        <button type="button" data-cv-colour-reset="${fieldHtml}" data-cv-default-colour="${htmlEscape(resetValue)}" title="Reset this colour">Reset</button>
        <button type="button" data-cv-colour-done="${fieldHtml}">Done</button>
      </span>
    </span>
  </span>`;
}

function defaultUiLayout() {
  const fields = {};
  const fields2 = {};
  for (const [key, _label, xy] of STATUS_PANEL_FIELD_DEFS) {
    const panel1xy = statusPanelFieldReference(key, 1, xy);
    fields[key] = [panel1xy[0], panel1xy[1]];
    fields2[key] = [xy[0], xy[1]];
  }
  return {
    port_led_shape: "circle",
    port_label_color: "#eef7ff",
    port_number_color: "#eef7ff",
    port_label_font_weight: "bold",
    port_number_font_size: 13,
    sfp_label_font_size: 13.5,
    status_leds: { hidden: [], text_color: "#eef7ff", font_family: "Arial, Helvetica, sans-serif", font_size: 16.5, font_weight: "900" },
    logo: {
      show: true,
      file: layout.logo?.file || "sv-logo.png",
      source: "default",
      x: Number(layout.logo?.x ?? 150),
      y: Number(layout.logo?.y ?? 60),
      width: Number(layout.logo?.width ?? 145),
      height: Number(layout.logo?.height ?? 79)
    },
    faceplate: {
      show: true,
      file: "__default__",
      fit: "fill",
      opacity: 1
    },
    calibration_button: { show: true, x: 1888, y: 14, width: 138, height: 34, anchor: "top_right" },
    status_panel: {
      show: true,
      x: 345,
      y: 47,
      width: 305,
      height: 132,
      font_family: "SwitchVisionCustom, Arial, Helvetica, sans-serif",
      font_size: 16,
      title_font_size: 16,
      font_weight: "bold",
      text_color: "#ffffff",
      label_color: "#eef2f4",
      title_color: "#f4f6f7",
      background_color: "transparent",
      border_color: "rgba(245,248,250,.75)",
      border_show: true,
      field_order: {
        switch: [...STATUS_PANEL_ROW_DEFS.switch.defaults],
        port: [...STATUS_PANEL_ROW_DEFS.port.defaults],
        sfp: [...STATUS_PANEL_ROW_DEFS.sfp.defaults]
      },
      hidden_fields: {
        switch: [...STATUS_PANEL_ROW_DEFS.switch.hidden],
        port: [...STATUS_PANEL_ROW_DEFS.port.hidden],
        sfp: [...STATUS_PANEL_ROW_DEFS.sfp.hidden]
      },
      fields
    },
    status_panel_2: {
      show: true,
      x: 1605,
      y: 20,
      width: 278,
      height: 55,
      font_family: "SwitchVisionCustom, Arial, Helvetica, sans-serif",
      font_size: 16,
      title_font_size: 16,
      font_weight: "bold",
      text_color: "#ffffff",
      label_color: "#eef2f4",
      title_color: "#f4f6f7",
      background_color: "transparent",
      border_color: "rgba(245,248,250,.75)",
      border_show: true,
      title_mode: "custom",
      title_field: "model",
      custom_title: "",
      field_order: { switch: ["vendor", "uptime", ...STATUS_PANEL_ROW_DEFS.switch.defaults.filter((field) => !["vendor", "uptime"].includes(field))], port: [...STATUS_PANEL_ROW_DEFS.port.defaults], sfp: [...STATUS_PANEL_ROW_DEFS.sfp.defaults] },
      hidden_fields: {
        switch: STATUS_PANEL_ROW_DEFS.switch.defaults.filter((field) => !["vendor", "uptime"].includes(field)),
        port: [...STATUS_PANEL_ROW_DEFS.port.hidden],
        sfp: [...STATUS_PANEL_ROW_DEFS.sfp.hidden]
      },
      fields: Object.fromEntries(Object.entries(fields2).map(([key, value]) => [key, [...value]]))
    }
  };
}

function ensureCalibrationUi(cal) {
  if (!cal) return cal;
  ensureCalibrationStack(cal);
  ensureCalibrationManagement(cal);
  const defaults = defaultUiLayout();
  cal.ui = cal.ui || {};
  cal.ui.port_led_shape = String(cal.ui.port_led_shape || defaults.port_led_shape || "circle").toLowerCase() === "rectangle" ? "rectangle" : "circle";
  for (const [field, fallback] of [["link_led_color", "#76ff33"], ["activity_led_color", "#ffb321"]]) {
    const value = String(cal.ui[field] || "").trim();
    if (value) cal.ui[field] = normaliseHexColour(value, fallback);
    else delete cal.ui[field];
  }
  cal.ui.port_label_color = normaliseHexColour(cal.ui.port_label_color, defaults.port_label_color || "#eef7ff");
  cal.ui.port_number_color = normaliseHexColour(cal.ui.port_number_color, cal.ui.port_label_color || defaults.port_number_color || "#eef7ff");
  cal.ui.port_label_font_weight = ["normal", "bold"].includes(String(cal.ui.port_label_font_weight || "").toLowerCase())
    ? String(cal.ui.port_label_font_weight).toLowerCase()
    : String(defaults.port_label_font_weight || "bold");
  cal.ui.port_number_font_size = Math.max(8, Math.min(50, Number(cal.ui.port_number_font_size ?? defaults.port_number_font_size ?? 13) || 13));
  cal.ui.sfp_label_font_size = Math.max(8, Math.min(50, Number(cal.ui.sfp_label_font_size ?? defaults.sfp_label_font_size ?? 13.5) || 13.5));
  cal.ui.status_leds = { ...defaults.status_leds, ...(cal.ui.status_leds || {}) };
  cal.ui.status_leds.hidden = [...new Set((Array.isArray(cal.ui.status_leds.hidden) ? cal.ui.status_leds.hidden : []).map((name) => String(name)))];
  cal.ui.status_leds.text_color = normaliseHexColour(cal.ui.status_leds.text_color, defaults.status_leds.text_color || "#eef7ff");
  cal.ui.status_leds.font_family = String(cal.ui.status_leds.font_family || defaults.status_leds.font_family || "Arial, Helvetica, sans-serif");
  cal.ui.status_leds.font_size = Math.max(8, Math.min(50, Number(cal.ui.status_leds.font_size ?? defaults.status_leds.font_size ?? 16.5) || 16.5));
  cal.ui.status_leds.font_weight = String(cal.ui.status_leds.font_weight || defaults.status_leds.font_weight || "900");
  cal.ui.logo = { ...defaults.logo, ...(cal.ui.logo || {}) };
  cal.ui.faceplate = normaliseFaceplateAsset({ ...defaults.faceplate, ...(cal.ui.faceplate || {}) });
  cal.ui.calibration_button = { ...defaults.calibration_button, ...(cal.ui.calibration_button || {}) };
const calibrationButtonUi = cal.ui.calibration_button;
if (
  Number(calibrationButtonUi.x) === 1888
  && Number(calibrationButtonUi.y) === 372
  && Number(calibrationButtonUi.width) === 138
  && Number(calibrationButtonUi.height) === 34
) {
  calibrationButtonUi.y = 14;
}
  cal.ui.status_panel = { ...defaults.status_panel, ...(cal.ui.status_panel || {}) };
  cal.ui.status_panel.font_size = Math.max(8, Math.min(50, Number(cal.ui.status_panel.font_size ?? defaults.status_panel.font_size ?? 16) || 16));
  cal.ui.status_panel.title_font_size = Math.max(8, Math.min(50, Number(cal.ui.status_panel.title_font_size ?? defaults.status_panel.title_font_size ?? 16) || 16));
  cal.ui.status_panel.fields = { ...defaults.status_panel.fields, ...(cal.ui.status_panel.fields || {}) };
  delete cal.ui.status_panel.fields.title;
  ensureStatusPanelFieldState(cal.ui.status_panel);
  cal.ui.status_panel_2 = { ...defaults.status_panel_2, ...(cal.ui.status_panel_2 || {}) };
  cal.ui.status_panel_2.font_size = Math.max(8, Math.min(50, Number(cal.ui.status_panel_2.font_size ?? defaults.status_panel_2.font_size ?? 16) || 16));
  cal.ui.status_panel_2.title_font_size = Math.max(8, Math.min(50, Number(cal.ui.status_panel_2.title_font_size ?? defaults.status_panel_2.title_font_size ?? 16) || 16));
  cal.ui.status_panel_2.fields = { ...defaults.status_panel_2.fields, ...(cal.ui.status_panel_2.fields || {}) };
  delete cal.ui.status_panel_2.fields.title;
  ensureStatusPanelFieldState(cal.ui.status_panel_2);
  cal.ports = cal.ports || {};
  for (const port of Object.values(cal.ports)) {
    if (!port || typeof port !== "object") continue;
    port.led_left_size = normalisePortLedRectangleSize(port.led_left_size);
    port.led_right_size = normalisePortLedRectangleSize(port.led_right_size);
    port.number_show = port.number_show !== false;
    const displayName = String(port.display_name || "").trim().slice(0, 96);
    if (displayName) port.display_name = displayName;
    else delete port.display_name;
  }
  cal.sfp = cal.sfp || {};
  for (const [name, sfp] of Object.entries(cal.sfp)) {
    if (!sfp || typeof sfp !== "object") continue;
    const fallback = layout.sfp?.labels?.[name];
    if (!Array.isArray(sfp.label) || sfp.label.length < 2) {
      sfp.label = fallback ? [Number(fallback.x), Number(fallback.y)] : [Number(sfp.center?.[0] || 0), Number(sfp.center?.[1] || 0) - 58];
    }
    sfp.label_show = sfp.label_show !== false;
    const displayName = String(sfp.display_name || "").trim().slice(0, 96);
    if (displayName) sfp.display_name = displayName;
    else delete sfp.display_name;
    if (Array.isArray(sfp.led_left_size)) sfp.led_left_size = normalisePortLedRectangleSize(sfp.led_left_size, layout.sfp?.r);
    if (Array.isArray(sfp.led_right_size)) sfp.led_right_size = normalisePortLedRectangleSize(sfp.led_right_size, layout.sfp?.r);
  }
  return cal;
}

function uiFromCalibration(cal) {
  return ensureCalibrationUi(cal)?.ui || defaultUiLayout();
}

function stableCalibrationSwitchKey(config = {}) {
  const raw = String(config?.selected_switch || config?.member || "").trim();
  if (!raw) return "";
  return raw.replace(/[^A-Za-z0-9_-]+/g, "_").replace(/^_+|_+$/g, "").slice(0, 96);
}

function applyConfigToCalibrationForEdit(cal, config = {}) {
  cal = ensureCalibrationUi(cloneCalibrationData(cal || calibration));
  const ui = uiFromCalibration(cal);

  // Start interactive calibration from the same UI currently being rendered.
  // Saved profile values are the base, then explicit Lovelace/card YAML overrides
  // are folded in once when calibration starts. Reset remains the fresh/default path.
  if (config.logo_show !== undefined) ui.logo.show = config.logo_show !== false;
  if (config.logo_file !== undefined) ui.logo.file = String(config.logo_file || ui.logo.file || "sv-logo.png");
  if (config.logo_x !== undefined) ui.logo.x = Number(config.logo_x);
  if (config.logo_y !== undefined) ui.logo.y = Number(config.logo_y);
  if (config.logo_width !== undefined) ui.logo.width = Number(config.logo_width);
  if (config.logo_height !== undefined) ui.logo.height = Number(config.logo_height);
  if (config.logo_source !== undefined) ui.logo.source = String(config.logo_source || ui.logo.source || "default");

  const faceplateAsset = ui.faceplate;
  // Native sidebar cards receive a generated faceplate_file/faceplate_show value
  // as their initial model default. Once a calibration profile has been saved,
  // that profile must remain authoritative; reapplying the generated card values
  // here would replace the user's custom faceplate every time the editor loads or
  // closes. Manual/custom YAML cards retain their existing explicit override
  // behaviour. Older saved profiles are also recognised by a non-default faceplate.
  const nativePanel = config.switch_vision_native_panel === true;
  const scopedCard = Boolean(stableCalibrationSwitchKey(config));
  const savedProfileMarker = Boolean(
    String(cal?.profile_name || "").trim() ||
    String(cal?.base_profile_name || "").trim() ||
    String(cal?.faceplate_file || "").trim()
  );
  const savedCustomFaceplate = faceplateAsset.show !== false &&
    String(faceplateAsset.file || SV_ASSET_DEFAULT) !== SV_ASSET_DEFAULT &&
    String(faceplateAsset.file || SV_ASSET_DEFAULT) !== SV_ASSET_NONE;
  const savedScopedFaceplateIsAuthoritative = scopedCard && (savedProfileMarker || savedCustomFaceplate);
  if (!savedScopedFaceplateIsAuthoritative) {
    if (config.faceplate_show !== undefined || config.faceplate_file !== undefined) {
      const requestedFile = String(config.faceplate_file || SV_ASSET_DEFAULT);
      if (config.faceplate_show === false || requestedFile === SV_ASSET_NONE) {
        // Hidden/legacy faceplate states are no longer supported. Always keep
        // a visible model-recommended faceplate as the safe fallback.
        faceplateAsset.show = true;
        faceplateAsset.file = SV_ASSET_DEFAULT;
        faceplateAsset.source = "default";
      } else if (requestedFile === SV_ASSET_DEFAULT) {
        faceplateAsset.show = true;
        faceplateAsset.file = SV_ASSET_DEFAULT;
        faceplateAsset.source = "default";
      } else {
        faceplateAsset.show = true;
        faceplateAsset.file = safeAssetFilename(requestedFile) || SV_ASSET_DEFAULT;
        faceplateAsset.source = faceplateAsset.file === SV_ASSET_DEFAULT ? "default" : "custom";
      }
    }
    if (config.faceplate_fit !== undefined) faceplateAsset.fit = String(config.faceplate_fit || "fill");
    if (config.faceplate_opacity !== undefined) faceplateAsset.opacity = Number(config.faceplate_opacity);
    cal.ui.faceplate = normaliseFaceplateAsset(faceplateAsset);
  }


  const panel = ui.status_panel;
  if (config?.__raw_config?.show_status_panel === false) panel.show = false;
  if (config.status_panel_x !== undefined) panel.x = Number(config.status_panel_x);
  if (config.status_panel_y !== undefined) panel.y = Number(config.status_panel_y);
  if (config.status_panel_width !== undefined) panel.width = Number(config.status_panel_width);
  if (config.status_panel_height !== undefined) panel.height = Number(config.status_panel_height);
  if (config.status_panel_font_family !== undefined) panel.font_family = String(config.status_panel_font_family);
  if (config.status_panel_font_size !== undefined) panel.font_size = Number(config.status_panel_font_size);
  if (config.status_panel_title_font_size !== undefined) panel.title_font_size = Number(config.status_panel_title_font_size);
  if (config.status_panel_font_weight !== undefined) panel.font_weight = String(config.status_panel_font_weight);
  if (config.status_panel_background_color !== undefined) panel.background_color = String(config.status_panel_background_color);
  if (config.status_panel_border_color !== undefined) panel.border_color = String(config.status_panel_border_color);
  if (config.status_panel_text_color !== undefined) panel.text_color = String(config.status_panel_text_color);
  if (config.status_panel_label_color !== undefined) panel.label_color = String(config.status_panel_label_color);
  if (config.status_panel_title_color !== undefined) panel.title_color = String(config.status_panel_title_color);

  ensureStatusPanelFieldState(panel);
  for (const type of Object.keys(STATUS_PANEL_ROW_DEFS)) {
    const scopedKey = `status_panel_${type}_fields`;
    const hiddenKey = `status_panel_${type}_hidden_fields`;
    let fields = fieldListFromValue(config[scopedKey]);
    if (!fields && config.status_panel_fields && typeof config.status_panel_fields === "object" && !Array.isArray(config.status_panel_fields)) {
      fields = fieldListFromValue(config.status_panel_fields[type]);
    }
    if (!fields && type === "switch") fields = fieldListFromValue(config.status_panel_fields);
    if (fields) panel.field_order[type] = normaliseStatusPanelFieldOrder(fields, type);

    const hidden = fieldListFromValue(config[hiddenKey]) || (type === "switch" ? fieldListFromValue(config.status_panel_hidden_fields) : null);
    if (hidden) {
      const defs = STATUS_PANEL_ROW_DEFS[type].defaults;
      panel.hidden_fields[type] = hidden.filter((field, idx, arr) => defs.includes(field) && arr.indexOf(field) === idx);
    }
  }

  const stack = configuredStackProfile(config, cal);
  cal.stack = stack;

  const switchIp = explicitConfigValue(config, ["switch_ip", "management_ip", "mgmt_ip", "host_ip", "snmp_host"]) ||
    managementIpFromCalibration(cal) ||
    "";
  ensureCalibrationManagement(cal);
  cal.management.switch_ip = String(switchIp || "").trim();

  return ensureCalibrationUi(cal);
}

function statusPanelUi(config, cal, panelNumber = 1) {
  const suffix = panelNumber === 2 ? "_2" : "";
  const ui = uiFromCalibration(cal)[panelNumber === 2 ? "status_panel_2" : "status_panel"];
  return {
    ...ui,
    show: config[`show_status_panel${suffix}`] !== false && ui.show !== false,
    x: Number(config[`status_panel${suffix}_x`] ?? ui.x),
    y: Number(config[`status_panel${suffix}_y`] ?? ui.y),
    width: Number(config[`status_panel${suffix}_width`] ?? ui.width),
    height: Number(config[`status_panel${suffix}_height`] ?? ui.height),
    font_family: String(config[`status_panel${suffix}_font_family`] ?? ui.font_family),
    font_size: Math.max(8, Math.min(50, Number(config[`status_panel${suffix}_font_size`] ?? ui.font_size) || 16)),
    title_font_size: Math.max(8, Math.min(50, Number(config[`status_panel${suffix}_title_font_size`] ?? ui.title_font_size) || 16)),
    font_weight: String(config[`status_panel${suffix}_font_weight`] ?? ui.font_weight ?? "normal"),
    background_color: String(config[`status_panel${suffix}_background_color`] ?? ui.background_color),
    border_color: String(
      panelNumber === 2 && calibrationControlsEnabled(config)
        ? ui.border_color
        : (config[`status_panel${suffix}_border_color`] ?? ui.border_color)
    ),
    border_show: panelNumber === 2 && calibrationControlsEnabled(config)
      ? ui.border_show !== false
      : (config[`status_panel${suffix}_border_show`] === undefined
        ? ui.border_show !== false
        : config[`status_panel${suffix}_border_show`] !== false),
    text_color: String(config[`status_panel${suffix}_text_color`] ?? ui.text_color),
    label_color: String(config[`status_panel${suffix}_label_color`] ?? ui.label_color),
    title_color: String(config[`status_panel${suffix}_title_color`] ?? ui.title_color),
    title_mode: String(config[`status_panel${suffix}_title_mode`] ?? ui.title_mode ?? "default"),
    title_field: String(config[`status_panel${suffix}_title_field`] ?? ui.title_field ?? "model"),
    custom_title: String(config[`status_panel${suffix}_custom_title`] ?? (ui.custom_title || (panelNumber === 2 ? (config.title || config.member || "Switch") : "")))
  };
}

function statusFieldPosition(panel, key, fallback) {
  const xy = panel.fields?.[key] || fallback;
  return [panel.x + Number(xy[0]), panel.y + Number(xy[1])];
}

function statusFieldDisplayPosition(panel, key, fallback, panelNumber = 1) {
  const reference = statusPanelFieldReference(key, panelNumber, fallback);
  const saved = panel.fields?.[key] || reference;
  const rowMatch = /^row(\d+)_(key|value)$/.exec(String(key));
  if (!rowMatch) return statusFieldPosition(panel, key, fallback);

  const rowIndex = Math.max(0, Number(rowMatch[1]) - 1);
  const kind = rowMatch[2];
  const fontSize = Math.max(8, Number(panel.font_size) || 16);
  const rowSpacing = Math.max(17, fontSize + 2);
  const horizontalShift = panelNumber === 2 ? 15 : 5;
  const panelLeft = Number(panel.x) + 4;
  const panelRight = Number(panel.x) + Number(panel.width) - 4;

  const defaultLabelX = Math.max(panelLeft + 4, Math.min(panelRight - 96, Number(panel.x) + Number(fallback[0]) - horizontalShift));
  // Move the shared value column another 10 px left while preserving
  // independent per-field nudging.
  const defaultValueX = Math.min(panelRight - 24, defaultLabelX + 33);
  const defaultX = kind === "key" ? defaultLabelX : defaultValueX;
  const x = defaultX + (Number(saved[0]) - Number(reference[0]));

  const compactBaseline = Number(panel.y) + 4 + fontSize + (rowIndex * rowSpacing);
  const y = compactBaseline + (Number(saved[1]) - Number(reference[1]));
  return [x, y];
}


function estimatedSvgTextWidth(value, fontSize) {
  const text = String(value ?? "");
  const size = Number(fontSize) || 14;
  let units = 0;
  for (const ch of text) {
    if (ch === " " || ch === "." || ch === ":" || ch === "/" || ch === "-") units += 0.32;
    else if (ch === "i" || ch === "l" || ch === "I" || ch === "1" || ch === "|") units += 0.34;
    else if (ch === "W" || ch === "M" || ch === "@" || ch === "#") units += 0.92;
    else if (/[A-Z]/.test(ch)) units += 0.68;
    else units += 0.56;
  }
  return units * size;
}

function truncateSvgText(value, fontSize, maxWidth) {
  const raw = usableValue(value) || "—";
  const limit = Number(maxWidth);
  if (!Number.isFinite(limit) || limit <= 0) return raw;
  if (estimatedSvgTextWidth(raw, fontSize) <= limit) return raw;

  const ellipsis = "…";
  let out = raw;
  while (out.length > 1 && estimatedSvgTextWidth(`${out}${ellipsis}`, fontSize) > limit) {
    out = out.slice(0, -1);
  }
  return `${out.trimEnd()}${ellipsis}`;
}

function addSvgTitle(el, value) {
  const raw = usableValue(value);
  if (!el || !raw) return el;
  const title = e("title");
  title.textContent = raw;
  el.appendChild(title);
  return el;
}

function stylePanelText(el, panel, kind = "value") {
  const fill = kind === "title" ? panel.title_color : (kind === "key" ? panel.label_color : panel.text_color);
  const fontSize = kind === "title" ? panel.title_font_size : panel.font_size;
  return applySvgStyle(el, {
    fontFamily: panel.font_family,
    fontSize: `${fontSize}px`,
    fontWeight: panel.font_weight || "normal",
    fill
  });
}

function drawStatusPanel(svg, hass, config, cal = calibration, panelNumber = 1) {
  const panel = statusPanelUi(config, cal, panelNumber);
  if (!panel.show) return;

  const x = Number(panel.x);
  const y = Number(panel.y);
  const w = Number(panel.width);
  const h = Number(panel.height);
  // Status Box 2 is an independent switch-summary panel. It must never inherit
  // selected port or SFP details from Status Box 1. Its calibrated dimensions
  // are authoritative: showing or hiding fields changes only the content and
  // never resizes the box at runtime.
  const selected = panelNumber === 2 ? null : selectedInterface(config);

  const panelRect = rect(svg, x, y, w, h, 0, "cv-status-panel-bg");
  const panelStroke = panel.border_show ? String(panel.border_color || "rgba(245,248,250,.75)") : "none";
  const panelStrokeWidth = panel.border_show ? "1.5" : "0";
  // Keep the background and the user-controlled border as separate SVG elements.
  // This prevents the calibration selection overlay and the shared background CSS
  // class from masking Status Box 2 border enable/colour changes.
  const rawPanelBackground = String(panel.background_color || "transparent");
  const panelBackground = rawPanelBackground;
  panelRect.setAttribute("fill", panelBackground);
  panelRect.setAttribute("stroke", "none");
  panelRect.setAttribute("stroke-width", "0");
  applySvgStyle(panelRect, {
    fill: panelBackground,
    stroke: "none",
    strokeWidth: "0"
  });
  if (panel.border_show) {
    const borderRect = rect(svg, x, y, w, h, 0, "cv-status-panel-border");
    borderRect.setAttribute("fill", "none");
    borderRect.setAttribute("stroke", panelStroke);
    borderRect.setAttribute("stroke-width", panelStrokeWidth);
    borderRect.setAttribute("vector-effect", "non-scaling-stroke");
    applySvgStyle(borderRect, {
      fill: "none",
      stroke: panelStroke,
      strokeWidth: panelStrokeWidth
    });
  }

  // Both status boxes are intentionally title-free. Legacy title settings remain
  // readable in saved calibration profiles for compatibility, but are ignored at
  // runtime so every enabled status row can use the freed vertical space.

  let rows;
  if (selected?.type === "port") {
    const details = selectedPortDetails(hass, config, selected.id);
    const rates = portTrafficRates(hass, config, selected.id);
    const values = {
      vlan: details.vlan,
      mode: details.mode,
      desc: details.description,
      link: details.link,
      rx: formatBps(rates.rxBps),
      tx: formatBps(rates.txBps)
    };
    let fields = statusPanelFieldSelection(config, "port", cal, panelNumber);
    // MODE is a Juniper-only optional row. Cisco keeps its established
    // VLAN/DESC/LINK/RX/TX presentation and never displays MODE.
    if (!details.isJuniper) fields = fields.filter((field) => field !== "mode");
    const labels = { ...STATUS_PANEL_ROW_DEFS.port.labels, vlan: details.vlanLabel || "VLAN" };
    rows = makeRows(values, fields, labels);
  } else if (selected?.type === "sfp") {
    const details = selectedSfpDetails(hass, config, selected.id);
    const rates = sfpTrafficRates(hass, config, selected.id);
    const values = {
      vlan: details.vlan,
      mode: details.mode,
      desc: details.description,
      link: details.link,
      rx: formatBps(rates.rxBps),
      tx: formatBps(rates.txBps)
    };
    let fields = statusPanelFieldSelection(config, "sfp", cal, panelNumber);
    if (!details.isJuniper) fields = fields.filter((field) => field !== "mode");
    rows = makeRows(values, fields, STATUS_PANEL_ROW_DEFS.sfp.labels);
  } else {
    rows = switchSummaryDetails(hass, config, cal, panelNumber).rows;
  }

  const visibleRows = Math.min(13, rows.length);
  const panelLeft = x + 4;
  const panelRight = x + w - 4;
  const panelTop = y + 4;
  const panelBottom = y + h - 4;
  const rowFontSize = Math.max(8, Number(panel.font_size) || 16);

  for (let i = 0; i < visibleRows; i++) {
    const row = rows[i] || ["", ""];
    const key = `row${i + 1}_key`;
    const val = `row${i + 1}_value`;
    // Resolve each field target through the same coordinate transform used by
    // the calibration overlay. This keeps the compact row grid as the default
    // while allowing every label and value target to be nudged independently.
    const keyDef = STATUS_PANEL_FIELD_DEFS.find(([id]) => id === key);
    const valDef = STATUS_PANEL_FIELD_DEFS.find(([id]) => id === val);
    const [kx, ky] = statusFieldDisplayPosition(panel, key, keyDef?.[2] || [24, 55], panelNumber);
    const [vx, vy] = statusFieldDisplayPosition(panel, val, valDef?.[2] || [281, 55], panelNumber);

    // Safety rule: never render a status row outside the calibrated status box.
    // This prevents identity or telemetry fields from overlapping ports or other faceplate elements.
    const rowInsidePanel =
      Number.isFinite(kx) && Number.isFinite(ky) && Number.isFinite(vx) && Number.isFinite(vy) &&
      kx >= panelLeft && kx <= panelRight &&
      vx >= panelLeft && vx <= panelRight &&
      (ky - (rowFontSize * 0.7)) >= panelTop && (ky + (rowFontSize * 0.35)) <= panelBottom &&
      (vy - (rowFontSize * 0.7)) >= panelTop && (vy + (rowFontSize * 0.35)) <= panelBottom;
    if (!rowInsidePanel) continue;

    const labelMaxWidth = Math.max(40, vx - kx - 10);
    const fullLabel = usableValue(row[0]) || "";
    const displayLabel = truncateSvgText(fullLabel, panel.font_size, labelMaxWidth);
    const labelEl = stylePanelText(text(svg, kx, ky, displayLabel, "cv-panel-key", "start"), panel, "key");
    if (displayLabel !== fullLabel) addSvgTitle(labelEl, fullLabel);

    // All values begin at the same fixed X coordinate and are left aligned. This
    // makes short and long values visually line up instead of appearing indented
    // according to their text width. Long values are truncated at the box edge.
    const valueMaxWidth = Math.max(18, panelRight - vx);
    const fullValue = usableValue(row[1]) || "—";
    const displayValue = truncateSvgText(fullValue, panel.font_size, valueMaxWidth);
    const valueEl = stylePanelText(text(svg, vx, vy, displayValue, "cv-panel-value", "start"), panel, "value");
    if (displayValue !== fullValue) addSvgTitle(valueEl, fullValue);
  }
}

function selectedInterface(config) {
  const selected = config.selected_interface || null;
  if (selected && selected.type) return selected;

  if (config.selected_port) {
    return { type: "port", id: Number(config.selected_port) };
  }

  return null;
}

function isSelected(config, type, id) {
  const selected = selectedInterface(config);
  return selected?.type === type && Number(selected.id) === Number(id);
}

function sfpPortNumber(name) {
  const nums = String(name).match(/\d+/g) || [];
  return Number(nums[nums.length - 1] || 0);
}


function calibrationEnabled(config) {
  return config.calibration_mode === true || config.calibration === true;
}

function calibrationTarget(config) {
  const target = config.calibration_target ?? config.calibration_item ?? "all";

  if (target === null || target === undefined || target === "") return "all";
  if (typeof target === "number") return { type: "port", id: Number(target) };

  if (typeof target === "object") {
    const type = String(target.type || "").toLowerCase();
    const id = target.id ?? target.name ?? target.port ?? target.led;
    return { type, id: String(id) };
  }

  const raw = String(target).trim();
  const low = raw.toLowerCase();
  if (["all", "ports", "sfp", "sfps", "uplinks", "status", "status_leds", "leds", "logo", "status_box", "status_fields"].includes(low)) return low;
  if (low.startsWith("status_field:")) return { type: "status_field", id: raw.split(":").slice(1).join(":") };

  const match = low.match(/^(port|p|sfp|status|led)[:\-_ ]?(.+)$/);
  if (match) {
    const type = match[1] === "p" ? "port" : (match[1] === "led" ? "status" : match[1]);
    return { type, id: match[2].toUpperCase() };
  }

  if (/^\d+$/.test(low)) return { type: "port", id: Number(low) };

  return { type: "status", id: raw.toUpperCase() };
}

function calibrationRawTarget(config) {
  return String(config?.calibration_target ?? config?.calibration_item ?? "all").trim().toLowerCase();
}


function parseCalibrationPortSelection(value, cal = null) {
  const raw = Array.isArray(value) ? value.join(",") : String(value || "");
  const values = new Set();
  const invalid = [];

  for (const tokenValue of raw.split(",")) {
    const token = String(tokenValue || "").trim();
    if (!token) continue;

    const range = token.match(/^(\d+)\s*-\s*(\d+)$/);
    if (range) {
      const a = Number(range[1]);
      const b = Number(range[2]);
      const start = Math.min(a, b);
      const end = Math.max(a, b);
      if (!Number.isFinite(start) || !Number.isFinite(end) || start < 1) {
        invalid.push(token);
        continue;
      }
      for (let n = start; n <= end; n += 1) values.add(String(n));
      continue;
    }

    if (/^\d+$/.test(token) && Number(token) >= 1) {
      values.add(String(Number(token)));
      continue;
    }

    invalid.push(token);
  }

  let keys = [...values].sort((a, b) => Number(a) - Number(b));
  const missing = [];
  if (cal?.ports) {
    keys = keys.filter((key) => {
      const exists = Boolean(cal.ports?.[key]);
      if (!exists) missing.push(key);
      return exists;
    });
  }

  return { keys, invalid, missing, normalised: keys.join(",") };
}

function calibrationSfpAliasMap(cal = null) {
  const aliases = new Map();

  for (const name of Object.keys(cal?.sfp || {})) {
    const key = String(name);
    const port = sfpPortNumber(key);
    const candidates = new Set([
      key,
      ...key.split(/[\\/|,]+/).map((item) => item.trim()).filter(Boolean)
    ]);

    if (port > 0) {
      candidates.add(`g${port}`);
      candidates.add(`te${port}`);
      candidates.add(`sfp${port}`);
      candidates.add(`sfp+${port}`);
      candidates.add(`uplink${port}`);
    }

    for (const candidate of candidates) {
      const alias = String(candidate || "").trim().toLowerCase();
      if (alias && !aliases.has(alias)) aliases.set(alias, key);
    }
  }

  return aliases;
}

function parseCalibrationCustomPortSelection(value, cal = null) {
  const raw = Array.isArray(value) ? value.join(",") : String(value || "");
  const portValues = new Set();
  const sfpValues = new Set();
  const invalid = [];
  const missing = [];
  const sfpAliases = calibrationSfpAliasMap(cal);

  for (const tokenValue of raw.split(",")) {
    const token = String(tokenValue || "").trim();
    if (!token) continue;

    const range = token.match(/^(\d+)\s*-\s*(\d+)$/);
    if (range) {
      const a = Number(range[1]);
      const b = Number(range[2]);
      const start = Math.min(a, b);
      const end = Math.max(a, b);

      if (!Number.isFinite(start) || !Number.isFinite(end) || start < 1) {
        invalid.push(token);
        continue;
      }

      for (let n = start; n <= end; n += 1) portValues.add(String(n));
      continue;
    }

    if (/^\d+$/.test(token) && Number(token) >= 1) {
      portValues.add(String(Number(token)));
      continue;
    }

    const sfpKey = sfpAliases.get(token.toLowerCase());
    if (sfpKey) {
      sfpValues.add(sfpKey);
      continue;
    }

    invalid.push(token);
  }

  let portKeys = [...portValues].sort((a, b) => Number(a) - Number(b));
  if (cal?.ports) {
    portKeys = portKeys.filter((key) => {
      if (cal.ports?.[key]) return true;
      missing.push(key);
      return false;
    });
  }

  const sfpOrder = Object.keys(cal?.sfp || {});
  const sfpKeys = sfpOrder.filter((key) => sfpValues.has(key));

  return {
    portKeys,
    sfpKeys,
    invalid,
    missing,
    normalisedPorts: portKeys.join(","),
    normalisedSfps: sfpKeys.join(",")
  };
}

function calibrationSelectedPortKeys(config, cal = null) {
  return parseCalibrationPortSelection(config?.calibration_port_selection, cal).keys;
}

function calibrationSelectedSfpKeys(config, cal = null) {
  const wanted = new Set(
    String(config?.calibration_sfp_selection || "")
      .split(",")
      .map((item) => item.trim().toLowerCase())
      .filter(Boolean)
  );

  return Object.keys(cal?.sfp || {}).filter((key) => wanted.has(String(key).toLowerCase()));
}

function calibrationSfpIsSelected(config, sfpPort, cal = null) {
  const found = sfpEntryForTarget(cal, sfpPort);
  if (!found) return false;
  return calibrationSelectedSfpKeys(config, cal).includes(found.key);
}

function calibrationPortIsSelected(config, portNumber, cal = null) {
  return calibrationSelectedPortKeys(config, cal).includes(String(Number(portNumber)));
}

function calibrationMatches(config, type, id, cal = null) {
  const target = normalCalibrationTarget(config);
  const targetType = String(target?.type || "").toLowerCase();
  const itemType = String(type || "").toLowerCase();
  const itemId = String(id || "").toUpperCase();

  if (targetType === "ports" && itemType === "port") {
    if (target.selection === "custom" || target.id === "custom") return calibrationPortIsSelected(config, id, cal);
    if (target.parity === "odd" || target.id === "odd") return Number(id) % 2 === 1;
    if (target.parity === "even" || target.id === "even") return Number(id) % 2 === 0;
    return true;
  }
  if (targetType === "sfps" && itemType === "sfp") {
    if (target.selection === "custom" || target.id === "custom") return calibrationSfpIsSelected(config, id, cal);
    return true;
  }
  if (targetType === "status_leds" && itemType === "status") return true;
  if (!target || typeof target !== "object") return false;

  return targetType === itemType && String(target.id || "").toUpperCase() === itemId;
}

function calibrationPortPartMatches(config, portNumber, part, cal = null) {
  const partName = String(part || "").toLowerCase();
  const target = normalCalibrationTarget(config);
  const targetType = String(target?.type || "").toLowerCase();
  const targetPart = String(target?.part || config?.calibration_part || "center").toLowerCase();

  if (targetType === "number_labels") return partName === "number";
  if (targetType === "ports") {
    if (targetPart !== partName) return false;
    if (target.selection === "custom" || target.id === "custom") return calibrationPortIsSelected(config, portNumber, cal);
    if (target.parity === "odd" || target.id === "odd") return Number(portNumber) % 2 === 1;
    if (target.parity === "even" || target.id === "even") return Number(portNumber) % 2 === 0;
    return true;
  }
  if (targetType !== "port") return false;
  return String(target.id) === String(portNumber) && targetPart === partName;
}

function calibrationInfoText(config) {
  const target = normalCalibrationTarget(config);
  const type = String(target?.type || "").toLowerCase();

  if (type === "ports") {
    const group = target.selection === "custom" || target.id === "custom"
      ? "SELECTED RJ45"
      : (target.parity === "odd" || target.id === "odd"
        ? "ODD RJ45"
        : (target.parity === "even" || target.id === "even"
          ? "EVEN RJ45"
          : "ALL RJ45"));
    return `${group} · ${String(target.part || config?.calibration_part || "entire").toUpperCase()}`;
  }

  if (type === "sfps") {
    const group = target.selection === "custom" || target.id === "custom"
      ? "SELECTED SFP"
      : "ALL SFP";
    return `${group} · ${String(target.part || config?.calibration_part || "center").toUpperCase()}`;
  }

  if (type === "number_labels") return "ALL PORT LABELS";
  if (type === "status_leds") return "ALL STATUS LEDS";
  if (type === "status_fields") return `STATUS BOX 1 ${statusFieldGroupDescription(target.part || config?.calibration_part)}`;
  if (type === "status_fields_2") return `STATUS BOX 2 ${statusFieldGroupDescription(target.part || config?.calibration_part)}`;
  if (type === "status_field") return `STATUS BOX 1 · ${String(target.id || "").toUpperCase()}`;
  if (type === "status_field_2") return `STATUS BOX 2 · ${String(target.id || "").toUpperCase()}`;
  if (type === "status_box") return "STATUS BOX 1";
  if (type === "status_box_2") return "STATUS BOX 2";
  if (type === "calibration_button") return "CALIBRATION BUTTON";
  if (type === "logo") return "LOGO";
  if (type === "port") return `RJ45 ${String(target.id || "")}`;
  if (type === "sfp") return `SFP ${String(target.id || "")}`;
  if (type === "status") return `STATUS ${String(target.id || "").toUpperCase()}`;

  return `${String(target?.type || "CALIBRATION").toUpperCase()} ${String(target?.id || "")}`.trim();
}

function visualHitboxSize(type, hitbox) {
  const [baseW, baseH] = hitbox || [0, 0];
  const w = type === "port" ? Number(baseW) + 18 : Number(baseW) + 12;
  const h = type === "port" ? Number(baseH) + 4 : Number(baseH) + 16;
  return [Math.max(8, w), Math.max(8, h)];
}

function calibrationOverlayPartActive(editable, itemType, itemKey, itemPart) {
  if (!editable) return false;

  const targetType = String(editable.type || "").toLowerCase();
  const type = String(itemType || "").toLowerCase();
  const key = String(itemKey ?? "").toLowerCase();
  const part = String(itemPart || "").toLowerCase();
  const targetPart = String(editable.part || "").toLowerCase();

  const sameKey = (value) =>
    value !== undefined &&
    value !== null &&
    String(value).toLowerCase() === key;

  const groupHasKey = () =>
    Array.isArray(editable.keys) &&
    editable.keys.some((value) => sameKey(value));

  // "Port Numbers" deliberately spans both RJ45 number positions and
  // SFP/uplink label positions.
  if (targetType === "number_labels") {
    return (type === "port" && part === "number") ||
      (type === "sfp" && part === "label");
  }

  if (targetType === "status_leds") {
    return type === "status";
  }

  if (targetType === "status_fields" && type === "status_field") {
    if (targetPart === "label") return key.endsWith("_key");
    if (targetPart === "value") return key.endsWith("_value");
    return true;
  }

  if (targetType === "status_fields_2" && type === "status_field_2") {
    if (targetPart === "label") return key.endsWith("_key");
    if (targetPart === "value") return key.endsWith("_value");
    return true;
  }

  if (targetType === "status_field" && type === "status_field") {
    return sameKey(editable.key ?? editable.id);
  }

  if (targetType === "status_field_2" && type === "status_field_2") {
    return sameKey(editable.key ?? editable.id);
  }

  if (["logo", "calibration_button", "status_box", "status_box_2"].includes(targetType)) {
    return type === targetType && part === "box";
  }

  if (targetType === "port" || targetType === "ports") {
    if (type !== "port") return false;

    const selected = targetType === "ports"
      ? groupHasKey()
      : sameKey(editable.key ?? editable.id);

    if (!selected) return false;

    if (targetPart === "entire") {
      return ["center", "led_left", "led_right"].includes(part);
    }

    return targetPart === part;
  }

  if (targetType === "sfp" || targetType === "sfps") {
    if (type !== "sfp") return false;

    const selected = targetType === "sfps"
      ? groupHasKey()
      : sameKey(editable.key ?? editable.id);

    if (!selected) return false;

    return targetPart === part;
  }

  if (targetType === "status" && type === "status") {
    return sameKey(editable.key ?? editable.id);
  }

  return false;
}

function drawCalibrationOverlay(svg, { config, calibration }) {
  if (!calibrationEnabled(config)) return;

  const showHitboxes = config.calibration_show_hitboxes !== false;
  const showLabels = config.calibration_show_labels !== false;
  const showLedRings = config.calibration_show_led_rings !== false;
  const editable = getEditableCalibrationTarget(calibration, config);

  text(
    svg,
    24,
    28,
    `CALIBRATION · ${calibrationInfoText(config)}`,
    "cv-calibration-title",
    "start"
  );

  const drawRing = (x, y, r, active) => {
    if (!Number.isFinite(Number(x)) || !Number.isFinite(Number(y))) return;
    const ring = e("circle");
    ring.setAttribute(
      "class",
      active
        ? "cv-calibration-ring cv-calibration-active"
        : "cv-calibration-ring"
    );
    ring.setAttribute("cx", x);
    ring.setAttribute("cy", y);
    ring.setAttribute("r", r);
    svg.appendChild(ring);
  };

  const drawHitbox = (type, key, center, hitbox, label) => {
    if (!Array.isArray(center) || center.length < 2) return;

    const [cx, cy] = center;
    const [w, h] = visualHitboxSize(type, hitbox);
    const active = calibrationOverlayPartActive(
      editable,
      type,
      key,
      "center"
    );

    if (showHitboxes) {
      const r = e("rect");
      r.setAttribute(
        "class",
        active
          ? "cv-calibration-hitbox cv-calibration-active"
          : "cv-calibration-hitbox"
      );
      r.setAttribute("x", cx - (w / 2));
      r.setAttribute("y", cy - (h / 2));
      r.setAttribute("width", w);
      r.setAttribute("height", h);
      r.setAttribute("rx", 4);
      r.setAttribute("ry", 4);
      svg.appendChild(r);
    }

    if (showLabels) {
      text(
        svg,
        cx,
        cy + 4,
        label,
        active
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }
  };

  // --------------------------------------------------------
  // RJ45
  // --------------------------------------------------------

  for (const [num, port] of Object.entries(calibration.ports || {})) {
    drawHitbox(
      "port",
      num,
      port.center,
      port.hitbox,
      String(port.display_name || `P${num}`)
    );

    const linkActive = calibrationOverlayPartActive(
      editable,
      "port",
      num,
      "led_left"
    );

    const activityActive = calibrationOverlayPartActive(
      editable,
      "port",
      num,
      "led_right"
    );

    const numberActive = calibrationOverlayPartActive(
      editable,
      "port",
      num,
      "number"
    );

    if (showLedRings) {
      if (Array.isArray(port.led_left)) {
        drawRing(
          port.led_left[0],
          port.led_left[1],
          7.5,
          linkActive
        );
      }

      if (Array.isArray(port.led_right)) {
        drawRing(
          port.led_right[0],
          port.led_right[1],
          7.5,
          activityActive
        );
      }

      // Number positions are normally left uncluttered. Draw their
      // calibration ring only when that number target is actually selected.
      if (numberActive && Array.isArray(port.number)) {
        drawRing(
          port.number[0],
          port.number[1],
          8,
          true
        );
      }
    }

    if (showLabels && numberActive && Array.isArray(port.number)) {
      text(
        svg,
        port.number[0],
        port.number[1] - 12,
        `NUMBER ${num}`,
        "cv-calibration-label cv-calibration-active-text"
      );
    }
  }

  // --------------------------------------------------------
  // SFP / UPLINK
  // --------------------------------------------------------

  for (const [name, sfp] of Object.entries(calibration.sfp || {})) {
    drawHitbox(
      "sfp",
      name,
      sfp.center,
      sfp.hitbox,
      String(sfp.display_name || name)
    );

    const linkActive = calibrationOverlayPartActive(
      editable,
      "sfp",
      name,
      "led_left"
    );

    const activityActive = calibrationOverlayPartActive(
      editable,
      "sfp",
      name,
      "led_right"
    );

    const labelActive = calibrationOverlayPartActive(
      editable,
      "sfp",
      name,
      "label"
    );

    if (showLedRings) {
      const left = sfpLedPoint(sfp, "led_left", false);
      const right = sfpLedPoint(sfp, "led_right", false);

      if (Array.isArray(left)) {
        drawRing(left[0], left[1], 8, linkActive);
      }

      if (Array.isArray(right)) {
        drawRing(right[0], right[1], 8, activityActive);
      }

      if (labelActive && Array.isArray(sfp.label)) {
        drawRing(sfp.label[0], sfp.label[1], 8, true);
      }
    }

    if (showLabels && labelActive && Array.isArray(sfp.label)) {
      text(
        svg,
        sfp.label[0],
        sfp.label[1] - 12,
        `${name} LABEL`,
        "cv-calibration-label cv-calibration-active-text"
      );
    }
  }

  // --------------------------------------------------------
  // STATUS LEDs
  // --------------------------------------------------------

  for (const [name, xy] of Object.entries(calibration.status_leds || {})) {
    if (String(name).toUpperCase() === "MODE") continue;
    if (!Array.isArray(xy)) continue;

    const [x, y] = xy;
    const active = calibrationOverlayPartActive(
      editable,
      "status",
      name,
      "center"
    );

    if (showLedRings) drawRing(x, y, 10, active);

    if (showLabels) {
      text(
        svg,
        x,
        y - 16,
        name,
        active
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }
  }

  const ui = uiFromCalibration(calibration);

  // --------------------------------------------------------
  // LOGO
  // --------------------------------------------------------

  const logoUi = ui.logo;
  const logoActive = calibrationOverlayPartActive(
    editable,
    "logo",
    "logo",
    "box"
  );

  if (logoUi && (logoUi.show !== false || logoActive)) {
    if (showHitboxes) {
      rect(
        svg,
        logoUi.x,
        logoUi.y,
        logoUi.width,
        logoUi.height,
        4,
        logoActive
          ? "cv-calibration-hitbox cv-calibration-active"
          : "cv-calibration-hitbox"
      );
    }

    if (showLabels) {
      text(
        svg,
        logoUi.x + (logoUi.width / 2),
        logoUi.y - 12,
        "LOGO",
        logoActive
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }
  }

  // --------------------------------------------------------
  // STATUS BOX 1
  // --------------------------------------------------------

  const panel = statusPanelUi(config, calibration, 1);

  const panelActive = calibrationOverlayPartActive(
    editable,
    "status_box",
    "status_box_1",
    "box"
  );

  const panelFieldTarget =
    ["status_fields", "status_field"].includes(
      String(editable?.type || "").toLowerCase()
    );

  if (panel && (panel.show !== false || panelActive || panelFieldTarget)) {
    if (showHitboxes) {
      rect(
        svg,
        panel.x - 5,
        panel.y - 5,
        panel.width + 10,
        panel.height + 10,
        4,
        panelActive
          ? "cv-calibration-hitbox cv-calibration-active"
          : "cv-calibration-hitbox"
      );
    }

    if (showLabels) {
      text(
        svg,
        panel.x + (panel.width / 2),
        panel.y - 12,
        "STATUS BOX 1",
        panelActive
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }

    if (panel.show !== false || panelFieldTarget) {
      for (const [key, label, fallback] of STATUS_PANEL_FIELD_DEFS) {
        const [fx, fy] = statusFieldDisplayPosition(
          panel,
          key,
          fallback,
          1
        );

        const active = calibrationOverlayPartActive(
          editable,
          "status_field",
          key,
          "field"
        );

        if (showLedRings) drawRing(fx, fy, 7, active);

        if (showLabels && active) {
          text(
            svg,
            fx,
            fy - 12,
            label,
            "cv-calibration-label cv-calibration-active-text"
          );
        }
      }
    }
  }

  // --------------------------------------------------------
  // CALIBRATION BUTTON
  // --------------------------------------------------------

  const buttonUi = ui.calibration_button;

  const buttonActive = calibrationOverlayPartActive(
    editable,
    "calibration_button",
    "calibration_button",
    "box"
  );

  if (buttonUi && (buttonUi.show !== false || buttonActive)) {
    if (showHitboxes) {
      rect(
        svg,
        buttonUi.x,
        buttonUi.y,
        buttonUi.width,
        buttonUi.height,
        4,
        buttonActive
          ? "cv-calibration-hitbox cv-calibration-active"
          : "cv-calibration-hitbox"
      );
    }

    if (showLabels) {
      text(
        svg,
        buttonUi.x + (buttonUi.width / 2),
        buttonUi.y - 8,
        "CALIBRATION BUTTON",
        buttonActive
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }
  }

  // --------------------------------------------------------
  // STATUS BOX 2
  // --------------------------------------------------------

  const panel2 = statusPanelUi(config, calibration, 2);

  const panel2Active = calibrationOverlayPartActive(
    editable,
    "status_box_2",
    "status_box_2",
    "box"
  );

  const panel2FieldTarget =
    ["status_fields_2", "status_field_2"].includes(
      String(editable?.type || "").toLowerCase()
    );

  if (panel2 && (panel2.show !== false || panel2Active || panel2FieldTarget)) {
    if (showHitboxes) {
      rect(
        svg,
        panel2.x - 5,
        panel2.y - 5,
        panel2.width + 10,
        panel2.height + 10,
        4,
        panel2Active
          ? "cv-calibration-hitbox cv-calibration-active"
          : "cv-calibration-hitbox"
      );
    }

    if (showLabels) {
      text(
        svg,
        panel2.x + (panel2.width / 2),
        panel2.y - 12,
        "STATUS BOX 2",
        panel2Active
          ? "cv-calibration-label cv-calibration-active-text"
          : "cv-calibration-label"
      );
    }

    if (panel2.show !== false || panel2FieldTarget) {
      for (const [key, label, fallback] of STATUS_PANEL_FIELD_DEFS) {
        const [fx, fy] = statusFieldDisplayPosition(
          panel2,
          key,
          fallback,
          2
        );

        const active = calibrationOverlayPartActive(
          editable,
          "status_field_2",
          key,
          "field"
        );

        if (showLedRings) drawRing(fx, fy, 7, active);

        if (showLabels && active) {
          text(
            svg,
            fx,
            fy - 12,
            label,
            "cv-calibration-label cv-calibration-active-text"
          );
        }
      }
    }
  }
}


function statusLedIsHidden(calibration, name) {
  const hidden = uiFromCalibration(calibration)?.status_leds?.hidden;
  if (!Array.isArray(hidden)) return false;
  const target = String(name).toUpperCase();
  return hidden.some((item) => String(item).toUpperCase() === target);
}

function drawPanel(svg, { hass, config, calibration, layout }) {
  const calibrationTestMode = calibrationTestModeEnabled(config);

  if (config.show_labels) {
    const ui = uiFromCalibration(calibration);
    const logoUi = ui.logo || layout.logo;
    if (logoUi?.show !== false && logoUi?.file) {
      image(
        svg,
        logoUi.x,
        logoUi.y,
        logoUi.width,
        logoUi.height,
        assetUrl("logos", logoUi.file, config.version || "dev"),
        "cv-logo-img"
      );
    } else if (logoUi?.show !== false && layout.logo) {
      text(svg, layout.logo.fallback_x, layout.logo.fallback_y, layout.logo.fallback_text, "cv-logo-word", "start");
    }

  }

  if (config.show_status_leds) {
    const poeActive = switchPoeActive(hass, config);
    // A generated card's explicit member number is itself confirmation that the
    // card belongs to a discovered stack. Keep calibration-driven stacks working
    // as before, while ensuring every generated member (including member 1) gets
    // the same blue STACK LED.
    const explicitStackMember = Number(config?.stack_member_number || 0);
    const stackActive = configuredStackProfile(config, calibration).enabled === true
      || (explicitStackMember >= 1 && explicitStackMember <= 16);

    for (const [name, xy] of Object.entries(calibration.status_leds)) {
      if (statusLedIsHidden(calibration, name)) continue;
      if (name === "MODE" && config.show_status_panel !== false) continue;

      const [x, y] = xy;
      const r = name === "MODE" ? layout.status.mode_radius : layout.status.radius;

      circle(
        svg,
        x,
        y,
        r,
        calibrationTestMode
          ? calibrationTestStatusLedClass(name)
          : statusLedClass(name, config.demo, { poeActive, stackActive })
      );

      if (config.show_labels) {
        const label = layout.status.labels[name];
        if (label) {
          const statusLabelText = text(svg, x + label.dx, y + label.dy, name, "cv-status-label", label.anchor);
          const statusLedUi = uiFromCalibration(calibration).status_leds || {};
          statusLabelText.style.fill = normaliseHexColour(statusLedUi.text_color, "#eef7ff");
          statusLabelText.style.fontFamily = String(statusLedUi.font_family || "Arial, Helvetica, sans-serif");
          statusLabelText.style.fontSize = `${Math.max(8, Math.min(50, Number(statusLedUi.font_size) || 16.5))}px`;
          statusLabelText.style.fontWeight = String(statusLedUi.font_weight || "900");
        }
      }
    }
  }

  for (const [num, port] of Object.entries(calibration.ports)) {
    const n = Number(num);
    if (!configuredPortCountAllows(config, "port_count", n)) continue;
    const up = portIsUp(hass, config, n);
    const speed = portSpeed(hass, config, n);

    if (config.show_port_leds) {
      portLed(
        svg,
        port.led_left[0],
        port.led_left[1],
        layout.ports.r,
        speedClass(speed, calibrationTestMode ? true : up),
        calibration,
        port.led_left_size,
        "led_left"
      );

      portLed(
        svg,
        port.led_right[0],
        port.led_right[1],
        layout.ports.r,
        activityClass(
          config,
          calibrationTestMode
            ? true
            : (up && testPortActivity(hass, config, n))
        ),
        calibration,
        port.led_right_size,
        "led_right"
      );
    }

    if (config.show_numbers && port.number_show !== false) {
      const [nx, ny] = port.number;
      const numberY = ny + (n % 2 === 1 ? layout.ports.odd : layout.ports.even);
      const numberCalActive = calibrationEnabled(config) && calibrationPortPartMatches(config, n, "number", calibration);
      const cls = numberCalActive ? "cv-port-num cv-calibration-active-text" : (isSelected(config, "port", n) ? "cv-port-num cv-port-num-selected" : "cv-port-num");

      // Apply the optional vendor/model-specific label offset when drawing
      // the white port-number overlay. This keeps calibration slots shared
      // while allowing zero-based faceplates such as Juniper EX3300.
      const visiblePort = displayedPortNumber(config, n);
      const portUi = uiFromCalibration(calibration);
      const visibleLabel = String(port.display_name || visiblePort);
      const portNumberText = text(svg, nx, numberY, visibleLabel, cls);
      portNumberText.style.fill = normaliseHexColour(portUi.port_number_color, portUi.port_label_color || "#eef7ff");
      portNumberText.style.fontSize = `${Math.max(8, Math.min(50, Number(portUi.port_number_font_size) || 13))}px`;
      portNumberText.style.fontWeight = String(portUi.port_label_font_weight || "bold").toLowerCase() === "normal" ? "400" : "800";
    }
  }

  for (const [name, sfp] of Object.entries(calibration.sfp)) {
    const [x, y] = sfp.center;
    const layoutLabel = layout.sfp.labels[name];
    const labelPoint = Array.isArray(sfp.label) ? sfp.label : (layoutLabel ? [layoutLabel.x, layoutLabel.y] : null);

    const sfpNumbers = String(name).match(/\d+/g) || [];
    const sfpPort = Number(sfpNumbers[sfpNumbers.length - 1] || 0);
    if (!configuredPortCountAllows(config, "sfp_port_count", sfpPort)) continue;

    if (config.show_labels && labelPoint && sfp.label_show !== false) {
      const currentCalibrationTarget = normalCalibrationTarget(config);
      const labelActive = calibrationEnabled(config) && (
        String(currentCalibrationTarget?.type || "").toLowerCase() === "number_labels"
        || (calibrationMatches(config, "sfp", sfpPort, calibration) && String(currentCalibrationTarget?.part || config.calibration_part || "center").toLowerCase() === "label")
      );
      const cls = labelActive ? "cv-sfp-label cv-calibration-active-text" : (isSelected(config, "sfp", sfpPort) ? "cv-sfp-label cv-sfp-label-selected" : "cv-sfp-label");
      const portUi = uiFromCalibration(calibration);
      const defaultSfpLabel = sfpVisibleLabel(config, sfpPort, layoutLabel?.text || name);
      const visibleSfpLabel = String(sfp.display_name || defaultSfpLabel);
      const sfpLabelText = text(svg, labelPoint[0], labelPoint[1], visibleSfpLabel, cls);
      sfpLabelText.style.fill = normaliseHexColour(portUi.port_label_color, "#eef7ff");
      sfpLabelText.style.fontSize = `${Math.max(8, Math.min(50, Number(portUi.sfp_label_font_size) || 13.5))}px`;
      sfpLabelText.style.fontWeight = String(portUi.port_label_font_weight || "bold").toLowerCase() === "normal" ? "400" : "850";
    }

    const up = sfpIsUp(hass, config, sfpPort);
    const liveSfpSpeed = sfpSpeedMbps(hass, config, sfpPort);
    sfpLed(
      svg,
      sfp,
      "led_left",
      speedClass(
        String(liveSfpSpeed || 10000),
        calibrationTestMode ? true : up
      ),
      calibration
    );

    sfpLed(
      svg,
      sfp,
      "led_right",
      activityClass(
        config,
        calibrationTestMode
          ? true
          : (up && testSfpActivity(hass, config, sfpPort))
      ),
      calibration
    );
  }

  // Draw both status boxes last so their expanded backgrounds, borders and text
  // remain above port numbers, LEDs, vents and uplink decorations. This is
  // especially important for Status Box 2 when it grows beyond its title-only
  // height after rows are enabled.
  drawStatusPanel(svg, hass, config, calibration, 1);
  drawStatusPanel(svg, hass, config, calibration, 2);
}


function cloneCalibrationData(source = calibration) {
  return ensureCalibrationUi(JSON.parse(JSON.stringify(source)));
}

function calibrationControlsEnabled(config) {
  return calibrationEnabled(config) && config.calibration_controls === true;
}

function calibrationTestModeEnabled(config) {
  return config?.calibration_test_mode === true;
}


async function copyTextToClipboard(text) {
  const value = String(text ?? "");

  if (navigator?.clipboard?.writeText && window.isSecureContext) {
    try {
      await navigator.clipboard.writeText(value);
      return true;
    } catch (err) {
      // Continue to the legacy fallback below.
    }
  }

  const textarea = document.createElement("textarea");
  textarea.value = value;
  textarea.setAttribute("readonly", "");
  textarea.style.position = "fixed";
  textarea.style.left = "-9999px";
  textarea.style.top = "0";
  textarea.style.opacity = "0";
  document.body.appendChild(textarea);

  const selection = document.getSelection();
  const previousRange = selection && selection.rangeCount ? selection.getRangeAt(0) : null;

  textarea.focus();
  textarea.select();
  textarea.setSelectionRange(0, textarea.value.length);

  let copied = false;
  try {
    copied = Boolean(document.execCommand("copy"));
  } catch (err) {
    copied = false;
  }

  textarea.remove();

  if (selection) {
    selection.removeAllRanges();
    if (previousRange) selection.addRange(previousRange);
  }

  return copied;
}


function safeDownloadName(value, fallback = "switch-vision-profile") {
  const cleaned = String(value || "")
    .trim()
    .replace(/[^a-zA-Z0-9._-]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^[-_.]+|[-_.]+$/g, "");
  return cleaned || fallback;
}

function downloadJsonFile(filename, payload) {
  const blob = new Blob([JSON.stringify(payload, null, 2) + "\n"], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  link.style.display = "none";
  document.body.appendChild(link);
  link.click();
  link.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}

const SV_PROFILE_MAX_BYTES = 2 * 1024 * 1024;
const SV_PROFILE_NAME_MAX_LENGTH = 128;
const SV_PROFILE_MAX_PORTS = 512;
const SV_PROFILE_MAX_UPLINKS = 128;
const SV_PROFILE_MAX_STATUS_LEDS = 128;
const SV_PROFILE_MAX_DIMENSION = 16384;
const SV_PROFILE_MAX_PIXELS = 64 * 1024 * 1024;

function profileNameValidationMessage(value) {
  const profile = String(value || "").trim();
  if (!profile) return "Profile name is required.";
  if (profile.length > SV_PROFILE_NAME_MAX_LENGTH) return `Profile name must be ${SV_PROFILE_NAME_MAX_LENGTH} characters or fewer.`;
  if ([".", ".."].includes(profile)) return "Profile name is not valid.";
  if ([...profile].some((character) => !/[^\u0000-\u001f\u007f]/.test(character) || '/\\<>"\'&'.includes(character))) {
    return "Profile name contains an unsupported control, path, or markup character.";
  }
  return "";
}

function isProtectedFactoryProfileName(value) {
  return Boolean(factoryCalibrationForProfile(String(value || "").trim()));
}

function normaliseImportedFaceplateProfile(raw) {
  const clean = cloneCalibrationData(raw || {});
  // Older v1 transfer files could recursively embed a second calibration under
  // ui.faceplate. Keep only actual faceplate presentation fields.
  if (clean.ui?.faceplate && typeof clean.ui.faceplate === "object" && !Array.isArray(clean.ui.faceplate)) {
    const faceplate = clean.ui.faceplate;
    for (const key of ["ui", "ports", "sfp", "stack", "management", "profile", "profile_name", "base_profile_name"]) {
      delete faceplate[key];
    }
  }
  // Transfer metadata is informational only. The card that launches Import is
  // the sole authority for the destination profile and scope.
  for (const key of ["profile_name", "profile_scope", "base_profile_name", "source_scope", "destination_scope", "mirrored_from_profile"]) {
    delete clean[key];
  }
  clean.schema_version = 2;
  clean.schema = "switch-vision-interactive-calibration-v1";
  return clean;
}

function faceplateTransferExportData(cal, { scope, baseProfile, profile, faceplate } = {}) {
  const payload = calibrationExportData(cal);
  payload.schema_version = 2;
  payload.transfer_type = "switch-vision-faceplate-profile-v2";
  payload.generated_by = `Switch Vision v${SV_VERSION}`;
  payload.source_scope = String(scope || "");
  payload.source_profile = String(profile || "");
  payload.source_base_profile = String(baseProfile || "");
  payload.required_faceplate = String(faceplate || SV_ASSET_DEFAULT);
  payload.faceplate_included = false;
  // These legacy fields are deliberately omitted so an imported file can
  // never choose its own destination.
  delete payload.profile_name;
  delete payload.profile_scope;
  delete payload.base_profile_name;
  return payload;
}

function jsonPayloadByteLength(value) {
  try {
    return new TextEncoder().encode(JSON.stringify(value)).length;
  } catch (_err) {
    return Number.POSITIVE_INFINITY;
  }
}

function calibrationCoordinateValid(value, { width, height, size = false } = {}) {
  if (!Array.isArray(value) || value.length !== 2 || !value.every((item) => Number.isFinite(Number(item)))) return false;
  const first = Number(value[0]);
  const second = Number(value[1]);
  if (size) return first > 0 && second > 0 && first <= SV_PROFILE_MAX_DIMENSION * 2 && second <= SV_PROFILE_MAX_DIMENSION * 2;
  if (!Number.isFinite(width) || !Number.isFinite(height)) return true;
  return first >= -width && first <= width * 2 && second >= -height && second <= height * 2;
}

function validateImportedCalibration(raw, currentCal = null) {
  const errors = [];
  const warnings = [];
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
    return { valid: false, errors: ["The imported file must contain one JSON object."], warnings, calibration: null, summary: null };
  }

  const payloadBytes = jsonPayloadByteLength(raw);
  if (!Number.isFinite(payloadBytes) || payloadBytes > SV_PROFILE_MAX_BYTES) {
    errors.push(`Profile payload exceeds the ${Math.round(SV_PROFILE_MAX_BYTES / 1024 / 1024)} MB safety limit.`);
  }

  const schemaVersion = Number(raw.schema_version ?? 0);
  if (schemaVersion && ![1, 2].includes(schemaVersion)) errors.push(`Unsupported schema_version ${schemaVersion}; this release supports schema versions 1 and 2.`);
  if (!schemaVersion) warnings.push("Legacy profile: schema_version is missing and will be added when saved or exported.");
  if (raw.schema && raw.schema !== "switch-vision-interactive-calibration-v1") warnings.push(`Unexpected schema label: ${raw.schema}`);

  const importedProfileName = String(raw.profile_name || "").trim();
  if (importedProfileName) {
    const profileError = profileNameValidationMessage(importedProfileName);
    if (profileError) errors.push(profileError);
  }
  for (const [key, maximum] of [["model", 512], ["profile", 512], ["generated_by", 512]]) {
    if (String(raw[key] || "").length > maximum) errors.push(`${key} is too long.`);
  }

  for (const field of ["ports", "sfp", "status_leds"]) {
    if (!raw[field] || typeof raw[field] !== "object" || Array.isArray(raw[field])) errors.push(`Missing or invalid ${field} object.`);
  }
  if (!raw.image || typeof raw.image !== "object" || Array.isArray(raw.image)) errors.push("Missing or invalid image object.");

  const width = Number(raw.image?.width);
  const height = Number(raw.image?.height);
  if (!Number.isFinite(width) || width <= 0 || !Number.isFinite(height) || height <= 0) {
    errors.push("Profile image width and height must be positive numbers.");
  } else {
    if (width > SV_PROFILE_MAX_DIMENSION || height > SV_PROFILE_MAX_DIMENSION) errors.push(`Canvas dimensions must not exceed ${SV_PROFILE_MAX_DIMENSION} px.`);
    if (width * height > SV_PROFILE_MAX_PIXELS) errors.push("Canvas contains too many pixels.");
  }

  const portCount = Object.keys(raw.ports || {}).length;
  const sfpCount = Object.keys(raw.sfp || {}).length;
  const statusCount = Object.keys(raw.status_leds || {}).filter((key) => String(key).toUpperCase() !== "MODE").length;
  if (!portCount) errors.push("The profile contains no RJ45 port positions.");
  if (portCount > SV_PROFILE_MAX_PORTS) errors.push(`The profile contains more than ${SV_PROFILE_MAX_PORTS} RJ45 ports.`);
  if (sfpCount > SV_PROFILE_MAX_UPLINKS) errors.push(`The profile contains more than ${SV_PROFILE_MAX_UPLINKS} SFP/uplinks.`);
  if (statusCount > SV_PROFILE_MAX_STATUS_LEDS) errors.push(`The profile contains more than ${SV_PROFILE_MAX_STATUS_LEDS} status LEDs.`);

  const validateEntries = (entries, type) => {
    for (const [key, item] of Object.entries(entries || {})) {
      if (!item || typeof item !== "object" || Array.isArray(item)) { errors.push(`${type} ${key} is not an object.`); continue; }
      if (type === "Port") {
        for (const part of ["center", "number", "led_left", "led_right"]) {
          if (!calibrationCoordinateValid(item[part], { width, height })) errors.push(`Port ${key} has an invalid or out-of-range ${part} coordinate.`);
        }
        if (!calibrationCoordinateValid(item.hitbox, { width, height, size: true })) errors.push(`Port ${key} has an invalid hitbox size.`);
        for (const sizePart of ["led_left_size", "led_right_size"]) {
          if (item[sizePart] !== undefined && !calibrationCoordinateValid(item[sizePart], { width, height, size: true })) errors.push(`Port ${key} has an invalid ${sizePart} size.`);
        }
      } else {
        if (!calibrationCoordinateValid(item.center, { width, height })) errors.push(`${type} ${key} has an invalid or out-of-range center coordinate.`);
        if (item.label !== undefined && !calibrationCoordinateValid(item.label, { width, height })) errors.push(`${type} ${key} has an invalid or out-of-range label coordinate.`);
        if (item.hitbox !== undefined && !calibrationCoordinateValid(item.hitbox, { width, height, size: true })) errors.push(`${type} ${key} has an invalid hitbox size.`);
      }
    }
  };
  validateEntries(raw.ports, "Port");
  validateEntries(raw.sfp, "SFP");
  for (const [key, point] of Object.entries(raw.status_leds || {})) {
    if (String(key).toUpperCase() === "MODE") continue;
    if (!calibrationCoordinateValid(point, { width, height })) errors.push(`Status LED ${key} has an invalid or out-of-range coordinate.`);
  }

  const ui = raw.ui;
  if (ui !== undefined && (!ui || typeof ui !== "object" || Array.isArray(ui))) errors.push("ui must be an object.");
  if (ui && typeof ui === "object" && !Array.isArray(ui)) {
    const faceplate = ui.faceplate;
    if (faceplate !== undefined && (!faceplate || typeof faceplate !== "object" || Array.isArray(faceplate))) {
      errors.push("ui.faceplate must be an object.");
    } else if (faceplate) {
      const file = String(faceplate.file || SV_ASSET_DEFAULT);
      if (![SV_ASSET_DEFAULT, SV_ASSET_NONE].includes(file) && !safeAssetFilename(file)) errors.push("ui.faceplate.file must be a filename, not a path.");
      if (file === SV_ASSET_NONE || faceplate.show === false || String(faceplate.source || "").toLowerCase() === "none") {
        warnings.push("Legacy hidden/no-faceplate state will be migrated to Default / recommended.");
      }
      const opacity = Number(faceplate.opacity ?? 1);
      if (!Number.isFinite(opacity) || opacity < 0 || opacity > 1) errors.push("ui.faceplate.opacity must be between 0 and 1.");
      if (faceplate.fit !== undefined && !["fill", "contain", "cover"].includes(String(faceplate.fit).toLowerCase())) errors.push("ui.faceplate.fit must be fill, contain, or cover.");
    }
    for (const boxName of ["logo", "calibration_button", "status_panel", "status_panel_2"]) {
      const box = ui[boxName];
      if (box === undefined) continue;
      if (!box || typeof box !== "object" || Array.isArray(box)) { errors.push(`ui.${boxName} must be an object.`); continue; }
      for (const coordinate of ["x", "y"]) {
        if (box[coordinate] === undefined) continue;
        const number = Number(box[coordinate]);
        const maximum = coordinate === "x" ? width : height;
        if (!Number.isFinite(number) || number < -maximum || number > maximum * 2) errors.push(`ui.${boxName}.${coordinate} is outside the safe canvas range.`);
      }
      for (const dimension of ["width", "height"]) {
        if (box[dimension] === undefined) continue;
        const number = Number(box[dimension]);
        if (!Number.isFinite(number) || number <= 0 || number > SV_PROFILE_MAX_DIMENSION * 2) errors.push(`ui.${boxName}.${dimension} must be a reasonable positive number.`);
      }
      if (box.fields !== undefined) {
        if (!box.fields || typeof box.fields !== "object" || Array.isArray(box.fields)) errors.push(`ui.${boxName}.fields must be an object.`);
        else for (const [field, point] of Object.entries(box.fields)) if (!calibrationCoordinateValid(point, { width, height })) errors.push(`ui.${boxName}.fields.${field} has an invalid coordinate.`);
      }
    }
  }

  const currentWidth = Number(currentCal?.image?.width);
  const currentHeight = Number(currentCal?.image?.height);
  if (Number.isFinite(currentWidth) && Number.isFinite(currentHeight) && (width !== currentWidth || height !== currentHeight)) {
    warnings.push(`Canvas mismatch: imported profile is ${width} × ${height}; current faceplate profile is ${currentWidth} × ${currentHeight}.`);
  }

  let calibration = null;
  if (!errors.length) {
    calibration = ensureCalibrationUi(cloneCalibrationData({
      ...normaliseImportedFaceplateProfile(raw),
      schema_version: 2,
      schema: "switch-vision-interactive-calibration-v1"
    }));
  }

  return {
    valid: errors.length === 0,
    errors,
    warnings,
    calibration,
    summary: {
      schemaVersion: schemaVersion || "legacy",
      model: String(raw.model || "unspecified"),
      profileName: importedProfileName,
      imageFile: String(raw.image?.file || "unspecified"),
      width,
      height,
      portCount,
      sfpCount,
      statusCount,
      payloadBytes
    }
  };
}


function htmlEscape(value) {
  return String(value ?? "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#39;");
}

function sfpEntryForTarget(cal, id) {
  const wanted = Number(id);
  for (const [name, item] of Object.entries(cal.sfp || {})) {
    if (sfpPortNumber(name) === wanted) return { key: name, item };
  }
  return null;
}

function statusKeyForTarget(cal, id) {
  const wanted = String(id || "").toUpperCase();
  for (const key of Object.keys(cal.status_leds || {})) {
    if (String(key).toUpperCase() === wanted) return key;
  }
  return null;
}

function normalCalibrationTarget(config) {
  // v0.5.11: handle exact quick-select group tokens before the generic
  // calibrationTarget() parser. Without this, values such as "ports_led_left"
  // were parsed as port:S_LED_LEFT, so the All link/speed, All activity, and
  // All numbers quick buttons selected a non-existent individual port.
  const rawTarget = String(config?.calibration_target ?? config?.calibration_item ?? "all").trim().toLowerCase();
  if (["ports_led_left", "link_leds", "speed_leds"].includes(rawTarget)) return { type: "ports", id: "all", part: "led_left" };
  if (["ports_led_right", "activity_leds"].includes(rawTarget)) return { type: "ports", id: "all", part: "led_right" };
  if (["all_numbers", "all_number_labels"].includes(rawTarget)) return { type: "number_labels", id: "all", part: "number" };
  if (["ports_numbers", "port_numbers", "number_labels"].includes(rawTarget)) return { type: "ports", id: "all", part: "number" };
  if (["sfps_led_left", "sfp_link_leds", "uplink_link_leds"].includes(rawTarget)) return { type: "sfps", id: "all", part: "led_left" };
  if (["sfps_led_right", "sfp_activity_leds", "uplink_activity_leds"].includes(rawTarget)) return { type: "sfps", id: "all", part: "led_right" };
  if (rawTarget === "ports_custom") return { type: "ports", id: "custom", selection: "custom", part: String(config?.calibration_part || "entire").toLowerCase() };
  if (rawTarget === "sfps_custom") return { type: "sfps", id: "custom", selection: "custom", part: String(config?.calibration_part || "center").toLowerCase() };
  const parityMatch = rawTarget.match(/^ports_(odd|even)(?:_(led_left|led_right|numbers|center|entire))?$/);
  if (parityMatch) {
    const partMap = { led_left: "led_left", led_right: "led_right", numbers: "number", center: "center", entire: "entire" };
    return { type: "ports", id: parityMatch[1], parity: parityMatch[1], part: partMap[parityMatch[2]] || "center" };
  }
  if (rawTarget === "logo") return { type: "logo", id: "logo", part: "box" };
  if (rawTarget === "calibration_button") return { type: "calibration_button", id: "calibration_button", part: "box" };
  if (rawTarget === "status_box" || rawTarget === "status_box_1") return { type: "status_box", id: "status_box_1", part: "box" };
  if (rawTarget === "status_box_2") return { type: "status_box_2", id: "status_box_2", part: "box" };
  if (rawTarget === "status_fields") return { type: "status_fields", id: "all", part: normaliseStatusFieldGroupPart(config?.calibration_part) };
  if (rawTarget === "status_fields_2") return { type: "status_fields_2", id: "all", part: normaliseStatusFieldGroupPart(config?.calibration_part) };
  if (rawTarget.startsWith("status_field_2:")) return { type: "status_field_2", id: rawTarget.split(":").slice(1).join(":"), part: "field" };
  if (rawTarget.startsWith("status_field:")) return { type: "status_field", id: rawTarget.split(":").slice(1).join(":"), part: "field" };

  const target = calibrationTarget(config);
  if (target === "ports") return { type: "ports", id: "all" };
  if (["sfp", "sfps", "uplinks"].includes(target)) return { type: "sfps", id: "all" };
  if (target === "status" || target === "status_leds" || target === "leds") return { type: "status_leds", id: "all" };
  if (target && typeof target === "object") return target;
  return { type: "port", id: 1 };
}

function targetLabelForConfig(config) {
  const target = normalCalibrationTarget(config);
  const type = String(target.type || "").toLowerCase();
  if (type === "ports" && target.part === "led_left") return "ports_led_left";
  if (type === "ports" && target.part === "led_right") return "ports_led_right";
  if (type === "number_labels") return "all_numbers";
  if (type === "ports" && target.part === "number") return "ports_numbers";
  if (type === "ports" && (target.selection === "custom" || target.id === "custom")) return "ports_custom";
  if (type === "sfps" && (target.selection === "custom" || target.id === "custom")) return "sfps_custom";
  if (type === "ports" && target.parity) {
    const suffix = target.part === "led_left" ? "led_left" : (target.part === "led_right" ? "led_right" : (target.part === "number" ? "numbers" : (target.part === "entire" ? "entire" : "center")));
    return `ports_${target.parity}_${suffix}`;
  }
  if (type === "logo") return "logo";
  if (type === "calibration_button") return "calibration_button";
  if (type === "status_box") return "status_box";
  if (type === "status_box_2") return "status_box_2";
  if (type === "status_fields") return "status_fields";
  if (type === "status_fields_2") return "status_fields_2";
  if (type === "status_field_2") return `status_field_2:${target.id}`;
  if (type === "status_field") return `status_field:${target.id}`;
  if (type === "ports") return "ports";
  if (type === "sfps" && target.part === "led_left") return "sfps_led_left";
  if (type === "sfps" && target.part === "led_right") return "sfps_led_right";
  if (type === "sfps") return "sfps";
  if (type === "status_leds") return "status_leds";
  return `${target.type}:${target.id}`;
}

function getEditableCalibrationTarget(cal, config) {
  const target = normalCalibrationTarget(config);
  const type = String(target.type || "port").toLowerCase();
  const rawId = target.id;
  let part = String(target.part || config.calibration_part || "entire").toLowerCase();

  ensureCalibrationUi(cal);

  if (type === "logo") {
    const item = cal.ui.logo;
    return { type, id: "logo", key: "logo", item, part: "box", point: [item.x, item.y], hitbox: [item.width, item.height], ui: true };
  }

  if (type === "calibration_button") {
    const item = cal.ui.calibration_button;
    return { type, id: "calibration_button", key: "calibration_button", item, part: "box", point: [item.x, item.y], hitbox: [item.width, item.height], ui: true };
  }

  if (type === "status_box") {
    const item = cal.ui.status_panel;
    return { type, id: "status_box_1", key: "status_box_1", item, part: "box", point: [item.x, item.y], hitbox: [item.width, item.height], ui: true };
  }

  if (type === "status_box_2") {
    const item = cal.ui.status_panel_2;
    return { type, id: "status_box_2", key: "status_box_2", item, part: "box", point: [item.x, item.y], hitbox: [item.width, item.height], ui: true };
  }

  if (type === "status_fields") {
    part = normaliseStatusFieldGroupPart(part);
    return { type, id: "all", key: "status_fields", item: cal.ui.status_panel, part, point: ["all", "all"], group: true };
  }
  if (type === "status_fields_2") {
    part = normaliseStatusFieldGroupPart(part);
    return { type, id: "all", key: "status_fields_2", item: cal.ui.status_panel_2, part, point: ["all", "all"], group: true };
  }

  if (type === "status_field_2") {
    const key = String(rawId || "row1_key").toLowerCase();
    const item = cal.ui.status_panel_2.fields;
    if (!item[key]) return null;
    return { type, id: key, key, item, part: "field", point: item[key] };
  }

  if (type === "status_field") {
    const key = String(rawId || "row1_key").toLowerCase();
    const item = cal.ui.status_panel.fields;
    if (!item[key]) return null;
    return { type, id: key, key, item, part: "field", point: item[key] };
  }

  if (type === "number_labels") {
    return { type, id: "all", key: "all_numbers", item: {}, part: "number", point: null, hitbox: null, group: true };
  }

  if (type === "ports") {
    const allowed = ["center", "led_left", "led_right", "number", "entire"];
    if (!allowed.includes(part)) part = "entire";
    const custom = target.selection === "custom" || String(rawId) === "custom";
    const parity = custom ? "custom" : (target.parity || (["odd", "even"].includes(String(rawId)) ? String(rawId) : "all"));
    const keys = custom ? calibrationSelectedPortKeys(config, cal) : calibrationPortKeysByParity(cal, parity);
    const representative = cal.ports?.[keys[0]] || null;
    const sizeKey = portLedSizeKey(part);
    const hitbox = sizeKey
      ? representative?.[sizeKey] || defaultPortLedRectangleSize()
      : (["center", "entire"].includes(part) ? representative?.hitbox || [34, 56] : null);
    return { type, id: parity, key: `ports_${parity}`, parity, keys, custom, item: representative || {}, part, point: null, hitbox, group: true };
  }

  if (type === "sfps") {
    if (!["center", "label", "led_left", "led_right"].includes(part)) part = "center";
    const custom = target.selection === "custom" || String(rawId) === "custom";
    const keys = custom ? calibrationSelectedSfpKeys(config, cal) : Object.keys(cal.sfp || {});
    const representative = cal.sfp?.[keys[0]] || null;
    const sizeKey = portLedSizeKey(part);
    const hitbox = sizeKey
      ? sfpLedRectangleSize(representative, part, false)
      : (part === "center" ? representative?.hitbox || [78, 58] : null);
    return {
      type,
      id: custom ? "custom" : "all",
      key: custom ? "sfps_custom" : "sfps",
      keys,
      custom,
      item: representative || {},
      part,
      point: null,
      hitbox,
      group: true
    };
  }

  if (type === "status_leds") {
    part = "center";
    return { type, id: "all", key: "status_leds", item: {}, part, point: null, group: true };
  }

  if (type === "port" || type === "ports") {
    const id = String(Number(rawId));
    const item = cal.ports?.[id];
    if (!item) return null;
    const allowed = ["center", "led_left", "led_right", "number", "entire"];
    if (!allowed.includes(part)) part = "entire";
    const sizeKey = portLedSizeKey(part);
    const hitbox = sizeKey ? item[sizeKey] : (["center", "entire"].includes(part) ? item.hitbox : null);
    return { type, id, key: id, item, part, point: item[part], hitbox };
  }

  if (type === "sfp") {
    const found = sfpEntryForTarget(cal, rawId);
    if (!found) return null;
    if (!["center", "label", "led_left", "led_right"].includes(part)) part = "center";
    const sizeKey = portLedSizeKey(part);
    const point = sizeKey ? sfpLedPoint(found.item, part, false) : found.item[part];
    const hitbox = sizeKey ? sfpLedRectangleSize(found.item, part, false) : (part === "center" ? found.item.hitbox : null);
    return { type, id: String(sfpPortNumber(found.key)), key: found.key, item: found.item, part, point, hitbox };
  }

  if (type === "status") {
    const key = statusKeyForTarget(cal, rawId);
    if (!key) return null;
    part = "center";
    return { type, id: key, key, item: cal.status_leds, part, point: cal.status_leds[key] };
  }

  return null;
}

function movePoint(point, dx, dy) {
  if (!Array.isArray(point)) return;
  point[0] = Math.round((Number(point[0]) + dx) * 10) / 10;
  point[1] = Math.round((Number(point[1]) + dy) * 10) / 10;
}

function sortedCalibrationPortKeys(cal) {
  return Object.keys(cal?.ports || {}).sort((a, b) => Number(a) - Number(b));
}

function calibrationPortKeysByParity(cal, parity = "all") {
  const keys = sortedCalibrationPortKeys(cal);
  if (parity === "odd") return keys.filter((key) => Number(key) % 2 === 1);
  if (parity === "even") return keys.filter((key) => Number(key) % 2 === 0);
  return keys;
}

function calibrationPortKeysForEditable(cal, editable) {
  if (Array.isArray(editable?.keys)) return editable.keys;
  return calibrationPortKeysByParity(cal, editable?.parity || "all");
}

function calibrationSfpKeysForEditable(cal, editable) {
  if (Array.isArray(editable?.keys)) return editable.keys;
  return Object.keys(cal?.sfp || {});
}

function calibrationCoordinatePoints(cal, editable, createMissing = false) {
  if (!editable) return [];

  const portPoint = (port) => {
    if (!port) return null;
    if (["center", "entire"].includes(editable.part)) return port.center;
    return port[editable.part];
  };

  const sfpPoint = (sfp) => {
    if (!sfp) return null;
    if (["led_left", "led_right"].includes(editable.part)) return sfpLedPoint(sfp, editable.part, createMissing);
    return sfp[editable.part];
  };

  let points = [];
  if (editable.group && editable.type === "number_labels") {
    points = [
      ...Object.values(cal.ports || {}).map((port) => port?.number),
      ...Object.values(cal.sfp || {}).map((sfp) => sfp?.label),
    ];
  } else if (editable.group && editable.type === "ports") {
    points = calibrationPortKeysForEditable(cal, editable).map((key) => portPoint(cal.ports?.[key]));
  } else if (editable.group && editable.type === "sfps") {
    points = calibrationSfpKeysForEditable(cal, editable).map((key) => sfpPoint(cal.sfp?.[key]));
  } else if (editable.group && editable.type === "status_leds") {
    points = Object.entries(cal.status_leds || {})
      .filter(([name]) => String(name).toUpperCase() !== "MODE")
      .map(([, point]) => point);
  } else if (editable.group && ["status_fields", "status_fields_2"].includes(editable.type)) {
    points = statusFieldPointsForPart(editable.item, editable.part);
  } else if (["logo", "calibration_button", "status_box", "status_box_2"].includes(editable.type)) {
    points = [[editable.item?.x, editable.item?.y]];
  } else if (editable.type === "port") {
    points = [portPoint(editable.item)];
  } else if (editable.type === "sfp") {
    points = [sfpPoint(editable.item)];
  } else if (Array.isArray(editable.point)) {
    points = [editable.point];
  }

  return points.filter((point) => Array.isArray(point) && point.length >= 2 && Number.isFinite(Number(point[0])) && Number.isFinite(Number(point[1])));
}

function commonCalibrationCoordinate(points, axis) {
  if (!Array.isArray(points) || !points.length) return null;
  const values = points.map((point) => Math.round(Number(point[axis]) * 10) / 10);
  return values.every((value) => value === values[0]) ? values[0] : null;
}

function calibrationSizePairs(cal, editable, createMissing = false) {
  if (!editable) return [];

  const portSize = (port) => {
    if (!port) return null;
    const sizeKey = portLedSizeKey(editable.part);
    if (sizeKey) {
      if (createMissing && !Array.isArray(port[sizeKey])) port[sizeKey] = [...defaultPortLedRectangleSize()];
      return Array.isArray(port[sizeKey]) ? port[sizeKey] : defaultPortLedRectangleSize();
    }
    return ["center", "entire"].includes(editable.part) ? port.hitbox : null;
  };

  const sfpSize = (sfp) => {
    if (!sfp) return null;
    const sizeKey = portLedSizeKey(editable.part);
    if (sizeKey) return sfpLedRectangleSize(sfp, editable.part, createMissing);
    return editable.part === "center" ? sfp.hitbox : null;
  };

  let sizes = [];
  if (["logo", "calibration_button", "status_box", "status_box_2"].includes(editable.type)) {
    sizes = [[editable.item?.width, editable.item?.height]];
  } else if (editable.group && editable.type === "ports") {
    sizes = calibrationPortKeysForEditable(cal, editable).map((key) => portSize(cal.ports?.[key]));
  } else if (editable.group && editable.type === "sfps") {
    sizes = calibrationSfpKeysForEditable(cal, editable).map((key) => sfpSize(cal.sfp?.[key]));
  } else if (editable.type === "port") {
    sizes = [portSize(editable.item)];
  } else if (editable.type === "sfp") {
    sizes = [sfpSize(editable.item)];
  }

  return sizes.filter((size) => Array.isArray(size) && size.length >= 2 && Number.isFinite(Number(size[0])) && Number.isFinite(Number(size[1])));
}

function nextCalibrationPortNumber(cal) {
  const nums = sortedCalibrationPortKeys(cal).map(Number).filter(Number.isFinite);
  return nums.length ? Math.max(...nums) + 1 : 1;
}

function cloneCalibrationPort(cal, sourceKey, newKey, offsetX = 12, offsetY = 0) {
  cal.ports = cal.ports && typeof cal.ports === "object" ? cal.ports : {};
  const source = cal.ports?.[String(sourceKey)] || Object.values(cal.ports)[0] || {
    center: [177, 243], number: [176, 181], led_left: [158, 230], led_right: [195, 230], hitbox: [34, 56], number_show: true
  };
  const copy = JSON.parse(JSON.stringify(source));
  for (const part of ["center", "number", "led_left", "led_right"]) {
    if (Array.isArray(copy[part])) {
      copy[part][0] = Math.round((Number(copy[part][0]) + offsetX) * 10) / 10;
      copy[part][1] = Math.round((Number(copy[part][1]) + offsetY) * 10) / 10;
    }
  }
  delete copy.display_name;
  cal.ports[String(newKey)] = copy;
  return copy;
}

function targetOptionsHtml(cal, selected) {
  const selectedValue = String(selected || "port:1").toLowerCase();
  const option = (value, label) => `<option value="${htmlEscape(value)}" ${String(value).toLowerCase() === selectedValue ? "selected" : ""}>${htmlEscape(label)}</option>`;
  const opts = [];
  opts.push(option("logo", "Logo"));
  opts.push(option("status_box", "Status Box 1"));
  opts.push(option("status_box_2", "Status Box 2"));
  opts.push(option("calibration_button", "Calibration button"));
  opts.push(option("status_fields", "Status Box 1 fields"));
  opts.push(option("status_fields_2", "Status Box 2 fields"));
  opts.push(`<optgroup label="Status Box 1 fields">`);
  for (const [key, label] of STATUS_PANEL_FIELD_DEFS) opts.push(option(`status_field:${key}`, label));
  opts.push(`</optgroup>`);
  opts.push(`<optgroup label="Status Box 2 fields">`);
  for (const [key, label] of STATUS_PANEL_FIELD_DEFS) opts.push(option(`status_field_2:${key}`, label));
  opts.push(`</optgroup>`);
  opts.push(option("ports", "All RJ45"));
  opts.push(option("ports_led_left", "RJ45 Link"));
  opts.push(option("ports_led_right", "RJ45 Activity"));
  opts.push(option("all_numbers", "Port Numbers"));
  opts.push(option("ports_numbers", "All RJ45 number labels"));
  opts.push(`<optgroup label="RJ45 ports">`);
  for (const key of sortedCalibrationPortKeys(cal)) {
    const display = String(cal.ports?.[key]?.display_name || "").trim();
    opts.push(option(`port:${key}`, display ? `Port ${key} · ${display}` : `Port ${key}`));
  }
  opts.push(`</optgroup>`);
  opts.push(option("sfps", "All SFP"));
  opts.push(option("sfps_custom", "Selected SFP / uplinks"));
  opts.push(option("sfps_led_left", "SFP Link"));
  opts.push(option("sfps_led_right", "SFP Activity"));
  opts.push(`<optgroup label="SFP / uplinks">`);
  for (const name of Object.keys(cal.sfp || {})) {
    const display = String(cal.sfp?.[name]?.display_name || "").trim();
    opts.push(option(`sfp:${sfpPortNumber(name)}`, display ? `${name} · ${display}` : name));
  }
  opts.push(`</optgroup>`);
  opts.push(option("status_leds", "All status LEDs"));
  opts.push(`<optgroup label="Status LEDs">`);
  for (const name of Object.keys(cal.status_leds || {})) {
    if (String(name).toUpperCase() === "MODE") continue;
    opts.push(option(`status:${name}`, name));
  }
  opts.push(`</optgroup>`);
  return opts.join("");
}

function calibrationPartOptionsHtml(target, selectedPart) {
  const type = String(target?.type || "port").toLowerCase();
  const part = String(selectedPart || "entire").toLowerCase();
  const option = (value, label) => `<option value="${htmlEscape(value)}" ${value === part ? "selected" : ""}>${htmlEscape(label)}</option>`;
  if (["logo", "status_box", "status_box_2", "calibration_button"].includes(type)) return option("box", "Box / size");
  if (["status_fields", "status_fields_2"].includes(type)) return [
    option("field", "All fields"),
    option("label", "All labels"),
    option("value", "All values")
  ].join("");
  if (["status_field", "status_field_2"].includes(type)) return option("field", "Field position");
  if (type === "number_labels") return option("number", "All number labels");
  if (type === "port" || type === "ports") {
    return [
      option("center", "Port box"),
      option("entire", "Entire Port"),
      option("led_left", "Link/speed LED"),
      option("led_right", "Activity LED"),
      option("number", "Number label")
    ].join("");
  }
  if (type === "sfp" || type === "sfps") return [
    option("center", type === "sfps" ? "All SFP port boxes" : "SFP port box"),
    option("led_left", type === "sfps" ? "All SFP link/speed LEDs" : "Link/speed LED"),
    option("led_right", type === "sfps" ? "All SFP activity LEDs" : "Activity LED"),
    option("label", type === "sfps" ? "All SFP labels" : "SFP label")
  ].join("");
  if (type === "status_leds") return option("center", "All status LED centres");
  return option("center", "LED centre");
}

function ensureCalibrationManagement(cal) {
  if (!cal) return cal;
  cal.management = cal.management && typeof cal.management === "object" && !Array.isArray(cal.management) ? cal.management : {};
  cal.management.switch_ip = String(cal.management.switch_ip || cal.management.management_ip || cal.management.host_ip || "").trim();
  return cal;
}

function managementFromCalibration(cal) {
  const management = ensureCalibrationManagement(cal)?.management || {};
  const switchIp = String(management.switch_ip || "").trim();
  return switchIp ? { switch_ip: switchIp } : {};
}

function calibrationExportData(cal) {
  const statusLeds = {};
  for (const [name, xy] of Object.entries(cal.status_leds || {})) {
    if (String(name).toUpperCase() === "MODE") continue;
    statusLeds[name] = xy;
  }
  return {
    schema_version: 1,
    schema: "switch-vision-interactive-calibration-v1",
    profile_name: "",
    model: cal.model || "cisco-3650-48p-2x10g",
    profile: cal.profile || "",
    generated_by: `Switch Vision v${SV_VERSION}`,
    image: cal.image,
    ports: cal.ports,
    sfp: cal.sfp,
    status_leds: statusLeds,
    ui: uiFromCalibration(cal),
    stack: stackFromCalibration(cal),
    management: managementFromCalibration(cal)
  };
}


const SV_DEVICE_VISUAL_RECOMMENDATIONS = [{"model":"WS-C3650-48PD-E","status":"confirmed","family":"Catalyst 3650","rj45":48,"uplinks":4,"visual_status":"confirmed","faceplate":"faceplates/48rj45-4sfp.png","optional_faceplates":[],"profile":"default_cisco_48_port","canvas":{"width":2048,"height":448}},{"model":"WS-C3650-48PD-L","status":"confirmed","family":"Catalyst 3650","rj45":48,"uplinks":4,"visual_status":"confirmed","faceplate":"faceplates/48rj45-4sfp.png","optional_faceplates":[],"profile":"default_cisco_48_port","canvas":{"width":2048,"height":448}},{"model":"WS-C2960X-48FPD-L","status":"confirmed","family":"Catalyst 2960X","rj45":48,"uplinks":2,"visual_status":"confirmed","faceplate":"faceplates/48rj45-2sfp.png","optional_faceplates":[],"profile":"cisco_2960s_48p","canvas":{"width":2048,"height":448}},{"model":"WS-C2960X-24PS-L","status":"experimental","family":"Catalyst 2960X","rj45":24,"uplinks":4,"visual_status":"confirmed","faceplate":"faceplates/24rj45-4sfp.png","optional_faceplates":[],"profile":"cisco_2960x_24p","canvas":{"width":2048,"height":448}},{"model":"WS-C2960X-24TS-L","status":"community_validated","family":"Catalyst 2960X","rj45":24,"uplinks":4,"visual_status":"confirmed","faceplate":"faceplates/24rj45-4sfp.png","optional_faceplates":[],"profile":"cisco_2960x_24p","canvas":{"width":2048,"height":448}},{"model":"WS-C2960S-48FPD-L","status":"confirmed","family":"Catalyst 2960S","rj45":48,"uplinks":2,"visual_status":"confirmed","faceplate":"faceplates/48rj45-2sfp.png","optional_faceplates":[],"profile":"cisco_2960s_48p","canvas":{"width":2048,"height":448}},{"model":"WS-C3560CG-8PC-S","status":"community_validated","family":"Catalyst 3560-C","rj45":8,"uplinks":2,"visual_status":"confirmed","faceplate":"faceplates/c3560cg-8pc-s.png","optional_faceplates":["faceplates/24rj45-2sfp.png"],"profile":"cisco_3560cg_8pc","canvas":{"width":2048,"height":329}},{"model":"EX3300-48P","status":"confirmed","family":"EX3300","rj45":48,"uplinks":4,"visual_status":"confirmed","faceplate":"faceplates/48rj45-4sfp.png","optional_faceplates":[],"profile":"default_cisco_48_port","canvas":{"width":2048,"height":448}},{"model":"SG500X-24","status":"community_validated","family":"Small Business SG500X","rj45":24,"uplinks":4,"visual_status":"experimental","faceplate":"faceplates/24rj45-4sfp.png","optional_faceplates":[],"profile":"cisco_2960x_24p","canvas":{"width":2048,"height":448}},{"model":"S5720-12TP-LI-AC","status":"community_validated","family":"S5720","rj45":8,"uplinks":4,"visual_status":"experimental","faceplate":"faceplates/24rj45-4sfp.png","optional_faceplates":[],"profile":"stock_24rj45_4sfp","canvas":{"width":2048,"height":448}},{"model":"XS1930-10","status":"experimental","family":"XS1930","rj45":8,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW-Enterprise-8-PoE","status":"experimental","family":"UniFi Switch Enterprise","rj45":8,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW-Pro-24-PoE","status":"experimental","family":"UniFi Switch Pro","rj45":24,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/unifi-24p-rj45-2sfp.png","optional_faceplates":["faceplates/24rj45-4sfp.png"],"profile":"unifi_24p_rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Lite 16 PoE","status":"experimental","family":"UniFi Switch Lite","rj45":16,"uplinks":0,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Pro XG 8 PoE","status":"experimental","family":"UniFi Switch Pro XG","rj45":8,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"S5735-L8P4X-A1","status":"community_validated","family":"S5735-L","rj45":8,"uplinks":4,"visual_status":"experimental","faceplate":"faceplates/24rj45-4sfp.png","optional_faceplates":[],"profile":"stock_24rj45_4sfp","canvas":{"width":2048,"height":448}},{"model":"UDM Pro","status":"experimental","family":"UniFi Dream Machine Pro","rj45":9,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"N2128PX-ON","status":"experimental","family":"Dell EMC Networking N2000","rj45":28,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/48rj45-4sfp.png","optional_faceplates":[],"profile":"default_cisco_48_port","canvas":{"width":2048,"height":448}},{"model":"US 48 PoE 500W","status":"experimental","family":"UniFi Switch","rj45":48,"uplinks":4,"visual_status":"experimental","faceplate":"faceplates/48rj45-4sfp.png","optional_faceplates":[],"profile":"stock_48rj45_4sfp","canvas":{"width":2048,"height":448}},{"model":"US 8 60W","status":"experimental","family":"UniFi Switch 8 60W","rj45":8,"uplinks":0,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Flex","status":"experimental","family":"UniFi Switch Flex","rj45":5,"uplinks":0,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Flex 2.5G 8 PoE","status":"experimental","family":"UniFi Switch Flex 2.5G 8 PoE","rj45":9,"uplinks":1,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Flex Mini","status":"experimental","family":"UniFi Switch Flex Mini","rj45":5,"uplinks":0,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW Pro 24","status":"experimental","family":"UniFi Switch Pro 24","rj45":24,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/unifi-24p-rj45-2sfp.png","optional_faceplates":[],"profile":"unifi_24p_rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW-16-PoE","status":"experimental","family":"UniFi Switch 16 PoE","rj45":16,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW-24-PoE","status":"experimental","family":"UniFi Switch 24 PoE","rj45":24,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/unifi-24p-rj45-2sfp.png","optional_faceplates":[],"profile":"unifi_24p_rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"USW-Lite-8-PoE","status":"experimental","family":"UniFi Switch Lite 8 PoE","rj45":8,"uplinks":0,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}},{"model":"UniFi Dream Machine PRO SE","status":"experimental","family":"UniFi Dream Machine Pro SE","rj45":9,"uplinks":2,"visual_status":"experimental","faceplate":"faceplates/24rj45-2sfp.png","optional_faceplates":[],"profile":"stock_24rj45_2sfp","canvas":{"width":2048,"height":448}}];

function exactModelVisualRecommendation(model) {
  const exact = String(model || "").trim().toUpperCase();
  if (!exact) return null;
  const canonical = exact.replace(/[^A-Z0-9]+/g, "");
  return SV_DEVICE_VISUAL_RECOMMENDATIONS.find((item) =>
    String(item.model || "").trim().toUpperCase().replace(/[^A-Z0-9]+/g, "") === canonical
  ) || null;
}

const SV_ASSET_DEFAULT = "__default__";
// Retained only to migrate profiles created by v1.9.35 or older builds that
// could hide the faceplate. It is never offered or preserved as an active state.
const SV_ASSET_NONE = "__none__";

function normaliseFaceplateAsset(value = {}) {
  const faceplate = { fit: "fill", opacity: 1, ...(value && typeof value === "object" ? value : {}) };
  const rawFile = String(faceplate.file || SV_ASSET_DEFAULT).trim();
  const source = String(faceplate.source || "").trim().toLowerCase();
  const hiddenOrLegacyNone = rawFile === SV_ASSET_NONE || source === "none" || faceplate.show === false;

  if (hiddenOrLegacyNone || rawFile === SV_ASSET_DEFAULT || source === "default") {
    // Switch Vision always keeps visible switch artwork. Legacy hidden/none,
    // blank, and invalid states migrate to the model-recommended faceplate.
    faceplate.show = true;
    faceplate.file = SV_ASSET_DEFAULT;
    faceplate.source = "default";
  } else {
    const safe = safeAssetFilename(rawFile);
    if (!safe) {
      faceplate.show = true;
      faceplate.file = SV_ASSET_DEFAULT;
      faceplate.source = "default";
    } else {
      faceplate.show = true;
      faceplate.file = safe;
      faceplate.source = "custom";
    }
  }

  faceplate.fit = ["fill", "contain", "cover"].includes(String(faceplate.fit || "fill").toLowerCase()) ? String(faceplate.fit).toLowerCase() : "fill";
  const opacity = Number(faceplate.opacity);
  faceplate.opacity = Number.isFinite(opacity) ? Math.max(0, Math.min(1, opacity)) : 1;
  return faceplate;
}

function faceplateSelectionValue(value = {}) {
  const faceplate = normaliseFaceplateAsset(value);
  if (faceplate.file === SV_ASSET_DEFAULT) return SV_ASSET_DEFAULT;
  return safeAssetFilename(faceplate.file) || SV_ASSET_DEFAULT;
}

function safeAssetFilename(value) {
  const file = String(value || "").trim();
  if (!file || file.includes("/") || file.includes("\\") || file === "." || file === "..") return "";
  return file;
}

function assetUrl(
  kind,
  file,
  version = SV_VERSION,
  refreshToken = ""
) {
  const safe = safeAssetFilename(file);
  if (!safe) return "";

  const refresh =
    String(refreshToken || "").trim();

  const suffix = refresh
    ? `&r=${encodeURIComponent(refresh)}`
    : "";

  return `/local/switch-vision/${kind}/${encodeURIComponent(safe)}?v=${encodeURIComponent(version)}${suffix}`;
}

function assetDisplayLabel(kind, file) {
  const shippedLabels = {
    faceplates: {
      "48rj45-4sfp.png": "48 RJ45 / 4 SFP",
      "48rj45-2sfp.png": "48 RJ45 / 2 SFP",
      "24rj45-4sfp.png": "24 RJ45 / 4 SFP",
      "24rj45-2sfp.png": "24 RJ45 / 2 SFP"
    }
  };
  if (shippedLabels[kind]?.[file]) return shippedLabels[kind][file];
  return String(file || "")
    .replace(/\.[^.]+$/, "")
    .replace(/[-_]+/g, " ")
    .replace(/\b\w/g, (char) => char.toUpperCase());
}

function assetOptionsHtml(kind, files, selected, defaultLabel) {
  const selectedValue = String(selected || SV_ASSET_DEFAULT);
  const option = (value, label, disabled = false) => `<option value="${htmlEscape(value)}" ${value === selectedValue ? "selected" : ""} ${disabled ? "disabled" : ""}>${htmlEscape(label)}</option>`;
  const rows = [option(SV_ASSET_DEFAULT, defaultLabel)];
  // Logos may be hidden; faceplates must always fall back to visible artwork.
  if (kind === "logos") rows.push(option(SV_ASSET_NONE, "None (no logo)"));
  const safeFiles = Array.isArray(files) ? files.map(safeAssetFilename).filter(Boolean) : [];
  if (safeFiles.length) {
    for (const file of safeFiles) rows.push(option(file, assetDisplayLabel(kind, file)));
  } else {
    rows.push(option("__empty__", `No custom ${kind} found`, true));
  }
  const specialValues = kind === "logos" ? [SV_ASSET_DEFAULT, SV_ASSET_NONE] : [SV_ASSET_DEFAULT];
  if (!specialValues.includes(selectedValue) && !safeFiles.includes(selectedValue)) {
    const suffix = kind === "faceplates" ? " (missing — recommended will be shown)" : " (missing)";
    rows.push(option(selectedValue, `${assetDisplayLabel(kind, selectedValue)}${suffix}`));
  }
  return rows.join("");
}


const SV_TYPE = "switch-vision-3650";

const SV_GLOBAL_UI_SETTINGS_PROMISES = new WeakMap();
const SV_GLOBAL_UI_SETTINGS_CACHE_MS = 5000;

function normalizeSwitchVisionGlobalUiSettings(result = {}) {
  const activity = result?.activity_leds && typeof result.activity_leds === "object"
    ? result.activity_leds
    : {};
  return {
    show_card_headers: result?.show_card_headers !== false,
    activity_leds: { ...activity },
  };
}

function activityConfigFromGlobalSettings(settings = {}) {
  const source = settings?.activity_leds && typeof settings.activity_leds === "object"
    ? settings.activity_leds
    : {};
  const mapped = {};
  const assignText = (target, sourceKey) => {
    const value = String(source[sourceKey] ?? "").trim();
    if (value) mapped[target] = value;
  };
  const assignNumber = (target, sourceKey) => {
    const value = Number(source[sourceKey]);
    if (Number.isFinite(value)) mapped[target] = value;
  };

  assignText("activity_led_sensitivity_preset", "sensitivity_preset");
  assignNumber("activity_slow_max_utilization_pct", "slow_max_utilization_pct");
  assignNumber("activity_medium_max_utilization_pct", "medium_max_utilization_pct");
  assignNumber("activity_slow_period_ms", "slow_period_ms");
  assignNumber("activity_medium_period_ms", "medium_period_ms");
  assignNumber("activity_fast_period_ms", "fast_period_ms");
  assignNumber("activity_hold_seconds", "hold_seconds");
  assignNumber("activity_hysteresis_pct", "hysteresis_pct");
  return mapped;
}

function cacheSwitchVisionGlobalUiSettings(hass, settings) {
  const connection = hass?.connection;
  if (!connection) return;
  SV_GLOBAL_UI_SETTINGS_PROMISES.set(connection, {
    promise: Promise.resolve(normalizeSwitchVisionGlobalUiSettings(settings)),
    fetchedAt: Date.now(),
  });
}

function switchVisionGlobalUiSettings(hass) {
  const connection = hass?.connection;
  if (!connection || typeof hass?.callWS !== "function") {
    return Promise.resolve(normalizeSwitchVisionGlobalUiSettings());
  }
  const cached = SV_GLOBAL_UI_SETTINGS_PROMISES.get(connection);
  if (cached && (Date.now() - Number(cached.fetchedAt || 0)) < SV_GLOBAL_UI_SETTINGS_CACHE_MS) {
    return cached.promise;
  }
  const pending = hass.callWS({ type: "switch_vision/get_ui_settings" })
    .then((result) => normalizeSwitchVisionGlobalUiSettings(result))
    .catch(() => normalizeSwitchVisionGlobalUiSettings());
  SV_GLOBAL_UI_SETTINGS_PROMISES.set(connection, { promise: pending, fetchedAt: Date.now() });
  return pending;
}


const SV_CALIBRATION_SECTION_ORDER = Object.freeze([
  "assets",
  "selection",
  "labels-leds",
  "status-boxes",
  "switch-stack",
  "position-size",
  "profile-actions",
]);
const SV_CALIBRATION_SECTION_DEFAULT_OPEN = Object.freeze({
  "assets": true,
  "selection": false,
  "labels-leds": false,
  "status-boxes": false,
  "switch-stack": false,
  "position-size": true,
  "profile-actions": false,
});
const SV_CALIBRATION_SECTION_ORDER_STORAGE_KEY = "switch-vision:calibration-section-order:v2";

function normaliseCalibrationSectionOrder(value) {
  const requested = Array.isArray(value) ? value.map((item) => String(item || "")) : [];
  const valid = [];
  for (const key of requested) {
    if (SV_CALIBRATION_SECTION_ORDER.includes(key) && !valid.includes(key)) valid.push(key);
  }
  for (const key of SV_CALIBRATION_SECTION_ORDER) {
    if (!valid.includes(key)) valid.push(key);
  }
  return valid;
}

class SwitchVision3650 extends HTMLElement {
  connectedCallback() {
    if (!this._boundCalibrationSavedHandler) {
      this._boundCalibrationSavedHandler = (event) => this.handleLocalCalibrationSaved(event);
    }
    window.addEventListener("switch-vision-calibration-saved", this._boundCalibrationSavedHandler);
    this.subscribeCalibrationUpdates();
    this.subscribeUiSettingsUpdates();
  }

  disconnectedCallback() {
    if (this._boundCalibrationSavedHandler) {
      window.removeEventListener("switch-vision-calibration-saved", this._boundCalibrationSavedHandler);
    }
    this.unsubscribeCalibrationUpdates();
    this.unsubscribeUiSettingsUpdates();
    clearInterval(this._activityRenderTimer);
    clearInterval(this._unifiRefreshTimer);
    clearTimeout(this._activityStopTimer);
  }

  handleLocalCalibrationSaved(event) {
    const detail = event?.detail || {};
    const profile = String(detail.profile || "").trim();
    const mine = this.calibrationProfileName();
    if (profile && profile !== mine) return;
    const saved = detail.calibration;
    if (!saved || typeof saved !== "object") return;
    this._profileCalibration = cloneCalibrationData(saved);
    this._profileLoadedProfile = mine;
    this._profileLoadRequested = mine;
    this._profileLoadInfo = {
      ...(this._profileLoadInfo || {}),
      profile: mine,
      exists: true,
      source: "saved Home Assistant profile",
      storage_key: "switch_vision_calibrations",
      storage_path: ".storage/switch_vision_calibrations",
      profile_path: `.storage/switch_vision_calibrations → profiles.${mine}`,
    };
    if (calibrationControlsEnabled(this.config)) {
      if (this._calibrationDirty !== true) {
        this._calibrationWorking = cloneCalibrationData(applyConfigToCalibrationForEdit(this._profileCalibration, this.config));
      }
      this.render();
    } else {
      this._calibrationWorking = null;
      this.render();
    }
  }

  setConfig(config) {
    this._rawConfig = { ...(config || {}) };
    this.config = {
      title: "Cisco 3650 Server Stack",
      member: "SW1",
      stack_enabled: false,
      stack_id: "",
      stack_member_number: null,
      stack_uptime_mode: null,
      stack_uptime_source: "",
      stack_member_uptime_source: "",
      demo: false,
      data_source: "home_assistant",
      unifi_device_id: "",
      unifi_refresh_seconds: 30,
      unifi_per_port_traffic: false,
      port_count: 0,
      sfp_port_count: 0,
      show_numbers: true,
      port_label_offset: 0,
      port_entity_offset: 0,
      show_port_leds: true,
      show_status_leds: true,
      show_status_panel: true,
      show_labels: true,
      show_legend: false,
      calibration_button: true,
      calibration_profile: null,
      selected_switch: null,
      discovery_selected_switch: null,
      calibration_profile_load: true,
      calibration_profile_auto_load: true,
      calibration_profile_auto_create: true,
      selected_interface: null,
      selected_port: null,
      calibration_mode: false,
      calibration_target: "all",
      calibration_show_hitboxes: true,
      calibration_show_labels: true,
      calibration_show_led_rings: true,
      calibration_controls: false,
      calibration_test_mode: false,
      calibration_part: "entire",
      calibration_step: 1,
      unknown_is_up: false,
      auto_speed_entity: true,
      activity_led_class: "cv-led-amber",
      activity_flash_ms: 800,
      activity_window_ms: 6500,
      activity_bridge_ms: 3500,
      activity_decay_ms: 3000,
      activity_slot_ms: 220,
      activity_heavy_slot_ms: 120,
      activity_animation_refresh_ms: 150,
      activity_led_sensitivity_preset: "normal",
      activity_slow_max_utilization_pct: 0.10,
      activity_medium_max_utilization_pct: 1.0,
      activity_slow_period_ms: 500,
      activity_medium_period_ms: 250,
      activity_fast_period_ms: 120,
      activity_hold_seconds: 12,
      activity_hysteresis_pct: 20,
      status_up_values: ["up", "on", "1", "true", "connected", "active", "online", "linkup", "link_up"],
      status_down_values: ["down", "off", "0", "2", "false", "disconnected", "inactive", "offline", "unavailable", "unknown", "linkdown", "link_down"],
      version: SV_VERSION,
      __raw_config: this._rawConfig,
      ...config,
      ...(this._globalActivitySettings || {})
    };

    const nextProfile = this.calibrationProfileName();
    if (this._lastConfiguredProfile && this._lastConfiguredProfile !== nextProfile) {
      this._profileLoadRequested = null;
      this._profileLoadedProfile = null;
      this._profileLoadInfo = null;
      this._profileLoadError = null;
      this._profileAutoCreateRequested = null;
      this._calibrationWorking = null;
      this._calibrationDirty = false;
    }
    this._lastConfiguredProfile = nextProfile;
  }

  set hass(hass) {
    const previousConnection = this._hass?.connection || null;
    const nextConnection = hass?.connection || null;
    this._hass = hass;
    if (previousConnection && previousConnection !== nextConnection) {
      this.unsubscribeCalibrationUpdates();
      this.unsubscribeUiSettingsUpdates();
    }
    const resolvedExactModel = this.detectedExactModel();
    if (resolvedExactModel !== this._lastResolvedExactModel) {
      this._lastResolvedExactModel = resolvedExactModel;
      if (this._calibrationDirty !== true) this._calibrationWorking = null;
    }
    if (this._pendingModelAwareReset === true && !this._modelAwareResetAllPromise) {
      // A Reset All received before model/profile context was available is
      // completed asynchronously as soon as a later Home Assistant update can
      // resolve a safe factory profile.
      void this.applyModelAwareResetAll();
    }
    this.subscribeCalibrationUpdates();
    this.subscribeUiSettingsUpdates();
    this.maybeLoadGlobalUiSettings();
    this.maybeLoadCalibrationProfile();
    this.maybeLoadAssetLibrary();
    this.ensureUnifiRefreshTimer();
    void this.maybeLoadUnifiDevice();

    clearInterval(this._activityRenderTimer);
    clearTimeout(this._activityStopTimer);
    this._activityRenderTimer = null;
    this._activityStopTimer = null;

    // v0.5.3: Home Assistant can call set hass repeatedly, and the normal
    // activity refresh timer re-rendered the whole shadow DOM every ~120ms.
    // That destroyed open <select> menus, making dropdowns appear to instantly close.
    // In interactive calibration mode, render only when controls change or when
    // no calibration control currently has focus.
    const controlsActive = calibrationControlsEnabled(this.config);
    const controlFocused = this.isCalibrationControlFocused();

    if (!this.shadowRoot || !this.shadowRoot.getElementById("s")) {
      this.render();
    } else if (!controlFocused) {
      this.redrawSwitchSvg();
    }

    if (controlsActive) return;

    const refreshMs = Number(this.config?.activity_animation_refresh_ms ?? 150);
    const windowMs = Number(this.config?.activity_window_ms ?? 6500);
    const holdMs = activityHoldMs(this.config || {});

    // v0.5.50: refresh only the SVG overlay in live mode. Rebuilding the full
    // shadow DOM repeatedly also recreated the switch <img>, which could cause
    // visible image flicker once the on-card calibration button was added.
    // v0.6.8: when activity_hold_seconds is configured, keep the redraw timer
    // alive for the full poll-aware hold window. The previous stop timer still
    // used activity_window_ms (~6.5s), so blinking could stop before the next
    // 30s traffic poll even though the activity state was still held active.
    this._activityRenderTimer = setInterval(() => this.redrawSwitchSvg(), Math.max(80, refreshMs));
    this._activityStopTimer = setTimeout(() => {
      clearInterval(this._activityRenderTimer);
      this._activityRenderTimer = null;
      this.redrawSwitchSvg();
    }, Math.max(500, windowMs + 300, holdMs + 1000));
  }

  applyGlobalUiSettings(settings, render = true) {
    const normalized = normalizeSwitchVisionGlobalUiSettings(settings);
    const nextHeaders = normalized.show_card_headers !== false;
    const nextActivity = activityConfigFromGlobalSettings(normalized);
    const previousActivity = JSON.stringify(this._globalActivitySettings || {});
    const nextActivityJson = JSON.stringify(nextActivity);
    const changed = this._globalShowCardHeaders !== nextHeaders || previousActivity !== nextActivityJson;

    this._globalShowCardHeaders = nextHeaders;
    this._globalActivitySettings = nextActivity;
    if (this.config) Object.assign(this.config, nextActivity);
    if (changed && render && this.isConnected) this.render();
  }

  handleUiSettingsUpdateEvent(event) {
    const settings = {
      show_card_headers: event?.data?.show_card_headers !== false,
      activity_leds: event?.data?.activity_leds || {},
    };
    cacheSwitchVisionGlobalUiSettings(this._hass, settings);
    this.applyGlobalUiSettings(settings);
  }

  unsubscribeUiSettingsUpdates() {
    this._uiSettingsEventConnection = null;
    const unsubscribe = this._uiSettingsEventUnsub;
    this._uiSettingsEventUnsub = null;
    if (typeof unsubscribe === "function") {
      try { unsubscribe(); } catch (err) {
        console.debug?.("Switch Vision UI-settings event unsubscription failed", err);
      }
    }
  }

  subscribeUiSettingsUpdates() {
    if (!this.isConnected) return;
    const connection = this._hass?.connection;
    if (!connection?.subscribeEvents) return;
    if (this._uiSettingsEventConnection && this._uiSettingsEventConnection !== connection) {
      this.unsubscribeUiSettingsUpdates();
    }
    if (this._uiSettingsEventUnsub && this._uiSettingsEventConnection === connection) return;
    this._uiSettingsEventConnection = connection;
    try {
      Promise.resolve(connection.subscribeEvents(
        (event) => this.handleUiSettingsUpdateEvent(event),
        "switch_vision_ui_settings_updated"
      )).then((unsubscribe) => {
        if (this._hass?.connection !== connection) {
          if (typeof unsubscribe === "function") unsubscribe();
          return;
        }
        this._uiSettingsEventUnsub = unsubscribe;
      }).catch((err) => {
        this._uiSettingsEventConnection = null;
        console.debug?.("Switch Vision UI-settings event subscription unavailable", err);
      });
    } catch (err) {
      this._uiSettingsEventConnection = null;
      console.debug?.("Switch Vision UI-settings event subscription unavailable", err);
    }
  }

  maybeLoadGlobalUiSettings() {
    const connection = this._hass?.connection || null;
    if (!connection || this._globalUiSettingsConnection === connection) return;
    this._globalUiSettingsConnection = connection;
    void switchVisionGlobalUiSettings(this._hass).then((settings) => {
      if (this._hass?.connection !== connection) return;
      this.applyGlobalUiSettings(settings);
    });
  }

  isCalibrationControlFocused() {
    if (!this.shadowRoot || !calibrationControlsEnabled(this.config)) return false;
    const active = this.shadowRoot.activeElement;
    if (!active) return false;
    return Boolean(active.closest?.('.cv-cal-tools'));
  }

  getCardSize() {
    return 2;
  }

  legend() {
    if (!this.config.show_legend) return "";

    return `<div class="cv-legend"><div class="cv-legend-row">
      <span class="cv-legend-item"><span class="cv-legend-dot" style="color:#222;background:#222"></span>Link Down</span>
      <span class="cv-legend-item"><span class="cv-legend-dot" style="color:#ffb321;background:#ffb321"></span>100 Mbps / PoE</span>
      <span class="cv-legend-item"><span class="cv-legend-dot" style="color:#76ff33;background:#76ff33"></span>1 Gbps</span>
      <span class="cv-legend-item"><span class="cv-legend-dot" style="color:#29a8ff;background:#29a8ff"></span>10G / SFP</span>
    </div></div>`;
  }

  calibrationButton() {
    if (this.config.calibration_button === false) return "";
    const active = calibrationControlsEnabled(this.config);
    const opening = this._calibrationOpening === true;
    const label = opening ? "Loading…" : (active ? "Done" : "Calibrate");
    const title = opening ? "Loading the saved Switch Vision calibration profile" : (active ? "Exit Switch Vision calibration mode" : "Enable Switch Vision calibration mode");
    const ui = uiFromCalibration(this.calibrationData()).calibration_button || {};
    if (ui.show === false) return "";
    const designWidth = 2048;
    const designHeight = 448;
    const width = Math.max(8, Math.min(designWidth, Number(ui.width) || 138));
    const height = Math.max(8, Math.min(designHeight, Number(ui.height) || 34));
    const x = Math.max(0, Math.min(designWidth - width, Number(ui.x) || (designWidth - width - 22)));
    const y = Math.max(0, Math.min(designHeight - height, Number(ui.y) || 14));
    const style = [
      `left:${(x / designWidth) * 100}%`,
      `top:${(y / designHeight) * 100}%`,
      `width:${(width / designWidth) * 100}%`,
      `height:${(height / designHeight) * 100}%`,
      "right:auto"
    ].join(";");
    const testModeActive = calibrationTestModeEnabled(this.config);
const testModeStyle = [
  `left:${(x / designWidth) * 100}%`,
  `top:calc(${((y + height) / designHeight) * 100}% + 30px)`,
  `width:${(width / designWidth) * 100}%`,
  `height:${(height / designHeight) * 100}%`,
  "right:auto",
  "z-index:16",
  "pointer-events:none"
].join(";");
const testModeBadge = testModeActive
  ? `<div class="cv-cal-toggle cv-cal-test-mode-badge is-active" style="${testModeStyle}" title="Calibration LED Test Mode is active">TEST MODE</div>`
  : "";
    return `<button type="button" class="cv-cal-toggle ${active ? "is-active" : ""}" style="${style}" data-cv-action="toggle-calibration" title="${title}" ${opening ? "disabled" : ""}>${label}</button>${testModeBadge}`;
  }

  showConfirmation(message, options = {}) {
    if (!this.shadowRoot) return Promise.resolve(false);
    const title = String(options.title || "Confirm action");
    const confirmLabel = String(options.confirmLabel || "OK");
    const cancelLabel = String(options.cancelLabel || "Cancel");
    const danger = options.danger === true;
    const previous = this.shadowRoot.querySelector(".cv-confirm-backdrop");
    if (previous) previous.remove();

    return new Promise((resolve) => {
      const backdrop = document.createElement("div");
      backdrop.className = "cv-confirm-backdrop";
      backdrop.setAttribute("role", "presentation");
      backdrop.innerHTML = `<section class="cv-confirm-dialog" role="alertdialog" aria-modal="true" aria-labelledby="cv-confirm-title">
        <div class="cv-confirm-header" id="cv-confirm-title"></div>
        <pre class="cv-confirm-message"></pre>
        <div class="cv-confirm-actions">
          <button type="button" data-cv-confirm-cancel></button>
          <button type="button" data-cv-confirm-ok class="${danger ? "is-danger" : ""}"></button>
        </div>
      </section>`;
      backdrop.querySelector(".cv-confirm-header").textContent = title;
      backdrop.querySelector(".cv-confirm-message").textContent = String(message || "");
      const cancel = backdrop.querySelector("[data-cv-confirm-cancel]");
      const ok = backdrop.querySelector("[data-cv-confirm-ok]");
      cancel.textContent = cancelLabel;
      ok.textContent = confirmLabel;
      const finish = (result) => {
        document.removeEventListener("keydown", onKeyDown, true);
        backdrop.remove();
        resolve(result);
      };
      const onKeyDown = (event) => {
        if (event.key === "Escape") { event.preventDefault(); finish(false); }
      };
      cancel.addEventListener("click", () => finish(false), { once: true });
      ok.addEventListener("click", () => finish(true), { once: true });
      backdrop.addEventListener("click", (event) => { if (event.target === backdrop) finish(false); });
      document.addEventListener("keydown", onKeyDown, true);
      this.shadowRoot.appendChild(backdrop);
      requestAnimationFrame(() => ok.focus());
    });
  }

  attachCalibrationButtonHandler() {
    if (!this.shadowRoot) return;
    const button = this.shadowRoot.querySelector('[data-cv-action="toggle-calibration"]');
    if (!button) return;
    button.addEventListener("click", async (event) => {
      event.preventDefault();
      event.stopPropagation();
      const active = calibrationControlsEnabled(this.config);
      if (!active) {
        // Native-panel profiles are loaded asynchronously from Home Assistant.
        // Open Calibration only after that load (and any starter-profile save)
        // has finished. Previously the editor opened immediately, allowing a
        // late default-profile response to replace a newly selected faceplate
        // just before Done saved it.
        if (this._calibrationOpening === true) return;
        this._calibrationOpening = true;
        this.render();
        try {
          const profile = this.calibrationProfileName();
          const info = await this.loadCalibrationProfile(true, { applyToWorking: false });
          if (!info?.exists && this.config?.calibration_profile_auto_create !== false) {
            await this.createStarterCalibrationProfile(profile, this.baseCalibrationData());
          }
          this._calibrationDirty = false;
          this._calibrationWorking = applyConfigToCalibrationForEdit(this.baseCalibrationData(), this.config);
        } finally {
          this._calibrationOpening = false;
        }
      } else {
        if (this._calibrationDirty === true && !(await this.showConfirmation("Save the current calibration changes and close the editor?\n\nChoose Cancel to keep editing.", { title: "Save calibration changes?", confirmLabel: "Save and close" }))) return;
        const working = this.calibrationData();
        const validation = validateImportedCalibration(calibrationExportData(working), working);
        if (!validation.valid) {
          this.setCalibrationSaveStatus(`Cannot finish: ${validation.errors[0] || "calibration validation failed"}`, true);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }
        try {
          const profile = await this.saveCalibrationProfile(working);
          this._calibrationDirty = false;
          this.setCalibrationSaveStatus(`Profile saved: ${profile}`, false);
        } catch (err) {
          console.error("Switch Vision automatic Done save failed", err);
          this.setCalibrationSaveStatus("Could not save profile — calibration remains open", true);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }
      }
      this.config = {
        ...this.config,
        calibration_mode: !active,
        calibration_controls: !active,
        calibration_test_mode: this.config?.calibration_test_mode === true,
        calibration_target: this.config.calibration_target || "all",
        calibration_part: this.config.calibration_part || "entire"
      };
      // Each new calibration session starts clean and compact. Section open
      // state is intentionally session-only and is not carried across closes.
      if (!active) this._calibrationSectionState = {};
      this.render();
    });
  }

  baseCalibrationData() {
    const saved = ensureCalibrationUi(this._profileCalibration || calibration);
    const recommendation = exactModelVisualRecommendation(this.detectedExactModel());
    const factory = recommendation?.profile ? factoryCalibrationForProfile(recommendation.profile) : null;
    if (!factory) return saved;

    // A profile explicitly saved in Home Assistant storage is user-authored and
    // therefore authoritative, including intentional custom port/uplink counts.
    // Exact-model reconciliation is only a starter/legacy repair path; it must not
    // rebuild deliberately removed or added visual ports after Done/Save Profile.
    const savedTopologyIsAuthoritative =
      this._profileLoadInfo?.exists === true &&
      (saved?.user_topology_authoritative === true ||
       Boolean(String(saved?.profile_name || "").trim()) ||
       Boolean(String(saved?.base_profile_name || "").trim()));
    if (savedTopologyIsAuthoritative) return saved;

    const expectedPorts = Number(recommendation.rj45 || 0);
    const expectedUplinks = Number(recommendation.uplinks || 0);
    const savedPorts = Object.keys(saved?.ports || {}).length;
    const savedUplinks = Object.keys(saved?.sfp || {}).length;
    // Older saved profiles did not include the factory-profile identifier. Do not
    // discard otherwise compatible user calibration data (including SFP labels)
    // solely because that optional metadata is absent.
    const profileMismatch = Boolean(String(saved?.profile || "").trim()) && String(saved.profile) !== String(recommendation.profile || "");
    const geometryMismatch = savedPorts !== expectedPorts || savedUplinks !== expectedUplinks;
    // The exact-model profile controls geometry, not the user's chosen artwork.
    // A custom faceplate is valid for any compatible geometry and must not cause
    // the saved profile to be discarded after Calibration is closed.
    if (!profileMismatch && !geometryMismatch) return saved;

    // Exact model mappings are authoritative. Use the matching factory geometry,
    // while preserving the user's status-box, logo, stack and management layout.
    const resolved = cloneCalibrationData(factory);
    const savedUi = uiFromCalibration(saved);
    resolved.ui = uiFromCalibration(resolved);
    for (const key of ["logo", "status_panel", "status_panel_2", "calibration_button"]) {
      if (savedUi?.[key]) resolved.ui[key] = cloneCalibrationData(savedUi[key]);
    }
    resolved.stack = cloneCalibrationData(saved?.stack || resolved.stack || {});
    resolved.management = cloneCalibrationData(saved?.management || resolved.management || {});
    resolved.profile = recommendation.profile || resolved.profile;
    // If factory geometry needs to be restored, retain the user's saved
    // faceplate selection rather than silently reverting to the model default.
    if (savedUi?.faceplate) {
      resolved.ui.faceplate = cloneCalibrationData(savedUi.faceplate);
    }
    return ensureCalibrationUi(resolved);
  }

  calibrationData() {
    const base = this.baseCalibrationData();
    if (!calibrationControlsEnabled(this.config)) return base;
    if (!this._calibrationWorking) this._calibrationWorking = applyConfigToCalibrationForEdit(base, this.config);
    return ensureCalibrationUi(this._calibrationWorking);
  }

  canLoadCalibrationProfile() {
    return this.config?.calibration_profile_load !== false && Boolean(this._hass?.callWS);
  }

  async loadCalibrationProfile(force = false, options = {}) {
    if (!this.canLoadCalibrationProfile()) return null;
    const profile = this.calibrationProfileName();
    if (!profile) return null;
    const applyToWorking = options.applyToWorking === true;
    if (!force && !applyToWorking && this._profileLoadRequested === profile) return this._profileLoadInfo || null;

    this._profileLoadRequested = profile;
    this._profileLoadError = null;
    this._profileLoading = true;
    if (applyToWorking) {
      this.setCalibrationSaveStatus(`Loading profile: ${profile}`, false);
      this.render();
    }

    try {
      const result = await this._hass.callWS({
        type: "switch_vision/get_calibration",
        profile
      });
      if (this.calibrationProfileName() !== profile) return null;
      const resolvedProfile = String(result?.profile || profile).trim() || profile;
      const expectedPrefix = `${profile}__faceplate__`;
      if (resolvedProfile !== profile && !resolvedProfile.startsWith(expectedPrefix)) {
        throw new Error(`Stored active faceplate profile escaped switch scope: ${resolvedProfile}`);
      }

      this._profileLoadInfo = {
        profile: resolvedProfile,
        requested_profile: profile,
        active_profile: String(result?.active_profile || ""),
        exists: Boolean(result?.exists),
        source: result?.source || (result?.exists ? "Home Assistant storage" : "current card config / baked defaults"),
        storage_key: result?.storage_key || "switch_vision_calibrations",
        storage_path: result?.storage_path || ".storage/switch_vision_calibrations",
        profile_path: result?.profile_path || `.storage/switch_vision_calibrations → profiles.${resolvedProfile}`,
      };

      if (result?.invalid) {
        this._profileCalibration = null;
        this._profileLoadedProfile = null;
        this._profileLoadInfo.exists = false;
        this._profileLoadInfo.invalid = true;
        this._profileLoadInfo.validation_error = String(result.validation_error || "Stored profile failed validation");
        if (applyToWorking) this.setCalibrationSaveStatus(`Stored profile rejected: ${this._profileLoadInfo.validation_error}`, true);
        console.error("Switch Vision rejected invalid stored profile", profile, this._profileLoadInfo.validation_error);
        this.render();
        return this._profileLoadInfo;
      }

      const loaded = result?.calibration;
      if (loaded && typeof loaded === "object") {
        const loadedValidation = validateImportedCalibration(loaded, this.baseCalibrationData());
        if (!loadedValidation.valid) {
          this._profileCalibration = null;
          this._profileLoadedProfile = null;
          this._profileLoadInfo.exists = false;
          this._profileLoadInfo.invalid = true;
          this._profileLoadInfo.validation_error = loadedValidation.errors[0] || "Stored profile failed validation";
          if (applyToWorking) this.setCalibrationSaveStatus(`Stored profile rejected: ${this._profileLoadInfo.validation_error}`, true);
          console.error("Switch Vision rejected invalid stored profile", profile, loadedValidation.errors);
          this.render();
          return this._profileLoadInfo;
        }
        this._profileCalibration = cloneCalibrationData(loadedValidation.calibration);
        this._profileLoadedProfile = resolvedProfile;
        this._profileLoadRequested = resolvedProfile;
        const loadedForEdit = applyConfigToCalibrationForEdit(this._profileCalibration, this.config);
        if (applyToWorking || !this._calibrationWorking) {
          this._calibrationWorking = cloneCalibrationData(loadedForEdit);
          this._calibrationDirty = false;
        }
        if (!calibrationControlsEnabled(this.config)) this._calibrationWorking = null;
        if (applyToWorking) this.setCalibrationSaveStatus(`Loaded profile: ${resolvedProfile}`, false);
        this.render();
        return this._profileLoadInfo;
      }

      const shouldCreateStarter =
        applyToWorking &&
        calibrationControlsEnabled(this.config) &&
        this.config?.calibration_profile_auto_create !== false;

      if (shouldCreateStarter) {
        return await this.createStarterCalibrationProfile(profile, options.starterCalibration || null);
      }

      if (applyToWorking) this.setCalibrationSaveStatus(`No saved profile found: ${profile}`, true);
      if (calibrationControlsEnabled(this.config)) this.render();
      return this._profileLoadInfo;
    } catch (err) {
      // Fallback to current card YAML/baked defaults if the integration or websocket command is unavailable.
      this._profileLoadError = err;
      this._profileLoadInfo = {
        profile,
        exists: false,
        source: "profile load unavailable; using current card config / baked defaults",
        storage_key: "switch_vision_calibrations",
        storage_path: ".storage/switch_vision_calibrations",
        profile_path: `.storage/switch_vision_calibrations → profiles.${profile}`,
      };
      if (applyToWorking) this.setCalibrationSaveStatus("Load failed — check browser console / HA logs", true);
      console.debug?.("Switch Vision profile load unavailable", err);
      if (calibrationControlsEnabled(this.config)) this.render();
      return this._profileLoadInfo;
    } finally {
      this._profileLoading = false;
    }
  }

  async createStarterCalibrationProfile(profile, starterOverride = null) {
    const targetProfile = String(profile || this.calibrationProfileName()).trim();
    if (!targetProfile) return null;
    if (this._profileLoadedProfile === targetProfile) return this._profileLoadInfo || null;
    if (this._profileAutoCreateRequested === targetProfile) return this._profileLoadInfo || null;

    this._profileAutoCreateRequested = targetProfile;
    const starterSource = starterOverride && typeof starterOverride === "object" ? starterOverride : this.baseCalibrationData();
    const starter = applyConfigToCalibrationForEdit(starterSource, this.config);
    this.setCalibrationSaveStatus(`Creating starter profile: ${targetProfile}`, false);
    if (calibrationControlsEnabled(this.config)) this.render();

    try {
      await this.saveCalibrationProfile(starter);
      if (this.calibrationProfileName() !== targetProfile) return null;

      this._calibrationWorking = cloneCalibrationData(
        applyConfigToCalibrationForEdit(this._profileCalibration, this.config)
      );
      this._calibrationDirty = false;
      this.setCalibrationSaveStatus(`Created starter profile: ${targetProfile}`, false);
      if (calibrationControlsEnabled(this.config)) this.render();
      this.clearCalibrationSaveStatusSoon();
      return this._profileLoadInfo;
    } catch (err) {
      this._profileAutoCreateRequested = null;
      this._profileLoadError = err;
      this.setCalibrationSaveStatus(`Could not create starter profile: ${targetProfile}`, true);
      console.debug?.("Switch Vision starter profile creation failed", err);
      if (calibrationControlsEnabled(this.config)) this.render();
      return this._profileLoadInfo || null;
    }
  }

  maybeLoadCalibrationProfile(force = false) {
    const profile = this.calibrationProfileName();
    const controlsActive = calibrationControlsEnabled(this.config);
    const autoLoadEnabled = this.config?.calibration_profile_auto_load !== false;
    const needsProfile = profile && this._profileLoadedProfile !== profile;
    const safeToApply = controlsActive && autoLoadEnabled && needsProfile && this._calibrationDirty !== true;

    // v0.6.18: when a calibration page is opened directly, Home Assistant may
    // render the editor from card YAML/baked defaults before the saved profile
    // websocket response returns. In that case, automatically apply the saved
    // profile into the active edit session. The manual load button remains as a
    // fallback/reload action.
    this.loadCalibrationProfile(force || safeToApply, { applyToWorking: safeToApply });
  }

  calibrationUpdateAppliesToThisCard(data = {}) {
    const profile = String(data.profile || "");
    const baseProfile = String(data.base_profile_name || "");
    const changedProfiles = Array.isArray(data.profiles_changed)
      ? data.profiles_changed.map((name) => String(name || "")).filter(Boolean)
      : (profile ? [profile] : []);
    const mine = this.calibrationProfileName();
    const myBase = this.baseCalibrationProfileName();
    const scopePrefix = String(data.scope_prefix || "");
    return {
      applies:
        !profile ||
        profile === "*" ||
        (scopePrefix && myBase.startsWith(scopePrefix)) ||
        changedProfiles.includes(mine) ||
        changedProfiles.includes(myBase) ||
        profile === mine ||
        profile === myBase ||
        (baseProfile && baseProfile === myBase),
      profile,
      baseProfile,
      changedProfiles,
      mine,
      myBase,
    };
  }

  async handleCalibrationUpdateEvent(event) {
    const data = event?.data || {};
    const routing = this.calibrationUpdateAppliesToThisCard(data);
    if (!routing.applies) return;

    const { profile, baseProfile, mine, myBase } = routing;
    const calibrations = data.calibrations && typeof data.calibrations === "object"
      ? data.calibrations
      : {};

    this._profileLoadRequested = null;
    this._profileLoadedProfile = null;
    if (data.action === "reset_all" || data.action === "reset_scope") {
      // Reset All is namespace-scoped. Only cards whose base profile matches
      // scope_prefix reach this branch, so native_ and custom_ stay isolated.
      await this.applyModelAwareResetAll();
      return;
    }
    if (data.action === "reset_switch") {
      const next = this.factoryCalibrationForCurrentSwitch({ preserveFaceplate: false });
      this._pendingModelAwareReset = false;
      this.clearLoadedCalibrationState(next);
      this.render();
      return;
    }
    if (data.action === "deleted") {
      // Clear the removed payload before reloading so a deleted faceplate or
      // mirrored base profile cannot remain visible from this card's cache.
      const next = this.factoryCalibrationForCurrentSwitch({ preserveFaceplate: false });
      this.clearLoadedCalibrationState(next);
      this.render();
      this.loadCalibrationProfile(true, { applyToWorking: false });
      return;
    }

    // Older Switch Vision backends may include the exact saved payload in the
    // event. Prefer it when present for compatibility. Current backends send
    // compact routing-only events and use the websocket reload path below.
    let targetProfile = "";
    let savedCalibration = null;
    if (calibrations[mine] && typeof calibrations[mine] === "object") {
      targetProfile = mine;
      savedCalibration = calibrations[mine];
    } else if (calibrations[myBase] && typeof calibrations[myBase] === "object") {
      targetProfile = myBase;
      savedCalibration = calibrations[myBase];
    } else if (data.calibration && typeof data.calibration === "object") {
      targetProfile = profile === myBase || baseProfile === myBase ? myBase : mine;
      savedCalibration = data.calibration;
    }

    if (savedCalibration) {
      this.handleLocalCalibrationSaved({ detail: { profile: targetProfile, calibration: savedCalibration } });
    } else if (data.active_profile_changed === true && baseProfile === myBase && this._calibrationDirty !== true) {
      // Another card for this same switch/scope changed the active faceplate.
      // Reload through the stable base so the backend can resolve the pointer.
      this._profileCalibration = null;
      this._calibrationWorking = null;
      this._profileLoadRequested = null;
      this._profileLoadedProfile = null;
      this.loadCalibrationProfile(true, { applyToWorking: false });
    } else {
      this.loadCalibrationProfile(true, { applyToWorking: false });
    }
  }

  scheduleCalibrationSubscriptionRetry() {
    if (!this.isConnected || this._calibrationEventRetryTimer) return;
    const attempt = Math.max(0, Number(this._calibrationEventRetryAttempt || 0));
    const delay = Math.min(30000, 1000 * (2 ** Math.min(attempt, 5)));
    this._calibrationEventRetryAttempt = attempt + 1;
    this._calibrationEventRetryTimer = setTimeout(() => {
      this._calibrationEventRetryTimer = null;
      if (this.isConnected) this.subscribeCalibrationUpdates();
    }, delay);
  }

  unsubscribeCalibrationUpdates() {
    clearTimeout(this._calibrationEventRetryTimer);
    this._calibrationEventRetryTimer = null;
    this._calibrationEventRetryAttempt = 0;
    this._calibrationEventSubscriptionToken = Number(this._calibrationEventSubscriptionToken || 0) + 1;
    this._calibrationEventSubscriptionRequested = false;
    this._calibrationEventConnection = null;
    const unsubscribe = this._calibrationEventUnsub;
    this._calibrationEventUnsub = null;
    if (typeof unsubscribe === "function") {
      try {
        unsubscribe();
      } catch (err) {
        console.debug?.("Switch Vision event unsubscription failed", err);
      }
    }
  }

  subscribeCalibrationUpdates() {
    if (!this.isConnected) return;
    const connection = this._hass?.connection;
    if (!connection?.subscribeEvents) {
      this.scheduleCalibrationSubscriptionRetry();
      return;
    }

    if (this._calibrationEventConnection && this._calibrationEventConnection !== connection) {
      this.unsubscribeCalibrationUpdates();
    }
    if (this._calibrationEventUnsub && this._calibrationEventConnection === connection) return;
    if (this._calibrationEventSubscriptionRequested && this._calibrationEventConnection === connection) return;

    clearTimeout(this._calibrationEventRetryTimer);
    this._calibrationEventRetryTimer = null;
    const token = Number(this._calibrationEventSubscriptionToken || 0) + 1;
    this._calibrationEventSubscriptionToken = token;
    this._calibrationEventSubscriptionRequested = true;
    this._calibrationEventConnection = connection;

    let subscriptionRequest;
    try {
      subscriptionRequest = connection.subscribeEvents(
        (event) => this.handleCalibrationUpdateEvent(event),
        "switch_vision_calibration_updated"
      );
    } catch (err) {
      if (token === this._calibrationEventSubscriptionToken) {
        this._calibrationEventSubscriptionRequested = false;
        this._calibrationEventConnection = null;
        console.debug?.("Switch Vision event subscription unavailable", err);
        this.scheduleCalibrationSubscriptionRetry();
      }
      return;
    }

    Promise.resolve(subscriptionRequest).then((unsubscribe) => {
      const stale =
        token !== this._calibrationEventSubscriptionToken ||
        !this.isConnected ||
        connection !== this._hass?.connection;
      if (stale) {
        if (typeof unsubscribe === "function") {
          try {
            unsubscribe();
          } catch (err) {
            console.debug?.("Switch Vision stale event unsubscription failed", err);
          }
        }
        return;
      }
      this._calibrationEventSubscriptionRequested = false;
      this._calibrationEventUnsub = unsubscribe;
      this._calibrationEventRetryAttempt = 0;
    }).catch((err) => {
      if (token !== this._calibrationEventSubscriptionToken) return;
      this._calibrationEventSubscriptionRequested = false;
      this._calibrationEventConnection = null;
      console.debug?.("Switch Vision event subscription unavailable", err);
      this.scheduleCalibrationSubscriptionRetry();
    });
  }

  isUnifiDataSource() {
    return String(this.config?.data_source || "").toLowerCase() === "unifi_api" && Boolean(String(this.config?.unifi_device_id || "").trim());
  }

  ensureUnifiRefreshTimer() {
    if (!this.isUnifiDataSource()) {
      clearInterval(this._unifiRefreshTimer);
      this._unifiRefreshTimer = null;
      return;
    }
    if (this._unifiRefreshTimer) return;
    const seconds = Math.max(10, Number(this.config?.unifi_refresh_seconds || 30));
    this._unifiRefreshTimer = setInterval(() => void this.maybeLoadUnifiDevice(true), seconds * 1000);
  }

  async maybeLoadUnifiDevice(force = false) {
    if (!this.isUnifiDataSource() || !this._hass?.callWS) return;
    if (this._unifiLoadPromise) return this._unifiLoadPromise;
    const now = Date.now();
    const minMs = Math.max(5000, Number(this.config?.unifi_refresh_seconds || 30) * 1000);
    if (!force && this._unifiLastFetchAt && (now - this._unifiLastFetchAt) < minMs) return;
    const deviceId = String(this.config.unifi_device_id || "").trim();
    const task = this._hass.callWS({ type: "switch_vision/get_unifi_device", device_id: deviceId })
      .then((result) => {
        this._unifiLastFetchAt = Date.now();
        const device = result?.device;
        if (!device || typeof device !== "object") {
          this._unifiLoadError = result?.error || "UniFi device is unavailable";
          return;
        }
        this._unifiLoadError = null;
        this.config = { ...this.config, __unifi_runtime: device };
        if (this.shadowRoot?.getElementById("s")) this.redrawSwitchSvg();
        else this.render();
      })
      .catch((err) => {
        this._unifiLastFetchAt = Date.now();
        this._unifiLoadError = String(err?.message || err || "UniFi device load failed");
      })
      .finally(() => {
        if (this._unifiLoadPromise === task) this._unifiLoadPromise = null;
      });
    this._unifiLoadPromise = task;
    return task;
  }

  async maybeLoadAssetLibrary(force = false) {
    if (!this._hass?.callWS) return null;
    if (this._assetLibraryLoading) return null;
    if (!force && this._assetLibrary) return this._assetLibrary;
    this._assetLibraryLoading = true;
    this._assetLibraryError = null;
    try {
      const result = await this._hass.callWS({ type: "switch_vision/list_assets" });
      this._assetLibrary = {
        logos: Array.isArray(result?.logos) ? result.logos : [],
        faceplates: Array.isArray(result?.faceplates) ? result.faceplates : [],
        logos_path: result?.logos_path || "/config/www/switch-vision/logos",
        faceplates_path: result?.faceplates_path || "/config/www/switch-vision/faceplates"
      };
      if (calibrationControlsEnabled(this.config)) this.render();
      return this._assetLibrary;
    } catch (err) {
      this._assetLibraryError = err;
      this._assetLibrary = { logos: [], faceplates: [], logos_path: "/config/www/switch-vision/logos", faceplates_path: "/config/www/switch-vision/faceplates" };
      console.debug?.("Switch Vision asset listing unavailable", err);
      if (calibrationControlsEnabled(this.config)) this.render();
      return null;
    } finally {
      this._assetLibraryLoading = false;
    }
  }

  saveCalibrationExportSize() {
    if (!this.shadowRoot) return;
    const textarea = this.shadowRoot.querySelector('.cv-cal-export');
    if (!textarea) return;
    const rect = textarea.getBoundingClientRect();
    if (rect.width > 0 && rect.height > 0) {
      this._calibrationExportSize = { width: Math.round(rect.width), height: Math.round(rect.height) };
    }
  }

  applyCalibrationExportSize() {
    if (!this.shadowRoot || !this._calibrationExportSize) return;
    const textarea = this.shadowRoot.querySelector('.cv-cal-export');
    if (!textarea) return;
    textarea.style.width = `${this._calibrationExportSize.width}px`;
    textarea.style.height = `${this._calibrationExportSize.height}px`;
  }

  baseCalibrationProfileName() {
    const switchKey = stableCalibrationSwitchKey(this.config);
    if (switchKey) {
      const scope = this.config?.switch_vision_native_panel === true ? "native" : "custom";
      return `${scope}_${switchKey}`;
    }
    return String(this.config?.calibration_profile || "default").trim() || "default";
  }

  activeFaceplateFilename(cal = null) {
    const data = cal && typeof cal === "object" ? cal : (this._calibrationWorking || this._profileCalibration || calibration);
    return faceplateSelectionValue(ensureCalibrationUi(data).ui?.faceplate || {});
  }

  recommendedFaceplateFilename(cal = null) {
    const recommendation = exactModelVisualRecommendation(this.detectedExactModel());
    const recommended = String(recommendation?.faceplate || "");
    if (recommended.startsWith("faceplates/")) return safeAssetFilename(recommended.split("/").pop());

    const data = cal && typeof cal === "object" ? cal : (this._calibrationWorking || this._profileCalibration || calibration);
    const factory = factoryCalibrationForProfile(factoryProfileNameForCalibration(data));
    const factoryFile = String(factory?.image?.file || data?.image?.file || calibration?.image?.file || "");
    if (factoryFile.startsWith("faceplates/")) return safeAssetFilename(factoryFile.split("/").pop());
    return safeAssetFilename(factoryFile) || "48rj45-4sfp.png";
  }

  resolvedFaceplateFilename(cal = null) {
    const selection = this.activeFaceplateFilename(cal);
    const recommended = this.recommendedFaceplateFilename(cal);
    if (selection === SV_ASSET_DEFAULT || selection === SV_ASSET_NONE) return recommended;
    const safe = safeAssetFilename(selection);
    if (!safe) return recommended;
    const knownFaceplates = this._assetLibrary?.faceplates;
    if (Array.isArray(knownFaceplates) && !knownFaceplates.includes(safe)) return recommended;
    return safe;
  }

  faceplateProfileToken(filename = this.activeFaceplateFilename()) {
    const file = String(filename || SV_ASSET_DEFAULT).trim();
    if (!file || file === SV_ASSET_DEFAULT || file === SV_ASSET_NONE) return "";
    const slug = file.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 48) || "faceplate";
    let hash = 2166136261;
    for (let i = 0; i < file.length; i += 1) {
      hash ^= file.charCodeAt(i);
      hash = Math.imul(hash, 16777619);
    }
    return `${slug}-${(hash >>> 0).toString(16).padStart(8, "0")}`;
  }

  calibrationProfileName(cal = null) {
    const base = this.baseCalibrationProfileName();
    const token = this.faceplateProfileToken(this.activeFaceplateFilename(cal));
    return token ? `${base}__faceplate__${token}` : base;
  }

  calibrationProfileDisplay(cal = null) {
    const faceplate = this.activeFaceplateFilename(cal);
    return {
      base: this.baseCalibrationProfileName(),
      faceplate: [SV_ASSET_DEFAULT, SV_ASSET_NONE].includes(faceplate) ? "Default / recommended" : faceplate,
      scope: [SV_ASSET_DEFAULT, SV_ASSET_NONE].includes(faceplate) ? "Switch default" : "Faceplate-specific",
      profile: this.calibrationProfileName(cal),
    };
  }

  calibrationProfileSourceDetails(profileName = this.calibrationProfileName()) {
    const explicit = String(this.config?.calibration_profile || "").trim();
    const selected = String(this.config?.selected_switch || this.config?.discovery_selected_switch || "").trim();
    const member = String(this.config?.member || "").trim();
    let profileSource = "default";
    if (explicit) profileSource = "calibration_profile";
    else if (selected) profileSource = "selected_switch";
    else if (member) profileSource = "member";

    const info = this._profileLoadInfo || {};
    const storagePath = info.storage_path || ".storage/switch_vision_calibrations";
    const profilePath = info.profile_path || `${storagePath} → profiles.${profileName}`;
    const loaded = info.exists === true || this._profileLoadedProfile === profileName;
    const source = loaded ? "saved Home Assistant profile" : (info.source || "current card config / baked defaults");
    return { profileSource, storagePath, profilePath, loaded, source };
  }

  setCalibrationSaveStatus(message, isError = false) {
    this._calibrationSaveStatus = { message, isError, ts: Date.now() };
  }

  markCalibrationDirty() {
    this._calibrationDirty = true;
  }

  clearCalibrationSaveStatusSoon() {
    clearTimeout(this._calibrationSaveStatusTimer);
    this._calibrationSaveStatusTimer = setTimeout(() => {
      this._calibrationSaveStatus = null;
      if (calibrationControlsEnabled(this.config)) this.render();
    }, 2200);
  }

  async saveCalibrationProfile(cal, profileOverride = null) {
    const profile = String(profileOverride || this.calibrationProfileName()).trim() || "default";
    const profileError = profileNameValidationMessage(profile);
    if (profileError) throw new Error(profileError);

    const validation = validateImportedCalibration(calibrationExportData(cal), cal);
    if (!validation.valid) throw new Error(validation.errors[0] || "Calibration profile is invalid");
    const payload = calibrationExportData(validation.calibration);
    payload.profile_name = profile;
    payload.profile_scope = this.calibrationProfileDisplay(cal).scope;
    payload.base_profile_name = this.baseCalibrationProfileName();
    payload.faceplate_file = this.activeFaceplateFilename(cal);
    // Mark this topology as an intentional user save. This keeps custom visual
    // port/uplink counts authoritative on future versions and fresh page loads.
    payload.user_topology_authoritative = true;

    if (!this._hass?.callService) {
      throw new Error("Home Assistant service API is not available");
    }

    // Save the active faceplate profile and mark it active for this switch scope.
    // v2.1.4 keeps the actual base/default calibration independent; the legacy
    // mirror_to_base flag is now interpreted by the backend as pointer-only.
    const baseProfile = this.baseCalibrationProfileName();
    const scopedProfile = Boolean(stableCalibrationSwitchKey(this.config));
    await this._hass.callService("switch_vision", "save_calibration", {
      profile,
      calibration: payload,
      mirror_to_base: scopedProfile && profile !== baseProfile
    });

    this._profileCalibration = cloneCalibrationData(payload);
    this._profileLoadedProfile = profile;
    this._profileLoadRequested = profile;
    this._profileLoadInfo = {
      profile,
      exists: true,
      source: "saved Home Assistant profile",
      storage_key: "switch_vision_calibrations",
      storage_path: ".storage/switch_vision_calibrations",
      profile_path: `.storage/switch_vision_calibrations → profiles.${profile}`,
    };

    window.dispatchEvent(new CustomEvent("switch-vision-calibration-saved", {
      detail: { profile, calibration: cloneCalibrationData(payload) }
    }));

    return profile;
  }

  factoryCalibrationForCurrentSwitch({ preserveFaceplate = false, requireModelAware = false, currentCalibration = null } = {}) {
    let recommendation = exactModelVisualRecommendation(this.detectedExactModel());

    // Reset All is received by every card, including native/custom YAML cards
    // that may not expose their exact-model sensor at that instant. Fall back
    // to the already-loaded calibration's stable factory profile so a 2960S,
    // 2960X, or 3560-C is never rebuilt as the generic 3650 48/4 layout.
    if (!recommendation && !requireModelAware) {
      for (const candidate of [this._calibrationWorking, this._profileCalibration]) {
        const profile = factoryProfileNameForCalibration(candidate);
        const candidateFactory = factoryCalibrationForProfile(profile);
        if (!candidateFactory) continue;
        recommendation = {
          profile,
          faceplate: String(candidateFactory?.image?.file || ""),
          source: "loaded calibration profile"
        };
        break;
      }
    }

    // Model-aware namespace resets must use the current exact-model
    // recommendation. An older saved factory profile may describe the previous
    // fallback visual and must not be persisted again after a model-policy
    // change.
    if (requireModelAware && !recommendation) return null;

    const modelFactory = recommendation?.profile
      ? factoryCalibrationForProfile(recommendation.profile)
      : null;
    if (requireModelAware && !modelFactory) return null;

    const currentSource = currentCalibration && typeof currentCalibration === "object"
      ? currentCalibration
      : (this._calibrationWorking || this._profileCalibration || calibration);
    const current = ensureCalibrationUi(currentSource);
    const selectedFaceplate = String(current.ui.faceplate?.file || "");
    const faceplateFactory = preserveFaceplate
      ? faceplateFactoryCalibrationForFile(selectedFaceplate)
      : null;
    const next = applyCalibrationIdentity(faceplateFactory || modelFactory || calibration, current);

    // applyCalibrationIdentity intentionally preserves saved namespace metadata,
    // but a model factory reset must retain the CURRENT factory profile
    // identity rather than restoring an obsolete saved fallback profile.
    if (!preserveFaceplate && modelFactory && recommendation?.profile) {
      next.profile = String(recommendation.profile);
    }

    ensureCalibrationUi(next);

    if (preserveFaceplate) {
      next.ui.faceplate = clonePlainData(current.ui.faceplate);
    } else if (recommendation?.faceplate) {
      // Reset means model-recommended artwork, not a faceplate-specific custom
      // selection. The actual bundled filename is resolved from the exact model.
      next.ui.faceplate = normaliseFaceplateAsset({
        ...next.ui.faceplate,
        show: true,
        file: SV_ASSET_DEFAULT,
        source: "default"
      });
    }

    return ensureCalibrationUi(next);
  }

  async applyModelAwareResetAll() {
    if (this._modelAwareResetAllPromise) return this._modelAwareResetAllPromise;

    const task = (async () => {
      const next = this.factoryCalibrationForCurrentSwitch({
        preserveFaceplate: false,
        requireModelAware: true
      });
      if (!next) {
        // Never persist or display the generic 3650 48/4 fallback for an
        // unresolved model. Retry when a later hass update restores context.
        this._pendingModelAwareReset = true;
        return false;
      }

      this._pendingModelAwareReset = false;
      const baseProfile = this.baseCalibrationProfileName();

      // Make the correct factory layout immediately authoritative for normal
      // (closed-editor) rendering, then persist it just like Reset Current
      // Switch does. This is the part v1.9.31 was missing.
      this.clearLoadedCalibrationState(next);
      this._profileCalibration = cloneCalibrationData(next);
      this._profileLoadedProfile = baseProfile;
      this._profileLoadRequested = baseProfile;
      this.render();

      try {
        await this.saveCalibrationProfile(next, baseProfile);
        if (!calibrationControlsEnabled(this.config)) this._calibrationWorking = null;
        this._lastModelAwareResetAllAppliedAt = Date.now();
        this.render();
        return true;
      } catch (err) {
        console.error("Switch Vision model-aware Reset All save failed", err);
        this.setCalibrationSaveStatus("Reset all could not save a model default — check browser console / HA logs", true);
        this.render();
        return false;
      }
    })();

    this._modelAwareResetAllPromise = task;
    try {
      return await task;
    } finally {
      if (this._modelAwareResetAllPromise === task) this._modelAwareResetAllPromise = null;
    }
  }

  async resetSavedCalibrations(scope) {
    if (!this._hass?.callService) throw new Error("Home Assistant service API is not available");
    const data = { scope };
    if (scope === "switch") data.base_profile = this.baseCalibrationProfileName();
    if (scope === "namespace") {
      const base = this.baseCalibrationProfileName();
      data.scope = base.startsWith("native_") ? "native" : "custom";
    }
    await this._hass.callService("switch_vision", "reset_calibrations", data);
  }

  clearLoadedCalibrationState(next = null) {
    this._profileCalibration = null;
    this._profileLoadedProfile = null;
    this._profileLoadRequested = null;
    this._profileAutoCreateRequested = null;
    this._profileLoadInfo = null;
    this._calibrationWorking = next ? cloneCalibrationData(next) : null;
    this._calibrationDirty = false;
  }


  detectedExactModel() {
    const member = normalizePanelMember(this.config).toLowerCase();
    const discoveredModel = this._hass ? firstEntityValue(this._hass, [
      `sensor.${member}_model`,
      `sensor.${member}_switch_model`,
      `sensor.${member}_device_model`,
      `sensor.${member}_system_model`,
    ]) : "";
    const candidates = [
      this.config?.exact_model,
      this.config?.switch_model,
      this.config?.detected_switch_model,
      this.config?.device_model,
      this.config?.model,
      this.config?.platform,
      discoveredModel,
    ];
    for (const value of candidates) {
      const raw = String(value || "").trim();
      if (raw && exactModelVisualRecommendation(raw)) return raw;
      const text = raw.toUpperCase();
      const cisco = text.match(/WS-C[A-Z0-9-]+/);
      if (cisco) return cisco[0];
      const juniper = text.match(/EX3300-48P/);
      if (juniper) return juniper[0];
      const zyxel = text.match(/\bXS1930-10\b/);
      if (zyxel) return "XS1930-10";
    }
    return "";
  }

  calibrationSectionOrder() {
    if (Array.isArray(this._calibrationSectionOrder)) return [...this._calibrationSectionOrder];
    let saved = null;
    try {
      const raw = window.localStorage?.getItem(SV_CALIBRATION_SECTION_ORDER_STORAGE_KEY);
      if (raw) saved = JSON.parse(raw);
    } catch (_err) {
      // Browser storage can be disabled. Section reordering still works for the
      // current card instance; only persistence is unavailable.
    }
    this._calibrationSectionOrder = normaliseCalibrationSectionOrder(saved);
    return [...this._calibrationSectionOrder];
  }

  persistCalibrationSectionOrder(order) {
    const normalised = normaliseCalibrationSectionOrder(order);
    this._calibrationSectionOrder = normalised;
    try {
      window.localStorage?.setItem(SV_CALIBRATION_SECTION_ORDER_STORAGE_KEY, JSON.stringify(normalised));
    } catch (_err) {
      // Keep the in-memory order if local browser storage is unavailable.
    }
    return [...normalised];
  }

  resetCalibrationSectionOrder() {
    this._calibrationSectionOrder = [...SV_CALIBRATION_SECTION_ORDER];
    this._calibrationSectionState = { ...SV_CALIBRATION_SECTION_DEFAULT_OPEN };
    try {
      window.localStorage?.removeItem(SV_CALIBRATION_SECTION_ORDER_STORAGE_KEY);
    } catch (_err) {
      // Reset still applies to this card instance when storage is unavailable.
    }
  }

  applyCalibrationSectionOrder() {
    if (!this.shadowRoot) return;
    const tools = this.shadowRoot.querySelector(".cv-cal-tools");
    const sticky = tools?.querySelector(".cv-cal-sticky-actions");
    if (!tools || !sticky) return;
    const sections = new Map(
      [...tools.querySelectorAll(":scope > details[data-cv-section]")]
        .map((section) => [section.getAttribute("data-cv-section"), section])
    );
    for (const key of this.calibrationSectionOrder()) {
      const section = sections.get(key);
      if (section) tools.insertBefore(section, sticky);
    }
  }

  persistCalibrationSectionOrderFromDom() {
    if (!this.shadowRoot) return;
    const order = [...this.shadowRoot.querySelectorAll(".cv-cal-tools > details[data-cv-section]")]
      .map((section) => section.getAttribute("data-cv-section"))
      .filter(Boolean);
    if (order.length) this.persistCalibrationSectionOrder(order);
  }

  clearCalibrationSectionDropMarkers() {
    if (!this.shadowRoot) return;
    for (const section of this.shadowRoot.querySelectorAll("details[data-cv-section]")) {
      section.classList.remove("cv-cal-drop-before", "cv-cal-drop-after", "cv-cal-dragging");
    }
  }

  moveCalibrationSectionByKeyboard(section, direction) {
    const tools = section?.parentElement;
    if (!tools) return false;
    const sections = [...tools.querySelectorAll(":scope > details[data-cv-section]")];
    const index = sections.indexOf(section);
    if (index < 0) return false;
    if (direction < 0 && index > 0) {
      tools.insertBefore(section, sections[index - 1]);
    } else if (direction > 0 && index < sections.length - 1) {
      tools.insertBefore(sections[index + 1], section);
    } else {
      return false;
    }
    this.persistCalibrationSectionOrderFromDom();
    return true;
  }

  attachCalibrationSectionReorderHandlers() {
    if (!this.shadowRoot) return;
    const tools = this.shadowRoot.querySelector(".cv-cal-tools");
    const sticky = tools?.querySelector(".cv-cal-sticky-actions");
    if (!tools || !sticky) return;

    const sections = [...tools.querySelectorAll(":scope > details[data-cv-section]")];
    for (const section of sections) {
      const summary = section.querySelector(":scope > summary");
      if (!summary) continue;

      const handle = document.createElement("button");
      handle.type = "button";
      handle.className = "cv-cal-drag-handle";
      handle.draggable = true;
      handle.textContent = "☰";
      handle.title = "Drag to reorder this section. Arrow Up / Down also moves it.";
      handle.setAttribute("aria-label", `Reorder ${summary.querySelector("span")?.textContent || "Calibration"} section`);
      summary.insertBefore(handle, summary.firstChild);

      handle.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
      });
      handle.addEventListener("pointerdown", (event) => event.stopPropagation());
      handle.addEventListener("keydown", (event) => {
        if (!['ArrowUp', 'ArrowDown'].includes(event.key)) return;
        event.preventDefault();
        event.stopPropagation();
        this.moveCalibrationSectionByKeyboard(section, event.key === 'ArrowUp' ? -1 : 1);
      });
      handle.addEventListener("dragstart", (event) => {
        this._calibrationDraggedSection = section;
        section.classList.add("cv-cal-dragging");
        if (event.dataTransfer) {
          event.dataTransfer.effectAllowed = "move";
          event.dataTransfer.setData("text/plain", section.getAttribute("data-cv-section") || "calibration-section");
        }
      });
      handle.addEventListener("dragend", () => {
        this._calibrationDraggedSection = null;
        this.clearCalibrationSectionDropMarkers();
      });

      section.addEventListener("dragover", (event) => {
        const dragged = this._calibrationDraggedSection;
        if (!dragged || dragged === section) return;
        event.preventDefault();
        if (event.dataTransfer) event.dataTransfer.dropEffect = "move";
        const rect = section.getBoundingClientRect();
        const before = event.clientY < rect.top + (rect.height / 2);
        section.classList.toggle("cv-cal-drop-before", before);
        section.classList.toggle("cv-cal-drop-after", !before);
      });
      section.addEventListener("dragleave", (event) => {
        if (event.relatedTarget && section.contains(event.relatedTarget)) return;
        section.classList.remove("cv-cal-drop-before", "cv-cal-drop-after");
      });
      section.addEventListener("drop", (event) => {
        const dragged = this._calibrationDraggedSection;
        if (!dragged || dragged === section) return;
        event.preventDefault();
        const placeBefore = section.classList.contains("cv-cal-drop-before");
        if (placeBefore) {
          tools.insertBefore(dragged, section);
        } else {
          const next = section.nextElementSibling;
          tools.insertBefore(dragged, next && next.matches("details[data-cv-section]") ? next : sticky);
        }
        this.persistCalibrationSectionOrderFromDom();
        this._calibrationDraggedSection = null;
        this.clearCalibrationSectionDropMarkers();
      });
    }
  }

  calibrationSectionOpen(key, defaultOpen = false) {
    this._calibrationSectionState = this._calibrationSectionState || {};
    return Object.prototype.hasOwnProperty.call(this._calibrationSectionState, key) ? this._calibrationSectionState[key] : defaultOpen;
  }

  calibrationSectionHtml(key, title, summary, body, defaultOpen = false) {
    const open = this.calibrationSectionOpen(key, defaultOpen) ? "open" : "";
    return `<details class="cv-cal-section" data-cv-section="${htmlEscape(key)}" ${open}><summary><span>${htmlEscape(title)}</span><small>${htmlEscape(summary || "")}</small></summary><div class="cv-cal-section-body">${body}</div></details>`;
  }

  calibrationControls(cal) {
    if (!calibrationControlsEnabled(this.config)) return "";

    const target = normalCalibrationTarget(this.config);
    const selectedTarget = targetLabelForConfig(this.config);
    const editable = getEditableCalibrationTarget(cal, this.config);
    const step = Math.max(1, Number(this.config.calibration_step || 1));
    const point = editable?.point || (editable?.group ? ["all", "all"] : ["?", "?"]);
    const item = editable?.item || {};
    const hitbox = Array.isArray(editable?.hitbox) ? editable.hitbox : null;
    const calibrationUi = uiFromCalibration(cal);
    const statusUi = calibrationUi.status_panel;
    const status2Ui = calibrationUi.status_panel_2;
    const logoUi = calibrationUi.logo;
    const faceplateAssetUi = calibrationUi.faceplate;
    const assetLibrary = this._assetLibrary || { logos: [], faceplates: [], logos_path: "/config/www/switch-vision/logos", faceplates_path: "/config/www/switch-vision/faceplates" };
    const logoSelection = logoUi.show === false ? SV_ASSET_NONE : (logoUi.source === "custom" ? String(logoUi.file || SV_ASSET_DEFAULT) : SV_ASSET_DEFAULT);
    const faceplateSelection = faceplateSelectionValue(faceplateAssetUi);
    const assetStatus = this._assetLibraryLoading ? "Scanning folders…" : (this._assetLibraryError ? "Asset listing unavailable — check the custom component" : `${assetLibrary.logos.length} logo(s) · ${assetLibrary.faceplates.length} faceplate(s)`);
    ensureStatusPanelFieldState(statusUi);
    // Status Box 1 controls always edit switch-summary rows. Port selection no
    // longer swaps this control to port fields; those have their own section.
    const statusRowType = "switch";
    const statusRowDefs = STATUS_PANEL_ROW_DEFS.switch;
    const statusRowOrder = normaliseStatusPanelFieldOrder(statusUi.field_order?.switch, "switch");
    const statusHiddenFields = new Set(fieldListFromValue(statusUi.hidden_fields?.switch) || []);
    const selectedStatusRowField = statusRowDefs.defaults.includes(String(this.config.status_panel_selected_field || "").toLowerCase())
      ? String(this.config.status_panel_selected_field).toLowerCase()
      : statusRowOrder[0];
    const selectedStatusRowHidden = statusHiddenFields.has(selectedStatusRowField);
    const statusRowOptions = statusRowOrder.map((field) => {
      const label = statusRowDefs.labels[field] || field.toUpperCase();
      const suffix = statusHiddenFields.has(field) ? " (hidden)" : "";
      return `<option value="${htmlEscape(field)}" ${field === selectedStatusRowField ? "selected" : ""}>${htmlEscape(label + suffix)}</option>`;
    }).join("");
    ensureStatusPanelFieldState(status2Ui);
    const status2RowOrder = normaliseStatusPanelFieldOrder(status2Ui.field_order?.switch, "switch");
    const status2HiddenFields = new Set(fieldListFromValue(status2Ui.hidden_fields?.switch) || []);
    const selectedStatus2RowField = STATUS_PANEL_ROW_DEFS.switch.defaults.includes(String(this.config.status_panel_2_selected_field || "").toLowerCase()) ? String(this.config.status_panel_2_selected_field).toLowerCase() : status2RowOrder[0];
    const selectedStatus2RowHidden = status2HiddenFields.has(selectedStatus2RowField);
    const status2RowOptions = status2RowOrder.map((field) => { const label = STATUS_PANEL_ROW_DEFS.switch.labels[field] || field.toUpperCase(); const suffix = status2HiddenFields.has(field) ? " (hidden)" : ""; return `<option value="${htmlEscape(field)}" ${field === selectedStatus2RowField ? "selected" : ""}>${htmlEscape(label + suffix)}</option>`; }).join("");
    const portRowDefs = STATUS_PANEL_ROW_DEFS.port;
    const juniperModeOption = isJuniperPortPresentation(this.hass, this.config);
    const portRowOrder = normaliseStatusPanelFieldOrder(statusUi.field_order?.port, "port").filter((field) => juniperModeOption || field !== "mode");
    const portHiddenFields = new Set(fieldListFromValue(statusUi.hidden_fields?.port) || []);
    const selectedPortRowField = portRowDefs.defaults.includes(String(this.config.port_status_selected_field || "").toLowerCase())
      ? String(this.config.port_status_selected_field).toLowerCase()
      : portRowOrder[0];
    const selectedPortRowHidden = portHiddenFields.has(selectedPortRowField);
    const portRowOptions = portRowOrder.map((field) => {
      const label = portRowDefs.labels[field] || field.toUpperCase();
      const suffix = portHiddenFields.has(field) ? " (hidden)" : "";
      return `<option value="${htmlEscape(field)}" ${field === selectedPortRowField ? "selected" : ""}>${htmlEscape(label + suffix)}</option>`;
    }).join("");
    const status1TitleOptions = statusRowOrder.map((field) => `<option value="${htmlEscape(field)}" ${field === String(statusUi.title_field || "model") ? "selected" : ""}>${htmlEscape(statusRowDefs.labels[field] || field.toUpperCase())}</option>`).join("");
    const status2TitleOptions = status2RowOrder.map((field) => `<option value="${htmlEscape(field)}" ${field === String(status2Ui.title_field || "faceplate") ? "selected" : ""}>${htmlEscape(STATUS_PANEL_ROW_DEFS.switch.labels[field] || field.toUpperCase())}</option>`).join("");
    const statusFontWeight = String(statusUi.font_weight || "normal");
    const statusBold = statusFontWeight === "bold" || Number(statusFontWeight) >= 600;
    const status2FontWeight = String(status2Ui.font_weight || "normal");
    const status2Bold = status2FontWeight === "bold" || Number(status2FontWeight) >= 600;
    const statusLedFontWeight = String(calibrationUi.status_leds?.font_weight || "900");
    const statusLedBold = statusLedFontWeight === "bold" || Number(statusLedFontWeight) >= 600;
    const portLabelFontWeight = String(calibrationUi.port_label_font_weight || "bold").toLowerCase();
    const portLabelBold = portLabelFontWeight !== "normal";
    const hiddenStatusLeds = new Set((cal.ui?.status_leds?.hidden || []).map((name) => String(name).toUpperCase()));
    const statusLedVisibilityHtml = Object.keys(cal.status_leds || {})
      .filter((name) => String(name).toUpperCase() !== "MODE")
      .map((name) => {
        const hidden = hiddenStatusLeds.has(String(name).toUpperCase());
        return `<label class="cv-cal-led-visibility"><input type="checkbox" data-cv-status-led="${htmlEscape(name)}" ${hidden ? "" : "checked"}> ${htmlEscape(name)}</label>`;
      }).join("");
    const customFontValue = "SwitchVisionCustom, Arial, Helvetica, sans-serif";
    ensureCalibrationStack(cal);
    const stackConfig = stackMemberSettings(this.config, cal);
    const stackUi = stackConfig.stack;
    const stackMember = stackConfig.member;
    const currentMemberKey = normalizePanelMember(this.config).toUpperCase();
    const stackMemberNumber = Number(stackMember.member_number || currentMemberKey.replace(/[^0-9]/g, "")) || "";
    const stackUptimeMode = normaliseStackUptimeMode(stackMember.uptime_mode);
    const stackUptimeSource = String(stackMember.uptime_source || stackUi.uptime_source || "");
    const switchIpValue = String(explicitConfigValue(this.config, ["switch_ip", "management_ip", "mgmt_ip", "host_ip", "snmp_host"]) || managementIpFromCalibration(cal) || "").trim();
    const exportProfile = this.calibrationProfileName(cal);
    const exportBaseProfile = this.baseCalibrationProfileName();
    const exportFaceplate = this.activeFaceplateFilename(cal);
    const exportPayload = faceplateTransferExportData(cal, {
      scope: exportBaseProfile.startsWith("native_") ? "native" : "custom",
      baseProfile: exportBaseProfile,
      profile: exportProfile,
      faceplate: exportFaceplate,
    });
    const exportJson = JSON.stringify(exportPayload, null, 2);
    const showExportJson = this._showCalibrationJson === true;
    const profileName = this.calibrationProfileName();
    const profileDetails = this.calibrationProfileSourceDetails(profileName);
    const profileLoadedText = this._profileLoading ? "loading" : (profileDetails.loaded ? "loaded" : "not saved / fallback");
    const saveStatus = this._calibrationSaveStatus;
    const saveStatusHtml = saveStatus?.message ? `<span class="cv-cal-save-status ${saveStatus.isError ? "is-error" : "is-ok"}">${htmlEscape(saveStatus.message)}</span>` : "";
    const partNames = { center: "port box", entire: "entire port", led_left: "link/speed LED", led_right: "activity LED", number: "number label", label: "label" };
    const partText = partNames[editable?.part] || editable?.part;
    let targetText = "no target";
    if (editable?.group && editable.type === "ports") {
      const groupLabel = editable.custom ? "selected" : (editable.parity === "odd" ? "odd" : (editable.parity === "even" ? "even" : "all"));
      const selectedCount = calibrationPortKeysForEditable(cal, editable).length;
      targetText = `${selectedCount} ${groupLabel} RJ45 ports · ${partText}`;
    }
    else if (editable?.group && editable.type === "sfps") {
      const sfpCount = calibrationSfpKeysForEditable(cal, editable).length;
      targetText = `${editable.custom ? sfpCount + " selected" : "all"} SFP / uplinks · ${partText}`;
    }
    else if (editable?.group && editable.type === "number_labels") targetText = "all RJ45 numbers and SFP labels";
    else if (editable?.group && editable.type === "status_leds") targetText = "all status LEDs · center";
    else if (editable?.group && editable.type === "status_fields") targetText = `all Status Box 1 ${statusFieldGroupDescription(editable.part).toLowerCase()}`;
    else if (editable?.group && editable.type === "status_fields_2") targetText = `all Status Box 2 ${statusFieldGroupDescription(editable.part).toLowerCase()}`;
    else if (editable?.type === "status_field") targetText = `Status Box 1 · ${statusFieldLabel(editable.id)} field`;
    else if (editable?.type === "status_field_2") targetText = `Status Box 2 · ${statusFieldLabel(editable.id)} field`;
    else if (editable) targetText = `${editable.type}:${editable.id} · ${partText}`;
    const portNumberKeys = editable?.type === "number_labels"
      ? sortedCalibrationPortKeys(cal)
      : (editable?.part === "number" && editable?.type === "port"
        ? [editable.key]
        : (editable?.part === "number" && editable?.type === "ports" ? calibrationPortKeysForEditable(cal, editable) : []));
    const sfpLabelKeys = editable?.type === "number_labels"
      ? Object.keys(cal.sfp || {})
      : (editable?.part === "label" && editable?.type === "sfp"
        ? [editable.key]
        : (editable?.part === "label" && editable?.type === "sfps" ? calibrationSfpKeysForEditable(cal, editable) : []));
    const visiblePortNumberCount = portNumberKeys.filter((key) => cal.ports?.[key]?.number_show !== false).length;
    const visibleSfpLabelCount = sfpLabelKeys.filter((key) => cal.sfp?.[key]?.label_show !== false).length;
    const numberLabelCount = portNumberKeys.length + sfpLabelKeys.length;
    const visibleNumberLabelCount = visiblePortNumberCount + visibleSfpLabelCount;
    const numberLabelVisibilityControls = numberLabelCount ? `<div class="cv-cal-tools-row cv-cal-port-number-visibility">
      <span class="cv-cal-quick-label">${sfpLabelKeys.length && portNumberKeys.length ? "Number labels" : (sfpLabelKeys.length ? "SFP label" : "Port number")}</span>
      <button type="button" data-cv-action="show-number-label">Show</button>
      <button type="button" data-cv-action="hide-number-label">Hide</button>
      <span class="cv-cal-current">${visibleNumberLabelCount === numberLabelCount ? "Visible" : (visibleNumberLabelCount === 0 ? "Hidden" : `${visibleNumberLabelCount} of ${numberLabelCount} visible`)}</span>
    </div>` : "";
    const isPortLedSize = ["led_left", "led_right"].includes(editable?.part);
    const visualSize = hitbox ? (isPortLedSize ? hitbox : visualHitboxSize(editable?.type, hitbox)) : null;
    const sizeText = visualSize ? `W ${visualSize[0]} · H ${visualSize[1]}` : "n/a";
    const dirty = this._calibrationDirty === true;
    const calibrationTestMode = calibrationTestModeEnabled(this.config);
    const dirtyLabel = dirty ? "Unsaved changes" : (profileDetails.loaded ? "Saved profile" : "Factory / fallback");
    const switchLabel = String(this.config?.title || this.config?.member || "Switch Vision switch");
    const modelLabel = String(this.detectedExactModel() || this.config?.switch_model || this.config?.model || cal?.model || "Unknown model");
    const faceplateLabel = this.calibrationProfileDisplay(cal).faceplate;
    const activeRj45Keys = editable?.type === "port"
      ? [editable.key]
      : (editable?.type === "ports" ? calibrationPortKeysForEditable(cal, editable) : []);
    const activeSfpKeys = editable?.type === "sfp"
      ? [editable.key]
      : (editable?.type === "sfps" ? calibrationSfpKeysForEditable(cal, editable) : []);
    const customPortListValue = editable?.type === "sfps" && editable?.custom
      ? activeSfpKeys.join(",")
      : (editable?.type === "ports" && editable?.custom
        ? activeRj45Keys.join(",")
        : String(this.config.calibration_sfp_selection || this.config.calibration_port_selection || ""));
    const customPortSummary = editable?.type === "sfps" && editable?.custom
      ? `${activeSfpKeys.length} SFP/uplink${activeSfpKeys.length === 1 ? "" : "s"} selected`
      : (editable?.type === "ports" && editable?.custom
        ? `${activeRj45Keys.length} RJ45 port${activeRj45Keys.length === 1 ? "" : "s"} selected`
        : String(this.config.calibration_port_selection_summary || "Enter RJ45 numbers/ranges or SFP/uplink names"));
    const displayNameEditable = ["port", "sfp"].includes(editable?.type);
    const displayNameValue = displayNameEditable ? String(editable?.item?.display_name || "") : "";
    const coordinatePoints = calibrationCoordinatePoints(cal, editable, false);
    const commonX = commonCalibrationCoordinate(coordinatePoints, 0);
    const commonY = commonCalibrationCoordinate(coordinatePoints, 1);
    const coordinateDisabled = coordinatePoints.length === 0;
    const sizePairs = calibrationSizePairs(cal, editable, false);
    const commonWidth = commonCalibrationCoordinate(sizePairs, 0);
    const commonHeight = commonCalibrationCoordinate(sizePairs, 1);
    const sizeDisabled = sizePairs.length === 0;
    const geometryDisabled = coordinateDisabled && sizeDisabled;
    const geometryCount = Math.max(coordinatePoints.length, sizePairs.length);
    const coordinateCountText = geometryCount > 1 ? `${geometryCount} targets` : (geometryCount === 1 ? "1 target" : "Unavailable");

    return `<div class="cv-cal-tools">
      <div class="cv-cal-workspace-head">
        <div class="cv-cal-context-grid">
          <span><small>Switch</small><b>${htmlEscape(switchLabel)}</b></span>
          <span><small>Model</small><b>${htmlEscape(modelLabel)}</b></span>
          <span><small>Faceplate</small><b>${htmlEscape(faceplateLabel)}</b></span>
          <span><small>Profile</small><b>${htmlEscape(profileName)}</b></span>
        </div>
        <div class="cv-cal-section-layout-help"><span>Drag <b>☰</b> to arrange sections for your current job.</span><button type="button" data-cv-action="reset-section-layout">Reset section layout</button></div>
      </div>
      <details class="cv-cal-section" data-cv-section="selection" ${this.calibrationSectionOpen("selection", false) ? "open" : ""}><summary><span>Selection</span><small>${htmlEscape(targetText)} · ${sortedCalibrationPortKeys(cal).length} ports</small></summary><div class="cv-cal-section-body">
<div class="cv-cal-tools-row cv-cal-port-manager-row cv-cal-port-manager-main">
        <label>Target
          <select class="cv-cal-select" data-cv-field="target">${targetOptionsHtml(cal, selectedTarget)}</select>
        </label>
        <label>Part
          <select class="cv-cal-select" data-cv-field="part">${calibrationPartOptionsHtml(target, this.config.calibration_part)}</select>
        </label>
        <span class="cv-cal-row-divider" aria-hidden="true"></span>
        <span class="cv-cal-quick-label">Port manager</span>
        <button type="button" data-cv-action="add-port">Add port</button>
        <button type="button" data-cv-action="duplicate-port">Duplicate port</button>
        <button type="button" data-cv-action="remove-port" ${["port", "sfp"].includes(editable?.type) ? "" : "disabled"}>Remove port</button>
        <span class="cv-cal-current">${sortedCalibrationPortKeys(cal).length} visual ports</span>
      </div>
      <div class="cv-cal-tools-row cv-cal-port-meta-row">
        <label>RJ45 key<input class="cv-cal-input" data-cv-field="port-number" type="number" min="1" step="1" value="${editable?.type === "port" ? htmlEscape(editable.key) : ""}" ${editable?.type === "port" ? "" : "disabled"}></label>
        <button type="button" data-cv-action="rename-port" ${editable?.type === "port" ? "" : "disabled"}>Renumber</button>
        <span class="cv-cal-row-divider" aria-hidden="true"></span>
        <label>Display name<input class="cv-cal-input" data-cv-field="port-display-name" maxlength="96" value="${htmlEscape(displayNameValue)}" placeholder="Optional label" ${displayNameEditable ? "" : "disabled"}></label>
        <button type="button" data-cv-action="set-port-display-name" ${displayNameEditable ? "" : "disabled"}>Set name</button>
        <button type="button" data-cv-action="reset-port-display-name" ${displayNameEditable && displayNameValue ? "" : "disabled"}>Reset name</button>
      </div>${numberLabelVisibilityControls}
      <div class="cv-cal-subsection-divider"><span>Quick selection</span></div>
<div class="cv-cal-tools-row cv-cal-status-quick-row">
        <span class="cv-cal-quick-label">Status Box 1</span>
        <button type="button" data-cv-action="select-target" data-target="status_box" data-part="box">Box</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields" data-part="field">All fields</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields" data-part="label">All labels</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields" data-part="value">All values</button>
        <span class="cv-cal-parity-divider" aria-hidden="true"></span>
        <span class="cv-cal-quick-label">Status Box 2</span>
        <button type="button" data-cv-action="select-target" data-target="status_box_2" data-part="box">Box</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields_2" data-part="field">All fields</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields_2" data-part="label">All labels</button>
        <button type="button" data-cv-action="select-target" data-target="status_fields_2" data-part="value">All values</button>
      </div>
      <div class="cv-cal-tools-row cv-cal-quick-row">
        <span class="cv-cal-quick-label">Quick select</span>
        <button type="button" data-cv-action="select-target" data-target="status_leds" data-part="center">Status LEDs</button>
        <button type="button" data-cv-action="select-target" data-target="ports" data-part="entire">All RJ45</button>
        <button type="button" data-cv-action="select-target" data-target="ports_led_left" data-part="led_left">RJ45 Link</button>
        <button type="button" data-cv-action="select-target" data-target="ports_led_right" data-part="led_right">RJ45 Activity</button>
        <button type="button" data-cv-action="select-target" data-target="sfps" data-part="center">All SFP</button>
        <button type="button" data-cv-action="select-target" data-target="sfps_led_left" data-part="led_left">SFP Link</button>
        <button type="button" data-cv-action="select-target" data-target="sfps_led_right" data-part="led_right">SFP Activity</button>
        <button type="button" data-cv-action="select-target" data-target="all_numbers" data-part="number">Port Numbers</button>
        <span class="cv-cal-parity-divider" aria-hidden="true"></span>
        <button type="button" data-cv-action="select-target" data-target="logo" data-part="box">Logo</button>
        <button type="button" data-cv-action="select-target" data-target="calibration_button" data-part="box">Calibration button</button>
      </div>
      <div class="cv-cal-tools-row cv-cal-parity-row">
        <span class="cv-cal-quick-label">Odd</span>
        <button type="button" data-cv-action="select-target" data-target="ports_odd_entire" data-part="entire">Odd ports</button>
        <button type="button" data-cv-action="select-target" data-target="ports_odd_led_left" data-part="led_left">Odd link LEDs</button>
        <button type="button" data-cv-action="select-target" data-target="ports_odd_led_right" data-part="led_right">Odd activity LEDs</button>
        <button type="button" data-cv-action="select-target" data-target="ports_odd_numbers" data-part="number">Odd numbers</button>
        <span class="cv-cal-parity-divider" aria-hidden="true"></span>
        <span class="cv-cal-quick-label">Even</span>
        <button type="button" data-cv-action="select-target" data-target="ports_even_entire" data-part="entire">Even ports</button>
        <button type="button" data-cv-action="select-target" data-target="ports_even_led_left" data-part="led_left">Even link LEDs</button>
        <button type="button" data-cv-action="select-target" data-target="ports_even_led_right" data-part="led_right">Even activity LEDs</button>
        <button type="button" data-cv-action="select-target" data-target="ports_even_numbers" data-part="number">Even numbers</button>
      </div>
      <div class="cv-cal-tools-row cv-cal-custom-port-row">
        <span class="cv-cal-quick-label">Custom ports</span>
        <input class="cv-cal-input cv-cal-port-list-input" data-cv-field="custom-port-list" value="${htmlEscape(customPortListValue)}" placeholder="1,3,5,6,11,21 or 1-6,11 or g1,te2">
        <label>Part
          <select class="cv-cal-select" data-cv-field="custom-port-part">
            <option value="center" ${String(editable?.part || this.config.calibration_part || "") === "center" ? "selected" : ""}>Port box</option>
            <option value="entire" ${String(editable?.part || this.config.calibration_part || "entire") === "entire" ? "selected" : ""}>Entire Port</option>
            <option value="led_left" ${String(editable?.part || this.config.calibration_part || "") === "led_left" ? "selected" : ""}>Link LEDs</option>
            <option value="led_right" ${String(editable?.part || this.config.calibration_part || "") === "led_right" ? "selected" : ""}>Activity LEDs</option>
            <option value="number" ${["number", "label"].includes(String(editable?.part || this.config.calibration_part || "")) ? "selected" : ""}>Port / uplink labels</option>
          </select>
        </label>
        <button type="button" data-cv-action="select-custom-ports">Select</button>
        <span class="cv-cal-current">${htmlEscape(customPortSummary)}</span>
      </div>

      </div></details>
      <details class="cv-cal-section" data-cv-section="position-size" ${this.calibrationSectionOpen("position-size", true) ? "open" : ""}><summary><span>Position & Size</span><small>${htmlEscape(targetText)} · step ${step}px</small></summary><div class="cv-cal-section-body"><div class="cv-cal-tools-row cv-cal-nudge-grid cv-cal-geometry-row">
        <span class="cv-cal-quick-label">Nudge</span>

        <button type="button" data-cv-action="move" data-dx="0" data-dy="-${step}">↑</button>
        <button type="button" data-cv-action="move" data-dx="-${step}" data-dy="0">←</button>
        <button type="button" data-cv-action="move" data-dx="${step}" data-dy="0">→</button>
        <button type="button" data-cv-action="move" data-dx="0" data-dy="${step}">↓</button>
        <button type="button" data-cv-action="size" data-dw="-${step}" data-dh="0" ${hitbox ? "" : "disabled"}>W−</button>
        <button type="button" data-cv-action="size" data-dw="${step}" data-dh="0" ${hitbox ? "" : "disabled"}>W+</button>
        <button type="button" data-cv-action="size" data-dw="0" data-dh="-${step}" ${hitbox ? "" : "disabled"}>H−</button>
        <button type="button" data-cv-action="size" data-dw="0" data-dh="${step}" ${hitbox ? "" : "disabled"}>H+</button>
        <label>Step
          <select class="cv-cal-select" data-cv-field="step">
            <option value="1" ${step === 1 ? "selected" : ""}>1px</option>
            <option value="5" ${step === 5 ? "selected" : ""}>5px</option>
            <option value="10" ${step === 10 ? "selected" : ""}>10px</option>
            <option value="20" ${step === 20 ? "selected" : ""}>20px</option>
            <option value="50" ${step === 50 ? "selected" : ""}>50px</option>
          </select>
        </label>
        <span class="cv-cal-row-divider" aria-hidden="true"></span>
        <span class="cv-cal-quick-label cv-cal-direct-label">Direct</span>
        <label>X
          <input class="cv-cal-input cv-cal-coordinate-input" data-cv-field="coordinate-x" type="number" step="0.1" value="${commonX === null ? "" : htmlEscape(commonX)}" placeholder="${coordinatePoints.length > 1 ? "Mixed" : "X"}" ${coordinateDisabled ? "disabled" : ""}>
        </label>
        <label>Y
          <input class="cv-cal-input cv-cal-coordinate-input" data-cv-field="coordinate-y" type="number" step="0.1" value="${commonY === null ? "" : htmlEscape(commonY)}" placeholder="${coordinatePoints.length > 1 ? "Mixed" : "Y"}" ${coordinateDisabled ? "disabled" : ""}>
        </label>
        <label>W
          <input class="cv-cal-input cv-cal-coordinate-input" data-cv-field="size-width" type="number" min="0.1" step="0.1" value="${commonWidth === null ? "" : htmlEscape(commonWidth)}" placeholder="${sizePairs.length > 1 ? "Mixed" : "W"}" ${sizeDisabled ? "disabled" : ""}>
        </label>
        <label>H
          <input class="cv-cal-input cv-cal-coordinate-input" data-cv-field="size-height" type="number" min="0.1" step="0.1" value="${commonHeight === null ? "" : htmlEscape(commonHeight)}" placeholder="${sizePairs.length > 1 ? "Mixed" : "H"}" ${sizeDisabled ? "disabled" : ""}>
        </label>
        <button type="button" data-cv-action="apply-coordinates" ${geometryDisabled ? "disabled" : ""}>Apply</button>
        <span class="cv-cal-current">${htmlEscape(coordinateCountText)} · blank values unchanged</span>
            </div></div></details>
      <details class="cv-cal-section" data-cv-section="assets" ${this.calibrationSectionOpen("assets", true) ? "open" : ""}><summary><span>Assets</span><small>${htmlEscape(assetStatus)}</small></summary><div class="cv-cal-section-body">
<div class="cv-cal-tools-row cv-cal-style-row cv-cal-assets-row">
        <span class="cv-cal-quick-label">Assets</span>
        <label>Logo
          <select class="cv-cal-select" data-cv-field="logo-file">${assetOptionsHtml("logos", assetLibrary.logos, logoSelection, "Use card default")}</select>
        </label>
        <label>Faceplate
          <select class="cv-cal-select" data-cv-field="faceplate-file">${assetOptionsHtml("faceplates", assetLibrary.faceplates, faceplateSelection, `${htmlEscape(this.config.switch_model || this.config.model || layout.model_text?.text || "Switch model")} (Default / recommended)`)}</select>
        </label>
        <button type="button" data-cv-action="refresh-assets" title="Scan the logo and faceplate folders again">Refresh files</button>
        <span class="cv-cal-current" title="Logos: ${htmlEscape(assetLibrary.logos_path)} · Faceplates: ${htmlEscape(assetLibrary.faceplates_path)}">${htmlEscape(assetStatus)}</span>
      </div>
      <div class="cv-cal-tools-row cv-cal-style-row cv-cal-faceplate-tip">
        <span class="cv-cal-current"><b>Tip:</b> Faceplates replace the switch artwork. For best results, use wide images (2048 px or wider).</span>
        <span class="cv-cal-current" data-cv-faceplate-info>Detected image dimensions appear after selection.</span>
      </div>
      </div></details>
      <details class="cv-cal-section" data-cv-section="labels-leds" ${this.calibrationSectionOpen("labels-leds", false) ? "open" : ""}><summary><span>Labels & LEDs</span><small>Visibility, text and port LED appearance</small></summary><div class="cv-cal-section-body">
<div class="cv-cal-tools-row cv-cal-style-row cv-cal-status-led-visibility">
        <span class="cv-cal-quick-label">Status LEDs</span>
        ${statusLedVisibilityHtml || '<span class="cv-cal-current">No status LEDs in this profile</span>'}
        <label>Font
          <select class="cv-cal-select" data-cv-field="status-led-font">
            <option value="Arial Narrow, Roboto Condensed, Helvetica Neue Condensed, Arial, Helvetica, sans-serif" ${String(calibrationUi.status_leds?.font_family || "").startsWith("Arial Narrow") ? "selected" : ""}>Condensed</option>
            <option value="Arial, Helvetica, sans-serif" ${String(calibrationUi.status_leds?.font_family || "Arial, Helvetica, sans-serif").startsWith("Arial,") ? "selected" : ""}>Arial</option>
            <option value="${customFontValue}" ${String(calibrationUi.status_leds?.font_family || "").startsWith("SwitchVisionCustom") ? "selected" : ""}>Custom / IntelOne</option>
            <option value="Courier New, monospace" ${String(calibrationUi.status_leds?.font_family || "").startsWith("Courier") ? "selected" : ""}>Courier</option>
            <option value="monospace" ${String(calibrationUi.status_leds?.font_family || "") === "monospace" ? "selected" : ""}>Monospace</option>
          </select>
        </label>
        <button type="button" class="cv-cal-bold-toggle ${statusLedBold ? "is-active" : ""}" data-cv-action="toggle-status-led-bold" title="Toggle status LED text bold"><b>B</b></button>
        <label>Font size
          <input class="cv-cal-input" data-cv-field="status-led-font-size" type="number" min="8" max="50" step="0.5" value="${htmlEscape(calibrationUi.status_leds?.font_size ?? 16.5)}">
        </label>
        <span class="cv-cal-colour-field"><span>Text colour</span>
          ${customColourControlsHtml("status-led-text-color", calibrationUi.status_leds?.text_color, "#eef7ff")}
        </span>
        <span class="cv-cal-current">Untick an LED to hide it from this faceplate profile.</span>
      </div>
<div class="cv-cal-tools-row cv-cal-style-row cv-cal-port-label-style">
        <span class="cv-cal-quick-label">Port labels</span>
        <button type="button" class="cv-cal-bold-toggle ${portLabelBold ? "is-active" : ""}" data-cv-action="toggle-port-label-bold" title="Toggle every RJ45 and SFP/uplink label bold"><b>B</b></button>
        <label>RJ45 size
          <input class="cv-cal-input" data-cv-field="port-number-font-size" type="number" min="8" max="50" step="0.5" value="${htmlEscape(calibrationUi.port_number_font_size ?? 13)}">
        </label>
        <label>SFP / uplink size
          <input class="cv-cal-input" data-cv-field="sfp-label-font-size" type="number" min="8" max="50" step="0.5" value="${htmlEscape(calibrationUi.sfp_label_font_size ?? 13.5)}">
        </label>
        <span class="cv-cal-current">One bold control applies to every RJ45 number/display label and SFP/uplink label.</span>
      </div>
<div class="cv-cal-tools-row cv-cal-style-row cv-cal-port-led-shape">
        <span class="cv-cal-quick-label">Port LEDs</span>
        <label>Shape
          <select class="cv-cal-select" data-cv-field="port-led-shape">
            <option value="circle" ${String(calibrationUi.port_led_shape || "circle") === "circle" ? "selected" : ""}>Circle</option>
            <option value="rectangle" ${String(calibrationUi.port_led_shape || "circle") === "rectangle" ? "selected" : ""}>Rectangle</option>
          </select>
        </label>
        <span class="cv-cal-colour-field"><span>Link LED colour</span>
          ${customColourControlsHtml("link-led-color", calibrationUi.link_led_color, "#76ff33", "__factory__")}
        </span>
        <span class="cv-cal-colour-field"><span>Activity LED colour</span>
          ${customColourControlsHtml("activity-led-color", calibrationUi.activity_led_color, "#ffb321", "__factory__")}
        </span>
        <span class="cv-cal-colour-field"><span>Port number colour</span>
          ${customColourControlsHtml("port-number-color", calibrationUi.port_number_color, "#eef7ff")}
        </span>
        <span class="cv-cal-colour-field"><span>Uplink label colour</span>
          ${customColourControlsHtml("port-label-color", calibrationUi.port_label_color, "#eef7ff")}
        </span>
        <span class="cv-cal-current">Link and activity colours apply to every RJ45 and SFP/uplink port. Reset restores the factory LED colours, including speed-based link colours.</span>
      </div>
      </div></details>
      <details class="cv-cal-section" data-cv-section="status-boxes" ${this.calibrationSectionOpen("status-boxes", false) ? "open" : ""}><summary><span>Status Boxes</span><small>Switch and interface information styling</small></summary><div class="cv-cal-section-body">
<div class="cv-cal-tools-row cv-cal-style-row">
        <span class="cv-cal-quick-label">Status Box 1 style</span>
        <label>Visible <input type="checkbox" data-cv-field="status-show" ${statusUi.show !== false ? "checked" : ""}></label>
        <label>Font
          <select class="cv-cal-select" data-cv-field="status-font">
            <option value="Arial Narrow, Roboto Condensed, Helvetica Neue Condensed, Arial, Helvetica, sans-serif" ${String(statusUi.font_family).startsWith("Arial Narrow") ? "selected" : ""}>Condensed</option>
            <option value="Arial, Helvetica, sans-serif" ${String(statusUi.font_family).startsWith("Arial,") ? "selected" : ""}>Arial</option>
            <option value="${customFontValue}" ${String(statusUi.font_family).startsWith("SwitchVisionCustom") ? "selected" : ""}>Custom / IntelOne</option>
            <option value="Courier New, monospace" ${String(statusUi.font_family).startsWith("Courier") ? "selected" : ""}>Courier</option>
            <option value="monospace" ${String(statusUi.font_family) === "monospace" ? "selected" : ""}>Monospace</option>
          </select>
        </label>
        <button type="button" class="cv-cal-bold-toggle ${statusBold ? "is-active" : ""}" data-cv-action="toggle-status-bold" title="Toggle status font bold"><b>B</b></button>
        <label>Font size
          <input class="cv-cal-input" data-cv-field="status-font-size" type="number" min="8" max="50" step="0.5" value="${htmlEscape(statusUi.font_size)}">
        </label>
        <span class="cv-cal-colour-field"><span>Text colour</span>

          ${customColourControlsHtml("status-text-color", statusUi.text_color, "#ffffff")}
        </span>
        <span class="cv-cal-colour-field"><span>Box colour</span>

          ${customColourControlsHtml("status-box-color", statusUi.background_color, "transparent")}
        </span>
        <label>Border <input type="checkbox" data-cv-field="status-border-show" ${statusUi.border_show !== false ? "checked" : ""}></label>
        <span class="cv-cal-colour-field"><span>Border colour</span>

          ${customColourControlsHtml("status-border-color", statusUi.border_color, "rgba(245,248,250,.75)")}
        </span>
      </div>
      <div class="cv-cal-tools-row cv-cal-status-row">
        <span class="cv-cal-quick-label">Status Box 1</span>
        <label>Field
          <select class="cv-cal-select" data-cv-field="status-row-field">${statusRowOptions}</select>
        </label>
        <button type="button" data-cv-action="status-row-up">Move up</button>
        <button type="button" data-cv-action="status-row-down">Move down</button>
        <button type="button" data-cv-action="status-row-toggle">${selectedStatusRowHidden ? "Show" : "Hide"}</button>
      </div>
      <div class="cv-cal-tools-row cv-cal-style-row">
        <span class="cv-cal-quick-label">Status Box 2 style</span>
        <label>Visible <input type="checkbox" data-cv-field="status2-show" ${status2Ui.show !== false ? "checked" : ""}></label>
        <label>Font
          <select class="cv-cal-select" data-cv-field="status2-font">
            <option value="Arial Narrow, Roboto Condensed, Helvetica Neue Condensed, Arial, Helvetica, sans-serif" ${String(status2Ui.font_family).startsWith("Arial Narrow") ? "selected" : ""}>Condensed</option>
            <option value="Arial, Helvetica, sans-serif" ${String(status2Ui.font_family).startsWith("Arial,") ? "selected" : ""}>Arial</option>
            <option value="${customFontValue}" ${String(status2Ui.font_family).startsWith("SwitchVisionCustom") ? "selected" : ""}>Custom / IntelOne</option>
            <option value="Courier New, monospace" ${String(status2Ui.font_family).startsWith("Courier") ? "selected" : ""}>Courier</option>
            <option value="monospace" ${String(status2Ui.font_family) === "monospace" ? "selected" : ""}>Monospace</option>
          </select>
        </label>
        <button type="button" class="cv-cal-bold-toggle ${status2Bold ? "is-active" : ""}" data-cv-action="toggle-status2-bold" title="Toggle Status Box 2 font bold"><b>B</b></button>
        <label>Font size<input class="cv-cal-input" data-cv-field="status2-font-size" type="number" min="8" max="50" step="0.5" value="${htmlEscape(status2Ui.font_size)}"></label>
        <span class="cv-cal-colour-field"><span>Text colour</span>${customColourControlsHtml("status2-text-color", status2Ui.text_color, "#ffffff")}</span>
        <span class="cv-cal-colour-field"><span>Box colour</span>${customColourControlsHtml("status2-box-color", status2Ui.background_color, "transparent")}</span>
        <label>Border <input type="checkbox" data-cv-field="status2-border-show" ${status2Ui.border_show !== false ? "checked" : ""}></label>
        <span class="cv-cal-colour-field"><span>Border colour</span>${customColourControlsHtml("status2-border-color", status2Ui.border_color, "rgba(245,248,250,.75)")}</span>
      </div>
      <div class="cv-cal-tools-row cv-cal-status-row">
        <span class="cv-cal-quick-label">Status Box 2</span>
        <label>Field<select class="cv-cal-select" data-cv-field="status2-row-field">${status2RowOptions}</select></label>
        <button type="button" data-cv-action="status2-row-up">Move up</button>
        <button type="button" data-cv-action="status2-row-down">Move down</button>
        <button type="button" data-cv-action="status2-row-toggle">${selectedStatus2RowHidden ? "Show" : "Hide"}</button>
      </div>
      <div class="cv-cal-tools-row cv-cal-status-row">
        <span class="cv-cal-quick-label">Interface Status Box</span>
        <label>Field<select class="cv-cal-select" data-cv-field="port-status-row-field">${portRowOptions}</select></label>
        <button type="button" data-cv-action="port-status-row-up">Move up</button>
        <button type="button" data-cv-action="port-status-row-down">Move down</button>
        <button type="button" data-cv-action="port-status-row-toggle">${selectedPortRowHidden ? "Show" : "Hide"}</button>
      </div>
      </div></details>
      <details class="cv-cal-section" data-cv-section="switch-stack" ${this.calibrationSectionOpen("switch-stack", false) ? "open" : ""}><summary><span>Switch & Stack</span><small>Management IP and stack member settings</small></summary><div class="cv-cal-section-body">
<div class="cv-cal-tools-row cv-cal-switch-stack-row">
        <span class="cv-cal-quick-label">Switch / Stack</span>
        <label>Switch IP
          <input class="cv-cal-input cv-cal-ip-input" data-cv-field="switch-ip" value="${htmlEscape(switchIpValue)}" placeholder="192.168.1.102">
        </label>
        <span class="cv-cal-current cv-cal-ip-help">Status row field: IP</span>
        <span class="cv-cal-row-divider" aria-hidden="true"></span>
        <label>Enabled
          <input type="checkbox" data-cv-field="stack-enabled" ${stackUi.enabled ? "checked" : ""}>
        </label>
        <label>Member
          <select class="cv-cal-select" data-cv-field="stack-member-number">
            <option value="" ${stackMemberNumber === "" ? "selected" : ""}>Auto</option>
            ${[1,2,3,4,5,6,7,8,9].map(v => `<option value="${v}" ${Number(stackMemberNumber) === v ? "selected" : ""}>${v}</option>`).join("")}
          </select>
        </label>
        <label>Uptime
          <select class="cv-cal-select" data-cv-field="stack-uptime-mode">
            <option value="standalone" ${stackUptimeMode === "standalone" ? "selected" : ""}>Own sensor</option>
            <option value="stack_source" ${stackUptimeMode === "stack_source" ? "selected" : ""}>Stack source</option>
            <option value="inherit_stack" ${stackUptimeMode === "inherit_stack" ? "selected" : ""}>Inherit stack</option>
          </select>
        </label>
        <label>Source
          <input class="cv-cal-input cv-cal-entity-input" data-cv-field="stack-uptime-source" value="${htmlEscape(stackUptimeSource)}" placeholder="sensor.sw1_uptime">
        </label>
      </div>
      </div></details>
      <details class="cv-cal-section cv-cal-advanced" data-cv-section="profile-actions" ${this.calibrationSectionOpen("profile-actions", false) ? "open" : ""}><summary><span>Profile & Advanced Actions</span><small>Scoped storage and reset tools</small></summary><div class="cv-cal-section-body">
        <div class="cv-cal-tools-row">
          <span class="cv-cal-current">${htmlEscape(profileName)} · ${htmlEscape(profileLoadedText)}</span>
          <button type="button" data-cv-action="download-profile">Export Faceplate Profile</button>
          <button type="button" data-cv-action="import-profile">Import Faceplate Profile</button>
          <input type="file" data-cv-profile-file accept="application/json,.json" hidden>
          <button type="button" data-cv-action="copy">Copy JSON</button>
          <button type="button" data-cv-action="toggle-json">${showExportJson ? "Hide JSON" : "Show JSON"}</button>
          <button type="button" data-cv-action="load-profile" title="Reload the last saved profile from Home Assistant storage">Reload Saved Profile</button>
          <button type="button" data-cv-action="reset-target">Reset Target</button>
        </div>
        ${showExportJson ? `<textarea class="cv-cal-export" readonly spellcheck="false">${htmlEscape(exportJson)}</textarea>` : ""}
      </div></details>
      <div class="cv-cal-sticky-actions" data-cv-profile-controls>
        <div class="cv-cal-sticky-state">
          <span class="cv-cal-state-dot ${dirty ? "is-dirty" : "is-saved"}"></span>
          <span>${htmlEscape(dirtyLabel)}</span>
          <span class="cv-cal-sticky-geometry" title="Live selected target geometry">${htmlEscape(targetText)} · X ${htmlEscape(point[0])} · Y ${htmlEscape(point[1])} · ${htmlEscape(sizeText)}</span>
          ${saveStatusHtml}
        </div>
        <div class="cv-cal-sticky-buttons">
          <button
            type="button"
            class="${calibrationTestMode ? "cv-cal-warning" : "cv-cal-secondary"}"
            data-cv-action="toggle-led-test-mode"
            title="Force all calibration LEDs solid ON"
          >${calibrationTestMode ? "Test Mode: ON" : "Test Mode"}</button>
          <button type="button" class="cv-cal-secondary" data-cv-action="cancel-calibration">Cancel</button>
          <button type="button" class="cv-cal-secondary" data-cv-action="refresh-faceplate">Refresh Faceplate</button>
          <button type="button" class="cv-cal-warning" data-cv-action="reset-current-faceplate">Reset Current Faceplate</button>
          <button type="button" class="cv-cal-danger" data-cv-action="reset-current-switch">Reset Current Switch</button>
          <button type="button" class="cv-cal-purple" data-cv-action="reset-all-switches">Reset All Switches</button>
          <button type="button" class="cv-cal-primary" data-cv-action="save-profile" ${dirty ? "" : "disabled"}>Save Profile</button>
        </div>
      </div>
    </div>`;
  }

  redrawSwitchSvg(activeCalibration = null) {
    if (!this.shadowRoot) return;
    const svg = this.shadowRoot.getElementById("s");
    if (!svg) return;

    const cal = activeCalibration || this.calibrationData();
    while (svg.firstChild) svg.removeChild(svg.firstChild);

    drawPanel(svg, {
      hass: this._hass,
      config: this.config,
      calibration: cal,
      layout
    });

    drawCalibrationOverlay(svg, {
      config: this.config,
      calibration: cal,
      layout
    });

    this.attachSelectionHandlers(svg, cal);
  }

  render() {
    if (!this.shadowRoot) this.attachShadow({ mode: "open" });
    this.saveCalibrationExportSize();

    const baseTitle = String(this.config.title || "Switch Vision").trim();
    const memberTitle = String(this.config.member || "").trim();
    const rawHeaderTitle = this.config.card_header_title;
    const candidateHeaderTitle = typeof rawHeaderTitle === "string" ? rawHeaderTitle.trim() : "";
    const customHeaderTitle = ["true", "false"].includes(candidateHeaderTitle.toLowerCase()) ? "" : candidateHeaderTitle;
    const baseEndsWithMember = memberTitle && baseTitle.toLowerCase().endsWith(memberTitle.toLowerCase());
    const automaticTitle = memberTitle && !baseEndsWithMember ? `${baseTitle} • ${memberTitle}` : baseTitle;
    const title = customHeaderTitle || automaticTitle;
    // Header visibility is integration-wide. Retired per-card flags are ignored
    // so native and custom YAML dashboards always share one source of truth.
    const showCardHeader = this._globalShowCardHeaders !== false;
    const activeCalibration = this.calibrationData();
    const faceplateFilename = this.resolvedFaceplateFilename(activeCalibration);
    const faceplateRefreshToken =
      String(this._faceplateRefreshToken || "");

    const faceplateUrl = assetUrl(
      "faceplates",
      faceplateFilename ||
        this.recommendedFaceplateFilename(activeCalibration),
      SV_VERSION,
      faceplateRefreshToken
    );

    const recommendedFaceplateUrl = assetUrl(
      "faceplates",
      this.recommendedFaceplateFilename(activeCalibration),
      SV_VERSION,
      faceplateRefreshToken
    );
    const faceplateImageHtml = `<img class="cv-image" data-cv-faceplate-image data-cv-faceplate-fallback="${htmlEscape(recommendedFaceplateUrl)}" src="${htmlEscape(faceplateUrl || recommendedFaceplateUrl)}" style="object-fit:${htmlEscape(activeCalibration.ui?.faceplate?.fit || "fill")};opacity:${Math.max(0, Math.min(1, Number(activeCalibration.ui?.faceplate?.opacity ?? 1)))}">`;

    this.shadowRoot.innerHTML = `<link rel="stylesheet" href="/local/switch-vision/css/switch-vision.css?v=${SV_VERSION}">
      <div class="cv-card" tabindex="0">
        ${showCardHeader ? `<div class="cv-header">
          <div class="cv-title">${htmlEscape(title)}</div>
          <div class="cv-status">v${SV_VERSION} · ${calibrationEnabled(this.config) ? (calibrationControlsEnabled(this.config) ? "interactive calibration" : "calibration") : (this.config.demo ? "demo" : "live")} · 2048 × 448</div>
        </div>` : ""}
        ${this.calibrationControls(activeCalibration)}
        <div class="cv-stage">
          ${this.calibrationButton()}
          ${faceplateImageHtml}
          <svg class="cv-svg" viewBox="0 0 2048 448" preserveAspectRatio="xMidYMid meet" id="s"></svg>
        </div>
        ${this.legend()}
      </div>`;

    this.redrawSwitchSvg(activeCalibration);
    this.applyCalibrationSectionOrder();
    this.attachCalibrationControlHandlers(activeCalibration);
    this.attachCalibrationButtonHandler();
    const positionTestMode = () => {
      const stage = this.shadowRoot?.querySelector(".cv-stage");
      const button = this.shadowRoot?.querySelector('[data-cv-action="toggle-calibration"]');
      const testMode = this.shadowRoot?.querySelector(".cv-cal-test-mode-badge");
      if (!stage || !button || !testMode) return;
      const stageRect = stage.getBoundingClientRect();
      const buttonRect = button.getBoundingClientRect();
      const testRect = testMode.getBoundingClientRect();
      if (!stageRect.width || !stageRect.height || !buttonRect.width || !buttonRect.height || !testRect.width || !testRect.height) return;
      const gap = 30;
      const buttonLeft = buttonRect.left - stageRect.left;
      const buttonRight = buttonRect.right - stageRect.left;
      const buttonTop = buttonRect.top - stageRect.top;
      const buttonBottom = buttonRect.bottom - stageRect.top;
      const testWidth = testRect.width;
      const testHeight = testRect.height;
      const candidates = [
        { left: buttonLeft, top: buttonBottom + gap },
        { left: buttonLeft, top: buttonTop - testHeight - gap },
        { left: buttonLeft - testWidth - gap, top: buttonTop },
        { left: buttonRight + gap, top: buttonTop },
      ];
      const fits = ({ left, top }) => (
        left >= 0
        && top >= 0
        && left + testWidth <= stageRect.width
        && top + testHeight <= stageRect.height
      );
      let chosen = candidates.find(fits);
      if (!chosen) {
        const fallbackCandidates = [
          { left: 4, top: 4 },
          { left: Math.max(4, stageRect.width - testWidth - 4), top: 4 },
          { left: 4, top: Math.max(4, stageRect.height - testHeight - 4) },
          { left: Math.max(4, stageRect.width - testWidth - 4), top: Math.max(4, stageRect.height - testHeight - 4) },
        ];
        const overlapsButton = ({ left, top }) => (
          left < buttonRight
          && left + testWidth > buttonLeft
          && top < buttonBottom
          && top + testHeight > buttonTop
        );
        chosen = fallbackCandidates.find((candidate) => fits(candidate) && !overlapsButton(candidate));
      }
      if (!chosen) {
        testMode.style.display = "none";
        return;
      }
      testMode.style.display = "";
      testMode.style.left = `${chosen.left}px`;
      testMode.style.top = `${chosen.top}px`;
    };
    positionTestMode();
    requestAnimationFrame(positionTestMode);
    if (this._testModeResizeObserver) this._testModeResizeObserver.disconnect();
    const testModeStage = this.shadowRoot?.querySelector(".cv-stage");
    if (testModeStage && typeof ResizeObserver !== "undefined") {
      this._testModeResizeObserver = new ResizeObserver(positionTestMode);
      this._testModeResizeObserver.observe(testModeStage);
    }
    const faceplateImage = this.shadowRoot.querySelector("[data-cv-faceplate-image]");
    const info = this.shadowRoot.querySelector("[data-cv-faceplate-info]");
    if (faceplateImage && info) {
      const updateInfo = () => { const w = faceplateImage.naturalWidth || 0; const h = faceplateImage.naturalHeight || 0; info.textContent = w && h ? `Detected image: ${w} × ${h} px · Aspect ratio ${(w/h).toFixed(2)}:1${w < 2048 ? " · Recommended width: 2048 px" : ""}` : "Detected image dimensions unavailable"; };
      faceplateImage.addEventListener("error", () => {
        const fallback = String(faceplateImage.dataset.cvFaceplateFallback || "");
        if (fallback && faceplateImage.src !== new URL(fallback, window.location.href).href) {
          faceplateImage.src = fallback;
          info.textContent = "Selected faceplate was unavailable; showing the recommended faceplate.";
        }
      });
      if (faceplateImage.complete && faceplateImage.naturalWidth) { updateInfo(); positionTestMode(); } else faceplateImage.addEventListener("load", () => { updateInfo(); positionTestMode(); }, {once:true});
    }
    this.applyCalibrationExportSize();
  }

  nudgeCalibrationTarget(cal, dx, dy) {
    const editable = getEditableCalibrationTarget(cal, this.config);
    if (!editable) return false;

    if (editable.type === "logo") {
      editable.item.x = Math.round((Number(editable.item.x) + dx) * 10) / 10;
      editable.item.y = Math.round((Number(editable.item.y) + dy) * 10) / 10;
      return true;
    }

    if (editable.type === "calibration_button") {
      editable.item.x = Math.round((Number(editable.item.x) + dx) * 10) / 10;
      editable.item.y = Math.round((Number(editable.item.y) + dy) * 10) / 10;
      return true;
    }

    if (editable.type === "status_box" || editable.type === "status_box_2") {
      editable.item.x = Math.round((Number(editable.item.x) + dx) * 10) / 10;
      editable.item.y = Math.round((Number(editable.item.y) + dy) * 10) / 10;
      return true;
    }

    if (editable.group && (editable.type === "status_fields" || editable.type === "status_fields_2")) {
      const p = editable.type === "status_fields_2" ? cal.ui.status_panel_2 : cal.ui.status_panel;
      for (const xy of statusFieldPointsForPart(p, editable.part)) movePoint(xy, dx, dy);
      return true;
    }

    if (editable.group && editable.type === "number_labels") {
      for (const port of Object.values(cal.ports || {})) movePoint(port?.number, dx, dy);
      for (const sfp of Object.values(cal.sfp || {})) movePoint(sfp?.label, dx, dy);
      return true;
    }

    if (editable.group && editable.type === "ports") {
      for (const key of calibrationPortKeysForEditable(cal, editable)) {
        const port = cal.ports?.[key];
        if (!port) continue;
        if (editable.part === "entire") {
          movePoint(port.center, dx, dy);
          movePoint(port.led_left, dx, dy);
          movePoint(port.led_right, dx, dy);
        } else {
          movePoint(port[editable.part], dx, dy);
        }
      }
      return true;
    }

    if (editable.group && editable.type === "sfps") {
      for (const key of calibrationSfpKeysForEditable(cal, editable)) {
        const sfp = cal.sfp?.[key];
        if (!sfp) continue;
        const point = ["led_left", "led_right"].includes(editable.part)
          ? sfpLedPoint(sfp, editable.part, true)
          : sfp?.[editable.part];
        movePoint(point, dx, dy);
      }
      return true;
    }

    if (editable.group && editable.type === "status_leds") {
      for (const [name, xy] of Object.entries(cal.status_leds || {})) {
        if (String(name).toUpperCase() === "MODE") continue;
        movePoint(xy, dx, dy);
      }
      return true;
    }

    if ((editable.type === "port") && editable.part === "entire") {
      movePoint(editable.item.center, dx, dy);
      movePoint(editable.item.led_left, dx, dy);
      movePoint(editable.item.led_right, dx, dy);
      return true;
    }

    if (editable.type === "sfp" && ["led_left", "led_right"].includes(editable.part)) {
      movePoint(sfpLedPoint(editable.item, editable.part, true), dx, dy);
      return true;
    }

    if (editable.point) {
      movePoint(editable.point, dx, dy);
      return true;
    }

    return false;
  }

  setCalibrationTargetCoordinates(cal, xValue, yValue) {
    const editable = getEditableCalibrationTarget(cal, this.config);
    if (!editable) return false;

    const hasX = Number.isFinite(xValue);
    const hasY = Number.isFinite(yValue);
    if (!hasX && !hasY) return false;
    const rounded = (value) => Math.round(Number(value) * 10) / 10;
    const setPoint = (point) => {
      if (!Array.isArray(point) || point.length < 2) return false;
      if (hasX) point[0] = rounded(xValue);
      if (hasY) point[1] = rounded(yValue);
      return true;
    };
    const moveEntirePort = (port) => {
      if (!port || !Array.isArray(port.center)) return false;
      const dx = hasX ? rounded(xValue - Number(port.center[0])) : 0;
      const dy = hasY ? rounded(yValue - Number(port.center[1])) : 0;
      movePoint(port.center, dx, dy);
      movePoint(port.led_left, dx, dy);
      movePoint(port.led_right, dx, dy);
      return true;
    };

    if (["logo", "calibration_button", "status_box", "status_box_2"].includes(editable.type)) {
      if (hasX) editable.item.x = rounded(xValue);
      if (hasY) editable.item.y = rounded(yValue);
      return true;
    }

    if (editable.group && editable.type === "number_labels") {
      let changed = false;
      for (const port of Object.values(cal.ports || {})) changed = setPoint(port?.number) || changed;
      for (const sfp of Object.values(cal.sfp || {})) changed = setPoint(sfp?.label) || changed;
      return changed;
    }

    if (editable.group && editable.type === "ports") {
      let changed = false;
      for (const key of calibrationPortKeysForEditable(cal, editable)) {
        const port = cal.ports?.[key];
        if (!port) continue;
        changed = editable.part === "entire" ? (moveEntirePort(port) || changed) : (setPoint(port[editable.part]) || changed);
      }
      return changed;
    }

    if (editable.group && editable.type === "sfps") {
      let changed = false;
      for (const key of calibrationSfpKeysForEditable(cal, editable)) {
        const sfp = cal.sfp?.[key];
        if (!sfp) continue;
        const point = ["led_left", "led_right"].includes(editable.part)
          ? sfpLedPoint(sfp, editable.part, true)
          : sfp?.[editable.part];
        changed = setPoint(point) || changed;
      }
      return changed;
    }

    if (editable.group && editable.type === "status_leds") {
      let changed = false;
      for (const [name, point] of Object.entries(cal.status_leds || {})) {
        if (String(name).toUpperCase() === "MODE") continue;
        changed = setPoint(point) || changed;
      }
      return changed;
    }

    if (editable.group && ["status_fields", "status_fields_2"].includes(editable.type)) {
      let changed = false;
      for (const point of statusFieldPointsForPart(editable.item, editable.part)) changed = setPoint(point) || changed;
      return changed;
    }

    if (editable.type === "port" && editable.part === "entire") return moveEntirePort(editable.item);
    if (editable.type === "port") return setPoint(editable.item?.[editable.part]);
    if (editable.type === "sfp") {
      const point = ["led_left", "led_right"].includes(editable.part)
        ? sfpLedPoint(editable.item, editable.part, true)
        : editable.item?.[editable.part];
      return setPoint(point);
    }
    return setPoint(editable.point);
  }

  setCalibrationTargetSize(cal, widthValue, heightValue) {
    const editable = getEditableCalibrationTarget(cal, this.config);
    if (!editable) return false;

    const hasWidth = Number.isFinite(widthValue);
    const hasHeight = Number.isFinite(heightValue);
    if (!hasWidth && !hasHeight) return false;
    const rounded = (value) => Math.round(Number(value) * 10) / 10;
    const setSize = (size, minimum = 8) => {
      if (!Array.isArray(size) || size.length < 2) return false;
      if (hasWidth) size[0] = Math.max(minimum, rounded(widthValue));
      if (hasHeight) size[1] = Math.max(minimum, rounded(heightValue));
      return true;
    };

    if (["logo", "status_box", "status_box_2", "calibration_button"].includes(editable.type)) {
      if (hasWidth) editable.item.width = Math.max(8, rounded(widthValue));
      if (hasHeight) editable.item.height = Math.max(8, rounded(heightValue));
      return true;
    }

    if (editable.group && editable.type === "ports") {
      const sizeKey = portLedSizeKey(editable.part);
      let changed = false;
      for (const key of calibrationPortKeysForEditable(cal, editable)) {
        const port = cal.ports?.[key];
        if (!port) continue;
        if (sizeKey && !Array.isArray(port[sizeKey])) port[sizeKey] = [...defaultPortLedRectangleSize()];
        if (sizeKey) changed = setSize(port[sizeKey], 2) || changed;
        else if (["center", "entire"].includes(editable.part)) changed = setSize(port.hitbox, 8) || changed;
      }
      return changed;
    }

    if (editable.group && editable.type === "sfps") {
      const sizeKey = portLedSizeKey(editable.part);
      let changed = false;
      for (const key of calibrationSfpKeysForEditable(cal, editable)) {
        const sfp = cal.sfp?.[key];
        if (!sfp) continue;
        if (sizeKey) changed = setSize(sfpLedRectangleSize(sfp, editable.part, true), 2) || changed;
        else if (editable.part === "center") changed = setSize(sfp?.hitbox, 8) || changed;
      }
      return changed;
    }

    if (editable.type === "port") {
      const sizeKey = portLedSizeKey(editable.part);
      if (sizeKey && !Array.isArray(editable.item?.[sizeKey])) editable.item[sizeKey] = [...defaultPortLedRectangleSize()];
      if (sizeKey) return setSize(editable.item?.[sizeKey], 2);
      if (["center", "entire"].includes(editable.part)) return setSize(editable.item?.hitbox, 8);
      return false;
    }

    if (editable.type === "sfp") {
      const sizeKey = portLedSizeKey(editable.part);
      if (sizeKey) return setSize(sfpLedRectangleSize(editable.item, editable.part, true), 2);
      if (editable.part === "center") return setSize(editable.item?.hitbox, 8);
    }

    return false;
  }

  resizeCalibrationTarget(cal, dw, dh) {
    const editable = getEditableCalibrationTarget(cal, this.config);
    if (!editable) return false;

    const resizeSize = (size, minimum = 8) => {
      if (!Array.isArray(size) || size.length !== 2) return false;
      size[0] = Math.max(minimum, Math.round((Number(size[0]) + dw) * 10) / 10);
      size[1] = Math.max(minimum, Math.round((Number(size[1]) + dh) * 10) / 10);
      return true;
    };

    if (["logo", "status_box", "status_box_2", "calibration_button"].includes(editable.type)) {
      editable.item.width = Math.max(8, Math.round((Number(editable.item.width) + dw) * 10) / 10);
      editable.item.height = Math.max(8, Math.round((Number(editable.item.height) + dh) * 10) / 10);
      return true;
    }

    if (editable.group && editable.type === "ports") {
      const keys = calibrationPortKeysForEditable(cal, editable);
      const sizeKey = portLedSizeKey(editable.part);
      let changed = false;
      for (const key of keys) {
        const port = cal.ports?.[key];
        if (!port) continue;
        if (sizeKey) changed = resizeSize(port[sizeKey], 2) || changed;
        else if (["center", "entire"].includes(editable.part)) changed = resizeSize(port.hitbox, 8) || changed;
      }
      return changed;
    }

    if (editable.group && editable.type === "sfps") {
      const sizeKey = portLedSizeKey(editable.part);
      let changed = false;
      for (const key of calibrationSfpKeysForEditable(cal, editable)) {
        const sfp = cal.sfp?.[key];
        if (!sfp) continue;
        if (sizeKey) changed = resizeSize(sfpLedRectangleSize(sfp, editable.part, true), 2) || changed;
        else if (editable.part === "center") changed = resizeSize(sfp?.hitbox, 8) || changed;
      }
      return changed;
    }

    if (editable.type === "port") {
      const sizeKey = portLedSizeKey(editable.part);
      if (sizeKey) return resizeSize(editable.item?.[sizeKey], 2);
      if (["center", "entire"].includes(editable.part)) return resizeSize(editable.item?.hitbox, 8);
      return false;
    }

    if (editable.type === "sfp") {
      const sizeKey = portLedSizeKey(editable.part);
      if (sizeKey) return resizeSize(sfpLedRectangleSize(editable.item, editable.part, true), 2);
      if (editable.part === "center") return resizeSize(editable.item?.hitbox, 8);
    }

    return false;
  }

  attachCalibrationControlHandlers(cal) {
    if (!calibrationControlsEnabled(this.config) || !this.shadowRoot) return;

    for (const section of this.shadowRoot.querySelectorAll("details[data-cv-section]")) {
      section.addEventListener("toggle", () => {
        this._calibrationSectionState = this._calibrationSectionState || {};
        this._calibrationSectionState[section.getAttribute("data-cv-section")] = section.open;
      });
    }
    this.attachCalibrationSectionReorderHandlers();

    const targetSelect = this.shadowRoot.querySelector('[data-cv-field="target"]');
    if (targetSelect) {
      targetSelect.addEventListener("change", (event) => {
        const value = event.target.value;
        const isPortTarget = value === "ports" || value.startsWith("port:") || /^ports_(odd|even)$/.test(value);
        const part = ["ports_led_left", "sfps_led_left"].includes(value)
          ? "led_left"
          : (["ports_led_right", "sfps_led_right"].includes(value)
            ? "led_right"
            : (value === "ports_numbers" ? "number" : (isPortTarget ? "entire" : "center")));
        this.config = { ...this.config, calibration_target: value, calibration_part: part };
        this.render();
      });
    }

    const partSelect = this.shadowRoot.querySelector('[data-cv-field="part"]');
    if (partSelect) {
      partSelect.addEventListener("change", (event) => {
        this.config = { ...this.config, calibration_part: event.target.value };
        this.render();
      });
    }

    const stepSelect = this.shadowRoot.querySelector('[data-cv-field="step"]');
    if (stepSelect) {
      stepSelect.addEventListener("change", (event) => {
        this.config = { ...this.config, calibration_step: Number(event.target.value || 1) };
        this.render();
      });
    }

    for (const input of this.shadowRoot.querySelectorAll('[data-cv-field="coordinate-x"], [data-cv-field="coordinate-y"], [data-cv-field="size-width"], [data-cv-field="size-height"]')) {
      input.addEventListener("keydown", (event) => {
        if (event.key !== "Enter") return;
        event.preventDefault();
        this.shadowRoot.querySelector('[data-cv-action="apply-coordinates"]')?.click();
      });
    }

    for (const input of this.shadowRoot.querySelectorAll('[data-cv-status-led]')) {
      input.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        const name = String(event.target.getAttribute("data-cv-status-led") || "");
        const target = name.toUpperCase();
        const hidden = new Set((cal.ui.status_leds.hidden || []).map((item) => String(item).toUpperCase()));
        if (event.target.checked) hidden.delete(target);
        else hidden.add(target);
        cal.ui.status_leds.hidden = Object.keys(cal.status_leds || {}).filter((item) => hidden.has(String(item).toUpperCase()));
        this.markCalibrationDirty();
        this.render();
      });
    }


    const statusLedFontSelect = this.shadowRoot.querySelector('[data-cv-field="status-led-font"]');
    if (statusLedFontSelect) {
      statusLedFontSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_leds.font_family = event.target.value;
        this.markCalibrationDirty();
        this.render();
      });
    }

    const portNumberFontSizeInput = this.shadowRoot.querySelector('[data-cv-field="port-number-font-size"]');
    if (portNumberFontSizeInput) {
      portNumberFontSizeInput.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.port_number_font_size = Math.max(8, Math.min(50, Number(event.target.value || 13)));
        this.markCalibrationDirty();
        this.render();
      });
    }

    const sfpLabelFontSizeInput = this.shadowRoot.querySelector('[data-cv-field="sfp-label-font-size"]');
    if (sfpLabelFontSizeInput) {
      sfpLabelFontSizeInput.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.sfp_label_font_size = Math.max(8, Math.min(50, Number(event.target.value || 13.5)));
        this.markCalibrationDirty();
        this.render();
      });
    }

    const statusLedFontSizeSelect = this.shadowRoot.querySelector('[data-cv-field="status-led-font-size"]');
    if (statusLedFontSizeSelect) {
      statusLedFontSizeSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_leds.font_size = Number(event.target.value || 16.5);
        this.markCalibrationDirty();
        this.render();
      });
    }

    const statusFontSelect = this.shadowRoot.querySelector('[data-cv-field="status-font"]');
    if (statusFontSelect) {
      statusFontSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_panel.font_family = event.target.value;
        this.markCalibrationDirty();
        this.render();
      });
    }

    const statusFontSizeSelect = this.shadowRoot.querySelector('[data-cv-field="status-font-size"]');
    if (statusFontSizeSelect) {
      statusFontSizeSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_panel.font_size = Number(event.target.value || 16);
        this.markCalibrationDirty();
        this.render();
      });
    }


    const statusTextColorSelect = this.shadowRoot.querySelector('[data-cv-field="status-text-color"]');
    if (statusTextColorSelect) {
      statusTextColorSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_panel.text_color = event.target.value;
        cal.ui.status_panel.label_color = event.target.value;
        cal.ui.status_panel.title_color = event.target.value;
        this.markCalibrationDirty();
        this.render();
      });
    }

    const statusBoxColorSelect = this.shadowRoot.querySelector('[data-cv-field="status-box-color"]');
    if (statusBoxColorSelect) {
      statusBoxColorSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.status_panel.background_color = event.target.value;
        this.markCalibrationDirty();
        this.render();
      });
    }

    const bindPanelStyleControls = (prefix, panelKey, configPrefix = null) => {
      const font = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-font"]`);
      const fontSize = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-font-size"]`);
      const textColor = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-text-color"]`);
      const boxColor = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-box-color"]`);
      const borderShow = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-border-show"]`);
      const borderColor = this.shadowRoot.querySelector(`[data-cv-field="${prefix}-border-color"]`);
      const setConfigOverride = (name, value) => {
        if (!configPrefix) return;
        this.config = { ...this.config, [`${configPrefix}_${name}`]: value };
      };
      // ensureCalibrationUi() replaces the nested panel objects while merging
      // defaults. Resolve the live panel after that merge for every event instead
      // of mutating a stale object captured when the handlers were attached.
      const update = (fn) => (event) => {
        ensureCalibrationUi(cal);
        const panel = cal.ui?.[panelKey];
        if (!panel) return;
        fn(event, panel);
        this.markCalibrationDirty();
        this.render();
      };
      if (font) font.addEventListener("change", update((event, panel) => { panel.font_family = event.target.value; setConfigOverride("font_family", panel.font_family); }));
      if (fontSize) fontSize.addEventListener("change", update((event, panel) => { panel.font_size = Number(event.target.value || 16); setConfigOverride("font_size", panel.font_size); }));
      if (textColor) textColor.addEventListener("change", update((event, panel) => {
        panel.text_color = event.target.value;
        panel.label_color = event.target.value;
        panel.title_color = event.target.value;
        setConfigOverride("text_color", panel.text_color);
        setConfigOverride("label_color", panel.label_color);
        setConfigOverride("title_color", panel.title_color);
      }));
      if (boxColor) boxColor.addEventListener("change", update((event, panel) => { panel.background_color = event.target.value; setConfigOverride("background_color", panel.background_color); }));
      if (borderShow) borderShow.addEventListener("change", update((event, panel) => {
        panel.border_show = event.target.checked === true;
        // Match Status Box 1: the calibration profile is the source of truth.
        // Remove any stale card-level override left by older handlers, otherwise
        // statusPanelUi() could keep reading that value after the profile changes.
        if (configPrefix) {
          const next = { ...this.config };
          delete next[`${configPrefix}_border_show`];
          this.config = next;
        }
      }));
      if (borderColor) borderColor.addEventListener("change", update((event, panel) => {
        panel.border_color = String(event.target.value || "rgba(245,248,250,.75)");
        if (configPrefix) {
          const next = { ...this.config };
          delete next[`${configPrefix}_border_color`];
          this.config = next;
        }
      }));
    };
    // Status Box 2 uses the same profile values as the renderer, with card-level
    // overrides kept in sync for the non-border style controls.
    bindPanelStyleControls("status2", "status_panel_2", "status_panel_2");
    const portLedShapeSelect = this.shadowRoot.querySelector('[data-cv-field="port-led-shape"]');
    if (portLedShapeSelect) {
      portLedShapeSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        cal.ui.port_led_shape = String(event.target.value || "circle").toLowerCase() === "rectangle" ? "rectangle" : "circle";
        this.markCalibrationDirty();
        this.render();
      });
    }

    const bindPanelVisibilityControl = (field, panelKey, configKey) => {
      const input = this.shadowRoot.querySelector(`[data-cv-field="${field}"]`);
      if (!input) return;

      input.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);

        const panel = cal.ui?.[panelKey];
        if (!panel) return;

        const visible = event.target.checked === true;
        panel.show = visible;

        // Keep the currently rendered card aligned with the profile while the
        // calibration editor is open. A future explicit YAML false remains an
        // external force-hide when the card configuration is loaded again.
        this.config = {
          ...this.config,
          [configKey]: visible
        };

        this.markCalibrationDirty();
        this.render();
      });
    };

    bindPanelVisibilityControl(
      "status-show",
      "status_panel",
      "show_status_panel"
    );

    bindPanelVisibilityControl(
      "status2-show",
      "status_panel_2",
      "show_status_panel_2"
    );

    const statusBorderShow = this.shadowRoot.querySelector('[data-cv-field="status-border-show"]');
    if (statusBorderShow) statusBorderShow.addEventListener("change", (event) => { cal.ui.status_panel.border_show = event.target.checked === true; this.markCalibrationDirty(); this.render(); });
    const statusBorderColor = this.shadowRoot.querySelector('[data-cv-field="status-border-color"]');
    if (statusBorderColor) statusBorderColor.addEventListener("change", (event) => { cal.ui.status_panel.border_color = event.target.value; this.markCalibrationDirty(); this.render(); });

    const refreshCustomColourPicker = (control, colour) => {
      if (!control) return;
      const hsv = hexToHsvColour(colour, "#ffffff");
      const swatch = control.querySelector('[data-cv-colour-open]');
      const preview = control.querySelector('[data-cv-colour-preview]');
      const hexInput = control.querySelector('[data-cv-colour-hex]');
      const area = control.querySelector('[data-cv-colour-sv]');
      const marker = control.querySelector('[data-cv-colour-marker]');
      const hue = control.querySelector('[data-cv-colour-hue]');
      const brightness = control.querySelector('[data-cv-colour-brightness]');
      if (swatch) swatch.style.background = colour;
      if (preview) preview.style.background = colour;
      if (hexInput) hexInput.value = colour.toUpperCase();
      if (area) {
        area.dataset.cvSaturation = hsv.s.toFixed(2);
        area.dataset.cvValue = hsv.v.toFixed(2);
        area.style.setProperty("--cv-picker-hue", hsv.h.toFixed(2));
      }
      if (marker) {
        marker.style.left = `${hsv.s.toFixed(2)}%`;
        marker.style.top = `${(100 - hsv.v).toFixed(2)}%`;
      }
      if (hue) hue.value = String(Math.round(hsv.h));
      if (brightness) brightness.value = String(Math.round(hsv.v));
    };

    const applyCustomColour = (field, value, { syncPicker = true } = {}) => {
      ensureCalibrationUi(cal);
      const factoryDefaults = { "link-led-color": "#76ff33", "activity-led-color": "#ffb321" };
      if (String(value || "") === "__factory__" && factoryDefaults[field]) {
        if (field === "link-led-color") delete cal.ui.link_led_color;
        if (field === "activity-led-color") delete cal.ui.activity_led_color;
        this.markCalibrationDirty();
        if (syncPicker) refreshCustomColourPicker(this.shadowRoot.querySelector(`[data-cv-colour-control="${field}"]`), factoryDefaults[field]);
        this.redrawSwitchSvg();
        return;
      }
      const colour = normaliseHexColour(value, factoryDefaults[field] || "#ffffff");
      const setters = {
        "link-led-color": () => { cal.ui.link_led_color = colour; },
        "activity-led-color": () => { cal.ui.activity_led_color = colour; },
        "port-number-color": () => { cal.ui.port_number_color = colour; },
        "port-label-color": () => { cal.ui.port_label_color = colour; },
        "status-led-text-color": () => { cal.ui.status_leds.text_color = colour; },
        "status-text-color": () => { cal.ui.status_panel.text_color = colour; cal.ui.status_panel.label_color = colour; cal.ui.status_panel.title_color = colour; },
        "status-box-color": () => { cal.ui.status_panel.background_color = colour; },
        "status-border-color": () => { cal.ui.status_panel.border_color = colour; },
        "status2-text-color": () => { cal.ui.status_panel_2.text_color = colour; cal.ui.status_panel_2.label_color = colour; cal.ui.status_panel_2.title_color = colour; },
        "status2-box-color": () => { cal.ui.status_panel_2.background_color = colour; },
        "status2-border-color": () => { cal.ui.status_panel_2.border_color = colour; }
      };
      if (!setters[field]) return;
      setters[field]();
      this.markCalibrationDirty();
      const preset = this.shadowRoot.querySelector(`[data-cv-field="${field}"]`);
      if (preset) preset.value = colour;
      if (syncPicker) refreshCustomColourPicker(this.shadowRoot.querySelector(`[data-cv-colour-control="${field}"]`), colour);
      this.redrawSwitchSvg();
    };

    const closeCustomColourPickers = (except = null) => {
      this.shadowRoot.querySelectorAll('[data-cv-colour-popover]').forEach((popover) => {
        if (except && popover === except) return;
        popover.hidden = true;
        const control = popover.closest('[data-cv-colour-control]');
        control?.querySelector('[data-cv-colour-open]')?.setAttribute("aria-expanded", "false");
      });
    };

    this.shadowRoot.querySelectorAll('[data-cv-colour-open]').forEach((button) => {
      button.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
        const control = event.currentTarget.closest('[data-cv-colour-control]');
        const popover = control?.querySelector('[data-cv-colour-popover]');
        if (!popover) return;
        const opening = popover.hidden;
        closeCustomColourPickers(opening ? popover : null);
        popover.hidden = !opening;
        event.currentTarget.setAttribute("aria-expanded", opening ? "true" : "false");
      });
    });

    this.shadowRoot.querySelectorAll('[data-cv-colour-sv]').forEach((area) => {
      const updateFromPointer = (event) => {
        const rect = area.getBoundingClientRect();
        if (!rect.width || !rect.height) return;
        const s = Math.max(0, Math.min(100, ((event.clientX - rect.left) / rect.width) * 100));
        const v = Math.max(0, Math.min(100, (1 - ((event.clientY - rect.top) / rect.height)) * 100));
        const control = area.closest('[data-cv-colour-control]');
        const field = String(area.dataset.cvColourSv || "");
        const hue = Number(control?.querySelector('[data-cv-colour-hue]')?.value || 0);
        area.dataset.cvSaturation = s.toFixed(2);
        area.dataset.cvValue = v.toFixed(2);
        const marker = area.querySelector('[data-cv-colour-marker]');
        if (marker) { marker.style.left = `${s.toFixed(2)}%`; marker.style.top = `${(100 - v).toFixed(2)}%`; }
        const brightness = control?.querySelector('[data-cv-colour-brightness]');
        if (brightness) brightness.value = String(Math.round(v));
        const colour = hsvToHexColour(hue, s, v);
        const swatch = control?.querySelector('[data-cv-colour-open]');
        const preview = control?.querySelector('[data-cv-colour-preview]');
        const hexInput = control?.querySelector('[data-cv-colour-hex]');
        if (swatch) swatch.style.background = colour;
        if (preview) preview.style.background = colour;
        if (hexInput) hexInput.value = colour.toUpperCase();
        applyCustomColour(field, colour, { syncPicker: false });
      };
      area.addEventListener("pointerdown", (event) => {
        event.preventDefault();
        area.setPointerCapture?.(event.pointerId);
        updateFromPointer(event);
      });
      area.addEventListener("pointermove", (event) => {
        if (!area.hasPointerCapture?.(event.pointerId)) return;
        updateFromPointer(event);
      });
      area.addEventListener("pointerup", (event) => area.releasePointerCapture?.(event.pointerId));
      area.addEventListener("pointercancel", (event) => area.releasePointerCapture?.(event.pointerId));
    });

    this.shadowRoot.querySelectorAll('[data-cv-colour-hue]').forEach((input) => {
      input.addEventListener("input", (event) => {
        const control = event.currentTarget.closest('[data-cv-colour-control]');
        const area = control?.querySelector('[data-cv-colour-sv]');
        if (!area) return;
        const h = Number(event.currentTarget.value || 0);
        const s = Number(area.dataset.cvSaturation || 0);
        const v = Number(area.dataset.cvValue || 100);
        area.style.setProperty("--cv-picker-hue", String(h));
        applyCustomColour(event.currentTarget.dataset.cvColourHue, hsvToHexColour(h, s, v));
      });
    });

    this.shadowRoot.querySelectorAll('[data-cv-colour-brightness]').forEach((input) => {
      input.addEventListener("input", (event) => {
        const control = event.currentTarget.closest('[data-cv-colour-control]');
        const area = control?.querySelector('[data-cv-colour-sv]');
        if (!area) return;
        const h = Number(control?.querySelector('[data-cv-colour-hue]')?.value || 0);
        const s = Number(area.dataset.cvSaturation || 0);
        const v = Number(event.currentTarget.value || 100);
        applyCustomColour(event.currentTarget.dataset.cvColourBrightness, hsvToHexColour(h, s, v));
      });
    });

    this.shadowRoot.querySelectorAll('[data-cv-colour-hex]').forEach((input) => {
      const commit = (event) => {
        const value = String(event.currentTarget.value || "").trim();
        if (!/^#[0-9a-f]{6}$/i.test(value) && !/^#[0-9a-f]{3}$/i.test(value)) return;
        applyCustomColour(event.currentTarget.dataset.cvColourHex, value);
      };
      input.addEventListener("input", commit);
      input.addEventListener("change", commit);
      input.addEventListener("keydown", (event) => { if (event.key === "Enter") { event.preventDefault(); commit(event); } });
    });
    this.shadowRoot.querySelectorAll('[data-cv-colour-reset]').forEach((button) => {
      button.addEventListener("click", (event) => {
        event.preventDefault();
        applyCustomColour(event.currentTarget.dataset.cvColourReset, event.currentTarget.dataset.cvDefaultColour);
      });
    });
    this.shadowRoot.querySelectorAll('[data-cv-colour-done]').forEach((button) => {
      button.addEventListener("click", (event) => {
        event.preventDefault();
        const popover = event.currentTarget.closest('[data-cv-colour-popover]');
        if (popover) popover.hidden = true;
        const control = event.currentTarget.closest('[data-cv-colour-control]');
        control?.querySelector('[data-cv-colour-open]')?.setAttribute("aria-expanded", "false");
      });
    });
    const calibrationCard = this.shadowRoot.querySelector('.cv-card');
    if (calibrationCard) {
      calibrationCard.addEventListener("pointerdown", (event) => {
        if (event.target.closest?.('[data-cv-colour-control]')) return;
        closeCustomColourPickers();
      });
      calibrationCard.addEventListener("keydown", (event) => {
        if (event.key === "Escape") closeCustomColourPickers();
      });
    }


    const statusRowFieldSelect = this.shadowRoot.querySelector('[data-cv-field="status-row-field"]');
    if (statusRowFieldSelect) {
      statusRowFieldSelect.addEventListener("change", (event) => {
        this.config = { ...this.config, status_panel_selected_field: event.target.value };
        this.render();
      });
    }



    const status2RowFieldSelect = this.shadowRoot.querySelector('[data-cv-field="status2-row-field"]');
    if (status2RowFieldSelect) status2RowFieldSelect.addEventListener("change", (event) => { this.config = { ...this.config, status_panel_2_selected_field: event.target.value }; this.render(); });

    const portStatusRowFieldSelect = this.shadowRoot.querySelector('[data-cv-field="port-status-row-field"]');
    if (portStatusRowFieldSelect) portStatusRowFieldSelect.addEventListener("change", (event) => { this.config = { ...this.config, port_status_selected_field: event.target.value }; this.render(); });

    const switchIpInput = this.shadowRoot.querySelector('[data-cv-field="switch-ip"]');
    if (switchIpInput) {
      const updateSwitchIp = () => {
        ensureCalibrationManagement(cal);
        const value = String(switchIpInput.value || "").trim();
        cal.management.switch_ip = value;
        this.markCalibrationDirty();
        this.render();
      };
      switchIpInput.addEventListener("change", updateSwitchIp);
      switchIpInput.addEventListener("blur", updateSwitchIp);
    }

    const stackEnabledInput = this.shadowRoot.querySelector('[data-cv-field="stack-enabled"]');
    if (stackEnabledInput) {
      stackEnabledInput.addEventListener("change", (event) => {
        ensureCalibrationStack(cal);
        cal.stack.enabled = event.target.checked === true;
        this.config = { ...this.config, stack_enabled: cal.stack.enabled };
        this.markCalibrationDirty();
        this.render();
      });
    }

    const stackMemberSelect = this.shadowRoot.querySelector('[data-cv-field="stack-member-number"]');
    if (stackMemberSelect) {
      stackMemberSelect.addEventListener("change", (event) => {
        ensureCalibrationStack(cal);
        const key = normalizePanelMember(this.config).toUpperCase();
        cal.stack.members[key] = cal.stack.members[key] || {};
        cal.stack.members[key].member_number = Number(event.target.value || "") || "";
        this.config = { ...this.config, stack_member_number: cal.stack.members[key].member_number || null };
        this.markCalibrationDirty();
        this.render();
      });
    }

    const stackModeSelect = this.shadowRoot.querySelector('[data-cv-field="stack-uptime-mode"]');
    if (stackModeSelect) {
      stackModeSelect.addEventListener("change", (event) => {
        ensureCalibrationStack(cal);
        const key = normalizePanelMember(this.config).toUpperCase();
        cal.stack.members[key] = cal.stack.members[key] || {};
        cal.stack.members[key].uptime_mode = normaliseStackUptimeMode(event.target.value);
        this.config = { ...this.config, stack_uptime_mode: cal.stack.members[key].uptime_mode };
        this.markCalibrationDirty();
        this.render();
      });
    }

    const stackSourceInput = this.shadowRoot.querySelector('[data-cv-field="stack-uptime-source"]');
    if (stackSourceInput) {
      const updateStackSource = () => {
        ensureCalibrationStack(cal);
        const value = String(stackSourceInput.value || "").trim();
        const key = normalizePanelMember(this.config).toUpperCase();
        cal.stack.uptime_source = value;
        cal.stack.members[key] = cal.stack.members[key] || {};
        cal.stack.members[key].uptime_source = value;
        this.config = { ...this.config, stack_uptime_source: value, stack_member_uptime_source: value };
        this.markCalibrationDirty();
        this.render();
      };
      stackSourceInput.addEventListener("change", updateStackSource);
      stackSourceInput.addEventListener("blur", updateStackSource);
    }

    const logoFileSelect = this.shadowRoot.querySelector('[data-cv-field="logo-file"]');
    if (logoFileSelect) {
      logoFileSelect.addEventListener("change", (event) => {
        ensureCalibrationUi(cal);
        const value = String(event.target.value || SV_ASSET_DEFAULT);
        if (value === SV_ASSET_NONE) {
          cal.ui.logo.show = false;
          cal.ui.logo.source = "none";
        } else if (value === SV_ASSET_DEFAULT) {
          cal.ui.logo.show = true;
          cal.ui.logo.source = "default";
          cal.ui.logo.file = layout.logo?.file || "sv-logo.png";
        } else {
          cal.ui.logo.show = true;
          cal.ui.logo.source = "custom";
          cal.ui.logo.file = safeAssetFilename(value);
        }
        this.markCalibrationDirty();
        this.render();
      });
    }

    const faceplateFileSelect = this.shadowRoot.querySelector('[data-cv-field="faceplate-file"]');
    if (faceplateFileSelect) {
      faceplateFileSelect.addEventListener("change", async (event) => {
        ensureCalibrationUi(cal);
        const value = String(event.target.value || SV_ASSET_DEFAULT);
        if (value === SV_ASSET_NONE || value === SV_ASSET_DEFAULT) {
          cal.ui.faceplate = normaliseFaceplateAsset({ ...cal.ui.faceplate, show: true, file: SV_ASSET_DEFAULT, source: "default" });
        } else {
          cal.ui.faceplate = normaliseFaceplateAsset({ ...cal.ui.faceplate, show: true, file: safeAssetFilename(value), source: "custom" });
        }
        const requestedFaceplate = cloneCalibrationData(cal.ui.faceplate);

        // A faceplate change selects a different calibration namespace. A bundled
        // faceplate can provide its own factory starter geometry; otherwise retain
        // the current switch geometry. An existing saved faceplate profile still
        // takes precedence when loaded below.
        const faceplateFactory = value !== SV_ASSET_DEFAULT && value !== SV_ASSET_NONE
          ? faceplateFactoryCalibrationForFile(value)
          : null;
        // A previously unsaved faceplate must start from a clean factory/model
        // calibration, never from the currently edited faceplate. Cloning `cal`
        // here was a second cross-taint path for Status Box visibility/order.
        const modelStarter = this.factoryCalibrationForCurrentSwitch({
          preserveFaceplate: false,
          currentCalibration: cal
        });
        const starter = applyCalibrationIdentity(faceplateFactory || modelStarter || calibration, cal);
        ensureCalibrationUi(starter).ui.faceplate = cloneCalibrationData(requestedFaceplate);
        const targetProfile = this.calibrationProfileName(starter);
        this._calibrationWorking = starter;
        this._calibrationDirty = false;
        this._profileCalibration = null;
        this._profileLoadedProfile = null;
        this._profileLoadRequested = null;
        this._profileAutoCreateRequested = null;
        this.setCalibrationSaveStatus(`Loading faceplate profile: ${targetProfile}`, false);
        this.render();
        await this.loadCalibrationProfile(true, { applyToWorking: true, starterCalibration: starter });
        if (this._calibrationWorking && this._profileLoadRequested === targetProfile) {
          ensureCalibrationUi(this._calibrationWorking).ui.faceplate = cloneCalibrationData(requestedFaceplate);
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`Selected faceplate state: ${this.calibrationProfileDisplay(this._calibrationWorking).faceplate}`, false);
          this.render();
        }
      });
    }

    const exportTextarea = this.shadowRoot.querySelector('.cv-cal-export');
    if (exportTextarea) {
      const saveSize = () => this.saveCalibrationExportSize();
      exportTextarea.addEventListener("pointerup", saveSize);
      exportTextarea.addEventListener("mouseup", saveSize);
      exportTextarea.addEventListener("blur", saveSize);
      exportTextarea.addEventListener("input", saveSize);
    }

    for (const button of this.shadowRoot.querySelectorAll('[data-cv-action]')) {
      button.addEventListener("click", async (event) => {
        event.preventDefault();
        event.stopPropagation();
        const action = button.getAttribute("data-cv-action");
        const editable = getEditableCalibrationTarget(cal, this.config);
        const currentScope =
          this.config?.switch_vision_native_panel === true
            ? "Native"
            : "Custom";

        if (action === "toggle-led-test-mode") {
          this.config = {
            ...this.config,
            calibration_test_mode: this.config.calibration_test_mode !== true
          };
          this.render();
          return;
        }

        if (action === "reset-section-layout") {
          this.resetCalibrationSectionOrder();
          this.setCalibrationSaveStatus("Calibration section layout restored", false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "cancel-calibration") {
          if (this._calibrationDirty === true && !(await this.showConfirmation("Discard all unsaved calibration changes and close the editor?", { title: "Discard calibration changes?", confirmLabel: "Discard", danger: true }))) return;
          const restored = this._profileCalibration
            ? applyConfigToCalibrationForEdit(this._profileCalibration, this.config)
            : applyConfigToCalibrationForEdit(this.baseCalibrationData(), this.config);
          this._calibrationWorking = cloneCalibrationData(restored);
          this._calibrationDirty = false;
          this.config = {
            ...this.config,
            calibration_mode: false,
            calibration_controls: false,
            calibration_test_mode: false
          };
          this._calibrationSectionState = {};
          this.setCalibrationSaveStatus("Calibration changes cancelled", false);
          this.render();
          return;
        }

        if (action === "refresh-assets") {
          this._assetLibrary = null;
          await this.maybeLoadAssetLibrary(true);
          this.setCalibrationSaveStatus("Asset folders refreshed", false);
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "select-custom-ports") {
          const input = this.shadowRoot.querySelector('[data-cv-field="custom-port-list"]');
          const partInput = this.shadowRoot.querySelector('[data-cv-field="custom-port-part"]');
          const parsed = parseCalibrationCustomPortSelection(input?.value, cal);
          const requestedPart = String(partInput?.value || "entire");

          if (parsed.portKeys.length && parsed.sfpKeys.length) {
            this.setCalibrationSaveStatus("Select RJ45 ports or SFP/uplinks in one operation, not both types together", true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          if (!parsed.portKeys.length && !parsed.sfpKeys.length) {
            const details = parsed.invalid.length
              ? `Invalid: ${parsed.invalid.join(", ")}`
              : (parsed.missing.length ? `Ports not present: ${parsed.missing.join(", ")}` : "Enter at least one existing RJ45 or SFP/uplink port");
            this.setCalibrationSaveStatus(details, true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          const skipped = [];
          if (parsed.invalid.length) skipped.push(`invalid ${parsed.invalid.join(", ")}`);
          if (parsed.missing.length) skipped.push(`missing ${parsed.missing.join(", ")}`);

          if (parsed.sfpKeys.length) {
            const part = requestedPart === "entire"
              ? "center"
              : (requestedPart === "number" ? "label" : requestedPart);
            const summary = `Selected ${parsed.sfpKeys.length}: ${parsed.sfpKeys.join(", ")}${skipped.length ? ` · skipped ${skipped.join("; ")}` : ""}`;

            this.config = {
              ...this.config,
              calibration_target: "sfps_custom",
              calibration_part: part,
              calibration_sfp_selection: parsed.normalisedSfps,
              calibration_port_selection_summary: summary
            };
            this.setCalibrationSaveStatus(summary, false);
            this.render();
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          const summary = `Selected ${parsed.portKeys.length}: ${parsed.portKeys.join(", ")}${skipped.length ? ` · skipped ${skipped.join("; ")}` : ""}`;

          this.config = {
            ...this.config,
            calibration_target: "ports_custom",
            calibration_part: requestedPart,
            calibration_port_selection: parsed.normalisedPorts,
            calibration_port_selection_summary: summary
          };
          this.setCalibrationSaveStatus(summary, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (["show-number-label", "hide-number-label", "show-port-number", "hide-port-number"].includes(action)) {
          const portKeys = editable?.type === "number_labels"
            ? sortedCalibrationPortKeys(cal)
            : (editable?.type === "port" && editable?.part === "number"
              ? [editable.key]
              : (editable?.type === "ports" && editable?.part === "number" ? calibrationPortKeysForEditable(cal, editable) : []));
          const sfpKeys = editable?.type === "number_labels"
            ? Object.keys(cal.sfp || {})
            : (editable?.type === "sfp" && editable?.part === "label"
              ? [editable.key]
              : (editable?.type === "sfps" && editable?.part === "label" ? Object.keys(cal.sfp || {}) : []));
          if (!portKeys.length && !sfpKeys.length) return;
          const show = ["show-number-label", "show-port-number"].includes(action);
          for (const key of portKeys) {
            if (cal.ports?.[key]) cal.ports[key].number_show = show;
          }
          for (const key of sfpKeys) {
            if (cal.sfp?.[key]) cal.sfp[key].label_show = show;
          }
          const count = portKeys.length + sfpKeys.length;
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`${show ? "Shown" : "Hidden"} ${count} number label${count === 1 ? "" : "s"}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "select-target") {
          const nextTarget = button.getAttribute("data-target") || "ports";
          const nextPart = button.getAttribute("data-part") || "center";

          let resolvedPortKeys = null;
          const allPortKeys = sortedCalibrationPortKeys(cal);

          if (["ports", "ports_led_left", "ports_led_right", "ports_numbers"].includes(nextTarget)) {
            resolvedPortKeys = allPortKeys;
          } else if (nextTarget.startsWith("ports_odd_")) {
            resolvedPortKeys = allPortKeys.filter((key) => Number.parseInt(key, 10) % 2 === 1);
          } else if (nextTarget.startsWith("ports_even_")) {
            resolvedPortKeys = allPortKeys.filter((key) => Number.parseInt(key, 10) % 2 === 0);
          }

          const nextConfig = {
            ...this.config,
            calibration_target: nextTarget,
            calibration_part: nextPart
          };

          // RJ45 Quick Selection also populates the Custom Ports field with the
          // exact resolved visual-port set. This keeps Quick Selection as the
          // active target while making its result visible and immediately
          // editable through Custom Ports.
          if (resolvedPortKeys !== null) {
            nextConfig.calibration_port_selection = resolvedPortKeys.join(",");
            nextConfig.calibration_sfp_selection = "";
            nextConfig.calibration_port_selection_summary =
              `${resolvedPortKeys.length} RJ45 port${resolvedPortKeys.length === 1 ? "" : "s"} selected`;
          }

          this.config = nextConfig;
          this.render();
          return;
        }

        if (action === "add-port" || action === "duplicate-port") {
          const selected = editable?.type === "port" ? editable.key : sortedCalibrationPortKeys(cal).at(-1);
          const newKey = String(nextCalibrationPortNumber(cal));
          cloneCalibrationPort(cal, selected, newKey, action === "add-port" ? 12 : 0, 0);
          this.config = { ...this.config, calibration_target: `port:${newKey}`, calibration_part: "entire" };
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`${action === "add-port" ? "Added" : "Duplicated"} visual port ${newKey}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "set-port-display-name" && ["port", "sfp"].includes(editable?.type)) {
          const input = this.shadowRoot.querySelector('[data-cv-field="port-display-name"]');
          const displayName = String(input?.value || "").trim().slice(0, 96);

          if (displayName) editable.item.display_name = displayName;
          else delete editable.item.display_name;

          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(
            displayName
              ? `Display name set: ${displayName}`
              : `Display name cleared for ${editable.key}`,
            false
          );
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "reset-port-display-name" && ["port", "sfp"].includes(editable?.type)) {
          delete editable.item.display_name;
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`Display name reset for ${editable.key}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "rename-port" && editable?.type === "port") {
          const input = this.shadowRoot.querySelector('[data-cv-field="port-number"]');
          const next = String(Math.max(1, Math.trunc(Number(input?.value || 0))));
          if (!next || next === "NaN") return;
          if (next !== editable.key && cal.ports?.[next]) {
            this.setCalibrationSaveStatus(`Port ${next} already exists`, true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }
          if (next !== editable.key) {
            cal.ports[next] = cal.ports[editable.key];
            delete cal.ports[editable.key];
          }
          this.config = { ...this.config, calibration_target: `port:${next}`, calibration_part: this.config.calibration_part || "center" };
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`Renamed visual port ${editable.key} to ${next}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "remove-port" && ["port", "sfp"].includes(editable?.type)) {
          if (editable.type === "port") {
            const keys = sortedCalibrationPortKeys(cal);
            if (keys.length <= 1) {
              this.setCalibrationSaveStatus("At least one visual RJ45 port must remain", true);
              this.clearCalibrationSaveStatusSoon();
              return;
            }
            const oldIndex = keys.indexOf(editable.key);
            delete cal.ports[editable.key];
            const remaining = sortedCalibrationPortKeys(cal);
            const nextKey = remaining[Math.min(Math.max(oldIndex, 0), remaining.length - 1)] || remaining[0];
            this.config = { ...this.config, calibration_target: `port:${nextKey}`, calibration_part: "entire" };
            this.markCalibrationDirty();
            this.setCalibrationSaveStatus(`Removed visual port ${editable.key}`, false);
            this.render();
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          const keys = Object.keys(cal.sfp || {});
          if (!keys.includes(editable.key)) return;
          const oldIndex = keys.indexOf(editable.key);
          delete cal.sfp[editable.key];
          const remaining = Object.keys(cal.sfp || {});
          const nextKey = remaining[Math.min(Math.max(oldIndex, 0), remaining.length - 1)] || remaining[0];
          this.config = {
            ...this.config,
            calibration_target: nextKey ? `sfp:${sfpPortNumber(nextKey)}` : `port:${sortedCalibrationPortKeys(cal)[0]}`,
            calibration_part: "center"
          };
          this.markCalibrationDirty();
          this.setCalibrationSaveStatus(`Removed visual uplink ${editable.key}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "select-status-row") {
          const field = String(button.getAttribute("data-field") || "model");
          this.config = { ...this.config, status_panel_selected_field: field };
          this.render();
          return;
        }

        if (action === "toggle-port-label-bold") {
          ensureCalibrationUi(cal);
          cal.ui.port_label_font_weight =
            String(cal.ui.port_label_font_weight || "bold").toLowerCase() === "normal"
              ? "bold"
              : "normal";
          this.markCalibrationDirty();
          this.render();
          return;
        }

        if (action === "toggle-status-led-bold") {
          ensureCalibrationUi(cal);
          const current = String(cal.ui.status_leds.font_weight || "900");
          cal.ui.status_leds.font_weight = (current === "bold" || Number(current) >= 600) ? "normal" : "900";
          this.markCalibrationDirty();
          this.render();
          return;
        }

        if (action === "toggle-status-bold" || action === "toggle-status2-bold") {
          ensureCalibrationUi(cal);
          const panel = action === "toggle-status2-bold" ? cal.ui.status_panel_2 : cal.ui.status_panel;
          const current = String(panel.font_weight || "normal");
          panel.font_weight = (current === "bold" || Number(current) >= 600) ? "normal" : "bold";
          if (action === "toggle-status2-bold") {
            this.config = { ...this.config, status_panel_2_font_weight: panel.font_weight };
          }
          this.markCalibrationDirty();
          this.render();
          return;
        }

        if (["status2-row-up", "status2-row-down", "status2-row-toggle"].includes(action)) {
          ensureCalibrationUi(cal);
          const panel = ensureStatusPanelFieldState(cal.ui.status_panel_2);
          const defs = STATUS_PANEL_ROW_DEFS.switch;
          const selected = defs.defaults.includes(String(this.config.status_panel_2_selected_field || "").toLowerCase()) ? String(this.config.status_panel_2_selected_field).toLowerCase() : normaliseStatusPanelFieldOrder(panel.field_order?.switch, "switch")[0];
          const order = normaliseStatusPanelFieldOrder(panel.field_order?.switch, "switch");
          panel.field_order.switch = order; panel.hidden_fields.switch = fieldListFromValue(panel.hidden_fields?.switch) || [];
          if (action === "status2-row-toggle") { const hidden = new Set(panel.hidden_fields.switch); if (hidden.has(selected)) hidden.delete(selected); else hidden.add(selected); panel.hidden_fields.switch = [...hidden].filter((field) => defs.defaults.includes(field)); this.markCalibrationDirty(); }
          if (action === "status2-row-up" || action === "status2-row-down") { const idx = order.indexOf(selected); const swap = action === "status2-row-up" ? idx - 1 : idx + 1; if (idx >= 0 && swap >= 0 && swap < order.length) { [order[idx], order[swap]] = [order[swap], order[idx]]; panel.field_order.switch = order; this.markCalibrationDirty(); } }
          this.render(); return;
        }

        if (["port-status-row-up", "port-status-row-down", "port-status-row-toggle"].includes(action)) {
          ensureCalibrationUi(cal);
          const panel = ensureStatusPanelFieldState(cal.ui.status_panel);
          const defs = STATUS_PANEL_ROW_DEFS.port;
          const selected = defs.defaults.includes(String(this.config.port_status_selected_field || "").toLowerCase())
            ? String(this.config.port_status_selected_field).toLowerCase()
            : normaliseStatusPanelFieldOrder(panel.field_order?.port, "port")[0];
          const order = normaliseStatusPanelFieldOrder(panel.field_order?.port, "port");
          panel.field_order.port = [...order];
          panel.field_order.sfp = normaliseStatusPanelFieldOrder(panel.field_order?.sfp, "sfp");
          panel.hidden_fields.port = fieldListFromValue(panel.hidden_fields?.port) || [];
          panel.hidden_fields.sfp = fieldListFromValue(panel.hidden_fields?.sfp) || [];
          if (action === "port-status-row-toggle") {
            const hidden = new Set(panel.hidden_fields.port);
            if (hidden.has(selected)) hidden.delete(selected); else hidden.add(selected);
            const nextHidden = [...hidden].filter((field) => defs.defaults.includes(field));
            panel.hidden_fields.port = [...nextHidden];
            panel.hidden_fields.sfp = [...nextHidden];
            this.markCalibrationDirty();
          }
          if (action === "port-status-row-up" || action === "port-status-row-down") {
            const idx = order.indexOf(selected);
            const swap = action === "port-status-row-up" ? idx - 1 : idx + 1;
            if (idx >= 0 && swap >= 0 && swap < order.length) {
              [order[idx], order[swap]] = [order[swap], order[idx]];
              panel.field_order.port = [...order];
              panel.field_order.sfp = [...order];
              this.markCalibrationDirty();
            }
          }
          this.render(); return;
        }

        if (["status-row-up", "status-row-down", "status-row-toggle"].includes(action)) {
          ensureCalibrationUi(cal);
          const panel = ensureStatusPanelFieldState(cal.ui.status_panel);
          const rowType = "switch";
          const defs = STATUS_PANEL_ROW_DEFS.switch;
          const selected = defs.defaults.includes(String(this.config.status_panel_selected_field || "").toLowerCase())
            ? String(this.config.status_panel_selected_field).toLowerCase()
            : normaliseStatusPanelFieldOrder(panel.field_order?.[rowType], rowType)[0];
          const order = normaliseStatusPanelFieldOrder(panel.field_order?.[rowType], rowType);
          panel.field_order[rowType] = order;
          panel.hidden_fields[rowType] = fieldListFromValue(panel.hidden_fields?.[rowType]) || [];

          if (action === "status-row-toggle") {
            const hidden = new Set(panel.hidden_fields[rowType]);
            if (hidden.has(selected)) hidden.delete(selected);
            else hidden.add(selected);
            panel.hidden_fields[rowType] = [...hidden].filter((field) => defs.defaults.includes(field));
            this.markCalibrationDirty();
          }

          if (action === "status-row-up" || action === "status-row-down") {
            const idx = order.indexOf(selected);
            const swap = action === "status-row-up" ? idx - 1 : idx + 1;
            if (idx >= 0 && swap >= 0 && swap < order.length) {
              [order[idx], order[swap]] = [order[swap], order[idx]];
              panel.field_order[rowType] = order;
              this.markCalibrationDirty();
            }
          }

          this.render();
          return;
        }

        if (action === "apply-coordinates") {
          const xInput = this.shadowRoot.querySelector('[data-cv-field="coordinate-x"]');
          const yInput = this.shadowRoot.querySelector('[data-cv-field="coordinate-y"]');
          const widthInput = this.shadowRoot.querySelector('[data-cv-field="size-width"]');
          const heightInput = this.shadowRoot.querySelector('[data-cv-field="size-height"]');
          const xRaw = String(xInput?.value ?? "").trim();
          const yRaw = String(yInput?.value ?? "").trim();
          const widthRaw = String(widthInput?.value ?? "").trim();
          const heightRaw = String(heightInput?.value ?? "").trim();
          const xValue = xRaw === "" ? Number.NaN : Number(xRaw);
          const yValue = yRaw === "" ? Number.NaN : Number(yRaw);
          const widthValue = widthRaw === "" ? Number.NaN : Number(widthRaw);
          const heightValue = heightRaw === "" ? Number.NaN : Number(heightRaw);

          const invalidPosition = (xRaw !== "" && !Number.isFinite(xValue)) || (yRaw !== "" && !Number.isFinite(yValue));
          const invalidSize = (widthRaw !== "" && (!Number.isFinite(widthValue) || widthValue <= 0)) || (heightRaw !== "" && (!Number.isFinite(heightValue) || heightValue <= 0));
          if (invalidPosition || invalidSize) {
            this.setCalibrationSaveStatus("Enter valid positive W/H values and valid X/Y coordinates", true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }
          if (xRaw === "" && yRaw === "" && widthRaw === "" && heightRaw === "") {
            this.setCalibrationSaveStatus("Enter an X, Y, W, or H value", true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          const editableNow = getEditableCalibrationTarget(cal, this.config);
          const positionRequested = xRaw !== "" || yRaw !== "";
          const sizeRequested = widthRaw !== "" || heightRaw !== "";
          if (positionRequested && calibrationCoordinatePoints(cal, editableNow, false).length === 0) {
            this.setCalibrationSaveStatus("The selected target does not support direct coordinates", true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }
          if (sizeRequested && calibrationSizePairs(cal, editableNow, false).length === 0) {
            this.setCalibrationSaveStatus("The selected target does not support direct size values", true);
            this.clearCalibrationSaveStatusSoon();
            return;
          }

          const positionChanged = positionRequested ? this.setCalibrationTargetCoordinates(cal, xValue, yValue) : false;
          const sizeChanged = sizeRequested ? this.setCalibrationTargetSize(cal, widthValue, heightValue) : false;
          if (positionChanged || sizeChanged) {
            const values = [
              xRaw !== "" ? `X ${xValue}` : "",
              yRaw !== "" ? `Y ${yValue}` : "",
              widthRaw !== "" ? `W ${widthValue}` : "",
              heightRaw !== "" ? `H ${heightValue}` : "",
            ].filter(Boolean).join(" · ");
            const appliedEditable = getEditableCalibrationTarget(cal, this.config);
            const appliedCount = Math.max(
              calibrationCoordinatePoints(cal, appliedEditable, false).length,
              calibrationSizePairs(cal, appliedEditable, false).length,
            );
            const appliedTargetText = appliedCount === 1 ? "1 target" : `${appliedCount} targets`;
            this.markCalibrationDirty();
            this.setCalibrationSaveStatus(`Applied ${values} to ${appliedTargetText}`, false);
            this.render();
            this.clearCalibrationSaveStatusSoon();
            return;
          }
          this.setCalibrationSaveStatus("The selected target does not support these direct values", true);
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "move") {
          const dx = Number(button.getAttribute("data-dx") || 0);
          const dy = Number(button.getAttribute("data-dy") || 0);
          if (this.nudgeCalibrationTarget(cal, dx, dy)) {
            this.markCalibrationDirty();
            this.render();
            return;
          }
        }

        if (action === "size") {
          const dw = Number(button.getAttribute("data-dw") || 0);
          const dh = Number(button.getAttribute("data-dh") || 0);
          if (this.resizeCalibrationTarget(cal, dw, dh)) {
            this.markCalibrationDirty();
            this.render();
            return;
          }
        }

        if (action === "reset-target" && editable) {
          const resetRecommendation = exactModelVisualRecommendation(this.detectedExactModel());
          const selectedFaceplate = String(ensureCalibrationUi(cal).ui.faceplate?.file || "");
          const resetFaceplateFactory = faceplateFactoryCalibrationForFile(selectedFaceplate);
          const resetFactory = resetFaceplateFactory || (resetRecommendation?.profile
            ? factoryCalibrationForProfile(resetRecommendation.profile)
            : null);
          const original = cloneCalibrationData(resetFactory || calibration);
          if (editable.type === "ports") cal.ports = original.ports;
          if (editable.type === "sfps") {
            if (editable.custom) {
              for (const key of calibrationSfpKeysForEditable(cal, editable)) {
                if (original.sfp?.[key]) {
                  cal.sfp[key] = JSON.parse(JSON.stringify(original.sfp[key]));
                }
              }
            } else {
              cal.sfp = original.sfp;
            }
          }
          if (editable.type === "number_labels") {
            for (const key of Object.keys(cal.ports || {})) {
              if (!original.ports?.[key]) continue;
              cal.ports[key].number = JSON.parse(JSON.stringify(original.ports[key].number));
              cal.ports[key].number_show = original.ports[key].number_show !== false;
            }
            for (const key of Object.keys(cal.sfp || {})) {
              if (!original.sfp?.[key]) continue;
              cal.sfp[key].label = JSON.parse(JSON.stringify(original.sfp[key].label));
              cal.sfp[key].label_show = original.sfp[key].label_show !== false;
            }
          }
          if (editable.type === "status_leds") cal.status_leds = original.status_leds;
          if (editable.type === "port" && original.ports?.[editable.key]) cal.ports[editable.key] = original.ports[editable.key];
          if (editable.type === "sfp" && original.sfp?.[editable.key]) cal.sfp[editable.key] = original.sfp[editable.key];
          if (editable.type === "status" && original.status_leds?.[editable.key]) cal.status_leds[editable.key] = original.status_leds[editable.key];
          if (editable.type === "logo") cal.ui.logo = original.ui.logo;
          if (editable.type === "calibration_button") cal.ui.calibration_button = original.ui.calibration_button;
          if (editable.type === "status_box") cal.ui.status_panel = original.ui.status_panel;
          if (editable.type === "status_box_2") cal.ui.status_panel_2 = original.ui.status_panel_2;
          if (editable.type === "status_fields") {
            if (normaliseStatusFieldGroupPart(editable.part) === "field") {
              cal.ui.status_panel.fields = original.ui.status_panel.fields;
              cal.ui.status_panel.field_order = original.ui.status_panel.field_order;
              cal.ui.status_panel.hidden_fields = original.ui.status_panel.hidden_fields;
            } else {
              for (const [key, point] of Object.entries(original.ui.status_panel.fields || {})) {
                if (!statusFieldKeyMatchesPart(key, editable.part)) continue;
                cal.ui.status_panel.fields[key] = clonePlainData(point);
              }
            }
          }
          if (editable.type === "status_fields_2") {
            if (normaliseStatusFieldGroupPart(editable.part) === "field") {
              cal.ui.status_panel_2.fields = original.ui.status_panel_2.fields;
              cal.ui.status_panel_2.field_order = original.ui.status_panel_2.field_order;
              cal.ui.status_panel_2.hidden_fields = original.ui.status_panel_2.hidden_fields;
            } else {
              for (const [key, point] of Object.entries(original.ui.status_panel_2.fields || {})) {
                if (!statusFieldKeyMatchesPart(key, editable.part)) continue;
                cal.ui.status_panel_2.fields[key] = clonePlainData(point);
              }
            }
          }
          if (editable.type === "status_field" && original.ui.status_panel.fields?.[editable.key]) cal.ui.status_panel.fields[editable.key] = original.ui.status_panel.fields[editable.key];
          if (editable.type === "status_field_2" && original.ui.status_panel_2.fields?.[editable.key]) cal.ui.status_panel_2.fields[editable.key] = original.ui.status_panel_2.fields[editable.key];
          this.markCalibrationDirty();
          this.render();
          return;
        }

        if (action === "refresh-faceplate") {
          this._faceplateRefreshToken =
            `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;

          this.setCalibrationSaveStatus(
            "Faceplate image refreshed — calibration unchanged",
            false
          );

          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "reset-current-faceplate") {
          const details = this.calibrationProfileDisplay(cal);
          if (!(await this.showConfirmation(`Reset the saved calibration for ${details.faceplate}?\n\nThe faceplate image will be kept and its calibration will return to the bundled defaults for this faceplate, or the switch model defaults when no faceplate-specific defaults exist.`, { title: "Reset Current Faceplate", confirmLabel: "Reset Faceplate", danger: true }))) return;
          try {
            const next = this.factoryCalibrationForCurrentSwitch({
              preserveFaceplate: true,
              currentCalibration: cal
            });
            this._calibrationWorking = next;
            const profile = await this.saveCalibrationProfile(next);
            this._calibrationDirty = false;
            this.setCalibrationSaveStatus(`Reset current faceplate: ${profile}`, false);
          } catch (err) {
            console.error("Switch Vision faceplate reset failed", err);
            this.setCalibrationSaveStatus("Faceplate reset failed — check browser console / HA logs", true);
          }
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "reset-current-switch") {
          const base = this.baseCalibrationProfileName();
          if (!(await this.showConfirmation(`Reset switch “${base}”?\n\nThis removes every saved faceplate-specific calibration for this switch and returns it to the recommended model defaults.`, { title: "Reset Current Switch", confirmLabel: "Reset Switch", danger: true }))) return;
          try {
            await this.resetSavedCalibrations("switch");
            const next = this.factoryCalibrationForCurrentSwitch({ preserveFaceplate: false });
            this.clearLoadedCalibrationState(next);
            await this.saveCalibrationProfile(next, base);
            this._calibrationWorking = cloneCalibrationData(next);
            this._calibrationDirty = false;
            this.setCalibrationSaveStatus(`Reset switch: ${base}`, false);
          } catch (err) {
            console.error("Switch Vision switch reset failed", err);
            this.setCalibrationSaveStatus("Switch reset failed — check browser console / HA logs", true);
          }
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "reset-all-switches") {
          if (!(await this.showConfirmation(`Reset ALL Switch Vision switches in ${currentScope}?\n\nThis permanently removes every saved calibration and faceplate override in the ${currentScope} scope only. The opposite dashboard scope and factory profiles will not be changed.`, { title: `Reset All ${currentScope} Switches`, confirmLabel: "Continue", danger: true }))) return;
          if (!(await this.showConfirmation(`Final confirmation: reset every saved calibration in the ${currentScope} scope?`, { title: "Final Reset Confirmation", confirmLabel: "Reset All", danger: true }))) return;
          try {
            const resetStartedAt = Date.now();
            await this.resetSavedCalibrations("namespace");
            // The backend event normally starts this for every loaded card. This
            // direct fallback covers the initiating card if its event delivery is
            // delayed or unavailable, without duplicating an in-flight save.
            if (Number(this._lastModelAwareResetAllAppliedAt || 0) < resetStartedAt) {
              await this.applyModelAwareResetAll();
            }
            this.setCalibrationSaveStatus(`Reset all ${currentScope} switches`, false);
          } catch (err) {
            console.error("Switch Vision global reset failed", err);
            this.setCalibrationSaveStatus("Reset all failed — check browser console / HA logs", true);
          }
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "load-profile") {
          await this.loadCalibrationProfile(true, { applyToWorking: true });
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "save-profile") {
          try {
            const profile = await this.saveCalibrationProfile(cal);
            this._calibrationDirty = false;
            this._profileLoadedProfile = profile;
            this._profileLoadRequested = profile;
            this._profileLoadInfo = {
              ...(this._profileLoadInfo || {}),
              profile,
              exists: true,
              source: "saved Home Assistant profile",
              storage_key: "switch_vision_calibrations",
              storage_path: ".storage/switch_vision_calibrations",
              profile_path: `.storage/switch_vision_calibrations → profiles.${profile}`,
            };
            this.setCalibrationSaveStatus(`Saved profile: ${profile}`, false);
          } catch (err) {
            console.error("Switch Vision save profile failed", err);
            this.setCalibrationSaveStatus("Save failed — check browser console / HA logs", true);
          }
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "download-profile") {
          const baseProfile = this.baseCalibrationProfileName();
          const profile = this.calibrationProfileName(cal);
          const faceplate = this.activeFaceplateFilename(cal);
          const payload = faceplateTransferExportData(cal, {
            scope: baseProfile.startsWith("native_") ? "native" : "custom",
            baseProfile,
            profile,
            faceplate,
          });
          const model = safeDownloadName(payload.model, "model");
          const scopePart = safeDownloadName(baseProfile, "scope");
          const faceplatePart = safeDownloadName(faceplate, "faceplate");
          downloadJsonFile(`switch-vision-faceplate-profile-${model}-${scopePart}-${faceplatePart}.json`, payload);
          this.setCalibrationSaveStatus(`Exported faceplate profile from ${baseProfile}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
          return;
        }

        if (action === "import-profile") {
          const input = this.shadowRoot.querySelector('[data-cv-profile-file]');
          if (input) input.click();
          return;
        }

        if (action === "toggle-json") {
          this._showCalibrationJson = !this._showCalibrationJson;
          this.render();
          return;
        }

        if (action === "copy") {
          const baseProfile = this.baseCalibrationProfileName();
          const profile = this.calibrationProfileName(cal);
          const exportData = faceplateTransferExportData(cal, {
            scope: baseProfile.startsWith("native_") ? "native" : "custom",
            baseProfile,
            profile,
            faceplate: this.activeFaceplateFilename(cal),
          });
          const payload = JSON.stringify(exportData, null, 2);
          const textarea = this.shadowRoot.querySelector(".cv-cal-export");
          if (textarea) textarea.value = payload;

          const copied = await copyTextToClipboard(payload);
          if (copied) {
            button.textContent = "Copied";
            this.setCalibrationSaveStatus("JSON copied to clipboard", false);
          } else {
            if (textarea) {
              textarea.focus();
              textarea.select();
            }
            this.setCalibrationSaveStatus("Clipboard blocked — use Show JSON and copy manually", true);
          }

          setTimeout(() => this.render(), 900);
          this.clearCalibrationSaveStatusSoon();
          return;
        }
      });
    }

    const importInput = this.shadowRoot.querySelector('[data-cv-profile-file]');
    if (importInput) {
      importInput.addEventListener("change", async () => {
        const file = importInput.files?.[0];
        importInput.value = "";
        if (!file) return;
        try {
          if (file.size > SV_PROFILE_MAX_BYTES) throw new Error(`Profile file exceeds the ${Math.round(SV_PROFILE_MAX_BYTES / 1024 / 1024)} MB safety limit`);
          const raw = JSON.parse(await file.text());
          const currentCal = this.calibrationData();
          const result = validateImportedCalibration(raw, currentCal);
          if (!result.valid) {
            this.setCalibrationSaveStatus(`Import rejected: ${result.errors[0] || "invalid profile"}`, true);
            window.alert(`Switch Vision profile import failed:\n\n${result.errors.join("\n")}`);
            this.render();
            return;
          }

          const summary = result.summary;
          const baseProfile = this.baseCalibrationProfileName();
          const stableSwitch = stableCalibrationSwitchKey(this.config);
          if (!stableSwitch || !/^(native|custom)_[A-Za-z0-9_.-]+$/.test(baseProfile)) {
            throw new Error("This card does not have a safe native_/custom_ scoped switch identity.");
          }
          if (isProtectedFactoryProfileName(baseProfile)) {
            throw new Error("Factory calibration profiles are read-only and cannot be import destinations.");
          }

          const imported = normaliseImportedFaceplateProfile(result.calibration);
          const requestedFaceplate = safeAssetFilename(
            raw.required_faceplate || raw.faceplate_file || imported?.ui?.faceplate?.file || ""
          );
          if (requestedFaceplate && ![SV_ASSET_DEFAULT, SV_ASSET_NONE].includes(requestedFaceplate)) {
            const known = Array.isArray(this._assetLibrary?.faceplates) ? this._assetLibrary.faceplates : [];
            if (known.length && !known.includes(requestedFaceplate)) {
              throw new Error(`Required faceplate “${requestedFaceplate}” is not installed in /config/www/switch-vision/faceplates.`);
            }
            imported.ui = imported.ui || {};
            imported.ui.faceplate = {
              ...(imported.ui.faceplate || {}),
              show: true,
              source: "custom",
              file: requestedFaceplate,
            };
          }

          const targetProfile = this.calibrationProfileName(imported);
          if (isProtectedFactoryProfileName(targetProfile)) {
            throw new Error("The resolved import destination is a protected factory profile.");
          }
          const details = [
            `Apply to card scope: ${baseProfile}`,
            `Destination: ${targetProfile}`,
            `Model: ${summary.model}`,
            `Canvas: ${summary.width} × ${summary.height}`,
            `RJ45 ports: ${summary.portCount}`,
            `SFP/uplinks: ${summary.sfpCount}`,
            `Status LEDs: ${summary.statusCount}`,
            `Required faceplate: ${requestedFaceplate || "Default / recommended"}`,
            "",
            "The source file cannot change this destination. The opposite native/custom scope and factory profiles will not be modified."
          ];
          if (result.warnings.length) details.push("", "Warnings:", ...result.warnings.map((warning) => `• ${warning}`));
          if (!(await this.showConfirmation(`Import this Switch Vision faceplate profile?\n\n${details.join("\n")}`, { title: "Import Faceplate Profile", confirmLabel: "Import Profile" }))) return;

          this._calibrationWorking = ensureCalibrationUi(cloneCalibrationData(imported));
          this.markCalibrationDirty();
          const savedProfile = await this.saveCalibrationProfile(this._calibrationWorking, targetProfile);
          this._calibrationDirty = false;
          this.setCalibrationSaveStatus(`Imported into ${baseProfile}: ${savedProfile}`, false);
          this.render();
          this.clearCalibrationSaveStatusSoon();
        } catch (err) {
          console.error("Switch Vision profile import failed", err);
          this.setCalibrationSaveStatus("Import failed — invalid JSON or storage error", true);
          window.alert(`Switch Vision profile import failed:\n\n${err?.message || err}`);
          this.render();
        }
      });
    }
  }


  attachSelectionHandlers(svg, activeCalibration = calibration) {
    if (!svg || !activeCalibration) return;

    svg.addEventListener("pointerdown", (event) => {
      if (event.target !== svg) return;

      event.preventDefault();
      event.stopPropagation();

      this.config = {
        ...this.config,
        selected_interface: null,
        selected_port: null
      };

      this.render();
    });
    const addHitbox = (type, id, item) => {
      if (!item?.center || !item?.hitbox) return;

      const [cx, cy] = item.center;
      const [w, h] = visualHitboxSize(type, item.hitbox);
      const el = document.createElementNS("http://www.w3.org/2000/svg", "rect");

      el.setAttribute("class", "cv-click-hitbox");
      el.setAttribute("x", cx - (w / 2));
      el.setAttribute("y", cy - (h / 2));
      el.setAttribute("width", w);
      el.setAttribute("height", h);
      el.setAttribute("rx", 4);
      el.setAttribute("ry", 4);
      const selectHitbox = (event) => {
        event.preventDefault();
        event.stopPropagation();
        event.stopImmediatePropagation();

        const nextConfig = {
          ...this.config,
          selected_interface: { type, id },
          selected_port: type === "port" ? id : null
        };

        if (calibrationControlsEnabled(this.config)) {
          nextConfig.calibration_target = `${type}:${id}`;
          nextConfig.calibration_part = type === "port" ? "entire" : "center";
        }

        this.config = nextConfig;
        this.render();
      };

      el.addEventListener("pointerdown", selectHitbox);

      svg.appendChild(el);
    };

    for (const [num, port] of Object.entries(activeCalibration.ports || {})) {
      addHitbox("port", Number(num), port);
    }

    for (const [name, sfp] of Object.entries(activeCalibration.sfp || {})) {
      const nums = String(name).match(/\d+/g) || [];
      const sfpPort = Number(nums[nums.length - 1] || 0);
      addHitbox("sfp", sfpPort, sfp);
    }
  }
}

// Expose the renderer version so host panels can detect stale custom-element
// registrations left alive by Home Assistant's single-page navigation.
SwitchVision3650.switchVisionVersion = SV_VERSION;

if (!customElements.get(SV_TYPE)) {
  customElements.define(SV_TYPE, SwitchVision3650);
}

// The native panel uses a versioned alias. Custom elements cannot be redefined
// once an older Switch Vision card has been registered in the current browser
// session, so a fixed tag can otherwise keep running stale renderer logic after
// an integration upgrade. A fresh subclass gives each release an independent
// native-panel element while manual YAML keeps the stable public tag.
const SV_NATIVE_TYPE = `${SV_TYPE}-v${SV_VERSION.replace(/[^0-9a-z]+/gi, "-").toLowerCase()}`;
if (!customElements.get(SV_NATIVE_TYPE)) {
  class SwitchVisionNativeCard extends SwitchVision3650 {}
  SwitchVisionNativeCard.switchVisionVersion = SV_VERSION;
  customElements.define(SV_NATIVE_TYPE, SwitchVisionNativeCard);
}

window.customCards = window.customCards || [];
if (!window.customCards.some(c => c.type === SV_TYPE)) {
  window.customCards.push({
    type: SV_TYPE,
    name: "Switch Vision 3650",
    description: "Switch Vision 3650 live switch renderer"
  });
}
